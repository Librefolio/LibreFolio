---
code: F-012
title: "BRIM Framework (Broker Report Import)"
layer: fullstack
status: implemented
mkdocs: "developer/backend/brim/architecture.md"
tags: [brokers, brim, import, architecture]
depends_on: [F-009, F-011, F-059]
enables: [F-013, F-049, F-046]
related_decisions: [decisions/brim-fake-asset-id.md, decisions/brim-parser-only.md]
---

# F-012 — BRIM Framework (Broker Report Import)

## What it does
A pipeline for importing broker transaction reports. Users upload a file, the system detects or selects the appropriate plugin, parses the file, reviews the extracted transactions in a staging area, resolves unknown assets, and commits to the database.

## Layer breakdown
**Backend**: `backend/app/services/brim_provider.py` (abstract base), `brim_providers/` (11 plugins)
**Frontend**: `frontend/src/lib/components/transactions/modals/ImportWizardModal.svelte`
plus the focused components under `frontend/src/lib/components/transactions/import/`.

## Flow (post-Revision 2)
```
Upload (F-011) → Detect plugin (F-013)
→ POST /import/files/:id/parse → BRIMParseOutput (tx, extracted_assets, warnings)
→ search_asset_candidates() → asset_mappings[]
→ detect_tx_duplicates() → duplicates
→ Import Wizard (F-049): unify/correct rows and resolve fake_id → real asset
→ TransactionBulkModal (F-048): validate at `/transactions/validate`, commit at `/transactions/commit`
```

## Dependencies
- [[F-009]] Broker CRUD — files are scoped to a broker
- [[F-011]] File Management — upload/list/delete
- [[F-059]] Provider Registry Pattern — BRIM plugins use same auto-discovery

## Enables
- [[F-013]] BRIM Plugins — the 11 concrete parsers
- [[F-049]] BRIM Import UI — the frontend staging + matching wizard
- [[F-046]] Transaction API — the final commit target

## Implementation notes

### Fake Asset ID Flow (key design)
BRIM plugins output transactions with reserved **positive high fake asset IDs** beginning
at `FAKE_ASSET_ID_BASE`. The frontend remaps them across files, then replaces them with
real asset IDs through explicit matching or creation before bulk handoff.

### Parse result structure
`parse_file()` → returns `(transactions, warnings, extracted_assets)`
- `extracted_assets`: hints about assets found in the file (ISIN, ticker, name) for the matching UI
- `warnings`: non-fatal issues (missing prices, unknown asset types)

### Plugin Version + Stale Detection (Phase 07 Part 2, Revision 2)
Each plugin declares `plugin_version: str` (default `"1.0.0"`). When a plugin is updated:
- `BRIMFileInfo.parse_is_stale = True` if `parsed_plugin_version != registry.current_version`
- Computed lazily in `get_file_info()` — no DB write needed
- UI can show a "stale parse" indicator and prompt re-parse

### BRIM is a Parser Only (Phase 07 Part 2, Revision 2)
An earlier v1 implementation added `BRIMCapabilities`, `asset_events` in parse output,
and a `POST /import/commit` endpoint. All were **removed** in Revision 2:
- Capabilities had no consumers
- Asset events (dividends) are the asset provider's job, not the broker parser's
- Commit uses the standard `/api/v1/transactions/commit` endpoint — no BRIM-specific path

See [[decisions/brim-parser-only]].

### Internal DRY refactoring (Phase 07 Part 2)
- `_build_file_info_from_metadata(meta_path)` — unified two identical inner helpers
- `_find_metadata_path(file_id)` — DRY metadata path scanning
- `AssetSourceManager.bulk_upsert_events_manual()` renamed to `bulk_upsert_events()`
  (suffix was redundant after `bulk_upsert_events_strict` was removed)

### Test standardization (Phase 07 Part 2)
Merged `TestPluginInterface` (Pattern A) and `TestBRIMPluginsContract` (Pattern B) into
a single `TestBRIMPlugin` class using Pattern B — fail-fast at collection time, consistent
with asset/FX provider tests.

## Status history
| Date | Status | Notes |
|------|--------|-------|
| 2025 | implemented | Core framework, Phase 3/4 of backend roadmap |
| 2026-04 | implemented | Phase 7 Part 2 Revision 2 — parser-only, plugin_version, stale detection |

## Source files

| Role | Path |
|------|------|
| Abstract base class | `backend/app/services/brim_provider.py` |
| Plugins directory | `backend/app/services/brim_providers/` |
| BRIM API and file routes | `backend/app/api/v1/brokers.py` |
| BRIM schemas and fake-ID contract | `backend/app/schemas/brim.py` |
| Import UI modal | `frontend/src/lib/components/transactions/modals/ImportWizardModal.svelte` |
| mkdocs | `mkdocs_src/docs/developer/backend/brim/architecture.md` |
| Primary mkdocs | `mkdocs_src/docs/developer/backend/brim/architecture.md` |
| Brim Plugin Guide mkdocs | `mkdocs_src/docs/developer/architecture/patterns/brim_plugin_guide.md` |
