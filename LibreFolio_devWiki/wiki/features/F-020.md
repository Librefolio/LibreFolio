---
code: F-020
title: "FX Currency Conversion Graph (Triangulation)"
layer: fullstack
status: implemented
mkdocs: "developer/frontend/fx-chain-algorithm.md"
tags: [fx, calculations, graph, triangulation]
depends_on: [F-016, F-017]
enables: [F-024, F-054, F-057]
related_decisions: [decisions/fx-sync-pair-based]
---

# F-020 — FX Currency Conversion Graph (Triangulation)

## What it does
Maintains a directed graph of currency conversion routes. Given available pairs (e.g. EUR/USD, USD/JPY), the system can compute indirect conversions (EUR/JPY) by traversing the graph — without requiring explicit direct pairs for every combination.

## Layer breakdown
**Backend**: `FxConversionRoute` table in `backend/app/db/models.py`. Graph traversal logic in `backend/app/services/fx.py`.
**Frontend**: `frontend/src/lib/stores/currencyGraphStore.ts` — client-side graph for UI use (e.g. showing conversion paths).

## Dependencies
- [[F-016]] FX Pair CRUD — pairs are the nodes/edges of the graph
- [[F-017]] FX Rate Sync — ensures edges have current rate data

## Enables
- [[F-024]] Assets — asset prices shown in user's preferred currency require conversion
- [[F-057]] Currency Conversion — the actual conversion calculations use this graph
- [[F-054]] Dashboard — portfolio total in single currency requires cross-pair conversion

## Implementation notes
- `FxConversionRoute` table stores: base, quote, provider assignments, priority ordering
- Graph traversal is depth-limited to avoid infinite loops on misconfigured cycles
- **Triangulation example**: EUR→USD→JPY gives EUR/JPY rate without an explicit EUR/JPY pair
- The graph is rebuilt on pair changes — no persistent graph structure, recomputed from DB rows

## Status history
| Date | Status | Notes |
|------|--------|-------|
| 2026 | implemented | Phase 5, FX sync redesign — `plan-fxSyncApiRedesign.prompt.md` |

## Related decisions

- [[decisions/fx-sync-pair-based]] — **not a decision about triangulation.** It
  settles what the *sync* endpoint takes as input (explicit pairs, not a flat list
  of currencies from which the backend derives a cartesian product). Triangulation
  is the *read* side: given the pairs that exist, find a conversion path between two
  currencies that have no direct pair. The two share the pair inventory and nothing
  else — pair-based sync is what guarantees the graph's edges are real rather than
  inferred, which is the precondition triangulation needs.

> **Path note (2026-09-01)**: this page pointed at `decisions/fx-triangulation.md`,
> which has never existed. Replaced with the nearest real decision above rather than
> left as a promise. **No decision page documenting the triangulation algorithm
> itself has been written** — if the rationale is worth recording, it is a gap, not
> a broken link.

## Source files

| Role | Path |
|------|------|
| FX service (conversion graph) | `backend/app/services/fx.py` |
| API endpoints (conversion route) | `backend/app/api/v1/fx.py` |
| Currency graph store | `frontend/src/lib/stores/currencyGraphStore.ts` |
| Primary mkdocs | `mkdocs_src/docs/developer/frontend/fx-chain-algorithm.md` |
