---
code: F-051
title: "Transaction AssetEvent Link"
layer: backend
status: in-progress
mkdocs: null
tags: [transactions, events, link, backend]
depends_on: [F-046, F-031]
enables: []
related_decisions: []
related_problems: []
---

# F-051 — Transaction AssetEvent Link

## What it does
A foreign key relationship between `Transaction` and `AssetEvent` that enables DIVIDEND/INTEREST transactions to reference the global asset event they derive from. This bidirectional link is the prerequisite for a future "smart assistant" that auto-links historical transactions to their corresponding events retroactively.

## Layer breakdown
**Backend**: `backend/app/db/models.py` — `Transaction.related_asset_event_id` FK to `AssetEvent.id`, `backend/app/services/transaction_service.py` — event suggestion endpoint, `backend/app/alembic/versions/001_initial.py` — migration

## Dependencies
- [[F-046]] Transaction Model — the FK lives on Transaction
- [[F-031]] Asset Events — the target of the FK

## Enables
- (future smart assistant for retroactive event matching — not yet planned)

## Implementation notes
- Phase 7 Part 1: `asset_event_id` column added to `Transaction` (nullable FK to `AssetEvent.id`)
- **Phase 7 Part 3**: `POST /transactions/events/suggest` endpoint implemented:
  - Input: batch of transaction stubs (asset_id, date, type, amount)
  - Output: per-TX list of candidate AssetEvents within ±`tolerance_days` (0–7), ordered by date distance
  - Used by the Staging Modal for auto-linking DIVIDEND/INTEREST transactions to events
- The suggest endpoint preserves request order in the response
- Tolerance slider (0–7 days): `tolerance_days=0` requires exact date match
- Link is optional (nullable FK): manually entered transactions don't require an event link
- Type mapping: DIVIDEND TX → DIVIDEND event; INTEREST TX → INTEREST event; others → SPLIT/PRICE events

## Status history
| Date | Status | Notes |
|------|--------|-------|
| 2026-04 | in-progress | Phase 7 Part 1: DB column; Phase 7 Part 3: events/suggest endpoint |

## Source files

| Role | Path |
|------|------|
| DB model (Transaction.related_asset_event_id) | `backend/app/db/models.py` |
| Transaction API | `backend/app/api/v1/transactions.py` |
| Asset events API | `backend/app/api/v1/assets.py` |
