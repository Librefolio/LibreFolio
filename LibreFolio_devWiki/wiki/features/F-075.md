---
code: F-075
title: "TanStack Table v9 Migration"
layer: frontend
status: planned
mkdocs: null
tags: [frontend, table, tanstack, svelte5]
depends_on: []
enables: [F-077]
related_decisions: []
related_problems: [tanstack-svelte5-incompatibility]
---

# F-075 — TanStack Table v9 Migration

## What it does
Migrate the DataTable from the current custom Svelte 5 adapter for TanStack Table v8 to the official `@tanstack/svelte-table` adapter when v9 becomes stable. The v8 official adapter is incompatible with Svelte 5 runes, so a custom adapter was built as a workaround.

## Implementation notes
- Current location: **removed** (was `frontend/src/lib/tanstack-table/`) (custom Svelte 5 adapter)
- Target: `@tanstack/svelte-table` official package (v9, not yet stable at time of planning)
- Migration is low risk but requires v9 to reach stable release first
- All DataTable components in `lib/components/table/` use the custom adapter

## Source files

| Role | Path |
|------|------|
| Custom adapter (current) | **removed** (was `frontend/src/lib/tanstack-table/`) |
| DataTable component | `frontend/src/lib/components/table/DataTable.svelte` |

## Status history
| Date | Status | Notes |
|------|--------|-------|
| 2026-05-10 | planned | Low priority — blocked on TanStack Table v9 stable release |

> **Path note (2026-09-01)**: `frontend/src/lib/tanstack-table/` no longer exists,
> and `@tanstack/*` appears **nowhere** in `frontend/package.json` or in any source
> file. The workaround adapter this page describes was not merely refactored — the
> dependency it worked around was **dropped entirely**, which is the outcome the page
> argues for. `DataTable.svelte` is now a plain Svelte 5 component with no external
> table engine. The row is kept below as a historical reference and marked as gone.
