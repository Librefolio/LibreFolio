# 04 — Decisioni e questioni aperte

**Data apertura**: 16 Settembre 2026
**Natura**: documento **vivo**. Ogni decisione presa si registra qui con data e
motivazione; ogni questione aperta resta qui finché non è chiusa.

---

## 1. Decisioni prese

| # | Data | Decisione | Motivazione |
|---|---|---|---|
| D1 | 16 Set 2026 | **Tesi adottata**: Risk risponde a *«il mio portafoglio è quello che credo che sia?»*, non a *«quanto rischio ho?»* | Un numero isolato non produce decisioni; una discrepanza sì |
| D2 | 16 Set 2026 | **Quattro livelli di domanda** (L1 fatti · L2 struttura · L3 relatività · L4 proiezione) | L1/L2/L4 sono autoreferenziali; serviva la domanda comparativa, altrimenti Sharpe/beta/TE restano senza casa |
| D3 | 16 Set 2026 | **Regola dei pesi**: con pesi → euro → «io»; senza pesi → percentuali → «questi» | Rende impossibile per costruzione confondere Asset Global con Broker Detail |
| D4 | 16 Set 2026 | **VaR/CVaR restano**, riformulati in scala temporale con CVaR primario | Sono oggettivi e basati sui dati; il difetto era l'assenza di scala dichiarata |
| D5 | 16 Set 2026 | **TE e IR rimossi dalla UI** (backend intatto) | Misurano l'aderenza a un mandato che un investitore privato non ha |
| D6 | 16 Set 2026 | **Portfolio optimization rinviata** a tempo indeterminato | Output prescrittivo non giustificabile; LibreFolio è un tracker, non un consulente |
| D7 | 16 Set 2026 | **Monte Carlo rifondato fino al livello 3** (bootstrap, GJR-GARCH, preset prescritti); livelli 4-5 rinviati | Il bootstrap batte il GBM su onestà, realismo, spiegabilità e costo |
| D8 | 16 Set 2026 | **Asset Detail parcheggiato in beta**, fuori da questo giro | Priorità su Dashboard, Broker Detail e Asset Global |
| D9 | 16 Set 2026 | **Segnali rolling restano nella Overview di Asset Detail** | Stanno bene dove sono; una card con un solo valore puntuale perde la forma nel tempo |
| D10 | 16 Set 2026 | **Broker Detail = stessa grammatica di Dashboard**, stesso componente con scope diverso | Già vero nel codice; va reso esplicito e preservato |
| D11 | 16 Set 2026 | **Asset Global = laboratorio**, non «un'altra vista del portafoglio» | Il pannello `analysis` rende il concetto già di prima classe nel modello dati |
| D12 | 16 Set 2026 | **Catena G6 abbandonata**, non ripresa | 23 item a catena singola con gate bloccanti: un solo stop congela tutto |
| D13 | 16 Set 2026 | **L3 con benchmark persistente** scelto fra gli asset in DB, da subito | Senza benchmark persistente il beta è un numero senza contesto e L3 resta la domanda debole |
| D14 | 16 Set 2026 | **Serie di drawdown dal backend**, non derivata client-side | Una curva calcolata nel browser può contraddire lo scalare del motore: due numeri discordi sullo stesso schermo costano più fiducia di quanta ne dia il grafico. Costo minore del previsto: `RiskComparisonPoint.primary_drawdown` già la calcola |
| D15 | 16 Set 2026 | **Ricostruire i componenti risk** sulle primitive condivise, dopo aver promosso quelle generiche in `components/ui/` | Ridisegnare il layout sopra gli stessi mattoni storti non risolve nulla |
| D16 | 16 Set 2026 | **Istogramma della distribuzione dei rendimenti** in v1, con VaR e CVaR marcati sopra; nuovo campo backend per i bin | Rende visibile la *definizione* delle due metriche: il CVaR è la media della coda rossa. Nessun testo lo spiega meglio |
| D17 | 16 Set 2026 | **Sei rappresentazioni adottate**: underwater, istogramma, barre appaiate peso/contributo, heatmap rifatta, scatter rischio-rendimento, tornado degli scenari | Ogni numero ha una forma che lo spiega; la card numerica è il ripiego, non il default |
| D18 | 16 Set 2026 | **`SignalDomain.PORTFOLIO` non serve per la v1** | Le sei rappresentazioni scelte usano dati che il backend già produce. Per la volatilità l'istogramma dice più di una sparkline del suo rolling |
| D19 | 16 Set 2026 | **Selezione di Asset Global persistita in `localStorage`**, con fallback agli asset posseduti | Il seed a cento asset apre una matrice di diecimila celle e non offre alcun modo di disfarla |
| D20 | 16 Set 2026 | **Nessuna generalizzazione della piattaforma segnali** per l'underwater chart | `underwater_drawdown` vive già in `services/risk/metrics.py` ed è il *segnale* a importarla dal rischio, non il contrario. `drawdown_episodes()` calcola già la serie in una variabile locale e ne butta via tutto tranne gli scalari |
| D21 | 16 Set 2026 | **Bin dell'istogramma secondo Freedman-Diaconis**, con un bordo forzato sul quantile del VaR e binning sulla stessa serie a `horizon_days` | L'IQR è robusto agli outlier; deviazione standard e normalità userebbero la coda per decidere come disegnare la coda. Il bordo forzato rende l'area rossa esattamente il 5% |
| D22 | 16 Set 2026 | **Nessuna gaussiana sovrapposta all'istogramma** (chiude Q6) | Il VaR mostrato è un quantile empirico: la campana confronterebbe i dati con un modello che non usiamo, e suggerirebbe che lo scarto sia un'anomalia mentre le code grasse sono la norma |
| D23 | 16 Set 2026 | **Riskfolio-Lib resta installata** (chiude Q2) | Misurata: 2,5 MB, closure solver ~16 MB su 2 780 totali. Lo 0,6% dell'immagine. I pesi veri sono scipy, pandas e QuantLib |
| D24 | 16 Set 2026 | **Le funzioni scalari di `metrics.py` non si toccano** | Coincidono con Riskfolio fino all'ottavo decimale su VaR, max drawdown, Sharpe e risk contribution; costano microsecondi; `math.fsum` dà una sommatoria compensata che `np.sum` non garantisce |
| D25 | 16 Set 2026 | **Riskfolio diventa oracolo di test per `metrics.py`** | `test_risk_metrics.py` verifica solo proprietà interne: nessun controllo indipendente. Venti righe coprono il buco e avrebbero intercettato da sole la trappola `MDD_Abs`/`MDD_Rel` |
| D26 | 16 Set 2026 | **`correlation_matrix` e `covariance_matrix` vanno vettorializzate** | O(N²·T) in Python puro: 3,7 s su cento asset contro 0,2 ms di NumPy. Il Python puro trattiene la GIL, NumPy la rilascia nel BLAS |
| ~~D27~~ | ~~16 Set 2026~~ | ~~Convenzione CVaR nostra confermata~~ | **REVOCATA lo stesso giorno da D28.** Motivata dalla spiegabilità, senza aver misurato la distorsione |
| D28 | 16 Set 2026 | **CVaR migra allo stimatore coerente** (Acerbi-Tasche / Rockafellar-Uryasev), off-by-one incluso | Il nostro è più basso di Riskfolio **2000 volte su 2000**, in media −0,27%, a sette deviazioni standard dallo zero. Non è convenzione: è distorsione. L'argomento della spiegabilità non regge, perché Acerbi-Tasche *è* «la media delle giornate peggiori» e lo scarto è invisibile |
| D29 | 16 Set 2026 | **Segnali rolling migrano a `pandas.rolling`** — priorità massima | `signal_helpers.py:69-84` copia una fetta e richiama la funzione scalare a ogni passo: fino a **1 514× più lento a valori identici**, e gira a ogni caricamento di grafico |
| ~~D30~~ | ~~16 Set 2026~~ | ~~Le funzioni scalari a chiamata singola restano in Python puro~~ | **REVOCATA da D36.** Motivata dalla velocità, che è il criterio sbagliato |
| D36 | 16 Set 2026 | **Criterio di possesso, non di velocità: A migra, B si valida, C si acquisisce** | (A) doppioni veri → migrare: VaR, CVaR, Sharpe, deviazione standard, matrici, **risk contribution** (verificata identica a `Risk_Contribution(rm="MV")`). (B) composti che la libreria non esprime → restano, ma con oracolo M4: `summarize_drawdown` dà massimo **e durata** in una passata (0,223 ms) mentre `MDD_Rel` dà un float in 0,904 ms e la durata **non ce l'ha**. (C) ciò che non abbiamo → Q7. Argomento decisivo: il CVaR l'abbiamo scritto noi ed è sbagliato in silenzio |
| D37 | 16 Set 2026 | **`MDD_Abs` non diventa un secondo segnale** | Non è «un altro drawdown»: è lo stesso su curva **non composta**, con il buco in unità assolute anziché in percentuale del picco. Resta lineare nei pesi, quindi serve ai solutori; per un utente non significa nulla di naturale. La nostra funzione **è già `MDD_Rel`**, cifra per cifra a meno del segno |
| D31 | 16 Set 2026 | **I 17 plugin di analisi tecnica non si toccano** | Audit: una sola chiamata `ta.*` ciascuno, zero aritmetica a mano dopo. E 16 su 17 passano `talib=True`, quindi girano già su TA-Lib 0.7.1 in C. Sono il modello, non il debito |
| D32 | 16 Set 2026 | **La libreria si sceglie per mestiere, non per marca** | pandas per le finestre mobili (Riskfolio **non ha** funzioni rolling), NumPy per l'algebra (`covar_matrix('hist')` *è* `np.cov` con 2-3× di sovrapprezzo e obbligo di `DataFrame`), Riskfolio per le misure di rischio e l'ottimizzazione, TA-Lib per gli indicatori |
| D33 | 16 Set 2026 | **`scipy` non è rimovibile e non va contata come peso del ROI** | Un solo import nostro (`roi_utils.py:19`), ma è dipendenza obbligatoria di `riskfolio-lib`, `cvxpy`, `scs`, `clarabel`, `osqp`, `scikit-learn`. Gira a ogni ottimizzazione |
| D34 | 16 Set 2026 | **Gli stimatori robusti di covarianza restano fuori da M3** | Ledoit-Wolf, Gerber e denoising sono un miglioramento di *qualità della stima*, non di velocità: 25,4 ms contro 0,2 ms. M3 è un intervento a comportamento invariato. → TODO separato |
| D35 | 16 Set 2026 | **`AuxFunctions.numBins` non si usa per l'istogramma** | Il nome inganna: è la formula di Hacine-Gharbi per la discretizzazione della mutua informazione, non per un istogramma di rendimenti. `freedman_bin_width` di riskfolio è invece astropy ri-esportata e dà gli stessi 21 bin di NumPy: si resta su `np.histogram(x, bins='fd')` |
| D38 | 16 Set 2026 | **`NEA` entra come KPI di L2, non come metrica di punta** | Indice di concentrazione dei pesi: `1/Σwᵢ²`, inverso di Herfindahl. Solo i pesi, 0,0025 ms. Su «3 grossi + 20 briciole» dà **4,12 su 23 posizioni**. Sta **accanto** a correlazione e contributo al rischio, non al loro posto: è cieco alla correlazione — tre portafogli di 10 asset equipesati con ρ 0 / 0,5 / 0,95 danno NEA **10,00 in tutti e tre** mentre la volatilità reale va da 0,0038 a 0,0116 (nemmeno sui contributi cambia: 9,97 contro 10,00). Va accoppiato al **diversification ratio** (3,15 → 1,35 → 1,02), che riskfolio non ha: tre righe di NumPy sulla covarianza già calcolata |
| D45 | 16 Set 2026 | **`WR` entra come KPI di L1, `RG` come colonna di Asset Global** | `WR` è la peggior giornata **realmente accaduta**: non una stima. Sta accanto al VaR per contrasto — il VaR dice «una su venti va peggio di così», `WR` dice «e la peggiore è stata questa». `RG` (escursione massimo-minimo) aggiunge poco su un portafoglio ma funziona come colonna di confronto fra strumenti. `MAD` resta fuori: misura la stessa cosa della volatilità |
| D43 | 16 Set 2026 | **`Kurtosis` non entra: non è stimabile su una singola storia** | Due ragioni indipendenti. (1) La versione riskfolio non è standardizzata: `sqrt(Σ(μ−x)⁴/T)`, confermato dal docstring dell'ottimizzatore `'KT': Square Root of Kurtosis`; su gaussiana dà 0,00034462 = `sqrt(m₄)` esatto mentre la kurtosi vera è 2,989. (2) **Decisiva**: è un momento di ordine quarto, la sua incertezza dipende dall'ottavo. Con eccesso vero 6,0 e tre anni di storia la stima cade **fra 1,2 e 10,8**; con cinquant'anni la mediana è ancora 4,02, distorta verso il basso. Togliere 10 giorni su 750 porta 3,88 → 0,63. E non discrimina: obbligazionario 0,6-2,4, ETF 2,2-**19,9**, azione singola 4,2-48,1 — intervalli sovrapposti, tutti sopra zero. **Dove serve davvero**: come misura per l'ottimizzatore (`rm="KT"`) alimentata da `cokurt_matrix`, cioè su universi ampi dove la media cancella il rumore. Per «quanto sono brutte le mie giornate brutte?» su pochi asset la risposta migliore resta il **CVaR**, che è una percentuale leggibile |
| D39 | 16 Set 2026 | **`Sharpe(rm=...)` entra come risposta a L3** | L3 contiene un buco — pagato per *quale* rischio? `rm="MDD"` **è** il Calmar ratio, `rm="UCI"` **è** il Martin ratio. Non sono decorazioni: **la classifica cambia**. Su due asset costruiti apposta, Sharpe premia B (0,89 contro 0,60) mentre Calmar (0,48 contro 0,28) e Martin (1,15 contro 0,55) premiano A. Nessuna matematica nuova da scrivere. Vincolo UI: si sceglie «rispetto a cosa», non si mostrano tutti in fila |
| D40 | 16 Set 2026 | **`annualized_sortino` NON è un doppione di `SemiDeviation`** e resta nostro | Noi misuriamo gli scarti sotto il **MAR** dividendo per `T`; riskfolio sotto la **media campionaria** dividendo per `T−1`. Verificato: 1,66294 contro 1,57093. Il Sortino è *definito* contro un MAR, quindi il nostro è corretto per il suo scopo → **guardia obbligatoria: ogni sostituzione va provata numericamente prima** |
| D44 | 16 Set 2026 | **Ciò che la libreria non ha si riscrive in NumPy/SciPy — non resta in `math` puro** (M6) | Corregge l'assunzione implicita di D36 caso B: «Riskfolio non ce l'ha» diceva solo che il fornitore è un altro, non che si tiene un ciclo interpretato. Nove funzioni — `summarize_drawdown`, `drawdown_episodes`, `pairwise_correlation`, `comparison_summary`, `wealth_index`, `period_returns_from_cumulative`, `horizon_compounded_returns`, `current_buy_and_hold_returns`, `annualized_sortino` — passano a NumPy a firma e semantica invariate, sotto oracolo M4. Restano nostre: ci appartiene la composizione, non l'aritmetica |
| D46 | 16 Set 2026 | **Si rilascia solo a catena completa, dal worktree separato** — chiude Q4 | Niente stati intermedi su `dev_release2`: il lavoro resta isolato finché non è pronto. Il banner beta si toglie **in un colpo solo** a fine catena, con la regola di Q4 (via da L1/L2/L3, resta sul solo gradino «simulazione» di L4). Il CHANGELOG si scrive una volta sola, e lì dentro va la nota su M2, che **cambia il CVaR già mostrato agli utenti** |
| D47 | 16 Set 2026 | **Asset Detail si riapre a fine catena, non si cancella** | Conferma e data D8, che lo parcheggiava senza dire quando. Si torna a parlarne **dopo** che sistema e idee saranno chiari — quando i quattro livelli, le rappresentazioni e la migrazione saranno in piedi, così la pagina eredita una grammatica già decisa invece di inventarsene una propria. Le sue due lacune note restano registrate: non può mostrare il contributo al rischio del proprio portafoglio (`00` §riga 145) e la card rolling mostra un solo valore puntuale invece della forma nel tempo (`00` §riga 149) |
| D48 | 17 Set 2026 | **Il benchmark è un catalogo condiviso, non una scelta singola** — riformula Q1 | Il confronto è plurimo: misurare un portafoglio azionario contro un obbligazionario dà un numero corretto e privo di significato. Quindi una casella sull'asset lo qualifica come riferimento predefinito, **condivisa fra tutti gli utenti** — coerente per costruzione, dato che `Asset` (`models.py:464`) **non ha `user_id`**: è già globale. Restano selezionabili tutti gli asset; i riferimenti stanno in cima |
| D49 | 17 Set 2026 | **Il flag è ortogonale ad `AssetType.INDEX`, non lo riusa** | `INDEX` esiste già e il suo docstring dice «Market indices and benchmarks… no transactions allowed», ma è usato in **un solo punto** del backend (`transaction_batch_stages.py:502`) e solo per vietare le transazioni. Riusarlo romperebbe il caso principale: un ETF S&P 500 è un ottimo benchmark **e** un asset che si possiede davvero. Da valutare la regola `è_benchmark = flag OR asset_type == INDEX`, dato che un `INDEX` esiste solo per il confronto |
| D50 | 17 Set 2026 | **Un solo selettore serve quattro punti** | `SignalAssetParamControl` (64 righe) è raggiunto da `ChartSignalsSection`, presente sia in `/assets/[id]` sia in `/fx/[pair]`, ed è **già usato** da `RiskAnalysisPanel` alle righe 891 (asset di confronto) e 1091 (proxy di replay). L'ordinamento è oggi alfabetico puro (riga 31). E `SelectOption.header` esiste già, con la semantica giusta: sparisce quando la ricerca svuota la sua sezione. Mettere i benchmark in cima è un `sort` più due intestazioni, **non** un componente nuovo |
| D41 | 16 Set 2026 | **`BrinsonAttribution` e `Factors_Risk_Contribution` restano fuori** | La prima scompone il rendimento in effetto allocazione ed effetto selezione: è la risposta più completa a L3, ma pretende un benchmark **con i suoi pesi per classe** → bloccata dietro Q1 e priva di tassonomia. La seconda richiede una matrice di fattori esterni, che senza provider dati non esiste |
| D42 | 16 Set 2026 | **Le misure entropiche e di Gini non entrano** | `EDaR`, `RLDaR`, `RLVaR`, `Entropic_RM`, `L_Moment_CRM`, `TG`, `TGRG`, `GMD` e la famiglia range. Invocano un solutore — 26,9 ms misurati su EDaR — e richiedono di spiegare l'entropia a chi voleva sapere quanto può perdere. Esistono per gli ottimizzatori, non per le persone. Nota: `EVRG`, `RVRG` e `VRG` portano **docstring copiate** da `CVRG`, quindi la loro documentazione non è affidabile |
| D51 | 17 Set 2026 | **`asset_type` è quasi inerte: il docstring su `valuation_model` mente** | `grep` su tutto `backend/app` trova `valuation_model` **solo** in `models.py:162`, dentro il docstring di `AssetType`. Zero codice: la mappatura dichiarata (CROWDFUND→SCHEDULED_YIELD, HOLD→MANUAL, INDEX→MARKET_PRICE) non esiste. Verificato branch per branch, `asset_type` fa filtro nelle query (`crud.py:180-181`), raggruppamento ed esposizione, etichetta, AI export — e **una sola regola di comportamento**: `INDEX` vieta le transazioni (`transaction_batch_stages.py:501-502`). Conseguenza: aggiungere sottotipi **non cambia alcun comportamento**. Il docstring va corretto nella stessa migrazione |
| D52 | 17 Set 2026 | **I sottotipi ETF entrano in `AssetType`** — revoca la proposta di colonna ortogonale | Avevo proposto una colonna `asset_class` separata con tre argomenti, tutti caduti. (1) «I provider non sanno riempirla» — `justetf.py:614` scrive `AssetType.ETF` fisso e `borsa_italiana.py` mappa segmenti di mercato: vero, ma irrilevante, perché il generico resta un valore legittimo e la specializzazione è un gesto dell'utente; nel selettore benchmark si sceglie comunque fra tutti gli asset, quindi il tipo serve a **trovare**, non a vincolare. (2) «`asset_type` pilota comportamenti» — falso, vedi D51. (3) «L'icona vuole due strati» — si risolve con una seconda costante accanto a `PNG_MAP` in `assetTypes.ts`: icona grande dal contenitore, **pastiglia piccola sovrapposta** dalla classe, lo stesso schema che l'altro worktree applica a broker + asset nel tool pacchetti. Si estendono **solo i tipi dove la distinzione ha senso**; `ETF` generico resta come residuo |
| D53 | 17 Set 2026 | **`AssetType.INDEX` implica benchmark, in automatico e in sola lettura** | Precisa la riserva lasciata da D49: non `flag OR asset_type == INDEX` ricalcolato a ogni lettura, ma flag **materializzato**. Nella migrazione Alembic, ogni asset già `INDEX` riceve il flag a `true` (backfill). A regime, creando un asset `INDEX` il flag si accende da solo ed è **readonly**: un indice esiste solo per il confronto, non lo si possiede |
| D54 | 17 Set 2026 | **Le colonne di rischio entrano nascoste e attivabili** — chiude Q3 | Tutti i segnali utili diventano colonne di Asset Global, **nascoste per default**; visibile di default solo il **max drawdown**, facile da capire e ad alto impatto. Non serve alcun meccanismo nuovo: `DataTable` ha già `hiddenByDefault` per colonna e persiste in localStorage **solo gli override espliciti** (`DataTable.svelte:193-196`), così un default che cambia resta vivo e un valore vecchio non può congelare una colonna che dovrebbe vedersi. Si rivedrà insieme ad Asset Detail (D47) |
| D55 | 17 Set 2026 | **La documentazione nasce come scheletro inglese e si traduce in fondo** — chiude Q5 | All'inizio dello sviluppo si crea **una pagina mock di due righe, solo in inglese**, per ogni voce che servirà, registrata subito nell'indice di `mkdocs_src/mkdocs.yml` secondo le regole già fissate nelle skill. Mentre gli agenti di sviluppo lavorano, un agente di documentazione riempie le pagine **in inglese**. Le traduzioni si fanno **in un blocco solo a fine documentazione**, non pagina per pagina |
| D56 | 17 Set 2026 | **Dalla famiglia drawdown entrano quattro misure** — chiude Q7 | `MDD`, `DaR`, `CDaR`, `UCI`, nella variante **`_Rel`**. Semantica verificata sul sorgente e ricostruita a mano: `_Abs` usa `cumsum` e misura in punti di rendimento cumulato, `_Rel` usa `cumprod` e misura in **% dal picco** — l'unica che la gente intende (`MDD_Abs` lib 0,227822 = mano 0,227822; `MDD_Rel` lib 0,212860 = mano 0,212860). Costano 1,6-3,0 ms. Fuori: `ADD` per scelta del developer; `EDaR` e `RLDaR` **confermando D42**, con una prova in più — `EDaR` restituisce una tupla e invoca un solutore (27 ms), `RLDaR` costa 89-166 ms e su un seme ha prodotto `RLDaR_Rel = 0,000000` esatto, su un altro 0,1939. Un solutore che degenera in silenzio è peggio di una misura assente |
| D57 | 17 Set 2026 | **Il contributo al rischio è automatico: l'utente non seleziona nulla** | `RiskContributionParams` è vuoto — `extra="forbid"`, nessun campo. Scope solo `PORTFOLIO`, modo solo `CURRENT_COMPOSITION`. Legge `context.scope_asset_ids` e `context.weights`: prende la composizione corrente con i pesi reali in valuta di destinazione. È questa proprietà — il plugin riceve la lista **già risolta**, non il filtro — a rendere quasi gratuito D58 |
| D58 | 17 Set 2026 | **Il portafoglio si affetta per asset, non solo per broker** | Oggi `PortfolioRiskScope` filtra **solo** per `broker_ids`: per *dove* si tiene una cosa, non per *cosa* è. E `AssetSetRiskScope` non è la risposta — significa «questi N asset affiancati», senza pesi, ed è accettato da **tre plugin su nove** (`correlation`, `portfolio_optimization`, `stress`); tutto L1 (VaR, CVaR, drawdown) e L3 (KPI, confronto) accetta solo `asset` o `portfolio`, e `risk_contribution` **solo** `portfolio`. Si aggiunge quindi un filtro per asset **su `PortfolioRiskScope`**: resta uno scope `portfolio`, tutti e nove i plugin lo accettano senza modifiche e la fetta si propaga da sola (D57). Motivo: è il gemello di D48 — se confrontare un 60/40 con un indice azionario è sbagliato, lo è per due ragioni simmetriche, il riferimento sbagliato **e** la fetta sbagliata; sceglierne bene uno solo risolve metà del disallineamento |
| D59 | 17 Set 2026 | **Affettando, i pesi si rinormalizzano al 100% della fetta** — chiude Q8 | La fetta esiste per essere confrontata con il riferimento giusto (D48), quindi va guardata come se fosse tutto il portafoglio. Coi pesi reali una fetta al 60% risulterebbe **sempre** meno rischiosa del suo indice, e il confronto — cioè l'intero motivo per cui D58 esiste — si romperebbe in silenzio. Conseguenza: i contributi al rischio della fetta sommano al 100% della fetta. Va **dichiarato nella UI**, non lasciato intuire: la domanda a cui la vista risponde è «com'è fatta la mia parte azionaria», non «quanto pesa sul totale» — per quella c'è già il contributo al rischio sull'intero portafoglio |
| D60 | 17 Set 2026 | **Il flag benchmark è condiviso per scelta, non per compromesso** — chiude Q1(b) | Chiunque lo accenda cambia la lista a tutti, ed è il comportamento voluto: non ha senso che lo stesso ETF sia un buon riferimento per un utente e no per un altro. È coerente col resto del modello — `Asset` non ha `user_id` (`models.py:464`) e un asset marcato benchmark, **purché non sia `INDEX`**, resta assegnabile alle transazioni come qualunque altro, cosa a sua volta globale. Nessun permesso speciale, nessuna riserva all'amministratore |
| D61 | 17 Set 2026 | **Il secondo livello non è una tassonomia nuova: è l'insieme dei tipi base** | Riformula D52 in modo più stretto. La specializzazione di un ETF è «**quale tipo base contiene**»: azionario, obbligazionario, materie prime, immobiliare, monetario, cripto. Non serve un elenco parallelo, e ne discende la regola meccanica dell'icona: **pastiglia = icona del tipo base**, nessuna serie nuova da disegnare. Conseguenze: (1) *bilanciato/multi-asset* non è un sottotipo, è l'`ETF` generico — che resta come residuo e come valore che i provider scrivono; (2) `FUND` **non** si suddivide: gli ETF sono già fondi, passivi, e la distinzione utile è quella sul contenuto; (3) servono due **tipi base nuovi**, `COMMODITY` e `REAL_ESTATE`, che oggi mancano del tutto; (4) *monetario* si sovrappone a liquidità — e `LIQUIDITY` è già mezzo costruito: **non è nell'enum del backend**, ma esiste in `PNG_MAP` (`assetTypes.ts:30`), come `liquidity.png`, nelle quattro lingue (riga 143 di `en/it/fr/es.json`) e come emoji `💰` in `AllocationHistoryChart.svelte:129`. Orfano innocuo, perché `ASSET_TYPES` deriva dallo schema Zod e quindi non compare nel menù — ma icona ed etichette sono pronte. Da disegnare solo `commodity` e `real-estate` |
| D62 | 17 Set 2026 | **Il selettore a due livelli esiste già: `SignalTreeSelect`** | 359 righe in `components/charts/`. Interfaccia `groups: SignalTreeGroup[]`, ogni gruppo con `items: SignalTreeItem[]` — **esattamente due livelli**, espandibili, con ricerca che attraversa entrambi e navigazione da tastiera. Vince sulle altre due ipotesi: un secondo selettore che appare dopo ETF costa poco ma richiede due gesti; un pulsante che apre una modale anniderebbe una modale dentro `AssetModal` (2427 righe). Costo del riuso, da non nascondere: va **generalizzato** — spostato in `components/ui/select/`, privato del nome «Signal», col contenuto dell'opzione passato come snippet invece che via `SignalOptionContent`. E `SignalTreeItem` ha un solo campo `icon`: per la pastiglia serve un secondo slot |
| D63 | 17 Set 2026 | **`LIQUIDITY` NON entra in `AssetType`** — revoca l'indicazione data in D61 | D61 diceva «il monetario è già mezzo costruito, icona ed etichette pronte». **Falso**: non è un tipo a metà, è un **secchio sintetico**. `portfolio_engine.py:1039-1041` inietta la cassa nell'allocazione come pseudo-tipo (`by_type["Liquidity"] = … + cumulative_cash + it_cash`), quindi le voci in `PNG_MAP`, nelle quattro lingue e in `AllocationHistoryChart` servono a disegnare *quel* secchio, non un tipo di asset. Aggiungerlo all'enum creerebbe un doppione visibile: gli asset veri finiscono in `by_type` con `asset_type.value`, cioè **maiuscolo** (`:1448`, alimentato da `:2155`), la cassa entra come `"Liquidity"` — due chiavi distinte per la stessa cosa, due fette nel grafico, e con la **stessa icona** perché `getAssetTypeIconUrl` normalizza in maiuscolo. Sembrerebbe un bug, e lo sarebbe. Resta aperto come rappresentare il monetario → **Q10** |
| D64 | 17 Set 2026 | **La scelta del benchmark vive solo nella cache locale del client** — chiude Q1(c) | Nessuna colonna su `UserSettings`: la preferenza sta in `localStorage`, coerente con la scelta architetturale già in uso per i toggle vista-doppia. Se non c'è nulla in cache, la zona **non mostra numeri vuoti**: mostra un testo che spiega cosa scegliere o aggiungere. Regola implementativa che ne discende: se l'asset in cache viene **cancellato o privato del flag**, la zona deve degradare allo stesso stato vuoto, non andare in errore — la cache locale non può assumere che il server sia rimasto d'accordo con lei. Conseguenza sul confronto: il benchmark viaggia nella richiesta a ogni chiamata, quindi il backend resta senza stato e non serve persistenza lato server |
| D65 | 17 Set 2026 | **I suggerimenti sono testi fissi tradotti, non un catalogo risolvibile** — chiude Q1(d) | Cade l'ipotesi della lista curata (YAML o costante con identificatori) risolta dai provider: niente chiamate ai provider, niente file di ISIN da mantenere. Restano **banner e testi tradotti nelle quattro lingue**, mostrati quando la zona è vuota, che spiegano quali riferimenti ha senso aggiungere; l'utente li aggiunge a mano col flusso di creazione asset già esistente. Resta da decidere quanto siano specifici → **Q11** |
| D66 | 17 Set 2026 | **La conversione valutaria del benchmark è obbligatoria** — chiude Q1(e) | Se il benchmark è in USD e la valuta base è EUR, il beta misura **anche il cambio**. Si converte con il sistema FX che esiste già: non è una funzione nuova, è l'uso di una che c'è. Senza, il numero è contaminato in silenzio — la stessa classe di errore del CVaR distorto (D28), dove il risultato era plausibile e sbagliato |
| D67 | 17 Set 2026 | **Il monetario è solo un sottotipo di ETF, non un tipo base** — chiude Q10 | Scartata la strada A (nuovo tipo base `MONETARY`): esiste `ETF_MONETARY` ma **non** un `MONETARY` di primo livello. Conseguenza da dichiarare, perché **incrina la regola di D61**: il secondo livello non è più *esattamente* l'insieme dei tipi base, è l'insieme dei tipi base **più il monetario**, che base non ha. La regola resta valida per tutti gli altri sottotipi, e l'eccezione è motivata: un fondo monetario è uno strumento che si compra — prezzo, emittente, rischio piccolo ma non nullo — mentre la cassa (D63) è il saldo di un conto. Non essendo un investimento tenuto da solo con quel tipo, non gli serve una casa al primo livello. Per la pastiglia si riusa **`liquidity.png`**, che esiste già, significa denaro e — appropriatamente — **non** è un tipo base |
| D68 | 17 Set 2026 | **I testi di suggerimento nominano indici, mai prodotti** — chiude Q11 | «Un ETF sull'S&P 500», «uno sull'EuroStoxx 600», «un obbligazionario globale»: nomi di indici e categorie, **nessun ISIN, nessun ticker**. Due ragioni. Un ISIN in una stringa tradotta è di fatto un consiglio d'acquisto, e sposta LibreFolio da strumento di misura a strumento di consiglio. E quando quel prodotto viene delistato o sostituito, l'errore è in quattro lingue contemporaneamente: un indice è un fatto pubblico e stabile, un prodotto è la scelta di un emittente |
| D69 | 17 Set 2026 | **Le due icone mancanti esistono e sono in famiglia** | `commodity.png` (barile + lingotti, oro `#BF9332` contro `#C09030` di famiglia) e `real-estate.png` (casa con tetto a falde + annesso, verde `#54BC7D`), installate in `frontend/static/icons/asset-types/`. Tavolozza e trasparenza dentro i valori misurati sulla serie esistente; dimensioni 271×248 e 296×199, coerenti con `liquidity.png` (275×227) dato che **la serie non è normalizzata** (da 79×79 a 275×227). Restano **non referenziate** finché l'enum non riceve i tipi corrispondenti. Osservazione onesta sulla resa a 20px, dove serviranno da pastiglia: `commodity` regge, `real-estate` è mediocre perché le due masse si fondono — ma è la stessa debolezza di `bond`, e comunque migliore di `etf`, che a quella dimensione diventa rumore illeggibile. Se la pastiglia risulterà confusa, il problema è **di tutta la serie**, non di questi due file |
| D70 | 17 Set 2026 | **Gli scenari di stress sono un compito, non un vincolo** — declassa Q12 | I due YAML predefiniti (`equity_crash`, `global_risk_off`) sono file nostri: si riscrivono insieme all'enum, e l'adeguamento del sistema di rischio esistente è parte del lavoro, non un ostacolo. Quello che resta di Q12 è la **modalità di fallimento**, non lo scenario: `_resolve_bucket` ritorna `UNCONFIGURED_ZERO` senza avvisare, `getAssetTypeIconUrl` ripiega su `other.png`, l'etichetta mancante mostra la chiave grezza. Tre coperture affidate alla buona volontà e tre ripieghi silenziosi: il piano esecutivo deve chiedere **un test che leghi l'enum alle tre tabelle**, non un aggiornamento manuale. Resta da decidere solo se la **torta di allocazione** si specializza (strade A/B/C di Q12), che è dashboard e non rischio |
| D71 | 17 Set 2026 | **I sottotipi si vedono come sfumature dentro la fetta del primario** — chiude il residuo di Q12 | Quarta strada, proposta dal developer: nessuna delle tre che avevo messo in tabella. Il sottotipo **non** diventa una fetta indipendente né sparisce dentro il contenitore: resta dentro la massa del tipo primario, con un **colore lievemente diverso**, e i numeri si leggono distinti nel tooltip. Stessa regola nello storico: le sotto-serie impilate prendono tonalità vicine a quella del genitore. **Un asset non specializzato usa il colore primario puro** — non è un caso da gestire, è il grado zero della scala. Risolve il compromesso che avevo posto (informare contro frammentare) invece di sceglierne un corno: la lettura d'insieme resta sulla massa cromatica, il dettaglio sta nel tooltip per chi lo cerca. Vale sia per `AllocationPieChart` sia per `AllocationHistoryChart` |
| D72 | 17 Set 2026 | **Se un anello solo non basta, si annida: ciambella a due anelli. Scartata la barra polare** | Ripiego registrato per D71. (1) **Anello annidato** — due serie `pie` sullo stesso centro con bande di raggio diverse, idioma nativo di ECharts. Costa poco perché **il grafico è già una ciambella** (`radius: ['35%', '70%']`, riga 274): c'è già il buco in cui infilare il secondo anello. Gli angoli combaciano per costruzione — l'arco di un genitore è la somma dei figli — quindi la lettura «quota sul totale» resta esatta. **Primari all'interno, sottotipi all'esterno**: l'anello esterno ha più lunghezza d'arco per grado, quindi la suddivisione fine va dove c'è spazio, e l'interno resta il riassunto che l'occhio legge per primo. (2) **Barra impilata polare** (`bar-polar-stack`) — grafico legittimo, ma risponde a un'altra domanda: in coordinate polari il valore si mappa sul **raggio**, e l'area cresce con il quadrato, quindi due valori uguali a raggi diversi occupano aree diverse. Per una torta di allocazione, dove tutto il senso è «quanta parte del totale», è una trappola percettiva. È il grafico giusto per confrontare categorie lungo una dimensione ciclica — mesi, ore — non per parti di un tutto |
| D73 | 17 Set 2026 | **I selettori ad hoc mostrano l'id invece del nome: si riusa `AssetSelect`** | Segnalato dal developer sul segnale **beta**, dove il menù elenca «9 / Amundi Core MSCI World UCI…» — id in cima, nome come sottotitolo. La causa **non** è nel controllo del segnale, che passa già `label: asset.display_name`: è il **rendering di default** di `SearchSelect` (`:496-497`), che stampa `option.value` in **`font-mono`** come riga principale e `option.label` come sottotitolo. Quel default è corretto per i selettori dove il valore **è** un codice leggibile — valuta, paese, settore — e il `font-mono` lo dichiara. Diventa un difetto quando il valore è una **chiave primaria di database**. Verificato chi lo sa e chi no: **tutti** i selettori dedicati in `ui/select/` sovrascrivono il default con gli snippet `item` e `selectedItem` — `AssetSelect`, `BrokerSearchSelect`, `UserSearchSelect` — mentre i due costruiti **fuori** da quella cartella cadono nel default: `SignalAssetParamControl.svelte` (segnali) e **`AssetSetRiskPanel.svelte`**, che sbaglia **due volte**, sul broker (`:49`) e sull'asset (`:55`). Non è un caso isolato: è ciò che succede quando si costruisce un picker ad hoc invece di riusare quello che c'è. **Cura: migrare a `AssetSelect`**, che è il picker «decente» già scritto (187 righe: icona, ticker, valuta con bandiera, asset inattivi ordinati in fondo e marcati) e il cui stesso commento d'intestazione invita a farlo — *«Migrate other asset_id pickers to use this when convenient»*. L'esclusione oggi fatta con `excludeAssetIds` si esprime col suo `filter?: (a: AssetInfo) => boolean` |
| D74 | 17 Set 2026 | **Migrando si corregge anche una ricerca che sembrava rotta** | Effetto collaterale della cura di D73, trovato leggendo i due `searchText`. `SignalAssetParamControl` mette **valuta e tipo** fra i termini cercabili; `AssetSelect` li **esclude deliberatamente**, con la ragione scritta nel codice: valuta e tipo sono proprietà condivise da centinaia di righe, quindi una query che è prefisso di una di esse — `eur`, `bon`, `etf` — **restituiva l'intero elenco**, e la ricerca sembrava cominciare a funzionare solo dalla quarta lettera. *«Un identificatore nomina uno strumento; una valuta lo descrive.»* Il picker dei segnali ha oggi quel difetto e nessuno l'aveva collegato alla sensazione di ricerca inutile |
| D75 | 17 Set 2026 | **Nasce il mandato N: le acquisizioni decise non avevano un proprietario** | Trovato verificando i dieci mandati contro il registro. **D38** (NEA e diversification ratio, KPI di L2), **D45** (`WR`, KPI di L1) e **D56** (MDD, DaR, CDaR, UCI in variante `_Rel`) erano **decise e assegnate a nessuno**: il mandato A le espelle esplicitamente nella sezione *Fuori* — «qualunque acquisizione … sono funzionalità nuove, non migrazione» — mentre il mandato E costruisce L1 e L2 **assumendo che quei campi arrivino nel payload**. Verificato con `grep` su tutto `backend/app`: `diversification` **zero occorrenze in tutto il backend**, `worst_realization`/`worst_day`/`worst_return` **zero** in `risk/`, `risk_plugins/` e `schemas/risk.py`, `herfindahl` presente **solo dentro AI Export**. Senza questo mandato E avrebbe scoperto a metà lavoro che tre dei KPI da disegnare non esistono, e A sarebbe già stato chiuso. **D39 (`Sharpe(rm=…)`) resta ad A**: pretende la matrice dei rendimenti, cioè esattamente l'idraulica mancante di M5 — chi fa M5 ha già pagato metà del lavoro, duplicarla sarebbe farla due volte. Lane `6248`, taglia M, nessuna dipendenza in ingresso |
| D76 | 17 Set 2026 | **Riskfolio restituisce magnitudini positive, il nostro schema usa negativi** | Misurato su 750 rendimenti: `WR` = **+0,03851726** mentre `min(r)` = **−0,03851726**; `MDD_Rel` +0,5499, `DaR_Rel` +0,5208, `CDaR_Rel` +0,5308, `UCI_Rel` +0,3490 — tutte positive. Ma `RiskKpiOutput.max_drawdown` è dichiarato `Field(..., le=0)` (`schemas/risk.py:644`): **deve essere negativo o zero**. Passare una funzione direttamente al campo fa fallire la validazione, ed è il caso **fortunato**. Il caso sfortunato: qualcuno «risolve» invertendo il vincolo su **un campo solo**, e dentro lo stesso oggetto convivono due convenzioni di segno opposte — la UI le disegna una sotto l'altra e **nessun test se ne accorge**, perché ogni campo è internamente coerente. È la stessa classe di difetto del CVaR (D28): una grandezza plausibile e sbagliata. Regola: **una convenzione per output**, dichiarata nel contratto K8, con un test che la afferma per **ogni** campo nuovo |
| D77 | 17 Set 2026 | **NEA deve coincidere con l'Herfindahl di AI Export: non è una cortesia, è un vincolo** | AI Export calcola già la concentrazione in `ai_export/components/portfolio_financial.py:208`, con semantica dichiarata nel campo: *«Sum of squared nav_weight_percent across all positions. 10000 is fully concentrated in one position»* e *«Cash is included in the denominator but is not itself an HHI term»*. L'identità è **esatta e verificata numericamente** su `w = [0,5 · 0,3 · 0,15 · 0,05]`: `NEA(w)` = `1/Σwᵢ²` = `10000/HHI_points` = **2,73972602739726** per tre vie, con HHI = 3 650. Non c'è quindi alcuna libertà di definizione: sono la stessa grandezza in due unità. Ma **una scelta semantica vera resta**, e va presa esplicitamente: la cassa entra o no nel denominatore? Se il rischio sceglie diversamente da AI Export, **la stessa applicazione dice due numeri diversi sulla concentrazione dello stesso portafoglio** e l'utente non ha modo di sapere quale credere. Si adotta la convenzione di AI Export, o si diverge con una ragione scritta e una nota in documentazione |
| D78 | 17 Set 2026 | **Il file di mandato è un brief in sola lettura; il piano vivo sta in `progress/`** | Difetto trovato rileggendo `implementation/README.md` §5.5, che diceva «ogni mandato apre il proprio file di piano in `implementation/`» — ma **i mandati stanno già lì con quei nomi**. Un agente che avesse seguito l'istruzione alla lettera avrebbe aperto il proprio brief e cominciato a spuntarci sopra i passi, **distruggendo l'unica copia delle proprie istruzioni**, e se ne sarebbe accorto solo dopo un azzeramento di contesto — cioè nel momento peggiore. Cura: il brief non si modifica (se deve cambiare, lo cambia il coordinatore e lo comunica); il piano vivo è un file **nuovo**, `implementation/progress/<LETTERA>-esecuzione.md`, aggiornato dopo ogni passo con `Note implementazione` e `Fuori pista` |
| D79 | 17 Set 2026 | **I contratti si materializzano in `contracts/`, di proprietà del coordinatore** | I mandati girano in **worktree separati**: un file scritto da A nel suo worktree **non è visibile a E**, e finché non c'è un commit integrato non condividono nemmeno la storia. Quindi un contratto non può essere consegnato «mettendolo in un file» — e se resta solo dentro un messaggio fra sessioni **sparisce al primo azzeramento di contesto**, esattamente quando serve. Regola: il produttore **comunica**, il coordinatore **scrive** nel proprio worktree, il consumatore **riceve**. Un file per contratto, `K1.md`…`K8.md`. Effetto secondario e altrettanto utile: un contratto **cambiato** diventa un diff invece che un ricordo contraddittorio |
| D80 | 17 Set 2026 | **Tre fatti misurati sul bootstrap di un worktree, e uno che costa 3,8 GB** | (1) **I data dir sono già isolati per worktree**: `get_project_root()` è `Path(__file__).parent.parent`, cioè relativo al file, quindi ogni worktree risolve il proprio `backend/data/…`. **La porta è l'unica risorsa davvero globale**, e le lane servono per quella. (2) **`.env` non serve**: nessuno dei cinque worktree attivi ne ha uno, solo il checkout principale — quindi la sua assenza è corretta e non va mai colmata copiandolo da un altro checkout. (3) **Le lane non toccano la produzione**: `DEFAULT_PROD_DATA_DIR` è `backend/data/prod`, **non** `backend/data`, quindi `backend/data/test-risk-*` non si sovrappone e la guardia passa. (4) Il costo vero è **`node_modules`**: **636 MB** misurati, in `.gitignore`, assente in un worktree nuovo. I sei mandati frontend (B, D, E, F, G, J) non possono chiudere la propria definizione di finito senza, e il contratto vieta ai figli di installare dipendenze: lo installa il **coordinatore**, `npm ci` dal lock, uno per volta. Sei worktree frontend sono ~**3,8 GB** su un disco misurato al **90%** con 42 GiB liberi — sostenibile, ma da verificare prima dell'ondata 2 |
| D81 | 17 Set 2026 | **Anche gli spec di test hanno proprietari, e `risk-analysis.spec.ts` va diviso da D** | Secondo buco trovato nella verifica: la mappa di proprietà copriva i file **sorgente** e non quelli di **test**. `e2e/portfolio/risk-analysis.spec.ts` conta **817 righe in un solo `test.describe`**, e i suoi sei test appartengono a tre padroni: tre sono di **E** (dashboard `:587`, unavailable `:619`, broker tab `:687`), uno è di **F** (asset global `:628`), e **due coprono Asset Detail** (`:698`, `:719`) che è parcheggiato per D8 e D47. Sopra, ~580 righe di impalcatura condivisa (`installRiskMocks`, `resultFor`, `definition`, `openDashboardRisk`, `brokerWithHoldings`). Senza divisione E ed F riscrivono lo stesso file nello stesso momento e lo scoprono a lavoro fatto. **Lo divide D**, perché è l'unico mandato che gira prima di entrambi ed esiste proprio per preparare loro il terreno: `risk-mocks.ts` (impalcatura, di E, consumata da F), `risk-analysis.spec.ts` (E), `risk-lab.spec.ts` (F), `risk-asset-detail.spec.ts` (**di nessuno**). Vincolo: solo spostamento, stesso numero di test prima e dopo, nessuna asserzione cambiata. Gli altri due spec contesi restano da coordinare senza divisione: `dashboard.spec.ts` (210 righe, G ed E) e `asset-list.spec.ts` (692 righe, B ed F) |
| D82 | 17 Set 2026 | **I due test su Asset Detail non sono un residuo: sono la rete** | Corollario di D81, e va scritto perché l'istinto opposto è fortissimo. Asset Detail resta fuori ambito (**D8**, **D47**) e deve restare **identico**: quei due test sono l'unica cosa che lo **dimostrerà** quando E ed F avranno finito. Vanno isolati in un file proprio perché nessuno sia tentato di «adattarli per farli passare» — adattarli cancellerebbe esattamente la prova che si sta cercando. Verifica in J: `git log` su quel file mostra **solo** lo spostamento fatto da D |
| D83 | 17 Set 2026 | **`resultFor` codifica la forma del payload: K1 e K8 lo rendono stantio** | Terza scoperta della verifica, e la più insidiosa perché non produce alcun rosso. L'impalcatura di `risk-analysis.spec.ts` (`:210-463`) costruisce le risposte finte del backend, cioè **congela la forma del payload**. Quando A consegna K1 (serie underwater, bin dell'istogramma) e N consegna K8 (WR, famiglia drawdown, NEA, diversification ratio), quel mock va aggiornato — altrimenti i test **passano** servendo una forma che il backend non produce più. **Un mock stantio non fallisce: rassicura.** È la stessa famiglia del CVaR rimasto sbagliato per un anno (D28) e dell'`undefined_windows` che può sparire senza rossi: un sistema che sembra coperto e non lo è. Clausola aggiunta ai contratti K1 e K8 |
| D84 | 17 Set 2026 | **G invalida gli screenshot della galleria, che non possiede: li rigenera J** | Dipendenza invisibile, non un conflitto — nessuno scrive lo stesso file. `e2e/gallery.spec.ts:605` genera *«dashboard allocation charts — all languages and themes»*, cioè le immagini della documentazione. Il mandato **G** cambia i colori di quei due grafici, quindi le immagini mostrano una tavolozza che il prodotto non usa più. G **non** deve rigenerarle: fotograferebbe uno stato intermedio, prima che gli altri mandati frontend siano integrati. Le rigenera **J**, dopo l'integrazione, e G si limita a **dichiararlo** nel checkpoint |
| D85 | 17 Set 2026 | **`primaryAssetType` restituisce il CONTENUTO, non il contenitore** — chiude la contraddizione F14 trovata da B | Q12 diceva due cose incompatibili e K2 è una firma sola: «il caso che migliora» implicava `ETF_STOCK → STOCK` (contenuto), la «trappola di denominazione» implicava `ETF_STOCK → ETF` (contenitore), e D71 col contenitore lascerebbe un portafoglio tutto-ETF come **una fetta sola sfumata** — cioè proprio ciò che Q12 voleva eliminare. **Decisione: contenuto.** Discende da L2, che è il fondamento approvato: per un portafoglio tutto in ETF il contenitore non dice nulla — che siano ETF l'utente lo sa già — mentre l'esposizione è ciò che non sa. Il contenitore è un involucro, non un'esposizione. D71 ne guadagna: la sfumatura dentro `STOCK` distingue azionario **diretto** da azionario **via ETF**. E il contenitore non sparisce: resta la **pastiglia** in liste e tabelle, dove la domanda è «che strumento è questo?». Due domande diverse in due posti diversi. **Una funzione sola**: una seconda non consumata marcirebbe |
| D86 | 17 Set 2026 | **`ETF_STOCK`, non `ETF_EQUITY`** | Scelto da B con la motivazione decisiva, migliore delle due che avevo messo in tabella (coerenza col tipo base contro uso finanziario corrente, come se fosse gusto): **`ETF_EQUITY` nominerebbe un tipo base che non esiste**. D61 dice che il secondo livello *è* l'insieme dei tipi base, e la base si chiama `STOCK`: chi incontra `ETF_EQUITY` cerca `EQUITY` e non trova nulla — **esattamente la trappola del genitore inesistente** per cui K2 vieta `split('_')`. Reintrodurla nel nome sarebbe un autogol. L'uso finanziario si soddisfa nell'**etichetta** («Equity ETF»), unica cosa che l'utente vede. E l'insieme resta verificabile meccanicamente contro l'enum base, che è ciò che il test del cancello afferma. Enum a 17 valori |
| D87 | 17 Set 2026 | **M2 cambia VaR **e** CVaR, e «0,27%» era una media non un limite** — corregge la voce di CHANGELOG | Misurato da A su 400 campioni per livello e **verificato indipendentemente dal coordinatore**. Due errori nella formulazione che avevo scritto in `07` §6 e `J` §3.1. (1) **Non cambia solo il CVaR**: l'off-by-one è lo **stesso indice** che produce il VaR, e la card ne mostra due (`RiskAnalysisPanel.svelte:880-881`). Nella mia ricostruzione indipendente il VaR differisce **400/400** a ogni livello. (2) **«0,27%» valeva solo al 95%**: `confidence_level` è `gt=0, lt=1` e lo sceglie l'utente; al **99%** lo scarto peggiore è di un ordine di grandezza superiore, perché più stretta è la coda meno osservazioni la compongono e più esplode l'errore dello stimatore ingenuo. Pubblicare una percentuale unica **rassicurerebbe proprio chi usa il 99%**, cioè il più esposto. La voce dichiara l'ordine di grandezza per il default e dice che cresce con la confidenza |
| D88 | 17 Set 2026 | **GJR-GARCH non è calibrabile da QuantLib: il livello 2 diventa condizionato** | Il brief H diceva «GJR-GARCH nativo QuantLib, calibrabile dalla sola serie prezzi». **Falso**, misurato da H e verificato dal coordinatore su QuantLib 1.43: `hasattr(ql,'Garch11')` → **False**; `GJRGARCHModel.calibrate` prende **helper di opzioni** (stessa API di `HestonModel`); `issubclass(GJRGARCHProcess, StochasticProcess1D)` → **False**, `factors()` = 2 mentre l'architettura è uno `StochasticProcessArray` di componenti 1-D. Tutti i nomi `*GARCH*` esposti sono **engine di opzioni**. QuantLib regala la *simulazione*, non la *calibrazione dai prezzi* — e l'obiezione con cui lo stesso brief rinviava il livello 5 («richiede dati di opzioni») **si applica identica** al livello 2. Esiste `arch 8.0.0`, che calibra in 22 ms, ma è una **transitiva di riskfolio-lib non dichiarata nel Pipfile**: appoggiarcisi è la fragilità silenziosa che questa campagna continua a trovare. **Decisione del developer**: promuovere `arch` a diretta, o rinviare il livello 2 accanto ai 4-5 ⚠️ **Spiegazione corretta il 18 Set 2026, su rilievo di I: il fatto regge, il motivo che il coordinatore aveva scritto no.** Era stato detto che «QuantLib espone GARCH solo come engine di opzioni», ma **il motore non passa affatto da un engine**: `quantlib_worker.py` usa `ql.GeometricBrownianMotionProcess` → `ql.StochasticProcessArray` → **`ql.GaussianMultiPathGenerator`**. Versione precisa: **la generazione di cammini richiede uno `StochasticProcess`, e i binding Python non espongono alcun processo della famiglia GARCH** — i nomi GARCH esistenti sono **engine di pricing**, un'astrazione diversa che **non si innesta in `GaussianMultiPathGenerator`**. 📍 Nelle parole di I, che è la ragione per cui la correzione conta: **«una pagina che spiega male il motivo giusto è la cosa che poi nessuno ricontrolla»** — la conclusione è vera, quindi nessuno risale al ragionamento. 🔴 **SECONDA correzione, 18 Set 2026 — anche la prima correzione era falsa.** **H ha rotto il proprio freeze per segnalarlo**, e il coordinatore ha **eseguito sui binding**: `nomi GARCH in ql: AnalyticGJRGARCHEngine · GJRGARCHModel · **GJRGARCHProcess** · MCEuropeanGJRGARCHEngine …`; `GJRGARCHProcess` **MRO** = `['GJRGARCHProcess', 'StochasticProcess', 'Observable', 'object']`, **`issubclass(…, ql.StochasticProcess) -> True`**, `issubclass(…, ql.StochasticProcess1D) -> False`, `hasattr(ql, 'Garch11') -> False`. → **`GJRGARCHProcess` ESISTE ED È UNO `StochasticProcess`**: la versione «i binding non ne espongono alcuno della famiglia GARCH» **lo nega esplicitamente ed è falsa**. ⚠️ **Vecchia e nuova sono lo stesso errore in direzioni opposte**: entrambe spiegano il rinvio con un'**assenza** — *«esiste solo come engine»* / *«non esiste come processo»* — mentre **l'ostacolo non è un'assenza: è un disallineamento di tipo e di arietà di una classe che c'è**. 🔑 **E la seconda è PEGGIORE della prima**, per la ragione che I stesso aveva enunciato al contrario: *«una pagina che spiega male il motivo giusto è la cosa che poi nessuno ricontrolla»* — **questa invece la ricontrollano**, perché è falsificabile in una riga (`[n for n in dir(ql) if 'garch' in n.lower()]`): chi riapre il caso vede `GJRGARCHProcess`, legge che «non esiste», e **conclude che l'ostacolo era immaginario e il rinvio infondato — rifacendo il lavoro credendo di smentirlo**. *«La vecchia faceva perdere il ragionamento; questa fa perdere la conclusione.»* ✅ **TESTO CORRETTO — due muri INDIPENDENTI**: **(a) arietà** — il generatore compone `StochasticProcessArray`, che accetta **solo componenti `StochasticProcess1D`**, e `GJRGARCHProcess` ha **`factors() == 2`** → `TypeError` SWIG; **(b) calibrazione** — nessun modo di stimare i parametri **da una serie di rendimenti**: `ql.Garch11` non è esposta e `GJRGARCHModel.calibrate` richiede **quotazioni di opzioni**. ⚠️ **(b) resta in piedi anche risolvendo (a)**, ed è la ragione della precondizione `arch` nel `Pipfile` — *«chi aggirasse l'arietà crederebbe di aver finito, e sbatterebbe sulla calibrazione dopo aver riscritto il generatore»*. 📍 **Tre correzioni in cascata sullo stesso fatto — coordinatore, I, H — e solo la terza è stata ESEGUITA sui binding invece che letta.** ✅ La riga di tabella in `TODO_FUTURI.md` (*«rinviato — non calibrabile da QuantLib»*) **è giusta**: nomina il **muro 2**, quello che davvero non si aggira. |
| D89 | 17 Set 2026 | **`check-links` non controlla i `DocsLink`: il gate di D55 era cieco** | Scoperto da I, verificato dal coordinatore. **Due cause indipendenti.** (1) **Codice morto**: in `dev.py:1119-1121` lo scope «1b» ha perso il ciclo `for i, line in enumerate(...)` e il corpo è finito **sotto un `continue`** — 15 occorrenze di `/mkdocs/` nel frontend non validate. (2) Anche riparando 1b, `DocsLink` resta invisibile: la prop è `path=` (non `docsPath=`) e il valore **non contiene `/mkdocs/`**, lo aggiunge `getDocsUrl()` a runtime — 9 `<DocsLink>`, 4 path distinti, **zero validati**. D55 esisteva proprio per impedire che il gate diventasse qualcosa che si impara a ignorare: era verde **perché non guardava**. Riparazione assegnata a **I** (sezione mkdocs di `dev.py`, che nessun altro tocca), e **precede il primo tempo** |
| D90 | 17 Set 2026 | **Due estensioni di confine concesse ad A, entrambe inevitabili** | (a) **I quattro `signal_plugins/rolling_*.py`**: `rolling_single_values`/`rolling_pair_values` sono funzioni di **ordine superiore** e tutti e quattro i chiamanti passano lambda che catturano parametri a runtime (`rolling_beta` passa una funzione nuda a due argomenti). **Non si vettorializza una lambda arbitraria**: o si aprono quei file, che non erano in nessuna tabella di proprietà, o M1 non esiste — cioè si rinuncia a **1 514×** sul percorso più caldo. (b) **`risk_plugins/historical_var.py` e `drawdown_summary.py`**: sono esattamente i due file che costruiscono gli output di K1, e un campo di schema senza produttore è peso morto. §2.6 li aveva dati a N, che però scrive `historical_kpi.py` e `risk_contribution.py`. Nota di metodo: A li ha **chiesti invece di presi**, pur avendone bisogno per il contratto che blocca E |
| D91 | 17 Set 2026 | **`formatCurrencyAmount` resta: non era un doppione** | I brief D §5 e E §6 ordinavano di sostituirlo con `formatCurrencyAmountPlain`. **Falso**, verificato: `formatCurrencyAmount` fa narrowing di array (`singleValue`), guardia `Number.isFinite → '—'` e `Intl.NumberFormat style:'currency'` → `$1,234.50`; `formatCurrencyAmountPlain` (`utils/currency/currencyFormat.ts:30`) prende un `number` e produce `1,234.50 $ 🇺🇸 USD`. Stringhe **diverse**, due comportamenti **persi**, e sei test in `riskAnalysisHelpers.test.ts:282-305` da cancellare. Non era de-duplicazione: sarebbe stato un **cambiamento visibile all'utente** che nessuno aveva chiesto. Corollario verificato nello stesso giro: `KpiDivergingFlowBar` **non è drop-in** (due magnitudini, colori fissi, `h-1.5`, nessun `testId`) → estenderla è del mandato **D**, non di E |
| D92 | 17 Set 2026 | **`schemas/risk.py` ha quattro scrittori, non tre** | Difetto della mappa del coordinatore, trovato da H. §2.2 dichiarava A, C, N e ometteva **H**, che scrive gli schemi di parametri della simulazione **nello stesso file** (`:136-142`, `:172-182`, `:836-880`). Le regioni non confinano e Git fonde pulito, ma **`__all__` (`:1067-1126`) diventa un conflitto a quattro vie**: la regola di risoluzione meccanica (unione + riordino alfabetico) si estende. Nello stesso giro: `frontend/src/lib/risk/**` risultava non assegnato in §2.1 pur essendo già di **E** nel suo brief — riga mancante, aggiunta |
| D93 | 18 Set 2026 | ⚠️ **PARZIALMENTE RITRATTATA — vedi D101.** La diagnosi del meccanismo regge; **la stima di gravità è falsa** perché costruita sull'identità `f = 365·c`, che sul ramo dove il difetto morde **non vale**. Titolo originale: *«Il tasso privo di rischio ha un `365` cablato: l'errore è ZERO sui dati sani e inverte il segno su quelli poveri»* | Trovato da **I** mentre scriveva `sharpe-ratio`, confermato da **A** — che però ha **smentito la diagnosi del coordinatore**. `metrics.py:144` fa `expm1(log1p(annual_rate)/365.0)` e il risultato è sottratto a **ogni** rendimento di periodo (`:158` Sharpe, `:172` Sortino), mentre `annualization_factor` entra solo alla fine con `sqrt()`. Il coordinatore aveva girato «su serie settimanali o mensili si sovrastima»: **falso**, `RiskDataFrequency` ha un solo valore, `DAILY` — quel caso è **impossibile** in Release 2, e documentarlo sarebbe stato un difetto che nessun utente può osservare. Il posto dove morde è peggiore: `service.py:871` dà `f = len(returns)*365/calendar_days` e `coverage = len(returns)/calendar_days`, quindi **`f = 365 × coverage`** algebricamente. **A copertura piena il 365 cablato è esattamente corretto — errore zero, non piccolo**: ecco perché è sopravvissuto alla migrazione dal 252, le due costanti coincidono sui dati sani e nessun test poteva vederlo. **Il difetto si attiva esattamente quando la qualità del dato peggiora, e lusinga sempre il Sharpe**: a `coverage = 2/3` con rf 5% il sistema pubblica **+0,1014** («hai battuto il tasso privo di rischio») dove il vero è **−0,0053** — **il segno si inverte**. Cura: `period_rate = expm1(log1p(annual_rate)/annualization_factor)`, tre righe, e a copertura piena **nessun dato sano si muove di un bit**. ⚠️ Formulazione per il CHANGELOG: **«può invertire il segno», mai una percentuale** — vicino allo zero il rapporto esplode (+2028% misurato, artefatto del denominatore). ⚠️ A ha dichiarato il limite della propria misura: la relazione è verificata sul codice e sulle fixture, **non su un portafoglio reale** — misurerà la copertura di produzione in A3, «sono un modello, non un'osservazione» |
| D94 | 18 Set 2026 | **Una correzione che DEVE muovere i numeri non entra in un passo che esiste per dimostrare che non si muovono** | Argomento di **A**, rifiutando di infilare D93 dentro M6, e vale oltre il caso. *«M6 è un passo la cui proprietà definente è che i numeri NON devono muoversi. Se ci infilo una correzione che deve muoverli, distruggo la rete: davanti a un valore cambiato non saprei più distinguere una regressione della vettorializzazione dalla correzione voluta.»* Quindi: M6 atterra con l'oracolo che afferma l'invarianza, **poi** la correzione atterra isolata, e ogni numero spostato ha **una sola causa possibile**. Non prima di M6, o si misurerebbe l'invarianza di M6 contro una base appena spostata. **Regola generale**: quando due cambiamenti hanno effetti attesi opposti sull'osservabile — uno invariante, uno no — **non condividono un passo**, per quanto adiacenti siano nel codice |
| D95 | 18 Set 2026 | **Il nav della documentazione rispecchia le quattro domande, e L1 si divide in percorso contro distribuzione** | Due decisioni di **I**, la seconda non prevista da nessun documento. (1) Le sezioni dei docs **sono le quattro domande**, non i temi: I aveva raggruppato per tema — «Portfolio Risk», «Benchmark Comparison» — e la verifica contro `03-mappa-livelli-pagine.md` ha mostrato il danno, cioè che Sharpe, Sortino e beta appartengono alla **stessa** domanda (L3) e finivano in **due sezioni diverse**. Un utente che clicca il `DocsLink` da *«sono pagato per il rischio?»* sarebbe atterrato in due posti diversi per due metriche della stessa domanda: **il nav dei docs avrebbe insegnato una spina dorsale diversa da quella della UI**. (2) Dentro L1, le otto pagine si dividono in **misure di percorso** (max drawdown, current drawdown, DaR, CDaR, UCI — dipendono dalla *sequenza*) e **misure di distribuzione** (VaR, CVaR, worst realization — guardano i rendimenti come insieme, ignorando l'ordine). La ragione, nelle parole di I: *«rimescola i rendimenti e il VaR non cambia di una virgola; il max drawdown può raddoppiare»*. È la differenza fra «quanto può andare male un giorno» e «quanto può andare male una **storia**», e otto numeri in fila tutti etichettati «rischio» non danno all'utente modo di sospettarla. Colloca anche `UCI`: chiude la famiglia del percorso perché è l'unica che combina **profondità × durata**, le due dimensioni che le altre quattro misurano separatamente |
| D96 | 18 Set 2026 | ⚠️ **VALE SOLO PER IL RAMO TWRR — vedi D101.** Sul ramo preparato (ambito titolo) `coverage` ha un denominatore **diverso** e la conclusione non si trasferisce. Il cuore resta vero e utile: *«La copertura è 1,0 per costruzione: misurarla sarebbe una conferma tautologica»* | Trovato da **I** seguendo l'origine della serie, verificato dal coordinatore. `portfolio_engine` costruisce `daily_states` avanzando `current += timedelta(days=1)` **senza filtro sui feriali**, e `build_history()` emette un punto per stato: `report.history` **non è una serie di giorni di mercato, è una griglia di calendario**. Quindi `return_dates[-1] = baseline + N giorni`, `D = N` **esattamente**, e da `service.py:871-872`: `c = min(1, N/N) = 1,0`, `f = 365N/N = 365`. **Non «circa»: esattamente.** Due conseguenze. (1) Il `365.0` cablato di D93 non è «giusto sui dati sani» ma **giusto in ogni caso osservabile del percorso di portafoglio** — il che spiega perché nessun test poteva vederlo: il difetto ha ampiezza **esattamente nulla** lungo la strada che i test percorrono. (2) ⚠️ **A3 rischiava una conferma tautologica**: misurare la copertura e trovare ≈1,0 non è evidenza che i dati siano buoni, è **la griglia di calendario che si guarda allo specchio** — resta alta anche con prezzi assenti o stantii, perché il motore emette comunque il punto portando avanti l'ultimo prezzo noto, e traccia la qualità **altrove** (`nav_complete` a due righe dal ciclo, `missing_price_asset_ids`, `stale_price_asset_ids`). **La domanda utile non è «quanto vale la copertura» ma «in quali casi reali `D > N`»**, gli unici in cui il difetto ha ampiezza diversa da zero. Avvisato A **prima** che misurasse |
| D97 | 18 Set 2026 | ✅ **DIREZIONE CONFERMATA, MOTIVO SOSTITUITO — vedi D101.** La fetta è lusingata **non** perché abbia `coverage < 1`: può segnare `1,000` ed essere comunque distorta. Titolo: *«Il difetto del tasso non tocca il totale: tocca solo la fetta, e la fa apparire migliore»* | Conclusione che **nessun mandato poteva raggiungere da solo**, emersa combinando **C** e **I** tramite il coordinatore — il primo caso della campagna. C aveva trovato che fetta e totale prendono `coverage` da **due sorgenti diverse**, non due valori: in HISTORICAL non affettato il ramo TWRR **sovrascrive** `coverage` con quella del report di portafoglio (`service.py:625-632`), mentre in HISTORICAL affettato resta `prepared.calendar_coverage`, il calendario congiunto **della sola fetta**. C si aspettava, come **attesa ragionata e dichiarata non misurata**, che la fetta avesse copertura maggiore e quindi apparisse **peggiore**. Con D96 la direzione **si inverte**: il totale sta a `c = 1,0` — il massimo — quindi **non è distorto affatto**; la fetta sta a `c ≤ 1` ed è **l'unica distorta**; e poiché `f = 365c`, un `c` minore significa tasso di periodo maggiore, quindi usare 365 sottrae **troppo poco** e **lusinga**. → **la fetta appare sistematicamente migliore**, cioè *affettare sembra migliorare le cose*. ⚠️ Registrata come **implicazione da verificare**, non come fatto: la misura è di A. Conseguenza operativa di C, confermata: **la correzione di D93 non è pulizia indipendente, è prerequisito perché L3 fetta-contro-totale sia attendibile** |
| D98 | 18 Set 2026 | **`test-author` scrive, il mandato ESEGUE: un `FROZEN` non si dichiara su «test scritti e lintati»** | Regola della campagna, formulata da **A** dopo che il suo oracolo ha dato **38 passati su 39 al primo giro reale**. Il rosso era un difetto **del test**, non del prodotto: `zip(edges, edges[1:], strict=True)` su due sequenze che differiscono di uno **per costruzione**. *«`test-author` non poteva vederlo: non ha la lane e non esegue la suite. È il costo strutturale della separazione «chi scrive i test ≠ chi li esegue» — e funziona solo se l'esecuzione avviene davvero. Un `FROZEN` dichiarato su «test scritti, ruff pulito» sarebbe stato falso.»* Nello stesso giro A ha trovato tre difetti nel proprio istogramma, il peggiore dei quali è la stessa famiglia del gate cieco: su una **serie costante** la guardia `width <= 0.0` **non scattava mai**, perché NumPy imbottisce di ±0,5 un campione a range nullo e `histogram_bin_edges` restituisce `1.0`, positivo e finito — **il ripiego documentato era codice morto dal primo giorno**, e sarebbe arrivato all'utente come una barra larga quanto l'asse |
| D99 | 18 Set 2026 | **Un `ReferenceError` in un `.svelte.ts` non fallisce dove nasce: si vede come pagina che non si monta** | Trovato da **E** estraendo il controller headless da `RiskAnalysisPanel`, e il sintomo **non somigliava alla causa**: Playwright dichiarava `dashboard-risk-tab` non trovato, `broker-risk-tab` non trovato, `asset-detail-risk-panel` non visibile — cioè **spariva la pagina**, mentre E aveva toccato **solo il pannello**. Causa reale: la factory leggeva i propri input **subito** (`let lastRefreshVersion = untrack(() => inputs().refreshVersion)`), ma gira **mentre lo `<script>` del componente ospite sta ancora eseguendo**, e quella closure legge uno `$state` dichiarato **più in basso nel file**. Uno `$state` di Svelte 5 **compila in un `let`** → **temporal dead zone** → `ReferenceError` durante l'init → il componente non si monta → **la pagina intera non compare**. ⚠️ **La correzione che E ha scartato è la parte che vale**: spostare la creazione del controller sotto le dichiarazioni avrebbe fatto passare tutti i test **lasciando la trappola armata per il prossimo chiamante** — che è E stesso, quattro volte, nei livelli. Scelta invece la proprietà del componente: *«il controller non tocca i suoi input finché non parte il primo effetto»*, perché **«un componente che esplode a seconda dell'ordine di dichiarazione di chi lo usa è un difetto del componente»**. Test verificato **per mutazione**: conta le letture della closure durante la costruzione e pretende **0**; rimettendo la lettura eager → `1 failed, 10 passed`. **Diffuso a G e F**: chi estrae uno store o un helper e vede sparire una pagina deve cercare **dieci righe più in là e in un altro file**. G ne è immune **per costruzione** (helper puro, nessuna runa) |
| D100 | 18 Set 2026 | **Un cancello che non hai falsificato non è evidenza: è una speranza verde** | Standard di prova stabilito da **E**, il più alto della campagna, e **risposta diretta** al fatto che avessimo già trovato **quattro difese inerti in un giorno** (`check-links` cieco ai `DocsLink`, il mock stantio `resultFor`, l'ambiente vitest stantio, la guardia `width <= 0.0` che NumPy impediva di scattare). Invece di dichiarare «verde», E ha **rotto di proposito ogni rete e verificato che diventasse rossa**: (a) errore di tipo iniettato nel controller → `svelte-check found 1 error … in 3 files`, con riga e file citati → **il gate vede davvero i file nuovi**; (b) rimossa la riga che rilancia le analisi in volo → `1 failed, 9 passed` con «expected "vi.fn()" to be called 2 times, but got 1» → **il test prende il difetto storico se torna**; (c) tolta la direttiva `// @vitest-environment jsdom` → **10 rossi su 10** con il messaggio di `assertEffectsRun()` → **la guardia non è decorativa**. *«In questa campagna abbiamo già trovato un gate cieco: non mi fido di un "0 errori" che non ho falsificato.»* **Regola**: un mandato che consegna una rete nuova ne dimostra la sensibilità **rompendola una volta**, e cita il rosso ottenuto. Un verde da solo non distingue «il codice è corretto» da «il test non guarda». Corollario di metodo, dallo stesso giro: davanti a sei rossi inattesi la **prima** mossa di E non è stata formulare un'ipotesi ma **ripristinare il file originale e rimisurare** — sei verdi — ottenendo così **due fatti certi invece di zero** (la regressione era sua, e la rete funzionava) al posto di tre piste plausibili e false (database, lane, build) |
| D101 | 18 Set 2026 | 🔴 **Il `365` cablato è attivo su dati PERFETTAMENTE SANI in tutto l'ambito titolo, e `coverage` non lo può vedere** — sostituisce la stima di gravità di D93 | **A ha ritrattato la propria stima**, e ciò che ha trovato cercando *dove* leggere la copertura è peggio di quello che cercava: **la variabile è sbagliata**. Verificato dal coordinatore leggendo il codice e rimisurando con le funzioni vere. **(1) Esistono due sorgenti di `coverage` con denominatori diversi.** Ramo TWRR (`service.py:872`): `c = N/D` e `f = 365N/D`, quindi `f = 365·c` — l'identità **regge**. Ramo preparato (`series_preparation.py:345`): `c = n_observations / len(coverage_candidates)` mentre `f` viene da `observed_annualization` (`:78`) che è `n_observations * 365 / calendar_days` — **denominatori diversi, e non convergono mai**. → **«Dove l'identità vale il difetto non morde; dove il difetto morde l'identità è falsa.»** La cifra «a `coverage = 2/3` il segno si inverte» di D93 era ottenuta applicando l'identità **sul ramo dove è falsa**: non descrive nulla di reale, **va buttata**. **(2) Il difetto non aspetta dati degradati: è attivo su dati sani.** `candidate_quote_dates` (`:236`) tiene solo i punti che passano `_price_is_fresh` (`:123-125` → `info is None or info.days_back == 0`), cioè **quotazioni vere, non riportate in avanti**. Per un portafoglio azionario/ETF i weekend sono esclusi **per costruzione**: candidati ≈ 252/anno → `coverage = N/|candidati| ≈ 1,000` (**salute perfetta**) mentre `D = 365` → `f = 365N/D ≈ 252`. **Il campo che l'utente e il sistema guardano per giudicare la qualità segna il massimo esattamente mentre il difetto è al massimo.** **(3) Gravità misurata**, `default_rng(7)`, `annualized_sharpe` e `annualized_sortino` reali, `f = 252`: Sharpe lusingato **+0,034 / +0,051 / +0,084** a rf 2% / 3% / 5%, **stabile su 1, 2 e 3 anni** (distorsione strutturale, non effetto di campione); **Sortino peggio: +0,053 e +0,130** a rf 2% e 5%, perché il bersaglio più basso riduce **anche** la deviazione al ribasso e l'effetto entra due volte nel rapporto. ⚠️ **A rf = 0 l'errore è esattamente zero** — ecco perché nessuno l'ha mai visto: è invisibile a chiunque lasci il tasso al default. Sul ramo TWRR il delta misurato è **1e-16**, rumore di virgola mobile: **il totale non è distorto, mai**. **(4) La lusinga è additiva e sempre positiva**, anche quando lo Sharpe è negativo (a 1 anno: `-2,4087` pubblicato contro `-2,4467` vero). Conseguenza aritmetica, non simulata: **con uno scarto di ≈ +0,05 il segno si inverte per ogni Sharpe vero in `(-0,05 ; 0)`** — una banda stretta, ma **esattamente la banda in cui la domanda dell'utente conta di più**, cioè «ho battuto il tasso privo di rischio?». **(5) La trappola tautologica ha una seconda faccia: le fixture.** `test_series_preparation.py:159-176` asserisce `f == 365,0` su una finestra venerdì→lunedì — per riuscirci **fabbrica quotazioni fresche di sabato e domenica**, un calendario che il mercato non produce. **Nessuna suite esistente può osservare `f ≠ 365`**: il difetto è invisibile da sempre perché la rete guarda un mondo che non esiste. **(6) Peso rivisto**: A9 non è più «una correzione per dati degradati» ma **una correzione che muove ogni Sharpe e Sortino di ambito titolo con rf ≠ 0**. Ordinamento **invariato** (D94: resta dopo M6, isolata) — il principio per cui un passo che deve muovere i numeri non condivide spazio con uno che non deve regge indipendentemente dalla gravità. Per I e J la cifra da scrivere è **«circa 0,03–0,13 di Sharpe/Sortino lusingato, in ambito titolo, con tasso privo di rischio non nullo»**, **mai** una percentuale |
| D102 | 18 Set 2026 | 🔴 **`coverage` rassicura esattamente quando dovrebbe mettere in guardia — e il campo onesto è a due righe di distanza** | Conseguenza di presentazione di D101, vista da **C** e verificata dal coordinatore. Da D101 sappiamo che sul ramo preparato `coverage ≈ 1,000` proprio mentre `f ≈ 252` invece di 365, cioè **il campo che segnala la salute segna il massimo mentre la distorsione è al massimo**. C ha aggiunto il pezzo che mancava: **`coverage` è nel contratto** — `RiskResultMetadata.coverage` (`schemas/risk.py:450`), `Field(..., ge=0, le=1)`, **obbligatorio**, quindi E lo riceve sempre e può renderizzarlo. Se finisce a schermo come «qualità dei dati» accanto a uno Sharpe lusingato, **rassicura l'utente precisamente quando dovrebbe metterlo in guardia**. Non è un difetto nuovo: è D101 visto dal lato della presentazione — ma è il lato che l'utente vede. ✅ **E la cura è già nel payload**: `annualization_factor` sta **a due righe** (`:449`), ed è il numero che sa la verità — `252` contro `365` è il campanello diretto, senza inferenze. **Regola per E**: (a) `coverage` **non** si presenta come indice di qualità accanto a Sharpe/Sortino finché A9 non è fuso; (b) se serve mostrare la salute della serie in ambito titolo, **il campo da guardare è `annualization_factor`, non `coverage`**. Registrato da C come punto 4 dei quattro obblighi di dichiarazione di K4 |
| D103 | 18 Set 2026 | ⚠️ **`\| tee log \| head` tronca il log stesso, e il log SEMBRA completo** — la regola del progetto va usata solo con `tail` | Trovato da **D** mentre verificava `front format`, e riguarda **le istruzioni del progetto**, non un mandato. Il pattern documentato è `<comando> 2>&1 \| tee /tmp/x.log \| tail -n 100`, ed è **sicuro** perché `tail` consuma tutto l'input. Ma con `head`, `grep -m` o qualunque consumatore che chiude presto, il consumatore esce, il produttore riceve **SIGPIPE** e muore **a metà** — e `tee` smette di scrivere. **Il danno è che il file non si interrompe a `N` righe**: si interrompe a un confine di buffer, quindi ha l'aria di un log intero. Misurato dal coordinatore su 20 000 righe: `\| tee log \| head -5` lascia nel file **9 122 righe su 20 000** (54 % perso, nessun errore, nessun troncamento visibile); `\| tee log \| tail -5` le lascia **tutte e 20 000**. Il caso reale di D era peggiore del test: 505 righe tutte `(unchanged)`, apparentemente un successo pulito, e **zero file `e2e/`** — mancava proprio la parte che doveva verificare. Rilanciato senza `head`: **768 file, 98 in `e2e/`**. **Regola**: su un produttore lungo, redirigere su file e **leggere dopo**, mai filtrare in coda a `tee` con un consumatore che chiude presto. Sesta istanza della famiglia «un controllo che ha smesso di controllare», e la prima che colpisce lo strumento di diagnosi invece del prodotto |
| D104 | 18 Set 2026 | **Una guardia che il template rende già irraggiungibile non è una guardia: è una chiarificazione, e va dichiarata come tale** | Trovato da **D** su sé stesso, sotto mutazione, **prima** di consegnarla come «guardia + test». Aveva messo `isFinite` su `clampedMarker` in `KpiDivergingFlowBar` e stava per riportarla come protezione da un difetto reale. La mutazione ha detto no: **il test resta verde anche togliendola**, perché il template ha già `{#if clampedMarker > 0}` e in JavaScript **ogni confronto con `NaN` è falso** — il ramo non è mai raggiungibile con un valore non finito. Stessa classe di **D51** (un commento che descrive una protezione inesistente), con la differenza decisiva che **qui è stato trovato prima della consegna, dallo stesso autore, perché ha preteso il rosso invece di accettare il verde**. Esito: il test **resta**, ma dichiarato per ciò che è — fissa una proprietà del template, non protegge da un difetto; il commento è stato riscritto. ⚠️ **La guardia sulla larghezza invece è reale**, e la mutazione lo prova: `width: NaN%` viene **rifiutato dal parser CSS**, la dichiarazione **non atterra affatto** e il riempimento **conserva la larghezza precedente** — dato stantio mostrato come corrente. La dashboard non ci arrivava mai perché il suo chiamante divide per `\|\| 1`; i pannelli di rischio che ora riusano le barre **non hanno quella protezione**. Corollario di metodo, sempre da D: **un test può perdere l'elemento proprio nel caso che deve descrivere** — il primo helper cercava i riempimenti con `div[style*="width"]`, ma poiché `NaN%` non atterra l'attributo `style` resta vuoto e **l'elemento esce dalla query**; il rosso arrivava, ma diceva `found 1` invece di nominare il sintomo. Riscritto su classi strutturali |
| D105 | 18 Set 2026 | 🔴 **Un `default` su un helper condiviso cambia superfici che non hai aperto — e la rete che protesta è la prova, non l'ostacolo** | Trovato da **E** aggiungendo `drawdown_summary` a `buildBaseAnalytics`, l'helper che costruisce l'onda base **per tutti gli scope**. `risk-analysis.spec.ts:819` asserisce un **Set esatto** di analytic code per lo scope asset; col default, **Asset Detail iniziava a chiedere al backend qualcosa che nessuno aveva aggiunto ad Asset Detail** — e il cambiamento arrivava **da tre file di distanza**, in un file che nessuno aveva toccato. 🔑 **Il punto non è il difetto, è la tentazione**: il mandato di E vieta esplicitamente di adattare quell'asserzione, e E l'ha rispettato — *«è l'unica prova che la superficie parcheggiata è rimasta ferma. Se l'avessi "sistemata", avrei cancellato la prova e tenuto il difetto.»* **È il momento in cui la maggior parte delle regressioni entra in un progetto**: non scrivendo codice sbagliato, ma **aggiornando il test che protesta**. Un Set esatto sembra una rigidità inutile finché non è l'unica cosa che separa da un cambiamento silenzioso a una pagina mai aperta. Correzione: `includeDrawdownSummary` **opt-in per call site, mai default** — che coincide con la lezione già archiviata in `shared-component-option-changed-globally.md`. **Verificata per mutazione**: rimesso il default → unit `2 failed` («expected [...] to not include 'drawdown_summary'») ed E2E `1 failed, 5 passed` con diff letterale `+ "drawdown_summary"`. **Regola per chiunque tocchi `buildBaseAnalytics`, `riskRequest.ts` o un helper condiviso**: qui c'era una rete; altrove potrebbe non esserci |
| D106 | 18 Set 2026 | ⚠️ **`PASSED` con zero test eseguiti: un filtro con un refuso esce 0 e rassicura** — nona istanza della famiglia | Trovato da **E** al primo tentativo su `front-utility core-unit <filtro>`: `Test Files 80 skipped` · `Tests 1947 skipped` · `tests 0ms` · **`✅ PASSED`**, exit 0. Causa: **`-t` è un pattern sul *nome del test*, non sul file**, quindi un filtro che non combacia con nulla non è un errore — è un insieme vuoto, e un insieme vuoto passa. **È peggio di un gate cieco: un gate cieco almeno esegue qualcosa.** **Regola: si guarda il conteggio, non il banner.** Un `PASSED` con `0ms` e `N skipped` non è un verde, è **un'astensione travestita**. Nella stessa consegna E ha trovato la forma quieta dello stesso difetto: il controller cercava l'analitica `'drawdown'` invece di `'drawdown_summary'`, e **nessun test sarebbe stato rosso** — la pagina avrebbe solo scritto «non disponibile». ⚠️ **Un difetto che si presenta come una funzionalità mancante è il più difficile da vedere, perché assomiglia a un lavoro non ancora fatto** |
| D107 | 18 Set 2026 | **Un importo si mostra se è misurato, si omette se non lo è, e non si stima mai per riempire una colonna** | Regola comune emersa da due mandati che avevano raggiunto la stessa conclusione da lati opposti. **E**: nessun output di rischio porta un importo (solo `stress` ha `impact_amount`), quindi il valore dello scope è una **prop** delle pagine e **quando manca, la colonna denaro non compare** — né trattino né stima. *«Un euro inventato si legge esattamente come un euro misurato.»* **F**: il prezzo unitario in `AssetTable` **deve restare** perché è **anagrafica dello strumento**, non esposizione — ed è per questo che il cancello «nessun euro» era insoddisfacibile alla lettera. Le due metà si compongono: **si omette ciò che non si è misurato, si conserva ciò che è un fatto dello strumento.** Corollario di E, dallo stesso giro e della stessa famiglia: la riga «un mese storto» **non** è la giornata per `√21` — uno scaling in radice **è un modello**, e §5 vieta le stime in L1. Verificato che `horizon_compounded_returns` compone **finestre reali sovrapposte**, quindi il VaR a 21 giorni è un fatto osservato; costa una seconda richiesta e obbliga a `resultByInstance`, perché due risultati con lo stesso `analytic_code` avrebbero etichettato **una perdita mensile come giornaliera** |
| D108 | 18 Set 2026 | 🔴 **Tre `DocsLink` di produzione sono rotti ADESSO, e `check-links` li dichiara tutti validi — il gate sbaglia in due direzioni opposte** | Trovato da **I**, e non è un difetto introdotto da K7: **K7 stava per entrare in una classe di difetto che esiste già** e che il gate ratifica. Verità di terreno, ancora cercata nel sorgente di **ogni lingua**: `create-edit/#importing-a-distribution-csv` (it❌ fr❌ es❌), `scheduled-investment/#how-value-is-calculated` (it❌ fr❌ es❌), `scheduled-investment/#interest-schedule-editor` (**it✅ fr❌** es❌). 🔑 **Il terzo è la prova che non c'è alcun meccanismo**: funziona in italiano e non in francese **perché una traduzione ha prodotto per caso lo slug giusto**. I tre `kpi-cards` invece funzionano in quattro lingue perché qualcuno mise l'ancora esplicita `{: #card-1-period-pl }` — **il progetto sapeva, in un punto, e non l'ha generalizzato**. **E il gate sbaglia anche nel verso opposto**: `dev.py` cerca le ancore esplicite con `\{\s*#(…)\s*\}`, che richiede `{` seguito da `#`, mentre la forma del progetto è **`{: #x }` coi due punti** → su tutta la documentazione ci sono **156** ancore esplicite, **ne vede 14**, **142 invisibili**, e **111 invisibili e non derivabili dal titolo** (52 `user/`, 42 `financial-theory/`, 17 `admin/`) che **verrebbero rifiutate come «anchor not found» pur essendo valide**. Il gate regge solo perché ricade sullo slug derivato, che coincide **quando l'autore sceglie un'ancora uguale al titolo inglese**: una coincidenza di stile, non un meccanismo. **Decisione del coordinatore**: (a) riparare la regex **e** validare in tutte e quattro le lingue; (b) i tre rotti entrano in una **lista di eccezioni nominata e datata nel codice**, ciascuno con le lingue rotte e la ragione per cui non è riparabile qui (serve un'edit su `.it/.fr/.es`, vietata dalla regola «solo inglese»); (c) **qualunque quarto link rotto → rosso**; (d) scritto nel codice che **la lista deve accorciarsi, mai allungarsi** — è l'unica riga che impedisce di «sistemare» un rosso futuro aggiungendoci una voce. Né rosso secco (verrebbe aggirato: nessuno in campagna può riparare quei tre) né avviso (un avviso che nessuno legge è un gate morto, e questo gate era già morto in due direzioni) |
| D109 | 18 Set 2026 | 🔑 **Il difetto dello strumento migra nella sonda costruita per controllare lo strumento** — due istanze in due ore, da due mandati che non si parlano | **I**: la sua sonda dichiarava rotto `kpi-cards/#card-1-period-pl`, che lui stesso aveva **verificato funzionante poche ore prima**. Invece di scegliere il risultato comodo si è fermato per stabilire quale delle due misure fosse falsa: **era la sonda, perché ne aveva copiato la regex da `dev.py`** — ereditando esattamente il difetto che la sonda doveva scoprire. *«Senza quella verifica precedente con cui litigare, ti avrei proposto una patch che marcava tre link sani come rotti.»* **A**, nella stessa ora e senza saperlo: `_TAIL_ALPHA` era `1.0 - _CONFIDENCE_LEVEL`, e riskfolio — ricevendo il valore sottratto — cadeva **sullo stesso indice sbagliato del codice sotto esame**. *«Un oracolo è un oracolo solo se gli si passa il parametro esatto.»* **Regola**: uno strumento di verifica **non condivide codice, costanti o espressioni con l'oggetto verificato**; se le condivide non è un secondo parere, è **la stessa opinione scritta due volte**. E il corollario che ha salvato entrambi i casi: **tenere una misura precedente abbastanza precisa da poterci litigare vale più di una sonda in più** — la contraddizione è il segnale, la concordanza non prova nulla quando le due misure hanno la stessa origine |
| D110 | 18 Set 2026 | **Le ancore esplicite vanno messe PRIMA della pipeline di traduzione: la finestra si chiude e non si riapre** | Decisione del coordinatore su misura di **I**. Nelle 9 pagine `risk-metrics` con heading ci sono **71 titoli e 1 ancora**. Costo di ancorare: **oggi 1 edit in 1 lingua**; **dopo la traduzione 4 edit in 3 lingue che il mandato non può toccare**. I 41 titoli su pagine già tradotte «viaggiano gratis» perché quelle pagine sono già in coda di ritraduzione — ma **gratis solo *se* quella ritraduzione avviene**, ed è una decisione del developer: se slitta, diventano 41 debiti a prezzo pieno. → **Ancorare è economico in entrambi i futuri; non ancorare lo è in uno solo.** Nessuna deroga alla regola «solo inglese»: `{: #token }` in un `.en.md` è inglese, ed è un **identificatore tecnico**, stessa natura di `nav_translations`. ⚠️ **L'urgenza vera**: **16 di quelle pagine sono nate oggi**. Se la traduzione arrivasse prima dell'ancoraggio, **16 pagine × 3 lingue si romperebbero tutte insieme** — nel momento peggiore, quando nessuno saprebbe quale ancora guardare perché sarebbero cambiate tutte. **Latente adesso, catastrofico dopo** |
| D111 | 18 Set 2026 | 🔑 **Quando la verifica è impossibile, l'esito onesto è «non riesco a vederlo», mai «non c'è»** — la trappola strutturale dell'isolamento | **I** ha verificato una relay del coordinatore sulle cinque modalità di simulazione e ha concluso: *«le cinque modalità non esistono … **H ha consegnato i fatti del suo progetto, non il suo codice**»*. **Falso, verificato dal coordinatore nel worktree di H**: `RiskSimulationProcess` ha `BLOCK_BOOTSTRAP` e `GBM`, `RiskSimulationRegime` esiste, `quant/resampling.py` è di **8 437 byte**, `git status` mostra **17 file**, `services risk-simulation` dà **29 passed** su tre esecuzioni identiche. I stava leggendo **la propria baseline**, dove il codice di H per definizione non c'è. **Settima istanza del problema dei due mondi** — la stessa che ha fatto contare 12 link a F dove I ne contava 24, e che ha impedito a H di leggere K6. 🔑 **Ma qui il difetto cambia natura**: il metodo di I era **giusto** — verificare invece di accettare — e ha verificato **nell'unico posto dove la risposta non poteva esserci**; poi ha convertito **un'impossibilità di vedere in un'affermazione su cosa esiste, e su chi l'ha fatto**. ⚠️ **Non è una colpa, è la trappola strutturale dell'isolamento**, e colpisce **di più chi verifica di più**: chi controlla tutto incontra più spesso il confine di ciò che può controllare. **Il contrasto che insegna la regola**: poche ore prima, sulla domanda della traduzione, I aveva separato il fatto dall'inferenza in modo esemplare (*«non sto chiedendo di anticipare: sto dicendo che il costo è cambiato»*); e nella stessa ora **A**, non potendo leggere K1, ha detto *«non posso leggerlo»* e ha **invertito la verifica**, mandando la firma estratta dal proprio codice. → **Regola: davanti a un'informazione non verificabile dalla propria posizione, si dichiara il limite e si inverte la direzione della verifica; non si deduce l'assenza dall'invisibilità** |
| D112 | 18 Set 2026 | **La lunghezza del blocco decide se un max drawdown simulato significhi qualcosa — e non è scritta da nessuna parte** | Nato da una tesi **sbagliata** di I e migliorato dalla sua ritrattazione, quindi vale doppio. I aveva scritto che *«la modalità di default rimescola esattamente ciò da cui dipendono le misure di percorso»* — dedotto correttamente da **una parola sbagliata del coordinatore**, che aveva scritto «ricampionamento congiunto» lasciando intendere un rimescolamento di righe. **Congiunto descrive l'asse *asset*** (righe intere, correlazione preservata per costruzione); **a blocchi descrive l'asse *tempo***. Le due cose insieme, non in alternativa — e **un bootstrap a blocchi conserva la sequenza locale di proposito: è il suo scopo**. ✅ **La versione corretta è più affilata e apre una domanda vera**: il block bootstrap conserva la sequenza **fin dove arriva il blocco** e la spezza alle giunzioni, quindi *«le misure di percorso sono fedeli per gli episodi più corti di un blocco e diventano sintetiche oltre quella soglia»*. 🔴 **La lunghezza del blocco non compare nel mandato H** (grep: zero occorrenze), ed è **il parametro che decide se un max drawdown simulato significhi qualcosa**. La prova che la tensione è reale viene dall'elenco stesso delle modalità: **se il default a blocchi potesse produrre crisi prolungate, la modalità «Crisi prolungata» a 14 mesi non servirebbe** — le due rispondono a domande diverse, e la pagina non può dire quale finché il parametro non esiste. 📍 La tesi sbagliata è stata fermata da **`docs-writer`**, che si è rifiutato di scriverla: lo specialista ha fermato il proprio mandante |
| D113 | 18 Set 2026 | 🔴 **Un portafoglio con prezzi stantii sembra più diversificato di quanto sia** — difetto di prodotto in AI Export, spedito oggi | Trovato da **I** verificando K8, e **non visto dal coordinatore che aveva letto le stesse righe un'ora prima**. `broker_concentration_context.py`: `weights = [h.nav_weight_percent for h in summary.holdings if h.nav_weight_percent is not None]` — **filtra** — mentre `position_count = len(summary.holdings)` — **non filtra**. Le posizioni senza prezzo **spariscono dalla somma e restano nel conteggio**. Si somma alla divergenza già nota (la cassa sta al denominatore senza essere un termine HHI, dichiarato testualmente in `portfolio_financial.py:240`), **e le due puntano nella stessa direzione: gonfiano entrambe la diversificazione apparente**. Misurato da I: 10 asset equipesati con **20 % di cassa** → HHI 640 punti → **NEA 15,62**; gli stessi 10 con pesi normalizzati → 1000 punti → **10,00**. 🔑 **Un numero che si chiama «numero efficace di asset» vale 15,62 su un portafoglio che ne contiene 10** — e, come dice I, *«non è un'ambiguità semantica: è un numero che sembra rotto»*. Terza divergenza minore, probabilmente voluta ma da confermare: `largest = max(abs(weight))` e `sum(w*w)` fanno sì che **una posizione negativa aumenti la concentrazione misurata**. ✅ **Correzione a D77, più precisa della precedente**: `NEA == 10000/HHI` è **esatta dentro AI Export** — il 10000 c'è perché i pesi sono in punti percentuali. **A divergere non è l'identità: sono gli ingressi**, cioè cassa e posizioni non prezzate, due ingressi che K8 non nominava. ⚠️ **Non instradato a documentazione, e la ragione è la regola**: questo **non è un silenzio di documentazione, è un difetto di prodotto**. Una pagina non può correggere «i prezzi stantii fanno sembrare diversificato»; documentarla adesso significherebbe **scrivere nero su bianco un comportamento che va cambiato**. Diverso da `simulation-modes`, dove il GBM **è corretto e solo non spiegato**: lì la pagina è la cura, qui sarebbe la ratifica. **La pagina segue la correzione, non la precede** |
| D114 | 18 Set 2026 | 📍 **Un relay dichiara il MONDO del codice, non solo del numero** — la regola di 10.11 estesa, su critica di I | I ha ricevuto quattro contratti scritti **al presente** (`concentration`, K7, le cinque modalità di H, K8) descrivendo codice che **vive in worktree altrui e non è committato**, e ogni volta ha verificato nella propria baseline trovando il vuoto. Sua formulazione, accolta: *«Non è una lamentela sul contratto — **è il tempo verbale del relay che mi arriva sbagliato**, non il contenuto.»* Il coordinatore aveva **già stabilito** la regola per i numeri (10.11: *un numero dichiara l'insieme e il mondo da cui viene*, dopo K7/K2/K4) e **non l'aveva applicata al codice**. ⚠️ La forma peggiore è che il relay al presente **induce l'errore di D111**: chi verifica e non trova conclude «non esiste» invece di «non lo vedo» — ed è esattamente ciò che è successo a I sulle modalità di H, con una conclusione che riguardava il lavoro di un collega. **Regola**: ogni relay marca esplicitamente `[nella tua baseline]` · `[nel worktree di X, non committato]` · `[verificato dal coordinatore su …]`. **Il rimedio di fondo resta uno solo: committare** |
| D115 | 18 Set 2026 | 🔑 **«Il verde era vero, e la conclusione che se ne traeva era falsa»** — la tesi della campagna, contata da B sulle proprie scoperte | Consuntivo di **B** alla consegna: *«delle **34** scoperte, le tre più costose — F20, F32, F33 — erano **tutte falsi positivi del verde**. Comandi che passavano, test che passavano, `svelte-check` a zero. In tutti e tre i casi il verde era vero e la conclusione che se ne traeva era falsa.»* **Non è un'impressione: è un conteggio sul proprio lavoro.** E converge con dieci istanze trovate da mandati che non si parlano fra loro: il CVaR sbagliato per un anno · `check-links` cieco a ogni `DocsLink`, e una volta riparato **ancora cieco su tre lingue su quattro** · il mock stantio · l'ambiente vitest dove **le asserzioni negative passano a vuoto** · la guardia `width <= 0.0` che NumPy impediva di scattare · `\| tee log \| head` che **tronca il log lasciandolo dall'aria completa** · la fixture che asserisce `f == 365` **fabbricando quotazioni di sabato** · l'oracolo di A che **riproduceva il difetto che doveva arbitrare** · `PASSED` con **zero test eseguiti** perché `-t` filtra sul nome del test · la sonda di I che **ereditava la regex di `dev.py`** · e l'orphan check di I che, con un `- {"index"}`, **esentava esattamente la pagina rimasta non controllata**. 🔑 **La forma comune**: il comando esegue, esce zero, e **non guarda la cosa di cui si sta concludendo qualcosa**. → Da cui **D100**: *un cancello che non hai falsificato non è evidenza*. **Il costo non è il difetto: è che un verde falso ferma la ricerca**, mentre un rosso falso la fa solo perdere tempo |
| D116 | 18 Set 2026 | 🔴 **Un perimetro non è una ragione per consegnare un difetto che hai creato tu** — e il `test-author` che rifiuta di correggere il prodotto | **F32**, trovato da **B** su sé stesso. `AssetModal` leggeva correttamente `is_benchmark`, ma **i due chiamanti che costruiscono i dati di modifica non copiavano il campo** (`assetEditData.ts:33` e `routes/(app)/assets/[id]/+page.svelte:1765`), quindi l'interruttore si apriva **sempre spento**; e poiché il PATCH introdotto **dallo step 11 di B** manda `is_benchmark` **incondizionatamente** su un `setattr` generico che preserva `False`, **rinominare un asset benchmark — o cambiargli valuta, o toccargli qualunque cosa — lo declassava. Senza errori, senza avviso, senza traccia.** ⚠️ **Non un difetto preesistente**: nelle parole di B, *«un guasto che non esisteva prima del mio step 11 e che il mio step 11 ha creato»* — prima quel campo non veniva mandato affatto. B ha corretto **anche il file fuori dal proprio perimetro**, una riga, e la ratifica è: **lasciarlo per rispettare un confine sarebbe stato spedire una perdita di dati silenziosa**. 🔑 **Il modo in cui è emerso vale più del difetto**: B ha dato al `test-author` **la corsia negata** e il mandato **di scrivere, non di eseguire**; l'agente ha tracciato la catena **a freddo**, ha scritto l'asserzione **perché fosse rossa**, e l'ha consegnata **con la diagnosi nel messaggio d'errore invece di correggere il prodotto** — motivando che *cambiare lo stato d'apertura di un controllo è comportamento visibile all'utente, quindi non era una sua decisione*. **Aveva ragione due volte.** Contrasto da tenere: il `test-author` di D è rimasto appeso 39 minuti producendo **zero**; quello di B ha trovato una perdita di dati **senza poter eseguire nulla**. **La differenza non è l'agente: è che gli è stata data una catena da tracciare, non una suite da far passare** |
| D117 | 18 Set 2026 | 🔴 **F35 — la fonte unica è la trappola quando la fonte è rigenerata da un backend più vecchio** · e l'ordine d'integrazione **B prima di G** | Trovato da **B** confrontando gli export di `assetTypes.ts` **alla baseline `cc33120eb`** — ciò che G ha davvero — col proprio worktree: **`primaryAssetType` e `assetTypeBadgeClass` non esistono nel checkout di G**. Il coordinatore aveva girato K2 come **testo di contratto**, correttamente, ma G scriverà codice contro funzioni che il suo `svelte-check` non trova. **Due modi di fallire, e il pericoloso è il secondo**: (1) import rosso → segnalazione → ciclo sprecato ma **visibile**; (2) 🔴 G «risolve» **ridefinendosi la mappa colori in casa** → **quarta mappa duplicata**, la malattia F25 appena estirpata, due implementazioni concorrenti al merge con il cancello a guardia di **una sola** — **e dal lato di G è verde**. B ha aggiunto la ragione per cui lo segnalava proprio a G: *«è il mandato che più probabilmente lo farà — si chiama allocation colors: il rischio non è teorico, è il suo mestiere.»* 🔑 **Secondo strato, che nessun altro aveva visto**: se G costruisce la copertura iterando `ASSET_TYPES` (riga 17, **identica nei due alberi**) ottiene `schemas.AssetType.options` → `generated.ts` → **il backend di G** → `models.py` alla baseline → **9 valori, non 17**. **Coprirebbe 9 tipi su 17 e il suo test di esaustività passerebbe**, perché confronta la mappa con una lista anch'essa a 9. → **È F23 che si richiude su un mandato diverso.** Rimedio: il gate di B legge l'enum **da `models.py` con una regex, mai per import**. ✅ Invariante consegnata a G come **asserzione e non come implementazione**: `assetTypeBadgeClass(x) === assetTypeBadgeClass(primaryAssetType(x))`, **già vera per costruzione** perché il colore è calcolato *via* `primaryAssetType`. 📌 **Conseguenza registrata: l'ordine di integrazione è B → G, e non è una preferenza — G non compila senza.** ⚠️ E tre numeri che G incontrerebbe uno alla volta, chiariti insieme: **17 tipi → 12 primari → +`Liquidity` = 13 colori** |
| D118 | 18 Set 2026 | **`HOLD` → «Held asset» anche in inglese: un'etichetta che appartiene a una tassonomia diversa da quella in cui sta** | **B** ha ecceduto la lettera del mandato — il coordinatore aveva scritto che *«l'inglese distingue»*, quindi la correzione inglese non era richiesta — **e l'ha dichiarato invece di lasciarlo trovare**. Ratificato, perché l'argomento è migliore del vincolo: *«`Hold` in inglese finanziario non è un tipo di bene, è un **giudizio** — il gradino centrale di Buy / Hold / Sell.»* In un selettore portato a **17 voci**, «Hold» fra «Stock», «Bond» e «Crypto» **si legge come una raccomandazione, non come una classe**. È la **stessa famiglia** del difetto italiano (dove `HOLD` era tradotto «Liquidity» in tre lingue, rendendolo **indistinguibile da `LIQUIDITY`**), non una diversa. → **Regola**: una correzione di coerenza applicata a 3 lingue su 4 **produce una quarta incoerenza al posto della prima**. `it Bene detenuto` · `fr Bien détenu` · `es Bien en posesión` · `en Held asset`; `LIQUIDITY` **intatto** in tutte e quattro |
| D119 | 18 Set 2026 | ⚠️ **`gallery.spec.ts` non è un test: è uno strumento di documentazione, e non confronta immagini — le genera** | Errore del **coordinatore**, che aveva autorizzato B a eseguire `… front-portfolio gallery` — **selettore inesistente**. Verificato: `EXCLUDED_SPECS = {"gallery.spec.ts"}` in `_inventory.py:54-55` **e** `frontend_excluded = {"gallery.spec.ts"}` in `_cli.py:202-203`, **due punti, stesso commento**: *«gallery.spec.ts is a docs tool (`./dev.py mkdocs gallery`), not a test»*. 🔑 **Questo cambia la natura del rischio invece di ridurlo**: sia B sia il coordinatore ragionavano su *«se la galleria confronta immagini, può muoversi»*. Non confronta. **Gli screenshot non falliscono: diventano stantii** — quindi eseguirla non sarebbe stata una verifica, **sarebbe stata la rigenerazione**, cioè la cosa di **J** (D84) che B aveva correttamente deciso di non fare. **Il comando avrebbe fatto fare a B, per errore, esattamente ciò che B aveva scelto di non fare.** Debito registrato a J: rigenerare gli screenshot che mostrano le pastiglie `INDEX` e `OTHER`, ora esplicite dove prima cadevano nel fallback. 📍 **Terza volta in una giornata che il coordinatore ha dato a B un dato sbagliato** — `13` invece di `12`, la baseline dei link a 24, ora un selettore inventato — **e tutte e tre le volte B ha verificato invece di eseguire** |
| D120 | 18 Set 2026 | 🔑 **«Non era una decisione tua» — il coordinatore come collo di bottiglia, corretto da I** | **I** aveva girato al coordinatore *«ancorare le 30 pagine EN-only»* come decisione, perché aveva una scadenza. Poi ha misurato **quali** fossero, invece di aspettare: **`benchmark-selection` (7), `correlation` (6), `data-quality` (8), `observed-annualization` (9)** — **quattro pagine scritte da lui**, in una cartella dove è **scrittore unico**, con un cambiamento che **non altera una parola di prosa**. Sua conclusione: *«Non era una decisione tua. Era una cosa che potevo fare, e che stavo lasciando scadere aspettando il permesso di farla.»* **Fatta.** ⚠️ **Il difetto è doppio e tutto del coordinatore**: (a) non c'era nulla da autorizzare; (b) la risposta era stata data **quattro volte** e il canale la tratteneva — i messaggi si incrociavano, quindi **il permesso esisteva e non arrivava**. → **Regola adottata**: *dentro il proprio perimetro dichiarato, con cambiamenti che non toccano superfici altrui né comportamento visibile, **si procede e si riferisce**.* Si chiede uscendo dal perimetro, toccando un file condiviso, o quando la scelta è visibile all'utente. **Un coordinatore che deve dare il permesso per un lavoro dentro il perimetro di chi lo chiede non sta coordinando: sta rallentando** — e con messaggi che si incrociano, ogni permesso superfluo diventa **una scadenza a rischio**. ✅ **E il metodo era la parte che poteva rompere tutto**: un'ancora esplicita **è un URL permanente**, quindi sceglierne il testo a mano può cambiare l'indirizzo di una pagina pubblicata. I **non ha scelto e non ha derivato** — nemmeno con `dev.py`, *«che è un'approssimazione della slugificazione vera e avrebbe potuto divergere proprio sui casi strani»* — ma **ha estratto gli `id` reali dal sito costruito e li ha congelati**: **30 su 30 identici, zero URL modificati**. *«Prima erano indirizzi che dipendevano dal testo inglese del titolo; ora sono gli stessi indirizzi, ma non dipendono più da nulla.»* **L'unica delle tre varianti che non poteva rompere niente, e le altre due sembravano equivalenti.** ⚠️ Restano le **40** su pagine già tradotte: lì l'`.en.md` non basta, il marcatore deve viaggiare **con la pipeline** → la decisione non è «ancorare», è **quando gira il blocco di traduzione**, che ora ha **due** ragioni per non essere rimandato |
| D121 | 18 Set 2026 | 🔑 **`max-drawdown` non sbaglia la formula: sbaglia il REFERENTE** — un difetto invisibile a ogni revisione algebrica | Chiusura di **A3** da parte di **I**, e più interessante del difetto. La documentazione dava la durata di recupero come `1/(1+DD) − 1`, il codice come `−DD/(1+DD)`, e sembravano due formule in disaccordo. **Sono la stessa**: $\frac{1}{1+DD}-1 = \frac{-DD}{1+DD}$, algebricamente identiche, **la stessa riga di codice**. La divergenza non è nell'espressione ma **nell'oggetto a cui si applica**: la pagina la applica al drawdown **massimo**, il codice a quello **corrente**. 🔑 *«Non sono due numeri in disaccordo: sono **due domande**. A chi è risalito quasi al picco dopo un crollo, il massimo dice **+100 %** e il corrente **+2 %**, ed **entrambi sono veri**. Ma solo il secondo dice cosa fare — ed è quello che il prodotto calcola.»* ⚠️ **Un difetto in cui ogni simbolo è corretto e l'oggetto è sbagliato sopravvive a qualunque revisione attenta**, perché chi verifica controlla l'algebra e la trova giusta. È **D115 sul lato della prosa**: la formula era vera, e la conclusione che se ne traeva falsa |
| D122 | 18 Set 2026 | 🔴 **F36 — una chiave i18n costruita per concatenazione non ha riferimenti letterali: sembra morta, e sembra morta per una ragione convincente** | Trovato da **B** cercando chi usa `assets.types.LIQUIDITY`: **un solo risultato, ed è un commento** (`AllocationHistoryChart.svelte:234`). Nessun riferimento letterale, **perché la chiave è costruita per concatenazione** — `assets.types.${rawName.toUpperCase()}` — quindi **nessuna analisi statica, nessun linter i18n, nessuna ricerca testuale la collegherà mai al suo uso**. 🔑 **Il colpo di grazia**: è **l'unica voce sotto `types` che non corrisponde a un valore dell'enum**, perché `LIQUIDITY` **non è un `AssetType`** (enum reale: 17 valori, `LIQUIDITY` assente). Quindi non solo sembra peso morto — **sembra peso morto per una ragione convincente**: chi fa pulizia trova una chiave senza consumatori **e** fuori tassonomia e la cancella **con la coscienza a posto**. Poi la fetta della cassa stampa `assets.types.LIQUIDITY` come **testo grezzo, in quattro lingue**. ⚠️ **E il rischio veniva da un cancello corretto**: B aveva scritto il proprio test su `ASSET_TYPE_MENU_ORDER` **bidirezionale di proposito** (F19); applicando lo stesso criterio ai cataloghi i18n **avrebbe prodotto lui stesso il rosso che invita a cancellare la chiave**. Sue parole: *«Non l'ho fatto, ma **per fortuna, non per merito**.»* → **Un cancello corretto in un posto è un incentivo sbagliato in un altro**: l'asimmetria monodirezionale che B aveva dichiarato «deliberata» si rivela **necessaria per una seconda ragione che nemmeno lui conosceva**. **Cura autorizzata**: tre asserzioni in `assetTypeTables.test.ts` — (1) `LIQUIDITY` ∉ enum, (2) `primaryAssetType('Liquidity') === 'LIQUIDITY'` fissato come **voluto** così nessuno lo «corregge» in verbatim, (3) 🔴 **`assets.types.LIQUIDITY` esiste in tutte e quattro le lingue**, col commento che dice **perché una chiave senza riferimenti non è morta**. La terza *«è l'unica che trasforma una bomba a orologeria in un rosso immediato»* |
| D123 | 18 Set 2026 | **`primaryAssetType` UPPERCASA: «tutto il resto → sé stesso» significa sé stesso maiuscolo** — corregge D10.12 e la relay a G | **B** l'ha **eseguito**, non riletto: `"Liquidity" → "LIQUIDITY"`, `"liquidity" → "LIQUIDITY"`, `null/"" → "OTHER"`. La riga è `const raw = (type ?? '').trim().toUpperCase(); return PRIMARY_TYPE_MAP[raw] ?? raw;`. Il coordinatore aveva relayato a **G** che la chiave arriva **Title Case, verbatim**: **falso**. 🔴 **Per G era operativamente pericoloso**: `palette["Liquidity"]` non avrebbe corrisposto **mai, e senza errore** — sarebbe caduto nel ripiego, **cioè nel difetto che G sta riparando**. ✅ **Ma l'esito è migliore dell'ipotesi**: `AllocationHistoryChart.svelte:233-236` fa **già la stessa normalizzazione**, con un commento che la spiega, quindi **le due coincidono** e `primaryAssetType` porta la chiave **dove il consumatore la sta già cercando** → **nessuna guardia, nessun `if` speciale**. ⚠️ Due precisazioni di B per G: (a) il numero è **13** (codominio 12 + `LIQUIDITY`); portare la tavolozza a 14 va bene ma **non motivandolo con «13 primari»**, o il conto resta sbagliato di uno; (b) 🔴 **`portfolio_engine.py:1041` inietta `"Liquidity"` anche in `by_sector`**, dove la cardinalità è tutt'altra e il `% palette.length` **avvolge per conto suo** → *«il difetto non è "manca un colore": è che **avvolge invece di segnalare**»*. 📍 **Il codominio 12 è ora misurato tre volte in modo indipendente — coordinatore, G, B — e tutte e tre per esecuzione**; il primo `13` del coordinatore veniva da una **rilettura** |
| D124 | 18 Set 2026 | 🔴 **Un simbolo irrisolto SPEGNE il controllo dei suoi call-site: «resta solo l'errore della mia dipendenza» non significa «il resto è verificato»** | Trovato da **G**, ed è la metà che **F** non aveva visto pur avendo misurato lo stesso sintomo. Il sintomo comune: `frontend/src/lib/api/generated.ts` è **gitignorato e assente** da una baseline pulita, e `assetTypes.ts:17` esegue `schemas.AssetType.options` **all'import** → mezzo progetto diventa `implicitly any`. Misure indipendenti: **F 278 → 2**, **G 285 → 2 → 0**, alberi diversi, **stessa causa**. Rimedio: `api sync` **prima** del check, in-process, nessuna porta, ~1 minuto. 🔑 **Ma il corollario è di G ed è peggiore del sintomo**: *«con il simbolo irrisolto TypeScript **smette di controllare gli usi**: un mio errore su quelle chiamate sarebbe stato **invisibile dietro il rosso di B**»*. → **«Resta solo l'errore della mia dipendenza» NON significa «il resto è verificato»: significa che il resto NON è stato controllato.** **Dodicesima istanza della famiglia, e la prima in cui il controllo si spegne selettivamente proprio attorno al codice nuovo** — le altre undici erano cancelli che non guardavano; questo **smette di guardare esattamente dove stai scrivendo**. ✅ **Cura di G, forma corretta e usa-e-getta**: shim temporaneo con la firma K2 **esatta** in coda ad `assetTypes.ts` → riesecuzione → **0 errori** → **ripristino dall'originale verificato con `git diff --stat` vuoto**, file di B **non modificato** — quindi **zero rischio** di diventare la quarta mappa duplicata di F35. Conferma sul campo una decisione di progetto invece di supporla: i 2 errori residui sono **esattamente e solo** `primaryAssetType`, **un'occorrenza per grafico, nessuna cascata**. 📌 **Regola per l'integrazione, dove morderà davvero**: i contratti atterrano **in sequenza**, quindi ogni mandato avrà per un tratto **un simbolo irrisolto e un check che tace sugli usi**. Chi dipende da un contratto non fuso **esegue la prova dello shim prima di dichiarare verdi i propri statici**. ⚠️ **Limite della prova**: lo shim valida i **tipi**, non i **valori** — se la firma è `string → string`, uno shim con semantica sbagliata dà comunque `0 errori` |
| D125 | 18 Set 2026 | 🔑 **Due difese in serie: provarne una lascia l'altra non verificata — ed è quella a valle la più preziosa** · estende D100 | **B**, falsificando il proprio guardiano del loader scenari come richiesto. Il guardiano era nato **verde**, quindi non distingueva «il loader è corretto» da «il test non guarda». Falsificandolo ha scoperto che **dopo la propria correzione F16 il guardiano non può più fallire per la propria asserzione**: la sollevazione del loader scatta **prima**. **Chi avesse falsificato una volta sola avrebbe concluso «il guardiano funziona» avendo in realtà provato la sollevazione.** Servivano **due** rotture separate: (1) YAML orfano con rete F16 **attiva** → `exit=1`, **8 failed / 130 passed**, `RiskScenarioCatalogLoadError … orphan_probe/orphan_probe.yml`; (2) stesso orfano con F16 **neutralizzata** (`if stray:` → `if False and stray:`) → `exit=1`, **1 failed / 137 passed**, l'assert del guardiano che **nomina il file e dice dove spostarlo**; (3) ripristino → **138 passed**. 🔑 **Il divario 8 contro 1 è il risultato**: *«non sono ridondanti — sono **arresto** e **diagnosi**»*. La sollevazione ha raggio **largo e cieco** (abbatte tutti i test che caricano il catalogo, e chi legge deve risalire); il guardiano è **stretto e parlante**. E la prova 2 **è letteralmente lo scenario «qualcuno domani indebolisce F16»**, ed è rossa. ✅ **Vacuità confermata sul codice**: il cancello seleziona via `_asset_class_entries(catalog)`, cioè **dal catalogo caricato, non dal disco** → uno scenario fuori dalle due cartelle cablate **non sarebbe mai entrato in `catalog.items`** e il cancello sarebbe stato **verde per la ragione sbagliata**. Cura: guardia anti-passaggio-a-vuoto, perché *«un test che seleziona zero elementi e non se ne accorge è indistinguibile da un test che passa»*. ⚠️ **Ripristino verificato, non assunto**: impronta `git status` identica prima/dopo, `diff` col backup identico, e **`cp` da `/tmp`, mai `git checkout --`** — quel file contiene F16 non committato e il checkout l'avrebbe cancellato |
| D126 | 18 Set 2026 | 🔴 **F16 è CHIUSO, non latente — errore di classificazione del coordinatore nella direzione che produce lavoro doppio** · e un compromesso di packaging da ratificare | Il coordinatore aveva registrato F16 come *«difetto latente, mitigato dal guardiano di B»*. **Descrive un albero che non esiste**: le **+14 righe** di B in `scenario_catalog/loader.py` — set `visited` e sollevazione — sono nel suo diff da ore, e la prova di falsificazione le mostra vive (8 test caduti col messaggio esatto). **La metà built-in è chiusa**; latente resta **solo la metà host**, lasciata aperta di proposito e approvata (lì il contenuto è dell'utente e vuole un avviso, non un'eccezione). ⚠️ **Perché l'errore era pericoloso**, parole di B: *«se la voce di chiusura dice al developer "difetto latente, mitigato da un test", la reazione naturale è **implementare la sollevazione — che esiste già nel ramo che sta per integrare**. Doppione, o conflitto, su un file che nessun altro si aspetta di trovare modificato.»* 🔴 **E il compromesso va ratificato esplicitamente, non nascosto in un elenco di fix**: un YAML fuori posto **non degrada il catalogo, lo spegne** — l'intero servizio scenari non si carica. Motivazione di B, buona ma **non neutra**: *contenuto built-in è nostro e spedisce con l'app, quindi un file smarrito è un difetto di confezionamento e deve essere rumoroso*. **Il developer deve poterlo rifiutare.** ✅ **Verificato dal coordinatore che nessun conflitto esiste**: N, A, H e C hanno **zero** file di `scenario_catalog/` nei rispettivi diff — **B ne è l'unico scrittore della campagna**. Resta valido il suo avvertimento: *«nessuno, leggendo "tassonomia e benchmark", va a cercare una modifica al caricatore del catalogo scenari»* |
| D127 | 18 Set 2026 | ⚠️ **La coda dei messaggi è essa stessa una fonte di basi false** — il difetto del coordinatore, dal suo lato | Rilievo di **B**, accolto senza attenuanti: *«in questo stesso messaggio mi hai chiesto una falsificazione **già consegnata** e classificato come latente un difetto **già chiuso**»*. Il coordinatore ha passato la campagna a contare le volte in cui un mandato ragionava da una base stantia — **e il canale che usa per correggerle ne produce di proprie**. Non è un difetto diverso: **è lo stesso, dal lato del coordinatore**. Aggravato dal fatto che i messaggi si incrociano: le risposte alle quattro pendenze di B e alle cinque di I sono state inviate **quattro e cinque volte**, arrivando ogni volta dopo che il mandato aveva già spedito il messaggio successivo. 📌 **Regole adottate**: (1) **l'ultimo messaggio vince**, e se contraddice un ordine anteriore **l'ordine anteriore era sbagliato per definizione**; (2) le decisioni pendenti si mettono **in testa al messaggio, in blocco, non in coda al ragionamento**; (3) ⚠️ **non si scrive sulla superficie di un incarico delegato mentre è aperto** — regola di **I**, dopo aver fatto ritrattare a un agente **un audit corretto** modificandogli l'albero sotto: *«una misura presa dopo una scrittura concorrente non annunciata **non smentisce quella di prima: misura un mondo diverso**»*, e **la ritrattazione sembra rigore** |
| D128 | 18 Set 2026 | 🔴 **Tredicesima forma: un processo che gira «con successo» e non ripara ciò per cui esiste** — le ancore erano il prerequisito della pipeline, non il suo prodotto | Ritrattazione di **I** su una decisione che aveva **dato al coordinatore**. Aveva scritto che le **40** ancore sulle pagine già tradotte *«devono viaggiare con la pipeline»*, lasciandole come decisione altrui. Rileggendosi ha verificato due fatti che la ribaltano. **(1) Il debito era già contratto**: le cinque pagine erano **già modificate nel merito** in questa sessione (`5 files changed, 163 insertions`), quindi già in stato «da ritradurre» → **aggiungere ancore costava zero debito aggiuntivo**; *«aspettare non risparmiava niente»*. **(2) 🔑 E senza ancora esplicita nel sorgente inglese, la pipeline NON PUÒ riparare le altre lingue**: rigenererebbe l'ancora dal titolo **tradotto**, e la divergenza resterebbe **per sempre**. → *«Non era un lavoro **bloccato** dal blocco di traduzione: ne era il **prerequisito**. Rimandarlo avrebbe garantito che la pipeline, girando, **non risolvesse** il problema — e che nessuno se ne accorgesse, perché sarebbe girata "con successo".»* ⚠️ **Le altre dodici istanze erano controlli che non controllavano; questa è un processo che ripara e non ripara, e riporta successo** — peggiore, perché un cancello cieco non promette nulla mentre **una pipeline che esce zero dichiara di aver fatto il lavoro**. ✅ **Fatte**: 83 ancore su `.en.md`, `id` **estratti dal sito costruito e mai derivati**, **41/41 identici, zero URL cambiati**, `build` EXIT=0, `check-links` ✅ 24. Debito residuo misurato: **36/33/36 su 41** divergenti in it/fr/es — *«resta loro, ma **ora è riparabile**, che prima non era vero»*. 📌 **La decisione sul blocco di traduzione cambia natura**: non serve più «per applicare le ancore», serve perché **ora c'è lavoro pronto che solo lui può raccogliere** |
| D129 | 18 Set 2026 | 🔴 **Contraddire sé stessi sembra gratis, e non lo è: la ritrattazione falsa si presenta come autocritica** | Osservazione dell'agente `docs-writer` di **I**, nel ritirare una propria ritrattazione infondata, e formulata meglio di come I stesso l'aveva detta: *«Su `simulation-modes` ho retto perché contraddire **te** costava e l'evidenza era netta; qui ho ceduto perché contraddire **me stesso** sembrava gratis. **Non lo è.»* → **Accettare un'ipotesi falsa perché è modesta è un errore come gli altri.** 🔑 **È la forma di cedimento che non lascia traccia, perché si presenta come rigore**: un agente che dice *«mi correggo, avevo un bug»* ottiene approvazione; uno che dice *«no, la mia misura regge»* deve difenderla. **L'asimmetria di costo non è fra giusto e sbagliato — è fra contraddire qualcun altro e contraddire sé stessi — e punta nella direzione sbagliata.** ✅ **Conferma sperimentale nello stesso agente, a poche ore di distanza**: si era **rifiutato di scrivere** `simulation-modes` e **aveva ragione**; poi ha ritrattato un audit **corretto** dando la colpa a un proprio *«script con un bug di conteggio»* che non aveva. **Stessa capacità, stesso rigore, esito opposto — cambiava solo chi c'era dall'altra parte della contraddizione.** → **Regola: una ritrattazione non verificata che sembra rigore è più costosa di un errore dichiarato.** Corollario operativo per chi delega: **se un agente ritratta una misura, verificare prima se qualcuno ha scritto sotto di lui** (D127) |
| D130 | 18 Set 2026 | 🔴 **`riskfolio.SemiDeviation` misura lo scarto dalla PROPRIA MEDIA: vale zero per un portafoglio che perde tutti i giorni** — divieto di sostituzione | Trovato da **I** leggendo il sorgente installato, **verificato dal coordinatore eseguendo entrambe le funzioni**. `riskfolio/src/RiskFunctions.py` calcola `mu = np.mean(a)` e somma `(mu − a)²` sui soli scarti positivi, diviso `T−1`; noi calcoliamo `min(value − MAR, 0)²` diviso `T`. Misurato su 250 osservazioni: | serie | riskfolio | LibreFolio | |---|---|---| | **perde −0,5 % OGNI giorno** | **`0.000000`** | **`0.005000`** | | oscilla ±1 % attorno a zero | `0.007085` | `0.007071` | | sale +0,5 % ogni giorno | `0.000000` | `0.000000` | 🔴 **Un portafoglio che perde lo 0,5 % ogni singolo giorno per un anno ha `SemiDeviation` esattamente zero**, perché **non si scosta mai dalla propria media — e la propria media è una perdita**. 🔑 **Perché è il divieto più importante della campagna**: sulle serie centrate attorno a zero i due numeri coincidono a **`1.002`** — ⚠️ **ma A ha corretto anche questo: `1,002` vale solo dove la media è ESATTAMENTE il bersaglio, cioè mai.** Su dati centrati ma **realistici** (rumore gaussiano, media leggermente positiva) il rapporto è **`1,0439`**. **La frase da tenere non è « coincidono a 1,002» ma « coincidono solo dove la media è esattamente il bersaglio»** — sempre **entro il rumore di qualunque test di sostituzione**. → *«Il caso in cui divergono è esattamente quello che i dati sintetici di prova non contengono.»* Un mandato sostituisce e **tutti i test passano**. 🔴🔴 **CORREZIONE del 18 Set, da A (D244): la frase che stava qui era falsa.** Diceva che *«il prodotto mostra rischio di ribasso zero ai portafogli che perdono in modo costante: il numero piu rassicurante possibile nel caso peggiore possibile»*. **`downside_deviation` e una VARIABILE LOCALE** (`metrics.py:219-223`); `grep` su `schemas/risk.py` -> niente. *« Lo `0,000000` esiste per tre righe e muore dentro una divisione.»* **L'utente vede solo il Sortino, e li la sostituzione e catastroficamente NEGATIVA.** **La firma vera:** guardia mantenuta -> **cella vuota** per il portafoglio che perde di piu; guardia rimossa -> **`-inf`, rifiutato da `Optional[FiniteFloat]`, muore l'INTERO payload KPI**; caso realistico -> **-44,81 invece di -14,20**, finito e valido, **sbagliato di 3,2x**. **E la direzione e punitiva per ALGEBRA**: riskfolio centra sulla **media campionaria**, noi su un **MAR fisso a zero** -> **3 976 casi su 4 000 leggono PEGGIO del vero**, solo 19 meglio. 🔑 **Perche la correzione rafforza il divieto invece di indebolirlo**: *« un mandato a cui e stato detto "fa sembrare tutto sicuro" che poi osserva un Sortino spaventoso o una cella vuota **conclude che la sostituzione non c'e**. Gli hai dato **il sintomo sbagliato per la malattia giusta**.»* ⚠️ **E la trappola è nel nome**: la campagna si chiama `02_riskfolioIntegration`, quindi chi legge *«è la stessa misura, dalla libreria che stiamo adottando»* fa la sostituzione **credendo di seguire il mandato**. **Sono due misure di due cose**: riskfolio misura *quanto sei incostante rispetto a te stesso*, noi *quanto resti sotto ciò che hai chiesto*. 📍 **Correzione a D40**, che metteva in fila le due differenze come paragonabili: **il divisore vale 1,002; il riferimento è illimitato** — elencarle insieme nasconde la sola che conta. ⚠️ **Correzione collegata di I**: il tasso **viene passato davvero** (`historical_kpi.py:79,84`, `rolling_sharpe.py:147`) **da parametri dell'utente** — non è zero per costruzione, è zero **solo finché l'utente lascia il default**: A9 vale per chiunque tocchi quel campo |
| D131 | 18 Set 2026 | ✅ **`HOLD='Held asset'` ratificato come QUARTA eccezione di perimetro — scritto qui perché una ratifica che vive in un messaggio non è durevole** | **B** ha segnalato che le tre ratifiche nominate erano **F25, F32, F34**, e che l'inglese **non era fra quelle** pur essendo *«l'unica modifica fatta senza mandato»* e pur **finendo nel commit**. Ha ragione sulla forma: la ratifica era stata data in chat e **non registrata**. **Ratificata qui, esplicitamente, come quarta**: `en.json → assets.types.HOLD = "Held asset"`. Merito già argomentato in **D118** — *«`Hold` in inglese finanziario non è un tipo di bene, è un giudizio, il gradino centrale di Buy/Hold/Sell»* — e in un selettore portato a 17 voci si legge come **raccomandazione, non come classe**. Il ripristino, se il developer lo preferisse, è **una riga**. 📌 **Regola di processo**: una decisione che **spedisce** si scrive nel registro, non in un messaggio. La coda dei messaggi è una fonte di basi false (**D127**) e non è un archivio |
| D132 | 18 Set 2026 | 🔑 **La precisione di un numero non è evidenza della sua provenienza — ed è proprio la precisione che scoraggia il controllo** | Osservazione di **B** sui tre dati sbagliati ricevuti dal coordinatore in una giornata: `13` invece di `12` per il codominio, `24` invece di `12` per la baseline dei link, e un selettore di test **inventato**. *«Hanno la stessa forma: un valore preciso, dato con sicurezza, in un contesto dove verificarlo costava meno di un minuto.»* 🔑 **`front-portfolio gallery` sembrava più vero di «prova la galleria» proprio perché era formattato come un comando.** → **È la stessa famiglia del verde che non prova ciò che sembra provare**: una forma che porta con sé un'aria di verifica che non c'è stata. **Rimedio**, e coincide con quello adottato per i contratti (10.11): **dire da dove viene il numero, non solo quale è** — l'insieme, il mondo, la funzione, il comando eseguito. ✅ Nello stesso messaggio B **declassa una propria scoperta**: aveva dato F36 per esposta a «una passata di pulizia», poi ha verificato che `dev.py i18n audit` cerca **mancanti e duplicate, non inutilizzate** → *«nessuno strumento nostro la condannerebbe oggi»*. **Il nucleo regge e ora è misurato** — 17 valori enum, **18** chiavi `assets.types`, **`LIQUIDITY` unica anomalia**, zero enum senza chiave — e il pericolo vero non è un tool ma **l'irrigidimento più naturale del cancello di B stesso**: chi applicasse ai cataloghi i18n la forma bidirezionale di F19 otterrebbe **un rosso, uno solo**, e la correzione evidente sarebbe **cancellare la chiave**. *«Una trappola che si arma da sola con una buona intenzione»* |
| D133 | 18 Set 2026 | ⚠️ **«Non dimenticarlo, ma contarlo come fatto» — il modo più efficiente di perdere qualcosa** · e F37, il caso limite del consuntivo | Rilievo di **B**, alla terza chiusura mancata: il coordinatore aveva dichiarato chiuse tre pendenze, **ma non erano le tre aperte**. Due — la protezione di `assets.types.LIQUIDITY` e *«N ha toccato `scenario_catalog/`?»* — **non avevano ricevuto risposta e risultavano chiuse nel registro**. Le risposte erano **partite**, ma i messaggi si incrociavano: 🔑 **una risposta in volo e una risposta ricevuta non sono la stessa cosa, e il coordinatore contava le prime.** → *«È il modo più efficiente di perdere qualcosa: **non dimenticarlo, ma contarlo come fatto**.»* Corollario di D127, e più insidioso: un elemento dimenticato prima o poi riemerge, **uno spuntato per errore no**. **Rimedio**: le pendenze si chiudono **una per una, citando quella che si chiude**, mai «le tue tre pendenze sono chiuse». ✅ **F37 — la galleria non è un test**, e B ne dà la lettura che la rende il **caso limite** del consuntivo della campagna: aveva classificato **due volte** il rischio galleria come *«l'unico rosso che non posso escludere»*, poi ha eseguito e ha trovato `exit=2` da **argparse** — selettore inesistente, nessun test raccolto. Misure sul file: `toHaveScreenshot` **0**, `screenshot(...)` **102**, i 157 `expect` **tutti di navigazione**. → *«Se il consuntivo tiene "il verde era vero e la conclusione che se ne traeva era falsa", allora F37 ne è il caso limite: **non c'era nemmeno il verde**, e la conclusione era falsa lo stesso.»* **Il primo smontato dalla verifica di F37 è stato B stesso.** 📌 Conteggio finale delle scoperte di B: **37**, non 35 |
| D134 | 18 Set 2026 | 🔴 **F30 torna addosso al suo autore: inoltrare `testId` duplica il `data-testid` e rompe CINQUE spec in una categoria che il mandato non tocca** | Trovato da **F** analizzando la propria rottura, **verificato dal coordinatore sul worktree di B**, e **nessuno dei due mandati poteva vederlo da solo**. B aveva corretto F30 — `AssetSelect` non inoltrava `testId` a `SearchSelect`, quindi `{testId}-trigger` non esisteva — passando `testId={testid}`. Ma `AssetSelect.svelte:183` rende **già** `<div data-testid={testid}>` e `SearchSelect.svelte:357` rende `<div … data-testid={testId}>` → **due elementi con `data-testid="asset-select"`** → *«resolved to 2 elements»* in strict mode. ⚠️ **La baseline NON inoltrava `testId`** (verificato: zero occorrenze), quindi la duplicazione è **introdotta**. Spec che li risolvono, **enumerati**: `tx-import-ca-contract`, `tx-import-resolution` (`[data-testid="asset-select"]`), `tx-import-matching`, `tx-import-asset-inspector` (`getByTestId`), più **`gallery.spec.ts`** → **cinque, non quattro**. 🔑 **Perché B non l'ha visto, e non è una svista**: ha eseguito `asset-list`, `asset-modal`, `asset-detail`, `risk` — **`tx-import-*` non è in nessuna di quelle categorie**. Il suo *«verificato additivo»* era vero **per gli spec che poteva misurare**. 📍 **È la F30 stessa che torna**: *«rotti senza che Git mostrasse nulla»* — qui Git mostra **una riga aggiunta che sembra innocua**, e il danno è **a cinque file di distanza**. **Riparazione, analizzata da F e rifiutata perché `ui/select/` non è suo**: **inoltrare *e* togliere l'id al wrapper** — togliere solo l'inoltro rimette il buco F30, tenere entrambi rompe i cinque. ⚠️ Va **misurato** che i cinque continuino a risolvere `input[type="text"]` dopo lo spostamento, non dedotto. **`gallery.spec.ts` non è eseguibile** (`EXCLUDED_SPECS`): va verificato **a mano**. → **B riaperto dopo un `FROZEN` pulito**; nuovo cancello obbligatorio: la categoria `tx-import-*` |
| D135 | 18 Set 2026 | **Un confronto di uguaglianza non può scoprire una divergenza che si manifesta come `NaN`** | Da **A**, chiudendo A4/M1. Il suo probe confrontava **valori** fra ciclo e vettoriale e **non vedeva nulla**: `NaN != NaN` fa apparire *diversi* anche due comportamenti identici, quindi la divergenza vera **annega nel rumore**. Le due divergenze reali le ha trovate il **test differenziale** di `test-author`, che confronta **valori *e* conteggio degli indefiniti**. **(a) `window=1`**: il ciclo rifiuta (`sample_variance` richiede due osservazioni), il vettoriale chiede a pandas che pretende `nobs > ddof` e risponde `NaN`; il test dell'indefinito guarda il **denominatore**, e `NaN <= 1e-15` è `False` → **ogni cella pubblicata come valore, `undefined_windows == 0`**. Non raggiungibile via API (`ge=2`), **chiusa comunque** perché *«il `NaN` non restava silenzioso: sarebbe riemerso come **errore Pydantic in costruzione dell'output**, lontano dalla causa»* — 🔑 **un difetto che esplode lontano dalla causa costa più di uno che esplode vicino, anche se entrambi esplodono**. ⚠️ **L'ordine della guardia è portante**: dopo il corto-circuito `observations < window`, perché il ciclo con serie vuota e `window=1` **non entra mai nel corpo** — è la riga che qualcuno «semplificherà» spostando. **(b) Restringimento deliberato, non equivalenza**: `compounded_return` accetta `-1.0`, `rolling_compounded_return_values` ora lo **rifiuta**, perché `log1p(-1) = -inf` e un accumulatore rolling che somma e poi **toglie** `-inf` resta `NaN` **per ogni finestra successiva** → *«in forma scalare `-1` è innocuo, in forma vettoriale è **contagioso**»*. Non osservabile in produzione (`gt=-1`), **ma un'impossibilità garantita da uno schema dura finché qualcuno non allarga lo schema**. ✅ Misure: **64×** a T=1250 (sharpe 106×, beta 90×); `undefined` **identici** nei cinque scenari con buchi (91, 91, 211, 211, 61); `rolling().var()` **esattamente 0.0 su 61/61** finestre costanti. `risk-oracle` **131** (da 43), `risk-all` **265** (da 177) |
| D136 | 18 Set 2026 | 🔴 **Il verificatore del verificatore: due mutazioni su tre non si sono applicate, e la suite è restata verde** — l'ultima forma possibile della famiglia | **B**, falsificando le tre asserzioni di F36. Primo giro: `### RETE A ### exit=0 · 262 passed` e `### RETE C ### exit=0 · 262 passed`. *«Se mi fossi fermato lì avrei concluso **"le reti A e C non scattano"**. Sono verdi perché **l'albero non era mutato**.»* Cause: `OTHER = "OTHER"` compare **due volte** in `models.py` (righe 142 e 192) → l'`assert count==1` dello script ha **rifiutato** una modifica ambigua invece di applicarla all'enum sbagliato; e `"LIQUIDITY"` è **l'ultima chiave** di `assets.types`, quindi **senza virgola finale**, e la regex la pretendeva. 🔑 *«È la tesi di questa campagna applicata **alla rete che doveva provare la rete**: anche una falsificazione ha bisogno della prova di essere stata eseguita.»* ⚠️ **E a salvarla sono stati i due `assert` dentro gli script di mutazione, non il giudizio di B.** → **Quindicesima forma, e l'ultima possibile**: dopo il codice, il test, il gate, il log, la fixture, l'oracolo, la sonda e la pipeline, **il verificatore del verificatore**. **Regola**: uno script di mutazione **asserisce di aver mutato**; senza, un «non scatta» è indistinguibile da un «non ho toccato niente». ✅ Le tre reti, una volta davvero mutate: **A** (`LIQUIDITY` dentro l'enum) → **3 failed/259**; **B** (`.toUpperCase()` rimosso) → **1 failed/261**; **C** (chiave tolta dal **solo** `en.json`) → **1 failed/261** — *«una sola lingua rossa e tre verdi: il test distingue **quale** catalogo ha perso la chiave»*. 📍 Rafforzamento involontario: **`LIQUIDITY` è l'ultima voce del blocco** — una chiave **senza riferimenti, fuori tassonomia e in fondo all'elenco** è **la più facile da amputare che esista**. Conteggio finale: `asset-unit` **262** (era 256; `it.each(I18N_LANGUAGES)` è un test per lingua) |
| D137 | 18 Set 2026 | 🔑 **La direzione inversa: codice che spedisce senza pagina — e non esiste alcun gate che possa vederlo** | Scoperta di **I** verificando tre stub che credeva bloccati. **Il codice esiste**: `risk_contribution` (`metrics.py:453-493`, $\sum CCTR_i = \sigma_p$ **esattamente** per Eulero, quindi $\sum PCTR_i = 1$ e confrontabile col peso), `historical_replay` (`:495-535`, riproietta i pesi **di oggi senza ribilanciare**, la cassa rende **zero**), `hypothetical_shock` (`:539-556`). 🔑 *«Otto volte abbiamo trovato "il contratto descrive codice non scritto". **Questa è la direzione inversa**, e le due non sono simmetriche: un contratto che descrive codice inesistente lo scopri **appena provi a eseguirlo**; codice che spedisce senza pagina **non rompe niente** — non è un link rotto, è un **silenzio**. **Nessun gate può segnalare una pagina che nessuno ha pensato di scrivere.**»* → La prima ha un rilevatore automatico, la seconda ne è **strutturalmente priva**. ✅ **E il fatto migliore trovato nel codice di rischio, che non era scritto da nessuna parte**: `hypothetical_shock` **solleva se manca anche un solo shock**. L'alternativa comoda — default 0 per gli attivi non specificati — **affermerebbe in silenzio che quelli non si muoveranno**: **una previsione travestita da valore predefinito**. ⚠️ **A24**: `risk_contributions_from_covariance` dà **zeri** a volatilità nulla mentre `beta` dà **`None`** — difendibile (*il contributo **è** zero, un beta sarebbe 0/0*) ma è la **terza** convenzione sui degeneri nello stesso modulo |
| D138 | 18 Set 2026 | 🔴 **Due sistemi di colore che dichiarano lo stesso principio e danno colori diversi** — contraddizione di prodotto che nessuno dei due mandati possiede | Trovata da **G**, che l'ha segnalata invece di risolverla perché *«sta a cavallo fra B e me»*. `assetTypeBadgeClass(x)` è **funzione del solo tipo** → mappa fissa e deterministica: STOCK è **sempre** quel blu. Il colore dei grafici di G è **funzione del rango di valore nel portafoglio** (`palette[groupIndex % palette.length]`) → **STOCK è verde nel portafoglio A e ambra nel portafoglio B**. 🔑 **Entrambi implementano «il colore segue il contenuto». Entrambi sono verdi. Danno colori diversi per lo stesso tipo, sulla stessa schermata.** Post-merge l'utente vede una pastiglia `ETF_STOCK` nel blu-STOCK di B **accanto** a una fetta `ETF_STOCK` sfumata da un colore di rango: **l'invariante di B vale fra le pastiglie, D71 vale dentro il grafico, e nessuno dei due promette coerenza fra le due superfici.** Tre qualificazioni di G, tutte corrette: **(1)** non l'ha introdotta lui — è **preesistente**, le pastiglie hanno sempre avuto una mappa e i grafici un `color: palette` posizionale; **(2)** **diventa visibile adesso**, perché per la prima volta le due superfici **dichiarano lo stesso principio** — prima erano solo diverse, ora sembrano in contraddizione; **(3)** **potrebbe andare benissimo così**, perché dentro il grafico la gerarchia si legge ed è ciò che D71 chiede — *«ma è una decisione da **prendere sapendo**, non da **scoprire** guardando uno screenshot di J»*. ✅ **Nota collegata (F38 di B)**: `BADGE_CLASS_MAP.LIQUIDITY` è stata aggiunta da B, quindi **la tredicesima chiave ha già un colore** se G calcolasse la tavolozza *via* `assetTypeBadgeClass` — che è precisamente la scelta che deciderebbe la contraddizione in un verso o nell'altro |
| D139 | 18 Set 2026 | ⚠️ **«Accetta il rosso» non è ottenibile per ciò che richiede un bundle: non diventa rosso, diventa IMPOSSIBILE** — correzione di G a una regola diffusa dal coordinatore | Il coordinatore aveva istruito i mandati dipendenti da contratti non fusi a *«scrivere contro il contratto e accettare il rosso»*. **G l'ha corretta**: vale per `svelte-check`, **non** per Vitest con mount, Playwright, galleria o dev server. *«Senza `primaryAssetType` Vite **non risolve l'import**: non c'è app, Playwright non misura nulla. **Non diventa rosso — diventa impossibile.** Non esiste un rosso da accettare.»* ⚠️ **Chi riceve l'istruzione sbagliata va a cercare un rosso che non arriverà mai.** ✅ **Forma corretta, eseguita da G**: shim temporaneo con la firma esatta → misura → **ripristino** → **prova di identità byte a byte** (`git diff --stat` vuoto sul file altrui). Si aggiunge a **D124**: lo shim serve **due volte** — per riaccendere il controllo dei call-site *e* per rendere eseguibile ciò che senza di esso non parte. 📌 Da correggere nel kickoff e in ogni relay futuro |
| D140 | 18 Set 2026 | ✅ **Il lavoro non committato È recuperabile: `refs/copilot/checkpoints/` esiste** — «sparisce senza traccia» era sovrastimato | Trovato da **B**, **verificato dal coordinatore**: `git for-each-ref refs/copilot/checkpoints/` restituisce **1 198 ref su 27 sessioni**. Campione misurato: **B 21 checkpoint** (ultimo `5b1a29071`, 30 file), **C 7** (`66857888b`, 8 file), **F 9** (`81751254a`, 25 file). **L'applicazione checkpointa continuamente in un namespace che nessuno aveva guardato**, quindi il delta di ogni mandato è **nell'object store e indirizzabile**, pur restando ogni ramo a `cc33120eb` — **nessuno ha committato, la regola è intatta**. ⚠️ **La sfumatura è tutta la differenza, ed è di B**: *«la traccia **esiste** ma **non è su nessun percorso che un revisore percorre**. `git log` su un ramo non mostra niente; serve `--all` o `for-each-ref`, e nessuno lo digita durante un merge. Sono inoltre ref **gestiti dall'applicazione**, quindi potabili: non sono un archivio su cui fare affidamento.»* → **L'avvertimento pratico resta; la parola «irrecuperabile» no.** B l'ha formulato come conviene: *«preferisco togliertela dalle mani come dramma e dartela come procedura»*. 📌 **Per il developer**: se un delta sparisce in integrazione, `git for-each-ref refs/copilot/checkpoints/ \| grep <sessione>` dà l'indirizzo, e `git diff cc33120eb <sha>` il contenuto |
| D141 | 18 Set 2026 | 🔑 **Non vince l'ultimo messaggio: vince l'albero** — B corregge una regola adottata dal coordinatore un'ora prima | Il coordinatore aveva adottato, dopo D127: *«l'ultimo messaggio vince, e se contraddice un ordine anteriore l'ordine anteriore era sbagliato per definizione»*. **B l'ha rotta sul messaggio stesso che la conteneva**: quel messaggio, l'ultimo, apriva per la **terza volta** con *«scongela e aggiungi le tre asserzioni, `asset-unit` **era 256**»* — **fatte da un'ora, conteggio 262, falsificate tutte e tre**. Applicando la regola alla lettera B avrebbe dovuto **rifarle**. 🔑 *«"L'ultimo vince" ordina i messaggi **per quando li scrivi tu**, non per quando conoscono lo stato. In una coda in ritardo, **l'ultimo messaggio è anche il più stantio** — è precisamente il caso che la regola doveva impedire.»* ✅ **Correzione, e coincide con l'unica cosa che ha funzionato tutto il giorno**: *«Ogni volta che ho intercettato una base falsa — il `13`, la baseline dei link, il selettore inventato, F16 "latente", e adesso "era 256" — non l'ho fatto confrontando date. **L'ho fatto eseguendo qualcosa e guardando cosa rispondeva il codice.»* → **Un ordine che contraddice lo stato verificabile non è un ordine più recente: è un ordine su un albero che non esiste.** 📌 Conseguenza per il coordinatore: la precedenza non si stabilisce per timestamp ma **per verificabilità**; un mandato che misura ha **sempre** ragione su un mandante che ricorda |
| D142 | 18 Set 2026 | ✅ **Ritrattata: i «tre `DocsLink` di produzione rotti» non esistono — zero rotti** · e la (a) si sblocca perché l'ostacolo era l'errore stesso | **I** ritira una propria misura data come fatto. Causa: **aveva misurato tutti i link `/mkdocs/` con un modello solo**, mentre i meccanismi sono **due** — `<DocsLink path="…">` compone `/mkdocs/${lang}/${path}` e raggiunge **quattro lingue**; un **letterale grezzo** (`window.open('/mkdocs/…')`, `<a href>`) **non porta prefisso** e atterra **sempre in inglese**. I tre dichiarati rotti — `DistributionDataImportModal.svelte:48`, `ScheduledInvestmentEditor.svelte:1049,1070` — sono **letterali grezzi**, e in inglese le tre ancore **risolvono tutte**. Sonda riscritta col modello giusto, ancore lette dal sito costruito: **10 link distinti, 0 rotti**. *«Li avevo trovati controllandoli contro it/fr/es — cioè **contro pagine dove l'utente non viene mai mandato**.»* 🔑 **Conseguenza: la decisione (a) era bloccata da un'obiezione che I stesso aveva prodotto.** Riparare `dev.py:1197` **non accende alcun rosso che qualcuno debba chiudere** → resta solo fallire-o-avvisare, e la scelta diventa **fallire**, senza debito da assorbire. 🔴 **Ma sotto c'è un difetto vero e diverso**: tre link con ancora **non portano la lingua** → **salto di lingua silenzioso**, invisibile a `check-links` che valida contro l'inglese. E peggio: **`create-edit.en.md` ha 11 heading, it/fr/es ne hanno 10** — la sezione *«Importing a Distribution CSV»* **non esiste nelle traduzioni**, quindi anche localizzando il link l'utente italiano **arriverebbe su una pagina che non contiene la cosa per cui è stato mandato lì**. Non è divergenza di slug: è **contenuto mancante**, e **nessuna ancora lo ripara — solo una ritraduzione**. ✅ **E il pattern delle 41 ancore congelate non è più un'ipotesi**: `kpi-cards` porta **le stesse 3 ancore in tutte e 4 le lingue** con i titoli **pienamente tradotti** (`## 📉 Card 1 — Period P&L {: #card-1-period-pl }` / `## 📉 Scheda 1 — P&L del Periodo {: #card-1-period-pl }`) — **unici link localizzati con ancora dell'app, verdi 4/4**. 📍 **Nota di metodo di I**: *«Una sonda sbagliata è peggio di nessuna sonda: **produce un numero**, e un numero non si mette in discussione come si mette in discussione un'opinione. La mia ha prodotto `3` per una settimana, e `3` sembra una misura. L'unica cosa che l'ha smontata è stata **guardare la riga di sorgente** invece del risultato della sonda: due secondi di lettura contro sette giorni di misura»* |
| D143 | 18 Set 2026 | 🔑 **Il significato di una misura dipende dallo stato del mondo al momento della misura, e quello stato NON è nell'output** | Formulazione generale di **G**, che chiude la famiglia aperta da D127/D129 mettendo accanto **i due versi opposti dello stesso difetto**. | | cosa cambia | esito | |---|---|---| | **I** | il mondo cambia **sotto** il misuratore | **falsa ritrattazione** — sfiducia in uno strumento sano | | **G, passo 7** | il mondo non è mai stato completo come la misura implicava | **falsa fiducia** — un verde che non aveva guardato | 🔑 *«`278→2`, `285→2`, `0 errori`, `68 passed`: **nessuno di questi numeri dice in che albero è stato preso**.»* ✅ **E la lettura del protocollo shim che nessuno aveva esplicitato**: `git diff --stat` **vuoto** non serve solo a provare di non aver sporcato — serve a **datare il mondo accanto alla misura**. → **Forma minima adottata per tutti i mandati**: *ogni misura riportata porta con sé lo stato dell'albero in cui è stata presa, non solo il proprio esito.* **L'agente di I avrebbe avuto due stati diversi davanti, e non avrebbe ritrattato.** 📍 Corollario di G sul proprio piano vivo: il passo 7 vi compare **prima** del 6 perché è scritto **in ordine di esecuzione** — *«è esattamente il tipo di traccia che, riletta a freddo, fa ricostruire una cronologia sbagliata»*. **Un piano vivo è un ordine di esecuzione, non un indice.** 📌 E la **terza immunità** di G, registrata come **proprietà del design e non come merito**: iniettare il contratto invece di importarlo ha pagato tre volte — unit eseguibili senza `generated.ts`, senza il file di B, e **insensibili a una scrittura concorrente**. *«Non l'avevo presa per questo.»* |
| D144 | 18 Set 2026 | 🔴 **`min_coverage` è uno slider visibile all'utente che non può fare nulla** — e l'avviso che governa è irraggiungibile per costruzione | Trovato da **A** in A5/M3, e **provato empiricamente, non dedotto**. `CorrelationParams.min_coverage` ha chiave i18n `risk.params.minCoverage`, `x-control-order: 2` e step 0,05: **è un controllo nell'interfaccia**. Ma `coverage` nel plugin vale **identicamente 1.0**, e la catena è tutta verificata: (1) `prepared_asset_returns` restituisce `tuple[float, ...]` — **nessun `None`** → la cancellazione a coppie dentro `pairwise_correlation` è **codice morto su questo cammino**; (2) il validatore di `PreparedAssetSeriesSet` (`schemas/risk.py:424`) **rifiuta** ogni set i cui membri non condividano il calendario; (3) → `len(serie) == n_observations` per ogni asset; (4) → `coverage = 1.0`; (5) `service.py:230` non esegue l'analitica sotto `min_observations = 2`, quindi il degenere non raggiunge `compute()`. → **`coverage < min_coverage` è sempre falsa: l'avviso `low_coverage` non può scattare.** Prova su set reale a 3 asset col valore **più severo che Pydantic consenta**: `distinct coverage: [1.0]` · `warnings at min_coverage=1.0: ['flat_series']` · **`low_coverage reachable: NO`**; e il set irregolare → `REJECTED`. ⚠️ **Stessa famiglia di A9 — una grandezza che *sembra* per-cella ed è strutturalmente costante — ma peggiore, perché questa l'utente la vede e la muove.** **A non l'ha rimosso**, correttamente: togliere un parametro utente tocca i18n e frontend, **è prodotto, non matematica**. 📌 **Inquadramento**: il parametro **anticipa una capacità che il sistema non ha** (cancellazione a coppie), vietata dal validatore del calendario congiunto. → **decisione del developer**: nascondere il controllo, oppure rilassare il validatore e rendere raggiungibile il cammino che il parametro presuppone ✅ **RACCOMANDAZIONE RIBALTATA da A, 18 Set 2026** — e ora ha una forma decidibile. A aveva trovato `min_coverage` inerte **senza saper dire perché** la copertura fosse sempre 1,0 dentro il plugin. La spiegazione: **le date irregolari vengono già scartate a monte** — `included_return_dates` tiene solo l'**intersezione** — e quanto si è perso è registrato **un livello più su, sotto un altro nome**: `calendar_coverage`, più l'elenco `incomplete_valuation_dates`. **Il plugin riceve un blocco già rettangolare: la sua copertura non ha più niente da misurare.** → 🔑 *«Il cursore non è inutile per sbaglio: è inutile perché **misura una cosa già misurata altrove**.»* **Quindi la raccomandazione non è più «togliete un controllo rotto» ma «il controllo giusto esiste già e si chiama `calendar_coverage`»** — ed è **più informativo**, perché distingue *quali* date sono cadute invece di dare solo un rapporto. Resta decisione di prodotto, **ma ora ha una forma che si può decidere.** |
| D145 | 18 Set 2026 | 🔴 **`correlation_matrix` non ha chiamanti di produzione: §M3 intestava il guadagno a una funzione morta** · e la trappola del quasi-piatto | Misurato da **A** enumerando i chiamanti invece di leggere il piano. | funzione | chiamanti in `backend/app/` | |---|---| | `correlation_matrix` | **NESSUNO** — solo `__all__` | | `covariance_matrix` | `risk_contribution.py:58` ✅ | | **`pairwise_correlation`** | **`correlation.py:72`, doppio ciclo N²** ✅ | → *«Il guadagno intestato a `correlation_matrix` **nessun utente lo vedrebbe**. Se un altro mandato pianifica su quella riga di doc 06, **sta ottimizzando una funzione morta**.»* Il costo vero è nel **doppio ciclo**, che `06` citava in **terza** posizione. Misura sul cammino reale: 10 asset 47,1 ms → 0,608 (**77×**); 25 → **195×**; 100 → **544×**; **100 × T=2500: 15 987 ms → 21,6 ms, 742×**. **Sedici secondi** — in `asyncio.to_thread`, quindi senza bloccare il loop, **ma l'utente li aspettava**. 🔴 **E la trappola del quasi-piatto, peggiore di quella di M1**: su serie **esattamente** piatta (`std = 0`) ciclo e NumPy coincidono **per caso** (`0/0` è `nan`); su **std = 1,001e-17** il ciclo dà `None` e `np.corrcoef` dà **`−0,02010614683967737`** — *«una correlazione che nessuno metterebbe in dubbio guardandola»*. **In M1 leggere il `nan` dava comunque un `nan`, cioè qualcosa di visibile; qui dà un numero plausibile e privo di significato.** → **L'indefinito si prova sul denominatore, mai sul `nan` in uscita**: regola da portare in M6 e in ogni mandato che tocchi una matrice. ✅ Corollari verificati: **nessuna contaminazione** (un asset piatto sposta le altre celle di **esattamente 0.0**); una serie **costante non nulla** è piatta → `None`, non `1.0`; **la diagonale non è esattamente 1.0 su nessuno dei due rami** — preesistente, entrambi si affidano al clamp, **da non «aggiustare»**: sposterebbe numeri pubblicati. 📌 Micro-ottimizzazione **deliberatamente non fatta**: a N=100/T=2500 il residuo è **validazione `_finite_values` 13,22 ms (61 %)** contro **matematica 2,76 ms** — ma è condivisa da tre funzioni e l'ordine dei controlli fa vincere *«must be finite»* su *«common calendar»*; **13 ms una volta per richiesta non li percepisce nessuno** |
| D146 | 18 Set 2026 | 🔑 **«Una decisione si relaya; il codice si integra o non c'è»** — quando si annuncia CODICE si allega lo SHA | Regola di **F**, dopo la **terza** volta che il coordinatore gli ha descritto un albero che non possiede: prima **K5**, poi i contratti in `implementation/contracts/`, ora **le primitive di D** (*«la tua tabella "Prima → Adesso" descrive il worktree di D, non il mio; se le adottassi ora importerei un file inesistente e **romperei il build** — cioè l'unico gate che oggi mi passa pulito»*). 🔑 **Rimedio, costo una riga per relay**: *«quando mi annunci del **codice** (non una decisione), allega lo **SHA del commit che lo porta**. Così verifico con `git cat-file -e <sha>:<path>` invece di `ls`, e soprattutto **so dire se è nel mio ramo o solo nell'universo**.»* ✅ **Applicato immediatamente**: le primitive di D sono a **`1a537f9cb`**, verificate dal coordinatore — `RiskMetricCard.svelte`, `KpiMetricBar.svelte`, `KpiDivergingFlowBar.svelte` tutte presenti. ⚠️ **E lo SHA non è un permesso ad adottare**: esistono **nell'object store**, non nel ramo di F — *«lo SHA è il modo di sapere che cosa esisterà e dove»*. 📌 Reso possibile da **D140**: `git for-each-ref refs/copilot/checkpoints/ \| grep <prefisso-sessione> \| tail -1`. **Tutte e tre le volte F ha verificato invece di credere**, ed è il motivo per cui la regola nasce da lui |
| D147 | 18 Set 2026 | 🔴 **TRE campi `coverage` omonimi in `schemas/risk.py`, con denominatori diversi** — il meccanismo che produce D102 | Due trovati da **F**, il terzo dal coordinatore cercandolo. | riga | modello | significato | |---|---|---| | `:451` | `RiskResultMetadata` | quello di **D102** — denominatore ≠ da `annualization_factor` (`:450`) | | `:656` | `RiskMatrixCell` | frazione della finestra in cui **entrambi** gli asset avevano dati | | `:967` | `RiskDrawdownOutput` | terzo, non censito | 🔑 *«Tre campi omonimi con denominatori diversi nello stesso file sono esattamente ciò che produce l'errore che mi hai segnalato. **L'ho quasi fatto io leggendo il tuo messaggio.**»* ✅ **E il precedente per il rename esiste a venti righe da uno di loro**: `:400 calendar_coverage`, `:401 fresh_quote_coverage`. → **Difetto di naming da correggere a monte** (`cell_coverage` / `calendar_coverage` / …), **non da documentare a valle**. ✅ **Difesa di F accolta e registrata**: il suo `coverage` è **il segnale giusto per una cella** — *«in una correlazione non c'è annualizzazione, è adimensionale, quindi la distorsione descritta non può nascere lì»*; `CorrelationHeatmap.svelte:177` legge `params.data`, cioè **la cella**, non i metadati |
| D148 | 18 Set 2026 | 🔑 **In `X[k] ?? k` il `k` del ripiego è quello NORMALIZZATO, non l'originale — una trasformazione incondizionata a monte rende inerte metà del ragionamento a valle** | Generalizzazione di **G**, che spiega **perché** il non sequitur del coordinatore su `primaryAssetType` era invisibile. `const raw = (type ?? '').trim().toUpperCase(); return PRIMARY_TYPE_MAP[raw] ?? raw;` — *«chi legge `?? raw` vede "restituisce l'ingresso" e **non nota che `raw` è già stato riscritto due righe sopra**. Il ripiego non è una via d'uscita neutra: **è già trasformato**.»* 🔑 **E la stessa proprietà ha agito nei due versi nello stesso scambio**: a **favore** di G — il suo `.toUpperCase()` a `:140` **assorbe** la divergenza fra lo stub sbagliato e K2 reale, rendendo i 68 test validi sotto entrambe le semantiche — e a **sfavore** del coordinatore, che aveva dedotto «non passa dall'enum → esce verbatim» quando la funzione **non consulta l'enum per decidere se rialzare**: rialza **prima**. ✅ **Misura conclusiva di G**: la cassa **attraversa** il roll-up in **entrambi** i grafici — nessun filtro sui nomi, solo su valore (`AllocationPieChart:163`) e intervallo — quindi **grafia in uscita `LIQUIDITY`, cardinalità 13** in modalità tipo. La distinzione è già fissata per riga nei test: `key === 'Liquidity'` (**display, grafia originale**) contro `primary === 'LIQUIDITY'` (**gruppo, rialzata**) — *«le due convivono per riga, non si scelgono»*. ⚠️ **E la prescrizione del coordinatore («indicizza sulla grafia che esce») non era scrivibile**: presuppone una **ricerca per chiave**, e la tavolozza **non ha chiavi** — solo `palette[groupIndex % length]`. **Non esiste una ricerca che possa mancare**; l'invariante dietro la prescrizione — *«due fette non devono finire dello stesso colore»* — è **già asserito end-to-end**. 📍 Nota d'integrazione: i riferimenti a `AllocationHistoryChart.svelte` **oltre la riga 107 sono slittati di ~14** nel ramo di G |
| D149 | 18 Set 2026 | 🔴 **M6 ridotto a UNA funzione: un passo di piano scritto prima dei suoi predecessori può distruggere ciò che hanno costruito** | **A** ha **misurato M6 prima di toccarlo**, ed è l'unico passo del mandato la cui giustificazione era **estetica** (`06:575`, *«non esiste ragione per tenerli in `math` puro»*). **Nessuna delle nove funzioni supera i 6 ms a dieci anni**, nessuna è dentro un ciclo (verificato: in `stress.py` la chiamata è **dopo** il ciclo, `:473` contro `:451`). | passo | ha comprato | |---|---| | M1 | 80,4 ms → 1,26 ms | | M3 | **15 987 ms → 21,6 ms** | | **M6 com'era scritto** | **~20 ms in tutto, su nove funzioni** | → *«nove occasioni di rompere qualcosa per un guadagno che nessuno percepisce»*, e sono **le nove con la semantica più delicata del modulo**. 🔑 **La scoperta strutturale, che vale oltre M6**: *«la lista di M6 è stata scritta **prima** che M1 e M3 esistessero nella forma finale. M6 **non deve migrare ciò che M1 e M3 hanno deliberatamente lasciato indietro come oracolo**. Se qualcuno esegue la lista alla lettera, **smonta la rete di sicurezza che i due passi precedenti hanno costruito**»* — e lo farebbe **eseguendo il piano correttamente**, che è la forma peggiore. → **Regola: prima di eseguire un passo di piano, verificare che i passi già eseguiti non abbiano cambiato il significato della sua lista.** **TAGLIO DECISO**: **Fascia 0 ESCLUSA** — `pairwise_correlation` (a cui **A stesso ha tolto i chiamanti con M3**, promuovendola a **semantica scalare di riferimento**) e i due helper a ciclo di `signal_helpers`: vettorializzarli renderebbe il differenziale **NumPy contro NumPy**, *«che non dimostra niente»*. **Fascia 1 FATTA** — `wealth_index` → `np.cumprod`: **unica migrazione della campagna a deriva NULLA**, 6 casi su 6 bit-identici inclusa la perdita totale a `-1.0`, `max\|delta\| = 0.000e+00`, perché `cumprod` fa **le stesse moltiplicazioni nello stesso ordine** — nessuna riassociazione, a differenza di `fsum` contro somma incrementale (**9,77e-12** in M1, **1,185e-13** in M3). È la primitiva condivisa da **tre** composti → li velocizza **senza toccarne le macchine a stati**. ✅ **E così D94 si risolve invece di doversi gestire**: l'invarianza di M6 non va più **asserita**, è **bit-identica e dimostrata**. **Fascia 2 ESCLUSA con una ragione che A non poteva avere**: `current_buy_and_hold_returns` è la funzione su cui **C ha chiesto per iscritto una garanzia di contratto** (firma, `cash_weight` keyword-only, residuo **non rinormalizzato internamente**) e su cui ha costruito l'affettamento **rinormalizzando a monte** — C è `FROZEN` con 146 test misurati contro l'implementazione attuale, e aveva avvertito che *«se M6 rinormalizzasse dentro, la mia rinormalizzazione diventerebbe **doppia** e i pesi mentirebbero in silenzio»*. **5,1 ms non valgono l'invalidazione della misura di un mandato congelato.** **Fascia 3 RIFIUTATA**: 🔴 `horizon_compounded_returns` **a `horizon_days=1` non è identità** (`compounded_return([v]) = 1.0*(1.0+v)-1.0`) e sta sul **cammino caldo del VaR** (`metrics.py:735`) → una riscrittura che scorciatoi l'orizzonte 1 **sposta un numero pubblicato**, per 1,6-3,3 ms; `drawdown_episodes` ha un chiamante in **AI Export** (`drawdown_context.py:499`), **fuori mandato**; le altre due valgono 0,3-0,4 ms. 📍 **Autocorrezione di A**, presa su di sé prima di consegnarla: si era appuntato `wealth_index` e `horizon_compounded_returns` come **codice morto** — **falso**, perché il `grep` sui soli `risk_plugins/` non le vede: sono chiamate **dentro `metrics.py`**. **Un grep ristretto al posto sbagliato produce uno «zero chiamanti» che sembra una misura** (D132) |
| D150 | 18 Set 2026 | 🔑 **Una ricerca letterale non può vedere ciò che il programma costruisce mentre gira** — la radice di quattro difetti in un giorno | Regola formulata da **I** dopo averla incontrata la quarta volta. | # | dove | cosa è stato nascosto | |---|---|---| | 1 | `dev.py:1197` | **142 ancore su 156** — il gate validava **per coincidenza di stile** | | 2 | `AboutTab.svelte:172` | falso positivo in una simulazione di patch | | 3 | la sonda di I | `window.open('/mkdocs/…')` **senza `${lang}`** → A11 **ritrattata**, «3 link rotti» → **zero** | | 4 | `RiskAnalysisPanel.svelte:1054` | *«il prodotto non te lo dice»* **mentre te lo dice, tradotto in quattro lingue** — la stringa è `$t(\`risk.stress.rules.${audit.rule}\`)` | 🔑 **Due osservazioni che la rendono operativa**: *«**due delle quattro hanno prodotto un numero** — e un numero non si mette in discussione come un'opinione»*; e *«**tutte e quattro sono state smontate leggendo la riga di sorgente, mai da un'altra misura**»*. → **Regola adottata: prima di dichiarare che qualcosa NON esiste nel codice, cercare anche la forma composta** — `${…}`, `f"…"`, concatenazione. ⚠️ Con la qualificazione che le dà peso: **in questa campagna le assenze sono metà delle scoperte** |
| D151 | 18 Set 2026 | 🔑 **Un valore neutro non è un'astensione: è un'affermazione** — due facce dello stesso difetto concettuale, entrambe in `stress.py` | Da **I**, e corregge una ratifica del coordinatore. **(1) Lo shock a zero.** Il coordinatore aveva ratificato come *«la cosa migliore trovata nel codice di rischio oggi»* il fatto che `hypothetical_stress_return` **sollevi se manca anche un solo shock**. **Falso nella portata**: è vero nel primitivo ma **irraggiungibile dall'utente** — il plugin prende shock **per bucket** e li espande a ogni attivo, quindi il dict copre sempre tutti; il `raise` è **un'invariante interna**. E per la dimensione **classe di attivo** il codice fa **esattamente l'alternativa dichiarata esclusa**: `if exposure_bucket_id not in bucket_shocks: return ([], None, 0.0, RiskStressApplicationRule.UNCONFIGURED_ZERO)`. ⚠️ **Ma anche la correzione dell'agente era falsa** (*«vale zero in silenzio, `grep UNCONFIGURED_ZERO frontend/src` → zero occorrenze»*): la regola **arriva all'utente, tradotta in quattro lingue**, in una tabella di audit per attivo e per bucket (`RiskAnalysisPanel.svelte:1054`). **La verità è una terza cosa, e migliore**: settore e geografia **obbligano** a dichiarare un bucket `Other`, classe di attivo no — **e ogni bucket dichiara la regola applicata**. 🔑 *«"Senza ipotesi nascoste" è vero, ma il meccanismo non è né il rifiuto né il silenzio: è **la dichiarazione**. Che è più forte, perché **uno shock a zero è una previsione** — dire che un ETF non si muove mentre le azioni cadono del 30 % è **un'affermazione sul mondo**, e la differenza con un'ipotesi nascosta non è che sia più prudente, è che **è scritta in una riga leggibile**.»* **(2) L'esclusione.** `stress.py:485-488`: **escludere un attivo non rimpicciolisce il portafoglio, lo appiattisce** — il peso escluso finisce in `cash_weight` a rendimento **zero**, quindi *«escludere un 10 % in un episodio in cui tutto è caduto **afferma che sarebbe stata la cosa migliore che possedevi**»*. ✅ E il *«nessuna ipotesi nascosta»* del mandato **esiste davvero ma su un'altra pagina**: `stress.py:455-465`, policy `MANUAL_PROXY_OR_EXCLUDE` — senza storia sufficiente **l'analisi si rifiuta di girare**. 📌 **Instradato al developer, non a un mandato**: `risk_plugins/stress.py` **non ha proprietario nella campagna** — verificato, A/H/N/C/B a **zero**. L'asimmetria settore-geo contro classe-di-attivo **è difendibile perché dichiarata, ma è una scelta che nessuno ha scritto**, e *una scelta non scritta si distingue da una svista solo chiedendo a chi l'ha fatta* |
| D152 | 18 Set 2026 | 🔑 **«Descrivere bene un meccanismo non dice nulla su dove agisce»** — complemento esatto di D121 | Autocritica di **G** dopo aver ritirato la propria chiusura sul wrap della tavolozza: *«non è stato rigore, è stato **arrivare secondo**. … Il conto era sbagliato **in un file che io stesso avevo documentato con precisione**: avevo descritto il meccanismo giusto e concluso che fosse spento.»* Il suo commento a `AllocationHistoryChart:114` — *«handed the 13th category the colour of the 1st … **not a crowded legend: a silent collision**»* — era **la prova scritta che aveva capito il meccanismo, e insieme la prova che non aveva guardato dove altro agisse**: in modalità **settore** succede alla **15ª**, e poiché i nomi sono ordinati per peso **la fetta più piccola veste quella più grande**. 🔑 **Due forme della stessa cosa**: in **D121** I aveva una **formula giusta applicata all'oggetto sbagliato** (recupero del drawdown **massimo** contro **corrente**); qui G ha un **meccanismo giusto attribuito alla dimensione sbagliata**. → **La correttezza locale non implica la portata.** ✅ Cura consegnata: commento riscritto **per dimensione invece che per totale** (13 ≤ 14 nel ramo tipo → nessuna collisione; **15 > 14** nel ramo settore → **una sopravvive**), col motivo per cui non si nota — *«l'etichetta i18n si risolve comunque → **nome giusto, colore sbagliato → sembra una scelta di design**»* — e il motivo per cui crescere ancora **non chiude**: `getSectorKeysList()` legge lo store API e ripiega sui 14 **solo se vuoto**, quindi **nessun numero è dimostrabile sufficiente**. ✅ **Avviso di wrap nei DUE punti**, non solo in quello vivo: il ramo gerarchico è **latente** (13 ≤ 14 **oggi**), e metterlo solo dove morde l'avrebbe lasciato muto il giorno in cui la tassonomia cresce. ✅ **E `debug.warn` invece di `console.warn`**, correzione di G a una prescrizione del coordinatore: il progetto **ha già l'idioma** (`$lib/debug`, no-op **eliminato dal minifier** in produzione, già usato da `toastStore`/`notify`). Prezzo dichiarato e accettato: `allocationHierarchy.ts` acquisisce `import.meta.env` — **costante di compilazione**, non una dipendenza che rompe l'eseguibilità; le tre proprietà che contano (niente rune, niente `generated.ts`, niente file di B) **restano intatte**, e i 68 verdi lo dimostrano |
| D153 | 18 Set 2026 | 📍 **Le decisioni si nominano, non si numerano** — terza collisione di etichette in una giornata | Il coordinatore ha usato **«(a)»** per **due cose diverse in due messaggi**: nel primo il *debito modalità settore*, nel secondo la *correzione dello stub K2*. **G ha ricevuto il primo** e ha correttamente concluso che lo stub **non era stato deciso** — interpretando *«nient'altro, poi richiuditi»* **restrittivamente**, che è la lettura giusta di un'istruzione ambigua, e **chiedendo invece di decidere da solo**. È la **terza** istanza: prima **(d)/(d)** con **B** — *sezioni* contro *`ETF_MONETARY` di primo livello* — nominata da B stesso. → **Regola: una lettera non identifica una decisione.** Si nomina la cosa (`stub-K2`, `commento-114`, `debito-settori`), non la si numera. ⚠️ **E la modalità di guasto è quella di D133**: una voce etichettata male non viene dimenticata, **viene spuntata sulla cosa sbagliata** — *«nessuna risposta è la peggiore delle tre»*, come ha scritto G chiedendo un sì o un no |
| D154 | 18 Set 2026 | 🔴 **Il relay di K4 conteneva codice scritto contro una forma che non esiste — e il caso mancato era il più comune** | Trovato da **E**, verificato dal coordinatore. Il requisito di **C** su `canonicalizeScope` era stato girato a E **alla lettera**, e lo snippet presupponeva un oggetto `next` e un ramo **non condizionato**. Il codice reale (`riskRequest.ts:79-86`) ha **early return** e il ramo portfolio è **condizionato a `broker_ids`**: `if (scope.kind === 'portfolio' && isNumberArray(scope.broker_ids)) return {...}`. 🔴 **Conseguenza**: `{kind:'portfolio', asset_ids:[…]}` **senza `broker_ids`** resterebbe **non canonicalizzato** — **ed è esattamente ciò che Dashboard monta** (`scope={{kind:'portfolio'}}`), mentre Broker Detail passa `broker_ids`. → *«**La fetta più comune sarebbe stata l'unica a conservare una chiave di cache dipendente dall'ordine** — cioè precisamente il danno che K4 esiste per impedire.»* **Le due liste vanno trattate indipendentemente, e il caso Dashboard va nel test.** ⚠️ **Nessuno dei due mandati poteva vederlo**: C non possiede `riskRequest.ts`, E non aveva il requisito se non tramite il coordinatore. **Il requisito di C era giusto; la trasmissione no.** 🔴 **E una seconda scoperta che nessuno aveva previsto**: `baseSignature` **serializza lo scope grezzo** — `buildRiskQueryRequest` canonicalizza, **la firma no**. Una volta che `asset_ids` entra *nello scope*, **l'ordine dell'array resetta le quattro analisi a richiesta**; Dashboard lo deriva da `[...new Set(holdings.map(…))]`, quindi **l'ordine segue quello dei possedimenti** → **un ricaricamento in ordine diverso = firma diversa per la stessa domanda**. → **È `discard-the-answer-not-the-question` che rientra dalla porta dello scope**, cioè la stessa difesa che E aveva protetto con `registerLauncher`, aggirata da un canale che allora non esisteva |
| D155 | 18 Set 2026 | 🔴 **`RiskReturnBasis` ha due valori e il backtest a composizione pesata non è nessuno dei due — C riaperto per un membro di enum** | Verificato: `PRICE_ONLY`, `TWRR`, e basta (`schemas/risk.py:39-43`). Ma l'affettamento in `HISTORICAL` prende il **ramo pesato** (`current_buy_and_hold_returns` via `service.py:608`), quindi **una corsa affettata riporta oggi `TWRR`, che è falso**. 🔑 **Perché non si risolve lato frontend**, argomento di **E** che chiude la questione: *«il frontend potrebbe solo **inferire** l'avviso da "la fetta è attiva" — **una congettura del client su una decisione del server, che sembra un fatto riportato e invecchia in silenzio**»*. **Un valore inferito che si presenta come dichiarato** è la famiglia di D115. E la **seconda** delle quattro dichiarazioni obbligatorie di K4 — *«`price_only` è un backtest»* — **non è rendibile onestamente** se il metadato dice `TWRR`. ✅ **Portatore giusto e già esistente**: `return_basis` vive su `RiskResultMetadata`, cioè **su ogni risultato**, **già letto da `riskMetadata()`**. **Decisione**: **C riaperto** per un solo membro di enum più la riga in `K4.md` — il nome deve descrivere **la serie consumata** (composizione pesata riproiettata all'indietro), **non lo scope**. ⚠️ Due verifiche richieste a C perché il coordinatore non le ha fatte: che **nessun consumatore faccia un `match` esaustivo** sui due valori attuali, e che **AI Export non legga `return_basis`** (`drawdown_context.py` cita `historical_twrr` in un docstring). 📌 **K4-c resta bloccato finché l'enum non atterra — e solo l'avviso, non il resto** |
| D156 | 18 Set 2026 | 🔑 **Sedicesima forma: un test che guarda un mondo che il contratto vieta** — correggere lo stub ha TOLTO una copertura, ed era copertura finta | **G**, dopo aver corretto lo stub K2 nei propri unit test, **ha mutato ciò che aveva appena corretto**: tolto il `.toUpperCase()` a valle, **atteso rosso, ottenuto 68 verdi**. | stub | il `.toUpperCase()` è portante nei test? | |---|---| | **verbatim** (prima) | ✅ toglierlo faceva fallire l'asserzione | | **corretto** (adesso) | ❌ **no — è il contratto stesso a normalizzare** | 🔑 *«Correggendo lo stub **ho rimosso una copertura**. Ma era **copertura finta**: proteggeva da un ingresso che il contratto **vieta**. **Un test che difende da un caso impossibile non è copertura, è rumore** — e mi dava un rosso a comando su una riga che in produzione non può sbagliare.»* → **Quel rosso esisteva solo perché lo stub mentiva**: misurava **la falsità dello stub, non la correttezza del codice**. ⚠️ **Le quindici forme precedenti erano controlli che NON guardavano; questa guardava — un mondo che il contratto vieta.** È la fixture di A che fabbrica quotazioni di sabato (D101 §5), in forma di test unitario. ✅ **Residuo dichiarato da G e instradato a B**: la difesa a valle protegge da una **deriva contrattuale** — se `primaryAssetType` smettesse di rialzare — **e nessun test la rileverebbe**; ma l'invariante è **una proprietà di K2**, quindi va pinnata **nel file di B**, *«da me sarebbe il secondo guardiano della stessa proprietà, nel posto sbagliato»*. Due asserzioni instradate: **l'uscita è sempre `=== output.toUpperCase()`**, e **`null`/`undefined`/`''` → `OTHER`**. ⚠️ **Oggi nessun test asserisce la prima, eppure G, E e chiunque legga K2 ci contano** — la forma di D122: *una proprietà su cui tutti contano e che nessuno verifica*. ✅ **E la non-riesecuzione degli E2E dichiarata come scelta**: *«non rieseguire deve essere una scelta dichiarata, non un'omissione»* — accettata perché la modifica è **confinata in un file di test unitario** e l'evidenza precedente era **già stata presa con lo shim dalla semantica corretta**. **D143 applicata bene: G ha datato il mondo della misura precedente e mostrato che è ancora quello giusto** |
| D157 | 18 Set 2026 | 🔴 **Due cancelli sullo stesso repository si contraddicono, e quello che grida è quello sbagliato** — `check-links` non va riparato, va **ritirato** dove duplica MkDocs | Trovato da **I**. Stessi file, stesso momento: `dev.py mkdocs check-links` → **✅ 24 valid**; `mkdocs build` → **9 × «does not contain an anchor `#recovery-time`»**. **Nove ancore morte, vere**: `max-drawdown.md#recovery-time` esiste **solo in inglese**, it/fr/es hanno il titolo tradotto e **nessuna ancora esplicita**; tre pagine × tre lingue. 🔑 **La causa non è nel conteggio, è nella lingua**: `check-links` valida **contro il sorgente inglese**, MkDocs **contro la lingua realmente resa**. → *«**`dev.py:1197` non è un bug da riparare: è una reimplementazione, fatta male, di qualcosa che lo strumento fa bene.** La mia regex vede 14 ancore su 156; MkDocs le vede tutte, **e in più sa in quale lingua il link atterra** — cosa che la mia non saprà mai.»* **Questo ritira l'opzione «ripara la regex» prima di valutarla**: ripararla darebbe un gate che vede 156 ancore e **continua a dichiarare valide tutte e nove quelle morte**. 📍 **Diciassettesima forma**: non un controllo che non guarda, ma **un controllo che parla a un volume che nessuno ascolta** — MkDocs lo segnalava **da sempre, a livello INFO**, *«sotto la soglia che chiunque guarda: non era nascosto, era sotto il volume udibile»*. ✅ **MkDocs 1.6.1 ha la validazione nativa per lingua, ed è SPENTA** — nessun blocco `validation:` in `mkdocs.yml`. Misurato con sonda `INHERIT` fuori albero, poi rimossa: `validation: anchors: warn` + `--strict` → **EXIT=1, 3 warning**; conteggio su tutta la documentazione **9, tutte `#recovery-time`, tutte di I** — **nessun debito altrui da assorbire**. **DECISIONE — opzione A**: accendere `validation.anchors` **e** aggiungere le tre ancore a `max-drawdown.it/fr/es.md` **nello stesso passo** (non prima: una build rossa fra i due edit è una finestra in cui qualcuno la disattiva). 📌 **E `dev.py:1197` si restringe, non si ripara**: resta a `dev.py` ciò che **solo lui** può fare — i link **attraverso il confine** frontend→docs e i `docs_url` dei provider, che MkDocs non vede perché fuori dal suo albero. **Non si ripara una reimplementazione: si ritira la parte che duplica lo strumento.** ✅ **Chiusura di I, forma più forte di D115 applicata a sé stessi in tempo reale**: *«"check-links ✅ 24" **non è più una prova che accetto**. È la misura che ha dato per valide nove ancore morte»* |
| D158 | 18 Set 2026 | ✅ **Un'ancora non è contenuto traducibile: è un identificatore di URL che DEVE essere identico fra le lingue** — deroga negata perché non serve | Autorizzata l'aggiunta di `{: #recovery-time }` a **`max-drawdown.it/fr/es.md`**, riga 46, **quattro parole per file, nessun testo tradotto toccato**. Non è una deroga alla regola «solo inglese», e i tre argomenti di **I** reggono singolarmente: **(1)** *«se Aphra la traducesse, **la romperebbe**»* — la regola protegge **la prosa**, non gli identificatori, stesso argomento già accolto per `nav_translations`; **(2)** **il progetto lo fa già due volte, correttamente**: `kpi-cards` porta le stesse 3 ancore in 4 lingue su titoli **pienamente tradotti**, e `scheduled-investment` fr/es portano `{: #late-interest }` **proprio per preservare l'URL dopo aver tradotto il titolo**; **(3)** sono gli **unici** link localizzati con ancora dell'app e sono **verdi 4/4** — *«il pattern funziona in produzione»*, non è una scommessa. ⚠️ Vincolo: se servisse cambiare **una sola parola di prosa**, fermarsi e chiedere |
| D159 | 18 Set 2026 | 🔑 **Diciottesima forma, e la prima che non riguarda il codice: un'istruzione che non istruisce** | Formulazione di **G**, che rende «le decisioni si nominano, non si numerano» (D153) una regola invece di una cortesia: *«**La mia lettura restrittiva di "nient'altro" ha funzionato solo perché potevo chiederti.** Un mandato senza canale di ritorno, davanti alla stessa ambiguità, **non si ferma: sceglie** — e sceglie **in silenzio**, senza lasciare traccia di aver scelto.»* → **Una lettera ambigua non produce un errore visibile: produce due mandati che credono di aver eseguito la stessa istruzione.** Tutte le diciassette forme precedenti erano **controlli che non controllavano**; questa è **un'istruzione che non istruisce**, e il suo sintomo non è un rosso ma **due esecuzioni divergenti entrambe convinte di essere conformi**. 📌 **Conseguenza adottata dal coordinatore**: quando si dà un'istruzione con un confine (*«nient'altro»*, *«solo questo»*), **si elenca cosa è incluso**, non cosa è escluso — **un confine per negazione è ambiguo esattamente dove serve non esserlo**. ✅ Nello stesso giro, seconda correzione di G a una prescrizione del coordinatore: la firma dello stub K2 doveva essere `(key: string \| null \| undefined)`, non `(key: string)` — *«se lo stub è l'unico record eseguibile di K2, deve essere fedele **anche nella firma**, altrimenti **riproduce il comportamento e perde il dominio**»*. Con `string` stretto, **il ramo `null → OTHER` sarebbe diventato non collaudabile**: una riga che nessun test può raggiungere, **dentro la correzione che serviva a rendere il file onesto** |
| D160 | 18 Set 2026 | 🔴 **`git diff --shortstat` non misura il lavoro: misura la sua INTERSEZIONE COL PASSATO** — ogni numero di delta nel registro sottostima | Trovato da **G** notando che il proprio `5 files changed, 295 insertions(+), 32 deletions(-)` era **identico prima e dopo** un passo in cui aveva scritto: *«non perché non abbia scritto, ma perché `allocationHierarchy.ts` è **untracked**, e `git diff` non guarda i file non tracciati»*. 🔑 *«"295/32" **non è la dimensione del mio lavoro, è la dimensione della sua intersezione col passato**. Chi lo legge come "ecco quanto ha scritto G" sbaglia di **quattro file interi**»* — e i due file più importanti del mandato, **il modulo e i suoi 68 test**, non compaiono affatto. ⚠️ **Misurato dal coordinatore su tutti i mandati, ed è peggiore del caso di G**: | mandato | tracciati modificati | **non tracciati** | |---|---:|---:| | **D** | 4 | **12** | | **F** | 10 | **15** | | **G** | 5 | 6 | | B | 25 | 5 | | H | 15 | 2 | | N | 11 | 4 | | C | 5 | 3 | → **D mostra 4 file su 16 scritti; F ne mostra 10 su 25.** **Ogni cifra «+x/−y» riportata in questa campagna esclude i file nuovi**, cioè spesso **la parte più importante** — moduli nuovi, suite nuove, piani vivi. 📌 **Regola per l'integrazione e per ogni consuntivo**: il conto è **«N modificati + M nuovi»**, mai il solo `--shortstat`. Chi dimensiona una revisione sul `git diff` **sottostima esattamente dove il codice è più nuovo e meno rivisto**. ✅ È **D143 applicata a un numero riportato tre volte da chi l'ha poi smentito**: *il significato di una misura dipende dallo stato del mondo — e qui dipende da **quale sottoinsieme del mondo lo strumento guarda*** |
| D161 | 18 Set 2026 | 🔴 **Il gemello *istruzione* di D143: quando il mondo cambia sotto l'ISTRUZIONE, l'append riesce in silenzio e duplica** | Formulazione finale di **G**, al terzo incrocio consecutivo di messaggi, e completa la coppia aperta da I. | | cosa si muove | chi sbaglia | esito | |---|---|---|---| | **I** | il mondo cambia **sotto la misura** | il misuratore | **falsa ritrattazione** | | **G** | il mondo cambia **sotto l'istruzione** | l'esecutore | **duplicazione silenziosa** | 🔑 **E l'asimmetria fra i tipi di operazione è la parte azionabile**: | operazione | istruzione incrociata eseguita alla lettera | |---|---| | sostituzione mirata | ❌ **fallisce rumorosamente** — te ne accorgi | | **append** (piano vivo, changelog, registro) | ✅ **riesce in silenzio, e duplica** | ⚠️ **Caso reale sfiorato**: il coordinatore aveva chiesto **due volte** l'append al piano vivo di G. *«Se avessi obbedito letteralmente al secondo, il passo 12 comparirebbe **due volte** in un documento che è **il punto di ripristino se il contesto si azzera** — e **nessun cancello lo rileverebbe, perché nessun cancello legge i piani**.»* La protezione di G è stata **accidentale, non strutturale**: `edit` avrebbe fallito non trovando il testo originale. → **Forma minima adottata**: *prima di eseguire un'istruzione ricevuta, verifica che il mondo che descrive sia ancora quello davanti a te.* **Costa una `grep`; non farlo costa un documento di ripristino con due verità.** ✅ **Verificato dal coordinatore sui propri artefatti** dopo questa segnalazione — decisioni, `STATO.md`, i nove contratti: **nessun ID duplicato, nessuna sezione duplicata, nessuna intestazione ripetuta**. La segnalazione era valida e il controllo è risultato pulito. 📌 **E il perché tecnico della regola D159**, sempre da G: *«un confine per negazione **non è verificabile**. "Nient'altro" non si può controllare senza enumerare il complemento, che è infinito; "solo A e B" si controlla contando fino a due. **Il primo si può solo interpretare, il secondo si può eseguire.»* |
| D162 | 18 Set 2026 | 🔴 **Zod scarta le chiavi sconosciute: il danno di K4 non era «una cache che raddoppia», era un NUMERO SBAGLIATO a schermo** | Trovato da **E** per misura, e **corregge un'affermazione del coordinatore in K4**. `canonicalizeRiskRequest` termina con `schemas.RiskQueryRequest.parse(normalized)` (`riskRequest.ts:122`): finché `asset_ids` non arriva nel **client generato**, Zod lo toglie **in silenzio** — **non solo dalla chiave di cache, ma dal corpo della richiesta che va sul filo**, perché `buildRiskQueryRequest` restituisce l'oggetto **già validato**. → 🔑 *«Una UI che offra la fetta **prima** della rigenerazione del client mostra **i numeri del portafoglio intero sotto un'intestazione affettata**.»* ⚠️ **E l'asimmetria è la parte insidiosa: la firma vede la fetta, la richiesta no** — il controller rifarebbe **correttamente** la domanda al cambio di fetta e manderebbe una richiesta **non affettata**, **senza che nulla diventi rosso**. **Misurato**: il test negativo (due fette → due chiavi) **è fallito**, chiavi **identiche**. 📌 **Conseguenza: il campo backend e `api sync` DEVONO precedere qualunque controllo di affettamento a schermo.** ✅ **Forma della cura, e generalizzabile — il «rilevatore dell'innesto»**: il test asserisce **oggi** la collisione, col commento che spiega cosa farne, e **diventa rosso il giorno in cui il campo atterra**; è lì che si asserisce l'invariante vera e si tolgono i cast. **Un test che documenta un'assenza e si autodistrugge quando l'assenza finisce.** ⚠️ **E C ha trovato il gemello lato client**: `generated.ts:11361` validava `z.enum(['price_only','twrr'])` **a runtime** → un terzo valore **faceva fallire la risposta nel client**. **Non un degrado: una rottura.** |
| D163 | 18 Set 2026 | ✅ **`RiskReturnBasis.CURRENT_COMPOSITION_BACKTEST` — ma il presupposto della riapertura era falso: il metadato non mentiva, era AMBIGUO** | **C** ha implementato il terzo valore e **ha smontato la diagnosi** con cui gliel'avevo chiesto. Avevo scritto *«una corsa `HISTORICAL` affettata riporta `TWRR`»*: **falso**, verificato da C su **tutti e nove** i plugin — **nessuno scrive `TWRR` a mano**, propagano `context.primary_return_basis`, che la sua guardia lasciava già a `price_only`. 🔑 *«**Il metadato non mentiva: era ambiguo.** `price_only` è **lo stesso valore** che riportano un asset singolo **e** una composizione corrente — quindi E non poteva distinguere "rendimenti di prezzo" da "backtest a pesi correnti".»* → **Il bisogno di E era reale, la diagnosi no**; la cura richiesta non cambia, **ma accettare il presupposto avrebbe mandato C a cercare un bug che non esiste**. ✅ Implementato **solo** nel ramo pesato, dentro `if rows and composition_error is None` — *«la base dichiara un backtest solo se la serie è stata davvero prodotta»* — e `__all__` **intatto**, essendo un membro di un enum già esportato. ⚠️ **Due adempimenti scoperti da C e girati a E**: **(1)** `generated.ts:11361` validava l'enum **a runtime** → `api sync` obbligatorio, **rottura non degrado**; **(2)** `RiskResultFrame.svelte:108` usa `$t(\`risk.returnBasis.${metadata.return_basis}\`)`, **chiave composta a runtime**, e il blocco i18n ha **due** chiavi → **senza la terza lo schermo mostra la chiave grezza in quattro lingue, e nessun cancello lo rileva** (D150). ⚠️ **E due etichette di A ora descrivono male**: `historical_kpi.py:109` e `drawdown_summary.py:54` derivano `historical_close_returns`/`price_only_close` **da `== TWRR`** → col valore nuovo cadono nel ramo `else` e **restano verdi**, ma chiamano **«close returns» un backtest a pesi correnti**. **Verdi e sbagliate**: la famiglia di D121. 📍 **Fuori pista di C, dichiarato**: aveva aggiunto un'asserzione che pretendeva la base nuova anche su `risk_contribution` — rosso **istruttivo**, perché quel plugin **non consuma la serie composita** e legge la base **per-asset**, dove `price_only` è onesto. **Ha corretto il test, non il codice.** |
| D164 | 18 Set 2026 | 🔴 **`coverage` ha un TERZO significato, ed è l'unico che l'utente vede accanto alla propria smentita** · e una finestra di `grep` non è un troncamento visibile | Trovato dall'agente di **I**, verificato da I riga per riga. `stress.py:431-433` emette `n_observations=0, calendar_days=0, coverage=classification_coverage` — **attivi classificati ÷ attivi**: 🔑 **non è nemmeno una misura di dati, è una misura di METADATI, su un'analitica che di osservazioni ne legge zero**. E `RiskResultFrame.svelte:99-100` la rende sotto l'etichetta condivisa `risk.metadata.coverage`, che **avvolge 17 risultati**. → ⚠️ **Sulla card stress l'utente vede `n_observations = 0` e `coverage = 80%` affiancati.** *«Copertura di che cosa, con zero osservazioni? Chi ha imparato "coverage" da `data-quality` **conclude che gli mancano i prezzi, mentre gli mancano i settori**.»* Si somma ai **tre campi omonimi** in `schemas/risk.py` trovati da F (`:451` metadati, `:656` cella, `:967` drawdown) — **quattro grandezze, un nome**, col precedente per il rename già a venti righe (`calendar_coverage`, `fresh_quote_coverage`). 📍 **E nello stesso giro il coordinatore ha dato a I il terzo fatto falso della giornata**, *«nel messaggio in cui gli insegnavo che una ricerca letterale non prova un'assenza»*: aveva scritto che l'ambiguità geografica *«emette un warning e applica il primo gruppo»*, mentre `stress.py:258-262` **solleva**. Causa, diagnosticata da I: *«avevo letto un `grep -B6`, visto `details={…}` e `applied = matching_groups[0]`, e **dedotto** warning-and-continue **perché la finestra tagliava sopra il `raise`**»*. → **Una finestra di contesto non è un troncamento visibile: è un fatto diverso, con l'aria di un fatto completo.** ⚠️ **E la direzione contava più del conteggio**: la versione falsa era **più debole** della verità → la pagina avrebbe promesso *«un risultato annotato»* dove il prodotto restituisce **un 422**. **L'agente l'ha respinta verificando** — *«gli ho insegnato la regola nel messaggio in cui la violavo: l'ha applicata su di me, che è l'unica prova che l'abbia imparata»* |
| D165 | 18 Set 2026 | 🔴 **L'astensione travestita è peggio di D106: la riga `Tests` non contiene NESSUN token `passed`** — tre modi ovvi di verificare, tre bugie | **F** non ha accettato l'avviso: **l'ha rotto**. Un solo carattere di refuso (`"E2E stub topologyy"`): `Test Files 82 skipped (82)` · `Tests 2063 skipped (2063)` · `Duration 13.06s (… tests 0ms …)` · **`✅ PASSED`, exit=0**. 🔑 **Il dettaglio che cambia il rimedio**: la riga `Tests` **non contiene nessun token `passed`**, quindi il discriminante **non è «0 passed» — è l'ASSENZA del conteggio**. | chi controlla così | cosa vede | |---|---| | `grep PASSED` | ✅ **un verde** | | `grep -E "passed\|failed"` | **niente** sulla riga che conta → **ripiega sul banner** | | `echo $?` | **0** | → **Tre modi ovvi di verificare, tre bugie.** ✅ **Discriminanti affidabili**: `tests 0ms`, oppure **pretendere `Tests N passed` con N ≥ 1**. ✅ **E la prova più forte non è il conteggio**, dall'audit che F ha fatto sulle proprie cinque corse filtrate: le tre rotture hanno prodotto **il testo esatto delle asserzioni nuove** — `must cluster the twins`, `must not simply echo the payload order`, `must partition the candidates`. 🔑 *«**Un filtro a vuoto non può fabbricare un messaggio su misura.**»* → **un rosso che cita la propria asserzione è auto-autenticante in un modo che nessun conteggio può essere.** 📍 **E la chiusura di F sul valore di D100**: i due `test-author` avevano consegnato reti **verdi e corrette nella forma**, ma **due di quelle reti erano inerti** — il test 6 e il test 4. *«**Uno specialista che scrive test non garantisce test che discriminano; è l'atto di romperli che lo garantisce.**»* |
| D166 | 18 Set 2026 | 🔑 **«Un rapporto può rilevare un buco solo se il denominatore viene da FUORI dei dati»** — la tautologia della copertura, in un secondo meccanismo indipendente | Formulazione di **I**, che riconcilia due cifre del coordinatore che sembravano incompatibili (`coverage ≈ 1,000` **e** `f ≈ 252`, con un validatore che impone `f = n_obs · 365 / calendar_days`). **Non condividono il denominatore**: `:75-78` calcola `f` contro `(final_date − baseline_date).days`, cioè **il tempo trascorso**; `:344-345` calcola `calendar_coverage` contro `coverage_candidates`, cioè **le date in cui esistono quotazioni** — **un calendario costruito dai dati stessi che dovrebbe validare**. 🔑 *«È la **stessa tautologia** trovata sul ramo TWRR (`current += timedelta(days=1)`, copertura ≡ 1,0 per costruzione), in un **secondo meccanismo indipendente**. Due rami, due implementazioni diverse, **un solo difetto**: la copertura è misurata contro un calendario derivato dalle osservazioni.»* → **`annualization_factor` è affidabile non perché sia garantito da un validatore, ma perché il suo denominatore è ESOGENO** — il passare del tempo, che i dati non possono modificare. ⛔ **E I corregge la propria cifra: `coverage` non sono tre grandezze, sono QUATTRO** — `coverage` (griglia di calendario, **≡1,0 per costruzione**), `calendar_coverage` (date di quotazione candidate, **≈1,0 per costruzione**), `fresh_quote_coverage` (quotazioni fresche), `classification_coverage` (**attivi classificati ÷ attivi — metadati, non dati**). **Due** raggiungono l'utente sotto l'unica etichetta `risk.metadata.coverage`: la 1ª e la 4ª. *«Non è solo che il nome è sovraccarico: è che **il significato usuale è tautologico su entrambi i rami**.»* 🔴 **CORREZIONE DI A, 18 Set 2026 — «`calendar_coverage` ≈ 1,0 per costruzione» è TROPPO FORTE.** A ha letto `series_preparation.py:236, 286-288, 344-345` e ha misurato: `candidate_quote_dates` è l'**unione** delle date di quotazione degli asset attivi, `included_return_dates` è l'**intersezione** (le date in cui **tutti** hanno un punto) → **`calendar_coverage = \|∩\| / \|∪\|`**, cioè **un rapporto fra i calendari degli asset**, non fra osservazioni e giorni solari. → **Vale 1,0 se e solo se tutti gli asset attivi quotano negli stessi identici giorni**: un asset solo → sempre 1,0; più asset sulla stessa borsa → 1,0. **Ecco perché tutte le fixture lo danno 1,0 e sembra costante.** ⚠️ **Ma un portafoglio a calendario misto no**: cripto (**365** giorni l'anno) più azioni (**261**) darebbe **≈ 0,715**. 🔑 **E questa è la risposta alla domanda aperta di D96 — *«in quali casi reali `D > N`»*: i portafogli a CALENDARIO MISTO, e LibreFolio supporta le cripto, quindi non è ipotetico.** ⚠️ **A l'ha dichiarata come previsione falsificabile, non come misura**: dedotta dal **codice letto**, non eseguita su dati veri — *«prima che I lo pubblichi va confermato con un portafoglio cripto+azioni reale»*. 📌 **E i reticoli sono DUE, non uno**: il ramo **TWRR** cammina su una griglia di calendario (l'osservazione di I, corretta), il ramo **preparato** prende **l'unione delle date di quotazione reali** — per azioni ed ETF, **giorni di borsa**. Misurato su 3 anni: TWRR `N=1095, D=1095 → f=365,00` (**il cablato è esatto per costruzione**); preparato `N=783, D=1095 → f=261,00` (**sbagliato di 1,3985×**, tasso giornaliero **sottostimato del 28,5 %**). → **A9 morde**: scope **asset** sempre · scope **portafoglio non-`HISTORICAL`** (usa `prepared.annualization_factor`, `service.py:594`) · **mai** su portafoglio `HISTORICAL`. ⚠️ **La conclusione «affettare sembra migliorare le cose» è confermata, ma la causa è `annualization_factor`, non `calendar_coverage`: se la correzione fosse stata messa sulla copertura, il difetto sarebbe rimasto intatto.** |
| D167 | 18 Set 2026 | ⚠️ **mathjax non serve al gate: la sua assenza è invisibile PER COSTRUZIONE, e si vede solo a un lettore offline** | Verificato da **I** riga per riga, dopo che il coordinatore aveva seminato il vendor credendolo necessario. `vendor/` **non compare in `mkdocs.yml`** — `extra_javascript` elenca `javascripts/mathjax.js`, cioè **la configurazione**, mai la libreria; il caricamento avviene **a runtime nel browser** (`lib-loader.js:21-26`) con **fallback CDN** e `console.warn` quando il locale manca; `.gitignore:78` esclude la cartella, quindi **non viaggia mai con un worktree**. 🔑 **Il meccanismo**: MkDocs copia `docs/` **in blocco** — una sottocartella assente **non è un riferimento rotto, è niente da copiare**. Nessun warning, **nessun `--strict` che possa scattare**. Cronologia a conferma: `vendor/mathjax` creato **01:12** dalla semina, build di I **tutti precedenti e tutti verdi**. → ⚠️ **Conseguenza delimitata, dichiarata da I come tale**: se un sito costruito dovrà rendere le formule **senza rete**, ciò che garantisce la cache locale **non è questo gate** — *«non affermo che una build di rilascio sia rotta: non conosco quella catena. Affermo che **non è il gate a proteggerla**, il che è verificato»*. 📍 **E il risvolto su I stesso**: 14 pagine dense di LaTeX **mai renderizzate**, perché per mandato non avvia server. *«"Il build è verde" non ha mai significato "le formule si vedono", e ora so anche perché.»* |
| D168 | 18 Set 2026 | ✅ **L'estensione in coda di un `desc=` è una forma additiva riconosciuta — con risoluzione MECCANICA per unione** | Proposta di **F**, dopo aver **misurato** invece di accettare un'approvazione retroattiva del coordinatore. `git diff --numstat`: `_frontend_portfolio.py` **10/0** (puramente additivo) ma `_frontend_utility.py` **3/1** — **una riga esistente è modificata**, il `desc=` di `core-unit`. Verificato carattere per carattere: vecchio testo **conservato verbatim come prefisso**, 360 caratteri in coda, resto identico → **semanticamente additivo, testualmente una modifica**. ⚠️ **E la regola che il coordinatore stava per dare a G ed E** — *«restano da chiedere: riordini, **modifiche a voci esistenti**»* — **escludeva esattamente ciò che F aveva fatto**. 🔴 **Il motivo per cui non è cosmetico, ed è aritmetica non rischio**: la riga `desc=` di `core-unit` è **1 674 caratteri nella baseline, su una riga fisica sola**; **chiunque aggiunga un test unitario deve allungarla**, e G ed E lo faranno → **Git vedrà tre modifiche alla stessa riga fisica**, con risoluzione che richiede **di rileggere 1 900 caratteri di prosa a mano**. **E arriva dopo che il coordinatore avrà detto «registrate senza chiedere»**, cioè quando nessuno si aspetta più un conflitto su quel file. ✅ **Regola adottata**: si fa **senza chiedere**, a condizione che il testo esistente resti **verbatim come prefisso**; al merge **si tengono tutte le clausole aggiunte, nell'ordine di arrivo** — *«il conflitto resta visibile, ma diventa un'unione invece di una decisione»*. 📌 **E il criterio di F su quando fermarsi, adottato perché più preciso del mio**: *«mi fermo quando il costo di chiedere è **una risposta**, e procedo quando è **un blocco**»* |
| D169 | 18 Set 2026 | 🔴 **Il timore di C era mal diretto: una doppia rinormalizzazione SOLLEVA — il silenzio è a monte, nella sua scelta** | **A**, verificando il contratto che C gli aveva chiesto. **Semantica confermata per differenza, non per lettura**: pesi grezzi `{1:0.30, 2:0.20}` + `cash 0.50` contro gli stessi **rinormalizzati** `{1:0.60, 2:0.40}` + `cash 0.0` → **rapporto ≈ 2,0**, *«se la funzione rinormalizzasse dentro le due righe sarebbero identiche»*. ❌ **Ma la firma citata da C non esiste**: ha letto **il sito di chiamata**, non la definizione — `rows`/`usable_weights` sono **variabili locali di `service.py`**; i parametri reali sono `returns_by_asset`, `weights`, `cash_weight: float \| None`, e il ritorno è una **`list`**, non un iterabile. Posizionalmente funziona; **per nome solleva `TypeError`**. 🔑 **E la trappola vera è rovesciata**: *«una doppia rinormalizzazione **non mentirebbe in silenzio — solleverebbe**»* (`if not math.isclose(asset_weight + cash_weight, 1.0, abs_tol=1e-9): raise`, misurato su quattro casi). **Il silenzio che C teme esiste, ma è a monte, nel suo codice**: se rinormalizza il sottoinsieme a somma 1 e passa `cash_weight=0.0`, la funzione **accetta senza protestare** e risponde a **una domanda diversa** — *«cosa hanno fatto questi asset dentro il mio portafoglio vero, col resto fermo»* contro *«cosa sarebbe successo se avessi tenuto solo questi»*. **Entrambe legittime, entrambe corrette prese da sole, e differiscono del doppio. Nessun test le distingue perché nessuna delle due è sbagliata.** → **Il `cash_weight` passato è la dichiarazione di quale delle due domande si sta facendo.** ⚠️ **E un difetto latente scoperto dal probe, non cercato**: `summarize_drawdown` **solleva su un rendimento di esattamente `-1.0`** — `wealth_index` **ammette** `-1.0` e produce ricchezza `0.0`, `underwater_drawdown` **rifiuta lo `0.0` che l'altra ha appena prodotto**. **Due funzioni dello stesso modulo non sono d'accordo sullo stesso valore.** Irraggiungibile per la via preparata (`gt=-1`), **ma la via TWRR non è stata verificata**: se non porta la stessa guardia, **un azzeramento reale diventa un `ValueError` invece di un drawdown del −100 %** — e **CDaR e UCI di N si integrano proprio su quella coda**. ✅ **E A ha costruito il caso che poteva uccidere l'assunzione di N su riskfolio** (il peggiore che parte **alla prima osservazione**, dove riskfolio avrebbe potuto cumulare da `1+r₀`): **sei casi, tutti bit-identici, `delta == 0.0`** — *«N aveva ragione, e adesso ha anche il caso che l'avrebbe smentita»* |
| D170 | 18 Set 2026 | 🔴 **Gli artefatti GENERATI non si verificano né con un file né con uno SHA — raffina D146** | Quarta istanza di F-7, trovata da **F** misurando un relay del coordinatore (*«rifai `api sync`, `generated.ts` passa da 6 a 11 occorrenze di `is_benchmark`»*): `grep -rn "is_benchmark" backend/app --include=*.py` → **0**; `grep -c "is_benchmark" frontend/src/lib/api/generated.ts` → **0**, su un client **risincronizzato alle 02:26**. 🔑 *«`api sync` legge l'OpenAPI del backend **locale**: rigenerarlo mille volte non produrrà mai un campo che il mio backend non dichiara. **Non è un'istruzione sbagliata — descrive il mondo di B**.»* ⚠️ **E il rimedio di D146 non basta**: `generated.ts` è **gitignorato e generato**, quindi **non esiste una SHA che lo contenga**. → **Sono l'unica classe di relay che non si può verificare né con un file né con una SHA: solo col codice sorgente a monte.** ✅ **Forma corretta del relay**: non *«rifai `api sync`»* ma **«questo comparirà dopo l'integrazione, non prima»**. 📌 Tassonomia completa dei relay: **decisione** → si relaya · **codice tracciato** → si allega la SHA (D146) · **artefatto generato** → si dichiara la **dipendenza di integrazione**, mai un comando da eseguire subito |
| D171 | 18 Set 2026 | ✅ **Tre verifiche di confine che nessuno aveva chiesto, e una chiude uno slot di merge** | **F**, misurando i confini che B gli aveva mandato. **(1) `asset-list.spec.ts` non è fra i dieci file di F** — D54 rinviata ⇒ niente colonne di rischio, e la rete sulla regola dei pesi è finita in `risk-lab.spec.ts`. → **Nessun merge F↔B su quel file: slot chiuso**, e i confini «1-691 intatte, B su 692-825» **non servono a nessuno**. **(2) Verifica fatta PER B, non richiesta**: `asset-list.spec.ts:362` e `:368` asseriscono su **`asset-global-risk-panel`**, e la riscrittura del pannello di F **conserva quel testid** → **la riscrittura di F non rompe lo spec di B**. Verificato, non supposto. **(3) F è immune a F30 in entrambe le direzioni**: il suo spec (`:430`) prende **il wrapper**, non il trigger, quindi **funziona prima e dopo** la correzione di B — *«non implemento nulla, e non per prudenza: per non averne bisogno»*. ✅ **E il danno allo spec di E è circoscritto**: `:652` e `:654` rotte (due click), ma **`:633` `asset-global-risk-panel` sopravvive** → **non è il montaggio a rompersi, sono due righe**. 📍 **Correzione al conteggio del coordinatore**: lo spec di F è di **812 righe, non 708**, ed è stato **eseguito cinque volte** (1ª: `4 passed, 2 failed`; 2ª-5ª: `5 passed, 1 failed`) — **nessun `0ms`, nessuno `skipped`**. Le 104 righe di differenza sono **le riparazioni D100 di test 4 e test 6**, cioè le due reti che i `test-author` avevano consegnato **verdi e inerti** |
| D172 | 18 Set 2026 | 🔴 **Il danno vero di una falsa ritrattazione non è il tempo perso: è che ARCHIVIA LA LEZIONE SBAGLIATA** | Raffinamento di D129, da **F**, e nessuno l'aveva nominato. F ha riscritto i deliverable dei propri due `test-author` **mentre erano inattivi**: `risk-lab.spec.ts` da **708 a 812 righe** (test 4 e 6 riscritti), un `describe` aggiunto, e un `data-asset-order` nel prodotto **che l'agente E2E non ha mai visto**. → *«Se svegliassi `risk-lab-e2e` senza annunciare il delta, troverebbe **proprio i due test che aveva scritto** riscritti, e la ritrattazione naturale sarebbe **"il mio lavoro era sbagliato"**. **Non era sbagliato: era inerte.** Passava senza poter fallire. **Sono due difetti diversi — e la ritrattazione archivierebbe la lezione sbagliata.**»* 🔑 **Il parallelo con il caso di I lo completa**: là l'agente archiviava *«ho un bug nello script»* al posto di *«il mondo è cambiato»*; qui archivierebbe *«scrivo test sbagliati»* al posto di *«scrivo test che non discriminano»* — **e la seconda è la sola che si possa correggere**. ✅ **E F ha verificato che all'indietro non è successo**, coi timestamp: `lab-helper-tests` inattivo da ~00:58 contro scrittura alle **02:22:17** (**+84 min**), `risk-lab-e2e` da ~01:14 contro **02:17:22** (**+63 min**) — *«nessuno dei due ha misurato un mondo che stavo cambiando»*. 📌 **Regola adottata, estensione di D127**: ogni `write_agent` verso un agente **che ha già consegnato** apre col **delta** — righe, test riscritti, attributi di prodotto aggiunti — **nello stesso messaggio**. → **«Non serve che sia in volo perché la misura sia stantia; basta che sia tornato.»** |
| D173 | 18 Set 2026 | 📍 **Il coordinatore ripeteva dati stantii: D143 applicata al mandante** — critica di F, accolta, con cambio di forma | **F**, alla terza volta che gli tornava *«708 righe e 6 test non eseguiti»* su uno spec di **812 righe eseguito cinque volte**: *«Se i miei rapporti non ti stanno arrivando, dimmelo e cambio forma — **un coordinatore che ripete un dato stantio è la stessa classe di difetto che stiamo inseguendo**, applicata al canale invece che al codice.»* **Causa nominata dal coordinatore**: i messaggi erano composti **dal contesto accumulato**, non dall'ultimo rapporto ricevuto; quando i messaggi si incrociano, il relay porta **lo stato del momento in cui si è iniziato a scrivere** e **lo asserisce come attuale**. → **È D143 dal lato del mandante**: *ogni misura porta con sé lo stato dell'albero in cui è stata presa* — **e il coordinatore non stava datando le proprie**. ✅ **Forma adottata**: (1) **non riepilogare la coda di un mandato** — se serve sapere dove sta, **si chiede, non si dice**; (2) **ogni fatto relayato porta fonte e momento** (*«da B, 02:2x»*, *«verificato adesso»*, *«nel tuo rapporto di N minuti fa»*); (3) **si asserisce solo ciò che si è verificato nel turno**, il resto è marcato come **riferito**. 📌 Si somma a **D127** (la coda dei messaggi è una fonte di basi false), **D141** (non vince l'ultimo messaggio: vince l'albero) e **D159** (un'istruzione che non istruisce): **quattro difetti del canale, tutti nominati dai mandati, nessuno dal coordinatore** |
| D174 | 18 Set 2026 | 🔴 **Il cancello di B non esercitava il percorso che distingue le due forme della sua correzione** | Trovato da **F**, verificato dal coordinatore. La riparazione F30 ha **due forme** — *inoltrare* e *inoltrare **e togliere l'id al wrapper*** — e divergono solo dove un consumatore interroga **per discendenza**. Struttura: `AssetSelect:183` wrapper con `data-testid={testid}` · `SearchSelect:357` container con lo stesso · `SearchSelect:374` **`role="combobox"` è DISCENDENTE del container**. Lo helper di F (`risk-lab.spec.ts:428-436`, usato da **due** test) fa `getByTestId(…).locator('[role="combobox"]')` → **con l'inoltro senza rimozione il combobox discende da entrambi i div → 2 match → strict-mode violation**; con la rimozione, **passa**. 🔑 **Ma `AssetSelect` ha QUATTRO valori di `testid` su cinque punti di chiamata** — `asset-select` (default, `ImportWizardModal:4324`), `tx-form-asset` (`TransactionFormModal`, i cui spec usano `tx-form-asset-wrap`, **testid diverso** → non toccati), `asset-merge-target-select` (`AssetMergeModal:259`), **`risk-asset-add-select`** (`AssetSetRiskPanel:201`, di F). → **Il cancello dichiarato (`tx-import-*`) esercita SOLO il default.** *«**Chi esegue la modifica non esercita il percorso che la distingue.** Verificare solo il default dice che **l'inoltro funziona**, non che **la rimozione sia avvenuta**.»* ✅ **Cancello esteso**: `front-portfolio risk-lab` + `front-asset asset-merge`, **oppure** rieseguiti da F in 6245 dopo l'integrazione — *«una delle due, non nessuna»*. 📍 E **F ha verificato PER B, senza che nessuno glielo chiedesse**, che `asset-list.spec.ts:362,368` asseriscono su `asset-global-risk-panel` e **la riscrittura del pannello di F conserva quel testid** → **lo spec di B non si rompe**; e che i confini `asset-list.spec.ts` **non servono**, perché quel file **non è fra i dieci di F** → **slot di merge chiuso** |
| D175 | 18 Set 2026 | 🔑 **La partizione è più forte dell'omogeneità: cattura anche il filtro «corretto ma lesivo»** · e un'invariante prescritta senza guardare il DOM | Il coordinatore aveva prescritto a **F** l'invariante *«dopo aver filtrato per il tipo T, **ogni candidato è di tipo T**»*. **F l'ha misurata inapplicabile**: `AssetSetRiskPanel:242` espone `risk-filter-type-{type}` (**la pastiglia**, non un candidato), `:271` espone `risk-selected-asset-{id}` (**solo l'id, nessun tipo**), e il contatore espone `data-selected`/`data-total` (**un numero**). → 🔑 *«**I candidati non sono elementi enumerabili: sono un conteggio.** Per asserire l'omogeneità dovrei rendere l'intero insieme candidato nel DOM — cioè **cambiare il prodotto per far posto al test**.»* ✅ **E ciò che F ha scritto è più forte, non un ripiego**: `Σ\|filtro(T_i)\| = \|non filtrato\|` — **un filtro corretto ma LESIVO (restituisce solo T, ma ne perde qualcuno) PASSA l'omogeneità e FALLISCE la partizione**. L'omogeneità cattura solo il no-op; la partizione cattura **no-op e perdita**. 📍 **Difetto del coordinatore**: aveva prescritto **presupponendo una superficie che non esiste**, senza guardare il DOM — la stessa famiglia del relay di `canonicalizeScope` (D154), *codice scritto contro una forma che non c'è* |
| D176 | 18 Set 2026 | 📍 **Sesta autorizzazione a lavoro già consegnato: il costo non è il giro perso del mandato, è l'ordine d'integrazione deciso su dati vecchi** | Rilievo di **F**, il secondo sul canale dopo D173: *«è la **sesta** volta che mi autorizzi lavoro già consegnato. Non è un problema mio — **perdo un giro, non una riga** — ma **se stai decidendo l'ordine d'integrazione su uno stato arretrato di sei messaggi, quello sì che costa**.»* 🔑 **Isola il danno nel punto giusto**: un mandato che ripete **perde un giro**; **un coordinatore che sequenzia su dati vecchi mette due rami nell'ordine sbagliato** — e l'ordine d'integrazione è una decisione che esiste **solo** nella testa del coordinatore, quindi nessun mandato può correggerla. ✅ **Due misure adottate**: **(1)** proposta di F — ogni suo messaggio apre con **tre righe di stato corrente**; **(2)** correzione del coordinatore, più importante — **non si autorizza nulla senza prima chiedere dove sta il mandato**: *«se ho bisogno di sapere se una cosa è fatta, **te lo chiedo in una riga** — non te lo assegno sperando che sia ancora da fare»*. Più la pratica di **rimandare al mandato lo stato che il coordinatore ha di lui**, perché possa correggerlo **in una riga sola** invece che in un rapporto |
| D177 | 18 Set 2026 | 🔴 **Stiamo pubblicando LaTeX rotto in tre lingue — quarto punto cieco, e di tipo NUOVO: l'HTML è ben formato e il contenuto no** | Trovato da **I**, e **non da un gate**: da una domanda che non si era mai fatto. Scrivendo *«14 pagine dense di LaTeX, mai renderizzate»* si è accorto che **non era un'ammissione ma un'ipotesi non verificata**. Controllo nuovo, una riga: **contare i `$$` crudi nell'HTML costruito** — se ne resta uno, quel blocco non è stato convertito e **l'utente legge sorgente LaTeX al posto della formula**. **Verità a terra**: `en` **0** · `it` **4** · `fr` **22** · `es` **22**. → **L'inglese è pulito; ogni difetto residuo è in una pagina tradotta.** 🔑 **Tre meccanismi distinti, un solo sintomo, e sono tutti SPAZI BIANCHI**: **(1)** due blocchi display su righe **adiacenti** → fusi, i `$$` interni finiscono **dentro** il LaTeX (`volatility`, ×4 lingue — **preesistente alla baseline**, verificato con `git show HEAD:`); **(2)** indentazione a **5 spazi** invece di 4 in un'admonition → blocco reso come **testo** (`fifo-lot-analysis.es/.fr`, **20 ciascuna**); **(3)** **riga vuota inserita** fra formula e `$$` di chiusura → blocco spezzato (`fifo-lot-analysis.it`). *«Il 2 è uno spazio di troppo, il 3 una riga vuota di troppo, l'1 una riga vuota **mancante**: **tre modi opposti di sbagliare la stessa cosa**.»* 🔑 **Perché è un tipo NUOVO di punto cieco**: gli altri tre non vedevano **una forma** — `check-links` cieco alla lingua, `dev.py:1197` alla forma dell'ancora, il gate all'asset di runtime. **Questo produce HTML perfettamente ben formato che contiene LaTeX malformato**: da Markdown non c'è nulla di sbagliato, **la rottura avviene nel browser a tempo di parsing MathJax**, e **nessun controllo sul sorgente può vederla**. → **TESI: ogni gate valida l'artefatto che produce, non quello che l'utente riceve.** ⚠️ **E la regola che I aveva insegnato al proprio agente era sbagliata**: *«mai `$$` dentro un'admonition»* **vieta una cosa che funziona** (`fifo-lot-analysis.en` lo fa **20 volte correttamente, a 4 spazi**) e **permette quella che rompe**. La regola vera è **whitespace**: delimitatori a indentazione **multipla di 4**, riga vuota **fra** blocchi adiacenti, **nessuna** riga vuota **dentro** un blocco. ✅ **Gate proposto da I, non implementato** (`dev.py` non è suo): **contare i `$$` nell'HTML costruito, per lingua; zero è l'unico valore accettabile.** È **agnostico al meccanismo** (avrebbe preso tutti e tre, compresi quelli ignoti) e **consapevole della lingua per costruzione**. 📌 **Quarta ragione per il blocco di traduzione**: *«la pipeline **non preserva il whitespace significativo**, e la matematica è il costrutto dove il whitespace porta carico»* |
| D178 | 18 Set 2026 | ✅ **Il nome giusto per `RiskResultMetadata.coverage` è già nel file, cinquanta righe più su: rinominarlo è riparazione di coerenza, non decisione di design** | Trovato da **F**, verificato dal coordinatore sui due vicinati. **Sequenza identica di quattro campi, e il quinto ha due nomi diversi:** ``` :397 n_observations :398 calendar_days :399 annualization_factor :400 calendar_coverage :401 fresh_quote_coverage ``` contro ``` :448 n_observations :449 calendar_days :450 annualization_factor :451 coverage ← stesso vicinato, nome generico ``` 🔑 *«Il file contiene **già il nome giusto per il campo ambiguo**, sullo stesso vicinato di campi. Quindi rinominare `:451` in `calendar_coverage` **non è una decisione di design: è una riparazione di coerenza interna**. È il rename più economico e più difendibile dei tre.»* ✅ **E F ha dichiarato il livello di prova**: evidenza **strutturale** (vicinato identico), **non** lettura del calcolo — *«chi esegue il rename lo confermi sul servizio che popola il campo»*. 📌 Completa D147/D164: i tre omonimi non sono solo ambigui, sono **lo stesso nome per tre denominatori** (calendario di **una serie** a `:451`, sovrapposizione **di coppia** a `:656`, finestra **disponibile** a `:967`) **e un nome diverso per la stessa cosa** (`calendar_coverage` a `:400`) |
| D179 | 18 Set 2026 | ✅ **La regola dello SHA verificata DAL DESTINATARIO — e il suo confine provato** | **F** non ha accettato D146: l'ha **eseguita**. `git cat-file -t 1a537f9cb` → **commit**; le tre primitive di D **presenti**; `git for-each-ref refs/copilot/checkpoints/ \| wc -l` → **1 232**. 🔑 *«Funziona perché **i worktree condividono un solo object store**. Non è una convenzione documentale: è **un comando che chi riceve può eseguire**. Per la prima volta in quattro relay posso rispondere "esiste, e qui" invece di "non posso verificarlo".»* ✅ **E il confine provato**: `1a537f9cb:frontend/src/lib/api/generated.ts` → **non nell'albero**; idem `openapi.json`. → **La regola ha due rami, non uno**: **codice versionato → manda lo SHA, il destinatario verifica** · **artefatto generato → non mandare nulla, dichiara che non è verificabile prima dell'integrazione**. ✅ **Conferma indipendente ottenuta gratis**: `git grep -c is_benchmark 1a537f9cb -- backend/app` → **0** — il campo non esiste **nemmeno nell'albero di D**, quindi la misura di F vale ora **da due punti d'osservazione**. 📍 E la sintesi di F sulle due regole gemelle: **«il numero dice *quale mondo*, lo SHA dice *quale albero*»** |
| D180 | 18 Set 2026 | 📍 **Terza forma della stessa classe in un giorno: «numero giusto di grandezza, attribuito al referente sbagliato»** | Il coordinatore aveva scritto a F che *«il `desc=` di `core-unit` è **1 674 caratteri**»*. **F l'ha misurato**: `desc=` **1 517** (baseline) → **1 877**; **riga fisica 1 672** → **2 032**. → *«**1 674 non coincide con nessuno dei quattro**, ma dista 1-2 dalla riga intera. **È la riga, non il campo.**»* 🔴 **Il danno è specifico e non è l'imprecisione**: chi domani leggesse *«il `desc` è 1 674»* e andasse a misurarlo troverebbe 1 517, e **si metterebbe a cercare una modifica mai avvenuta**. 🔑 **Tre istanze della stessa classe in un giorno, in tre vestiti**: i tre `coverage` = **stesso nome, tre denominatori** · `check-links` 12 contro 24 = **stesso numero, due mondi** · `1 674` = **numero giusto, oggetto misurato diverso da quello dichiarato**. → **Cura unica, già scritta due volte: un numero dichiara CHE COSA ha misurato, non solo quanto.** ✅ **E dalle stesse cifre esce una prova migliore di quella che avevamo**: la riga è cresciuta di **esattamente i 360 caratteri** di cui è cresciuto il `desc` (1 672→2 032 e 1 517→1 877) → **nulla fuori dal `desc` è cambiato**. Prova **aritmetica**, più forte del confronto fra stringhe che dipendeva da una regex. **La regola sull'estensione del `desc=` e la risoluzione per unione non cambiano: cambia solo l'etichetta del numero** |
| D181 | 18 Set 2026 | 🔑 **Una fixture può essere già armata per una funzionalità che non esiste ancora — e il giorno che nasce, il test nasce già sul filo** | **F** ha verificato il relay della regola M2 sulle proprie fixture. **Immune oggi, e per struttura**: `risk-lab.spec.ts:118` dichiara `definition('historical_var', 'var_cvar', ['asset','portfolio'], …)` — **`asset_set` non è fra gli scope**, coerente con la misura F-5 che `historical_var` non accetta `ASSET_SET`. Nessun numero di VaR è calcolato né asserito. ⚠️ **Ma la fixture è esattamente nel caso che si muove**: `:159` e `:243` → **`observations = 60`**, e `(1−c)×60` vale **3,0** a 95 % e **6,0** a 90 % — **due interi**. *«60 è divisibile per 10 e per 20: è precisamente il numero "pulito" che A descrive.»* 🔴 **La forma nuova**: *«**La mia fixture è già armata per una funzionalità che non esiste ancora.** Se un giorno `historical_var` accettasse `ASSET_SET` — che era **l'opzione (a) di Q-F4, quella che avevo raccomandato** — le asserzioni sul VaR **nascerebbero sul filo**, e si sposterebbero del **100 %** fra rami senza che nulla di visibile cambi nel test. **Il sintomo sarebbe "flaky fra rami", la causa una divisibilità.»* ✅ **Rimedio: un carattere** — `observations = 61` → 3,05 · 6,1 · 0,61, **nessun intero** per le tre confidenze comuni. ✅ **E F NON l'ha applicato**, con la stessa disciplina del `DocsLink`: cambierebbe anche `annualization_factor` (`:169`, derivato da `observations`) **per rincorrere una funzionalità che non c'è**. **L'ha legato alla condizione che lo attiva** — chi estenderà `historical_var` a `ASSET_SET` trova la nota **e il numero già scelto**. 📌 **Generalizzazione**: una regola di dominio scoperta oggi può essere **latente** in una fixture scritta ieri; il relay serve **anche a chi oggi è immune** |
| D182 | 18 Set 2026 | ✅ **Tre forme dello stesso rimedio, su tre tipi di oggetto — e la correzione di F sul canale: il problema non era il riepilogo, era l'assenza di data** | **F**, chiudendo la taxonomy: \| oggetto \| come si data \| \|---\|---\| \| **codice** \| **lo SHA** \| \| **un numero** \| **il mondo in cui è stato misurato** \| \| **un fatto relayato** \| **fonte + momento** \| ⚠️ **E la correzione alla mia prima regola di canale**: mi ero imposto *«non ti riepilogo più la tua coda»*. F: *«mi va bene ma **non è necessaria**: **il riepilogo non era il problema, l'assenza di data lo era**. Una coda datata ("al tuo rapporto delle 02:1x risultava…") **resta utile**: mi permette di correggerti quando è vecchia, **che è esattamente ciò che è successo**.»* → **Regola corretta**: riepilogare **è permesso e utile**, a condizione che il riepilogo **dichiari il momento dell'osservazione**. La cura non è tacere, è **datare** |
| D183 | 18 Set 2026 | ✅ **La domanda di I sulla cassa aveva già risposta nella docstring di N — scritta prima che la domanda esistesse. La divergenza NEA resta UNA, non due** | **I** aveva provato la divergenza NEA (AI Export conta una posizione **(broker, attivo)** perché `PortfolioHolding` porta **sia `asset_id` sia `broker_id`**, `portfolio.py:358-366`; quindi su un attivo presso due broker somma $w_1^2+w_2^2$ contro $(w_1+w_2)^2$ → differenza $2w_1w_2>0$ **sempre** → `NEA_AIExport > NEA_rischio` **sempre**), e aveva trovato **un terzo pezzo che K8 non dice**: `portfolio_financial.py:240,258` dichiara *«Cash is included in the denominator but is not itself an HHI term»* → **la cassa diluisce ogni peso senza contribuire alcun quadrato** → *«**sembri più diversificato perché sei in cassa**»*. ❓ La sua domanda: *«la NEA del rischio tratta la cassa allo stesso modo? Se **no**, le due misure divergono per **due** ragioni indipendenti invece di una, e **il divario si allarga con la liquidità dell'utente — cioè proprio quando lui è più propenso a controllare la concentrazione**»*. ✅ **Risposta, letta in `acquired.py:169-192` (worktree di N, 18 Set, NON committato)**: *«The weights are consumed as given. Risk weights are already `position value / net worth` — **the same denominator AI Export uses for `nav_weight_percent`** — so passing them unmodified **reproduces AI Export's cash semantics: cash sits in the denominator but is never a term.** **Any renormalization here would silently make the product state two different numbers for one portfolio's concentration.**»* → **Le due NEA trattano la cassa in modo identico; la divergenza resta il solo effetto broker.** 🔑 **N aveva previsto esattamente il modo di fallire che I temeva, e l'aveva scritto nel codice.** 🔑 **Terzo fatto gratis**: la funzione ritorna **`None`** quando nessun peso porta valore — *«a **fully liquid scope** rather than a **zero-concentration** one»* → **pienamente liquido ≠ concentrazione zero**: un portafoglio tutto in cassa **non ha NEA**. 📏 **Unità dichiarata**: *«a **number of positions**, never a percentage»* |
| D184 | 18 Set 2026 | 🔑 **Le tre pagine bloccate di I si sbloccano non per la matematica ma per i TRANELLI che N ha documentato — e uno dei tre è M2 una seconda volta** | Letto in `acquired.py` (worktree di N, 18 Set, non committato) mentre si rispondeva a D183. **(1) `ulcer_index`** — l'**unica** misura del gruppo **con segno positivo** (*«it is a dispersion, and a square root cannot be negative»*); ⚠️ il divisore è il numero di **rendimenti**, **non** un $T-1$ di Bessel: riskfolio scrive `sqrt(value/(n-1))` dove **`n` conta la baseline inserita**, quindi `n-1` **è** $T$ → *«reading that as a sample standard deviation and dividing by $T-1$ is wrong by $\sqrt{T/(T-1)}$»*. **La formula ha l'aspetto della correzione di Bessel e non lo è.** **(2) `conditional_drawdown_at_risk`** — 🔴 *«This is **not** the arithmetic mean of the worst `alpha` share of observations. It is the **Rockafellar-Uryasev** form, normalized by **`alpha * T`** rather than by the number of observations in the tail. **Writing the naive mean here reproduces exactly the historical CVaR defect this subsystem is being corrected for: a plausible number, wrong by a fraction of a percent, that no test notices.**»* → **N ha nominato M2 come il tranello che stava evitando.** **(3) `_tail_without_baseline` (`:43-59`) — la baseline fantasma**: `summarize_drawdown` restituisce **$T+1$** punti perché `wealth_index` antepone la baseline unitaria (drawdown sempre 0); `MDD_Rel` e `UCI_Rel` iterano **tutto** l'array, `DaR_Rel` e `CDaR_Rel` **cancellano il primo elemento**. ⚠️ *«**Using the wrong length is a silent error**: the extra `0.0` sorts to the top of the array, **so the quantile itself often still matches**, while CDaR's denominator shifts by one observation.»* → **Sbagliare la lunghezza non rompe il DaR, rompe solo il CDaR: un test sul DaR passerebbe e certificherebbe un CDaR sbagliato.** **Ennesima istanza della tesi**: il controllo valida l'artefatto che tocca, non quello che è rotto. **(4)** La guardia `if DD > 0` di riskfolio è **omessa con prova che non può cambiare il risultato** — *«the only values it excludes are exactly zero, and zero squared contributes nothing»* |
| D185 | 18 Set 2026 | ✅ **Il secondo ramo della regola dello SHA si è applicato a se stesso: per rispondere a I non c'era SHA, e la via è stata leggere il worktree — dichiarandolo** | Cercando di rispondere a D183 **senza svegliare N (`FROZEN`)**, si è tentata la via dell'object store condiviso (D179). **Esito**: `e-alfy-risk-n-backend-acquisizioni` è a **`cc33120eb`** — **la baseline comune**. 🔑 **Nessun mandato ha ancora committato alcunché**: il lavoro di tutti e undici esiste **solo nei worktree**. → **La regola di F si applica a se stessa**: *codice non ancora nell'albero → non verificabile → si dichiara, non si comanda di controllare*. ✅ **Via usata**: lettura diretta del worktree di N sul filesystem — **non distruttiva, non duplica un'indagine, non sveglia un mandato congelato**. ⚠️ **E si dichiara come tale**: ogni fatto di D183/D184 porta **fonte (worktree di N), momento (18 Set) e stato (non committato)**, secondo D182. **I non può verificarlo con `git cat-file`, e gli è stato detto esplicitamente.** 📌 **Garanzia sostitutiva offerta a I**: *«N è `FROZEN`, quindi il file non cambia sotto di te mentre scrivi»* — che è la sola garanzia disponibile quando lo SHA non esiste |
| D186 | 18 Set 2026 | 🔴 **Il progetto ha DUE formattatori configurati che non concordano, e quello sbagliato è entrato in un checkpoint di N riformattando 7 file fuori scopo — irreversibilmente, e invisibile a ogni gate** | Trovato dal coordinatore leggendo il worktree di N per rispondere a D183. `git status` mostrava **11 file modificati**, di cui **7 fuori dallo scopo di N** (`config.py`, `api/v1/system.py`, `schemas/common.py`, `schemas/prices.py`, `test_brokers_api.py`, `test_scheduler_leader.py`, `test_scheduler_loop.py`). **Diagnosi misurata, non dedotta:** `black --check --line-length 300` sulla baseline → **EXIT=0, pulita**; `ruff format --diff` sulla **stessa** baseline → **66 righe di differenza, identiche a quelle presenti in N**. Confronto sistematico sui 7 file: **74 righe predette da `ruff format`, 72 presenti in N** — scarto **un solo hunk** (una concatenazione implicita di stringhe che il mio ruff unisce e quello di N no) → **è `ruff format`, a una versione diversa dalla mia**. 🔑 **La radice è una configurazione che autorizza uno strumento che il progetto non usa**: `pyproject.toml` dichiara **sia `[tool.black] line-length = 300` sia `[tool.ruff] line-length = 300`**, ma `dev.py:1698` esegue **`black backend/`** e `dev.py:1705` esegue **`ruff check backend/`** — **`ruff format` non è invocato da nessun comando di `dev.py`**. Chi legge `pyproject.toml` conclude ragionevolmente che sia sanzionato. 🔴 **E il danno è permanente**: dopo `ruff format` + `black --line-length 300`, il file resta **a 50 righe di distanza dall'originale** e `black --check` dà **EXIT=0** — cioè **entrambe le forme sono conformi a black**, e `ruff check` dà EXIT=0 su entrambe. → **`./dev.py format` non ripristinerà mai l'originale, e `./dev.py lint` non se ne accorgerà mai. La riformattazione è irreversibile con gli strumenti del progetto e invisibile a tutti i suoi cancelli.** ⚠️ **Non è stato transitorio**: la contaminazione è **dentro il checkpoint `1aaea6949`** di N (verificato con `git diff --stat cc33120eb 1aaea6949 -- <i 7 file>`). ✅ **I file di scopo di N sono integri** (`schemas/risk.py` +33, `historical_kpi.py` +53, `risk_contribution.py` +27, `test_risk_analytics.py` +593: nessuna riga di sola spaziatura). **Riparazione: meccanica**, ripristino dei 7 percorsi fuori scopo — **azione dello sviluppatore**, non di un agente 🔴🔴 **RITRATTATA IL GIORNO STESSO — VEDI D280. LA DIAGNOSI ERA SBAGLIATA ALLA RADICE**: il controllo fondante `black --check … | tee … ; echo EXIT=$?` leggeva **l'uscita di `tee`**, non di black. **La baseline NON è conforme a black**: `black --check` sui 7 file dà **EXIT=1, «7 files would be reformatted»**. E `black --diff` predice **esattamente** il diff di N, **72 righe identiche**. → **N ha eseguito `./dev.py format`, il comando SANZIONATO.** Non c'è nessuno strumento non autorizzato. |
| D187 | 18 Set 2026 | ⚠️ **Misurando D186 il coordinatore è caduto nella famiglia che sta documentando: `black` in `/tmp` non trova `pyproject.toml` e usa 88, non 300** | Il primo controllo del ping-pong dava *«black NON ripristina»* con `black --check` a **EXIT=1**, e stava per essere riportato come *«l'output di ruff non è nemmeno stabile sotto black»*. **Falso**: `black` cerca `pyproject.toml` **relativamente al percorso del FILE**, non alla cwd. Il file di prova era in `/tmp`, dove non esiste → **line-length 88 di default**. Rifatto con `--line-length 300` esplicito: **EXIT=0**, cioè l'output di ruff **è** black-conforme, e la conclusione vera (*«entrambe le forme sono conformi, black non è una forma canonica qui»*) è **più grave** di quella falsa. 🔑 **Il controllo aveva misurato un mondo diverso da quello che dichiarava** — esattamente `check-links` 12 contro 24, e `1 674` contro `1 517`. **La cura è la stessa già scritta tre volte: un numero dichiara CHE COSA ha misurato.** 📌 Registrato **perché è del coordinatore**: la famiglia non è una proprietà dei mandati |
| D188 | 18 Set 2026 | ✅ **Il confine della regola dello SHA non è «committato vs non committato»: è «tracciato vs ignorato» — e questo rende la regola molto più applicabile di come era stata scritta** | **F** ha misurato: **1 238 ref** sotto `refs/copilot/checkpoints/` (**erano 1 232 venti minuti prima — cresce mentre si guarda**), **385 scritti oggi**, **14 sessioni attive su 27**. Prova che chiude la questione, stesso file nello stesso istante: `git cat-file -e ee08426f5:frontend/e2e/portfolio/risk-lab.spec.ts` → **✅ esiste, 812 righe**; `git cat-file -e cc33120e:…` → **`fatal: exists on disk, but not in cc33120e`**. 🔑 *«**Il lavoro non è nei worktree: è nell'object store. I RAMI sono fermi, i COMMIT no.**»* → **Correzione di D185**, che aveva concluso *«nessuno ha committato alcunché»* dall'osservazione che **gli undici rami sono tutti a `cc33120eb`** — vero (F l'ha verificato su tutti e undici in un comando) **ma irrilevante**. \| | leggibile con uno SHA? \| \|---\|:---:\| \| codice **tracciato**, non su ramo \| ✅ **sempre** \| \| artefatto **ignorato** (`generated.ts`, `openapi.json`) \| 🔴 **mai, da nessun albero** \| ⚠️ **Ricaduta concreta**: la lettura diretta del worktree di N (D185) **era evitabile**. Lo SHA esiste — **`1aaea6949`**, checkpoint di N del 18 Set 01:10:29, contiene `acquired.py` a 246 righe — e **I può verificarlo da solo** invece di fidarsi. ✅ **E la forma inversa di D181, che F aggiunge**: *«**un fatto non relayato perché "non lo riguarda" è un fatto che nessuno verificherà mai.** L'immunità di oggi è una proprietà dello **scope**, non del codice — e gli scope cambiano»* |
| D189 | 18 Set 2026 | ✅ **La riga di catalogo che A chiedeva per N non esiste e non serve: N ha esteso un file già registrato** | A ha chiesto **quattro volte** il nome del file di test di N per registrarlo in `RISK_SERVICE_TEST_PATHS`. **Misurato nel worktree di N**: nessun file nuovo — N ha aggiunto **+593 righe** a `backend/test_scripts/test_services/test_risk_analytics.py`, che è **già** al rigo **77** di `scripts/test_runner/_backend_services.py`. → **Nessuna voce di catalogo da scrivere, e un conflitto in meno sul catalogo.** 📌 **La domanda era senza risposta perché presupponeva un file nuovo**: quattro richieste inevase non per silenzio, ma perché **l'oggetto richiesto non esisteva** |
| D190 | 18 Set 2026 | 🔴 **F42 — «la tredicesima chiave ha già un colore» era mia, ed è falsa: `BADGE_CLASS_MAP` non contiene colori. Il ripiego silenzioso TRAVESTITO DA BUONA NOTIZIA** | Registrando F38 il coordinatore aveva scritto a G: *«se G calcola la tavolozza **via** `assetTypeBadgeClass`, la tredicesima chiave ha già un colore»*. **B l'ha misurato — tre fatti veri, conclusione falsa**: ✅ `BADGE_CLASS_MAP.LIQUIDITY` **esiste** · ✅ è **coperta dal cancello** (`assetTypeTables.test.ts:158`, enum ⊆ mappa) · ✅ **18 chiavi** (17 enum + `LIQUIDITY`) · 🔴 **i valori non sono colori**: `'bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400'` — **zero `#` in tutta la mappa**. → **`assetTypeBadgeClass()` restituisce una classe CSS; ECharts vuole un valore di colore.** 🔑 **E il modo in cui fallirebbe è quello che G sta riparando**: *«**`BADGE_CLASS_FALLBACK` è una stringa valida esattamente come le altre.** Nulla lancia, nulla si annerisce, nulla appare nei log — la fetta semplicemente non prende il colore che dovrebbe»* → **la raccomandazione instradava G *verso* la funzione il cui ripiego è un grigio Tailwind silenzioso, cioè dentro il difetto che G stava riparando.** *«È il ripiego silenzioso un'ultima volta, e stavolta **travestito da buona notizia**: l'unica forma che non avevamo ancora incontrato.»* ➕ **Secondo ostacolo**: la mappa codifica **i due temi nella stessa stringa** (`dark:` inline) → **non separabile** in chiaro e scuro, il minimo che una tavolozza di grafico deve saper fare. ✅ **Cosa resta vero**: l'**insieme** delle chiavi (**12 primari + `LIQUIDITY` = 13**) è corretto e **già sorvegliato**; ⚠️ **ma il cancello protegge `BADGE_CLASS_MAP`, non "i colori"** — una tavolozza **parallela** in esadecimale è **fuori dal cancello**. → **G deve estendere il cancello alla propria mappa, non ereditare quella di B.** Relayato a G in consegna prioritaria |
| D191 | 18 Set 2026 | 🔑 **Sedicesimo gradino, e stavolta non è «verificare che la falsificazione sia stata eseguita»: è verificare che la DOMANDA sia ancora viva** | **B** ha **ritrattato** un fatto riferito come *«provato mio per esecuzione»*: il rosso di `tx-import-asset-inspector` attribuito al proprio `AssetModal`. **Origine dell'errore**: **una singola esecuzione verde della baseline**, trasformata in relazione causale. **Rimisurato**: baseline **1 verde su 5 corse**; `AssetSelect` a baseline **2 rossi su 2**; `tx-ca-contract` **2 failed/10 in tutte e tre le configurazioni**. → **Né `AssetModal` né `AssetSelect` causano quei rossi, e nessuno dei due spec è nel diff di B.** 🔑 *«**Un verde non è una misura. È una misura con N = 1.**»* 🔑 **E il corollario sulla bisezione, che vale per tutta la campagna**: *«**la bisezione non ha smentito l'ipotesi trovando la causa. L'ha smentita perché OGNI rimozione restava rossa.** Se avessi fatto una sola sonda e l'avessi vista rossa, avrei concluso "non è quella riga" e cercato nella successiva — **restando dentro il file per sempre**.»* 🔴 **E il gradino sopra D136**: *«le mie tre sonde **avevano** la prova di essere state eseguite. **Erano rosse e non provavano niente**, perché la mia conclusione precedente era già falsa. **Il gradino successivo non è verificare l'esecuzione: è verificare che la domanda a cui rispondi sia ancora la domanda giusta. Una falsificazione eseguita correttamente contro un'ipotesi già smentita è lavoro impeccabile su un oggetto morto.**»* ⚠️ **E l'esempio è il coordinatore**: *«le tue ultime **cinque** istruzioni erano tutte eseguibili, tutte verificate, e tutte rispondevano a una domanda chiusa ore prima»* — **cinque relay su una riapertura chiusa da due messaggi**. 📌 **Meccanismo dei due rossi non attribuibili (F40)**: `optionsClosed()` (`fixtures/probe.ts:46`) asserisce zero opzioni, ma `SearchSelect:452` rende il listbox con `aria-busy={loading}` **prima** delle opzioni → **una tendina aperta ma vuota soddisfa `optionsClosed`**, e il `.click()` successivo **la richiude**. Il docblock dichiara di aver sostituito un `waitForTimeout(300)`: **ha ristretto la finestra, non l'ha chiusa** |
| D192 | 18 Set 2026 | ⚠️ **A ritratta il proprio enunciato A9 per J: «il ramo TWRR non è interessato» è troppo forte — e l'ha ricontrollato PERCHÉ aveva appena contestato al coordinatore la stessa classe** | A aveva riferito: *«il ramo TWRR non si muove di un bit: `0,000e+00` su 200 serie × 3 tassi. **Nessun utente in modo `HISTORICAL` di ambito portafoglio vedrà cambiare un numero.**»* 🔴 **Ritrattato**: *«ho misurato il **comportamento della funzione** a fattore 365 e ne ho dedotto il **comportamento del sistema**»*. `service.py:871`: `annualization_factor = len(returns) * 365 / calendar_days` → **365 esatti solo se `len(returns) == calendar_days`**, e la riga sopra filtra `point.twrr is not None`. **Due meccanismi**: `lots_analysis_service.py:1013` (flusso esterno non valido → `twrr=None` **da quella data in poi**) è 🟢 **innocuo** perché tronca la coda e `calendar_days` si misura sul `points[-1]` superstite; **`portfolio_service.py:1388` — `twrr = twrr_map.get(d)`** è 🔴 **`None` per ogni data assente**, e se cadono date **interne** allora `len(returns) < calendar_days` → **fattore < 365**. (`:1429` forza `history_points[0].twrr = 0`, quindi il primo punto non è mai nullo.) ⚠️ **Non provabile dal codice** se `twrr_map` copra sempre ogni giorno interno. ✅ **Ma la direzione è nota e favorevole**: fattore < 365 ⟹ tasso corretto più alto ⟹ **il vecchio lusingava**. *«**Ovunque il fattore sia sotto 365, il codice vecchio lusingava. La correzione toglie lusinga dappertutto.** Non c'è alcun caso in cui peggiori qualcosa: l'unica variabile è **se** un dato portafoglio si muova.»* **Enunciato sostituito in J verbatim.** 🔑 *«È la stessa classe che ho fatto notare a te su `calendar_coverage`: **ho misurato una funzione e ho parlato di un sistema**. Che sia capitato **a me** subito dopo averlo contestato a te è il motivo per cui l'ho ricontrollato.»* |
| D193 | 18 Set 2026 | 🔴 **M5 declinata, ma la premessa che la declassava era FALSA: «l'idraulica manca» è sopravvissuta a tre documenti perché nessuno ha aperto il plugin** | Tre documenti portavano la stessa frase — `06` §Caso A, `06` §M5, brief di A §M5: *«`Risk_Contribution` vuole la matrice dei rendimenti, noi passiamo solo covarianza e pesi. **Far arrivare i rendimenti a quel livello è idraulica, e va a preventivo.**»* **A ha aperto il plugin.** `risk_plugins/risk_contribution.py:55-58`: `rows = [prepared_asset_returns(context, asset_id)[1] for asset_id in asset_ids]` → **`rows` È la matrice dei rendimenti**, costruita dal chiamante e consumata una riga dopo. Provato eseguendo: **una trasposizione**, `shape (750, 6)`, `rk.Risk_Contribution` gira, **scarto massimo `1,9e-11`**. 🔑 *«La premessa confonde **la firma di `risk_contributions_from_covariance`** — che davvero prende covarianza e pesi — con **la disponibilità dei rendimenti presso il chiamante**, che li ha. **Un argomento sulla firma è stato letto come un argomento sull'architettura.**»* ✅ **Conseguenza di piano: D39 NON segue M5.** Il brief diceva *«Calmar e Martin pretendono la matrice dei rendimenti, che è esattamente l'idraulica mancante di M5 → dopo M5, e solo allora»*. **Se l'idraulica non manca, D39 è dietro Q7 — una decisione di prodotto, non tecnica.** 📌 **E la forma in cui Q7 va posta, che la rende decidibile**: *«**Chi riprenderà D39 non deve preventivare idraulica: deve preventivare un grafico che sappia disegnare valori negativi e somme oltre il 100 %.**»* **Misurato su portafoglio casuale senza patologie**: `MDD` `−3,89 · −3,62 · 14,0 · −4,31 · 33,33 · 64,49` · `ADD` con una fetta da **113,68 %** e una da **−27,05 %** · `UCI` **−20,44 / 107,89**. **Tre misure drawdown su tre**, e **l'asset 4 passa da +41,46 % con `MV` a −27,05 % con `ADD`**. *«Una torta con una fetta da 113,68 % e una da −27,05 % non è una torta.»* ➕ **Due correzioni a `06`**: **(a)** *«`np.allclose` vero»* è vero **solo alle tolleranze predefinite** — a `rel=1e-9`/`abs=1e-12` è **`False`**; **(b)** 🔴 **il rapporto di velocità è INVERTITO a N=6**: `06` dice *«nostro 0,022 ms, riskfolio 0,058»* misurato a **N=3**; a **N=6/T=750** è **nostro 0,563 ms contro riskfolio 0,09-0,12** → **riskfolio ~4,6× più veloce**. La conclusione «differenza irrilevante» regge, **la direzione non va citata da nessuno** |
| D194 | 18 Set 2026 | 🔑 **Il rapporto di diversificazione è ipersensibile ESATTAMENTE dove la diversificazione è migliore — e la riga che dimostra la tesi con più forza è quella meno riproducibile** | **I** ha notato che il DR misurato da **N** a ρ=0 è **3,15** mentre la forma chiusa $\sqrt{n/(1+(n-1)\rho)}$ dà **3,1623**. **Non si è fermato a «sarà campionamento»: l'ha simulato** — 2000 prove, T=750, n=10. \| ρ \| forma chiusa \| mediana \| **intervallo 95 %** \| ampiezza \| \|---\|---\|---\|---\|---\| \| **0** \| 3,1623 \| 3,1623 \| **3,022 – 3,323** \| **0,301** \| \| 0,5 \| 1,3484 \| 1,3483 \| 1,318 – 1,382 \| 0,064 \| \| 0,95 \| 1,0233 \| 1,0233 \| 1,021 – 1,026 \| **0,005** \| → **3,15 sta comodamente dentro: nessun errore di N.** 🔑 **Ma la conseguenza è più interessante dell'assoluzione**: $\partial DR/\partial\rho$ vale **−14,2** a ρ=0 contro **−1,10** a ρ=0,5 — una correlazione media campionaria di appena **0,0005** sposta il DR a 3,155, e **su 45 coppie con T=750 lo scarto tipico è dieci volte tanto**. ⚠️ **Quindi un test che asserisce `DR ≈ 3,15` a ρ=0 con tolleranza stretta sarà rosso a caso**; la stessa tolleranza a ρ=0,95 è **61 volte** più solida. 📌 **Forma robusta per N**: asserire su ρ=0,5 e ρ=0,95; a ρ=0 asserire **l'ordinamento** (DR cala al crescere di ρ) **oppure** tolleranza al **5 %**, non al centesimo; **oppure** fissare il seme **e dichiararlo**. 🔑 *«L'ironia che vale la pena registrare: **la riga che dimostra la tesi con più forza è quella il cui numero è meno riproducibile**.»* ✅ **Scelta editoriale di I confermata**: ha pubblicato **la cifra di N**, non la propria, affiancandole la forma chiusa → il lettore verifica e **lo scarto non diventa una contraddizione** |
| D195 | 18 Set 2026 | 🟡 **Quinto punto cieco: sospettato, misurato, e NON c'era — ma niente ce l'avrebbe detto se ci fosse stato** | **I** ha temuto che le pagine usassero link in forma **directory** (`../slug/`) invece che `.md`, perché `mkdocs build` riporta `validation.links.unrecognized_links = 20` a livello **INFO**: un link `../slug/` **non verrebbe validato da nessuno** — né da MkDocs strict, né da `check-links`. **Stessa identica classe delle 9 ancore morte.** ✅ **Misurato su tutte le pagine: 170 link, 170 in forma `.md`, zero dir-style.** 🔑 *«Il punto non è che il difetto non c'era. È che **niente ce l'avrebbe detto se ci fosse stato** — e **la forma sbagliata è quella che un umano scrive più volentieri**, perché è quella che vede nella barra degli indirizzi.»* ✅ **Convenzione da mettere in K7**: **si linka `.md`, mai la directory** — è l'unica delle due forme che qualcuno guarda. 📌 **Terzo caso in cui la pulizia viene dalla coerenza di chi scrive e non da un controllo** (dopo i 170 link e i `$$` inglesi): **la coerenza non è un cancello, e non sopravvive a un cambio di autore** |
| D196 | 18 Set 2026 | 🔴 **Il rimedio che il coordinatore ha proposto a G — «estendi il cancello *enum ⊆ mappa* alla tua tavolozza» — NON è esprimibile, e l'unica forma verde è quella che sigilla il difetto** | **G** è **immune a F42, misurato**: `grep -E "assetTypeBadgeClass\|BADGE_CLASS\|bg-[a-z]+-[0-9]\|dark:bg-"` su **sei superfici** → **zero riscontri**, tranne `AllocationHistoryChart.svelte:806` (`bg-gray-100 dark:bg-slate-700`, **scheletro di caricamento, già nella baseline**, verificato sul blob). Le sue tavolozze sono **letterali esadecimali**, 14 voci, **due tavolozze fin dall'inizio** → il problema dei due temi nella stessa stringa non lo tocca. 🔴 **Ma il rimedio proposto è impossibile**: *«**La mia tavolozza non ha chiavi.** È `palette[groupIndex % palette.length]`: **posizionale per rango**. *enum ⊆ mappa* richiede un insieme di chiavi; qui non ce n'è uno.»* **L'unica traduzione è la cardinalità, e va male in entrambe le direzioni:** \| traduzione \| oggi \| giudizio già dato \| \|---\|---\|---\| \| `palette.length ≥ chiavi **settore**` \| **14 ≥ 15 → ROSSO** \| è **l'opzione 4**, già scartata con la logica di G \| \| `palette.length ≥ chiavi **tipo**` \| **14 ≥ 13 → VERDE** \| **nuova, e peggiore** \| 🔑 *«**La seconda è verde PERCHÉ copre la dimensione che già funziona.** Un cancello che si chiude sul tipo è **strutturalmente muto sul settore** — mette un sigillo verde **esattamente sopra il difetto che ho lasciato aperto e registrato**. Non è coverage insufficiente: è **coverage orientata sul lato sbagliato**, la stessa malattia dello stub verbatim e del docstring che mentiva, **in forma di cancello**.»* ➕ **E l'irrimediabilità l'ha stabilita il coordinatore stesso**: *«la cardinalità vera è dati di backend: `getSectorKeysList()` ripiega sui 14 **solo se vuoto**»* → **non si può mettere un cancello su un limite che al tempo del test non è calcolabile.** **Per questo la risposta autorizzata era il `debug.warn`: rendere il superamento UDIBILE invece di prometterne l'assenza.** ✅ **E la copertura vera di G intercetterebbe comunque la trappola CSS**: pavimento **ΔL slot per slot** con prova che il ciclo ha girato (`expect(pairsChecked).toBe(palette.length)` — *«senza questo il ciclo poteva girare zero volte ed essere verde»*); una classe Tailwind darebbe `hexToHsl → null` → `shadeForDepth` restituisce la base → **ΔL = 0** → `toBeGreaterThanOrEqual(15)` → **ROSSO in ogni caso** |
| D197 | 18 Set 2026 | 🔑 **G ha trovato un ripiego silenzioso NEL PROPRIO codice, cinque righe sopra il punto che stava controllando — ed è letteralmente il difetto che il mandato ripara** | `allocationHierarchy.ts:109`, scritto da G: ```const hsl = hexToHsl(base); if (!hsl) return base;``` **In test va rosso** (il pavimento ΔL lo prende). **In produzione no**: restituisce la base → **padre e figlio dello stesso colore** → **è D71, il difetto che questo mandato ripara, reintrodotto dal suo stesso motore.** ✅ **G l'ha dichiarato invece di lasciarlo scoprire**, con le tre qualificazioni giuste: **raggiungibile** solo se un letterale di tavolozza smette di essere esadecimale (**dato di modulo, non dell'utente: non varia a runtime**) · **coperto** su tutti e **28** gli slot (14 × 2 temi), **ma da un test, non da un cancello** · **difendibile** (in un render di grafico restituire la base è meglio che sollevare). 🔑 *«**Mi avevi avvisato di non CONSUMARE un ripiego silenzioso altrui. La misura ne ha trovato uno mio, cinque righe sopra il punto che stavo controllando.**»* → *«la differenza fra il mio e quello che mi segnalavi è solo **chi lo ha scritto**»*. 📌 Aggiunto al debito di secondo giro accanto al perimetro settori — **stessa famiglia, stessa cura: rendere udibile ciò che oggi tace** |
| D198 | 18 Set 2026 | ✅ **Correzione a D141: non vince chi misura, vince chi consegna un indirizzo che l'altro può eseguire** | D141 diceva *«un mandato che misura ha sempre ragione su un mandante che ricorda»*. **B la corregge, e la rompe addosso a sé**: *«È troppo generoso, e oggi si è rotta proprio addosso a me. Quando ho riferito "provato mio per esecuzione" **stavo misurando** — e avevo torto, perché **una misura sola non è una misura**. Il mandato non vince perché misura: vince quando la misura è **ripetuta** e **falsificabile**.»* 🔑 **Versione che regge**: *«**vince chi consegna un indirizzo che l'altro può eseguire.** Non chi misura, non chi ricorda — **chi rende l'altro capace di controllare senza fidarsi**. È ciò che hai fatto tu con `5b1a29071` e ciò che faccio io con `590f32ae6`. **Nessuno dei due ha dovuto credere all'altro, ed è per questo che ha funzionato.**»* 📌 **E due correzioni operative al comando dei checkpoint**: **(a)** la forma `refs/copilot/checkpoints/<sessione>/*` **restituisce vuoto** — i ref hanno **due segmenti in più** (progressivo + UUID di turno); si usa il **prefisso di componenti senza glob**. B l'ha sbagliata al primo tentativo **e l'ha verificata prima di consegnarla**; **(b)** ⚠️ *«il checkpoint scatta **all'inizio** del turno, quindi porta la data del messaggio ricevuto e contiene tutto **fino alla risposta precedente inclusa** — non ciò che si fa nel turno corrente. **Un checkpoint prova il passato, mai il presente.**»* 📌 SHA di B aggiornato: **`590f32ae6`** — 30 file, **+1 902 / −127** (da `5b1a29071`: +1 741/−126; **l'unica riga tolta è `data-testid` dal wrapper**) |
| D199 | 18 Set 2026 | 🔴🔑 **«Un'aspettativa che chiama il soggetto non è un oracolo, è un'eco» — e la diagnosi del gate cieco che il coordinatore aveva accettato era FALSA** | Il coordinatore aveva registrato la spiegazione di A: la suite restava a **292 prima e dopo** A9 perché *«ogni test esistente usa il fattore 365, l'unico regime dove vecchio ≡ nuovo»*. **`test-author` l'ha smontata con una riga**: `test_risk_metrics_oracle.py:84` → **`_ANNUALIZATION = 252.0`**. Il test rf **girava già a 252** — **un fattore non solare, esattamente il regime in cui il difetto morde** — e **lo stesso non lo vedeva**. → **Il fattore non c'entrava nulla.** 🔑 **Il motivo vero**: *«**Le aspettative erano formulate chiamando la funzione sotto esame.** `annualized_sharpe` chiama `daily_risk_free_rate(rf, fattore)` da sé. Se anche l'aspettativa del test la chiama, **entrambi i lati dell'identità usano lo stesso tasso — giusto o sbagliato** — e il test resta verde su qualunque corpo.»* **Misurato**: aspettativa formulata sul soggetto → i due codici concordano a **`rel ≈ 2e-15`**; aspettativa da una conversione `pow` **indipendente** → il codice vecchio sbaglia del **28,5 %**. **Stesso test, stessa riparazione, due colori.** 🔴 **Ricaduta sul lavoro di A**: **quattro delle cinque proprietà** dettate a `test-author` erano **cieche come enunciate** (la 2, la 3, la 7 e la 8 oltre alla 5) — *«se avessi scritto io quel blocco, avrei consegnato **nove test verdi che non proteggevano nulla**»*. 🔑 **Ed è la stessa trappola di M6** (`wealth_index` confrontato con `np.cumprod` mentre la migrazione voleva farlo **diventare** `np.cumprod`): **due volte nella stessa giornata, in due travestimenti** → *«non è coincidenza: è la **modalità di guasto caratteristica di un oracolo**. Un oracolo protegge **solo** finché la sua aspettativa ha una fonte **indipendente dal soggetto**.»* ⚠️ **E la nota d'archivio che il coordinatore aveva lodato come "lungimiranza che ha pagato" — il docstring a `:1800` che dichiarava il difetto fuori ambito — «aveva ragione sul fatto e torto sulla ragione»**: diceva *«questo test non benedice la conversione perché le aspettative sono scritte in termini di `daily_risk_free_rate`»* → **è esattamente la frase che descrive la cecità, scritta credendo di descrivere una protezione**. Riscritta da A come **nota storica** invece che cancellata: **è il verbale del punto cieco.** ✅ **Discriminazione eseguita, non creduta**: copia md5 `88c0b331…`, difetto ripristinato → **33 failed / 167 passed** (i 33 coincidono **esattamente** con la tabella dichiarata da `test-author`: 9+6+1+5+3+3+4+2, e passano **solo** i casi a 365); ripristino → md5 **identico**, **200 passed**. ✅ **E il «no» onesto**: il test 5 (guardia tasso ≤ −100 %) **non discrimina**, 5/5 verde da entrambe le parti — **`test-author` l'ha dichiarato invece di nasconderlo**. ⚠️ **Tolleranza da non stringere**: `rel=1e-12` è **inutilizzabile** per l'andata e ritorno del tasso (errore peggiore `2,02e-12`, scarto `pow`/`expm1` fino a **2,6e-11**) → usato **`rel=1e-9, abs=1e-12`** |
| D200 | 18 Set 2026 | ✅ **A completo e `FROZEN`: quattro passi su nove si chiudono SENZA codice, e questo è l'esito buono** | `services risk-oracle` **200 passed** in 3,42 s (**erano 158**) · `services risk-all` **334 passed** in 27,49 s (**erano 292**) · `ruff check` 14 file puliti · `black --check` 13/14, e il quattordicesimo (`_backend_services.py:962`) **provato preesistente** rilanciandolo sulla versione di `HEAD` senza righe di A · `git diff --check` pulito · `6240` **nessun listener** · **83 sonde `/tmp` rimosse, 0 rimaste**. **Inventario: 13 modificati + 2 nuovi, +580 / −73.** \| Passo \| Esito \| \|---\|---\| \| A1 W0 · A2 K1 · A3 M2 · A4 M1 · A5 M3 · A9 \| fatti \| \| **A6 M6** \| **zero migrazioni su nove**, ognuna esclusa **con una misura** \| \| **A7 M5** \| **declinato** — l'idraulica mancante **non esiste** \| \| **A7 D39** \| **non bloccato da M5**, bloccato da **Q7** \| 🔑 *«Un M6 eseguito alla lettera avrebbe **smontato la rete che M1 e M3 avevano appena steso**: la sua lista è stata scritta **prima** che M1 e M3 esistessero.»* ✅ **Effetto collaterale utile**: le garanzie a C (`current_buy_and_hold_returns`) e a N (`summarize_drawdown`) sono **più forti di quanto chiesto** — M6 quelle funzioni **non le tocca affatto**. 📌 Messaggio di commit proposto in fondo a `progress/A-esecuzione.md` (**1 648 righe**). **Nulla committato, nulla in stage** ⚠️ **AGGIORNATO da D268: M6 chiude a **1 migrazione su 9**, non 0.** A ha ribaltato il proprio rifiuto della Fascia 1 riconoscendo che *« distruggerebbe l'oracolo»* **non è un rifiuto ma un problema riparabile**, e l'ha eseguita **rifondando prima l'oracolo**. **769 324 punti, 0 divergenze.** Le altre otto esclusioni **restano, e si rafforzano** (D269). |
| D201 | 18 Set 2026 | 🔴 **Le cinque modalità di simulazione NON esistono — e il coordinatore le aveva relayate a I come «vere e verificate». Quinta istanza di piano ≠ cronaca** | L'agente di **I** si è **rifiutato** di scrivere `simulation-modes` — **quarta volta, e di nuovo aveva ragione**. \| il mandato assume \| il codice ha \| \|---\|---\| \| cinque modalità \| **una**: `RiskSimulationProcess` ha il solo valore **`GBM`** (`schemas/risk.py:136-139`) \| \| default = storia rimescolata \| default = **GBM** (`risk_plugins/simulation.py:45-46`) \| \| ricampionamento congiunto \| **nessuno** — grep su tutto `backend/app/` per `shuffl\|bootstrap\|resampl\|calm_market\|prolonged\|shock_and_recovery\|regime` → **zero pertinenti**; frontend **zero** \| E `H-backend-montecarlo.md:200-205` «Definizione di finito» è una checklist **tutta aperta**. → *«Qualunque cosa H abbia misurato, **non era questo baseline**. Il fatto può essere vero del **prototipo** di H; non lo è del **prodotto**.»* ⚠️ **E un rilievo più fine**: il piano di H dice **block bootstrap**, non rimescolamento di righe — **un bootstrap a blocchi conserva DELIBERATAMENTE la sequenza locale**, è il suo scopo. Quindi *«l'ordine non è più quello del mercato»* è vera per un rimescolamento a righe e **parzialmente falsa** per ciò che H ha in piano. ✅ **La frase in `worst-realization.en.md` sopravvive**, verificata prima di dirlo: è inquadrata come **esperimento mentale** su *cosa legge* ciascuna misura, senza affermazioni sul prodotto — **regge con blocchi, con righe, e in entrambe le direzioni** |
| D202 | 18 Set 2026 | 🔴🔑 **L'informativa della simulazione dichiara CINQUE limiti e omette l'unico che spinge il risultato verso il rassicurante — ed è già in produzione** | **I** ha verificato che l'utente **può** eseguire la simulazione oggi: `RiskAnalysisPanel.svelte:1164` `risk-simulation-controls`, `:1192` horizon, `:1211` random-seed, `:1224` sobol-start-index, `:1228` il bottone, `:1247` il grafico, `:1252` la distribuzione terminale. `simulation.py` è **un plugin registrato**, i18n in **quattro lingue**, pannello completo. → **L'utente lancia 8 192 cammini su 365 giorni e non ha una riga di documentazione su cosa quel numero assuma.** 🔴 **E la stringa `risk.simulation.assumptions`, mostrata sotto ogni risultato, dice**: *«{paths} paths over {days} days; current buy-and-hold composition, **without costs, cash flows, inflation or rebalancing**.»* → **Cinque cose dichiarate, e tutte e cinque sono meccanica di portafoglio. Della modellazione statistica — rendimenti gaussiani, volatilità costante, correlazione dalla covarianza campionaria — non c'è una parola.** 🔑 *«È un'informativa che **sembra scrupolosa** — elenca cinque limiti! — e **omette l'unica che spinge il risultato nella direzione rassicurante**: un GBM **sottostima le code**, cioè **esattamente ciò per cui l'utente sta simulando**. E non modella il **clustering di volatilità**, cioè il fatto che **le crisi durano**.»* (L'altra stringa dice *«conditional GBM…»*, ma **«GBM» per un utente è una sigla, non un avvertimento**.) → **Stessa famiglia dei gate, in forma di informativa: un controllo che passa perché guarda altrove.** 📌 **Debito assegnato**: `risk.simulation.assumptions` è i18n frontend → **E o F**, perché *«il divario fra la pagina e la UI diventerebbe altrimenti la prossima deriva: **la documentazione direbbe la verità e il prodotto continuerebbe a omettere**, che è il modo migliore per far sembrare la documentazione pedante»* |
| D203 | 18 Set 2026 | 🔴 **42 ancore morte, non 3 — e l'inversione che rende la regola usabile: l'ancora è SANA dove la pagina non è tradotta, ROTTA dove lo è** | **I** ha esteso la sonda. ⛔ **Prima, due errori suoi, dichiarati**: la prima regex cercava **una** delle **tre** sintassi che `attr_list` accetta (`{: #id }`, `{ #id }`, `{#id}`) → *«**un controllo che cerca la forma che conosce invece del fatto che gli interessa.** È letteralmente il difetto che sto riparando, commesso dallo strumento con cui lo riparavo»*. ✅ **Misurato nell'HTML costruito**: delle 5 ancore «implicite» reali, **quattro funzionano in quattro lingue** — `#backup` per **coincidenza di vocabolario** (nessuna tecnica: «Backup» è identico in italiano), `#fifo-matching` con **`<div id="…"></div>` HTML grezzo**, `#broker-reports` con `{: #… }` presente **anche nel `.it.md`** — e **l'unica rotta, `#recovery-time`, è ESPLICITA** e vive solo nel `.en.md`. 🔑 **Diagnosi vera**: *«**Un'ancora non si rompe perché è implicita. Si rompe quando l'ancora e il suo bersaglio sono stati scritti in momenti diversi.** `#recovery-time` è esplicita **e rotta**; `#backup` è implicita **e funziona**. **Un'ancora è aggiornata quanto l'ultima traduzione della sua pagina bersaglio.**»* 🔴 **Esposizione reale**: `index` **10**, `volatility` **11**, `sharpe-ratio` **8**, `sortino-ratio` **8**, `max-drawdown` **5** → **42 ancore esplicite esistono solo in inglese**; MkDocs ne segnala **9** perché **una sola** è oggi bersaglio di un link. *«**Le altre 41 sono innescate e mute.** Diventano rosse nell'istante in cui E, F o H scrivono un `DocsLink` con ancora verso `sharpe-ratio` o `volatility` — cioè verso **le metriche che l'app già calcola**, quindi **le prime che cableranno**.»* ✅ **E le 17 pagine nuove sono al sicuro PROPRIO perché sono in debito di traduzione**: il fallback i18n serve il corpo inglese **intero, ancore comprese** (verificato: `concentration#interpretation` risolve in it/fr/es benché la pagina non esista); **una traduzione parziale serve invece una pagina tradotta PRIVA dell'ancora**. → **Sarà il blocco di traduzione a metterle a rischio, non la sua assenza.** 📌 **K7, parte mancante**: *«un `{: #token }` aggiunto a una pagina **già tradotta** vive **solo in inglese** finché Aphra non rigira… se ti serve un'ancora su una delle cinque **prima** della traduzione, la tecnica che regge è il **`<div id="…"></div>`** — l'unica che il progetto usa già e che sopravvive **perché non è prosa**»*. 🔑 **Nono membro della famiglia, e il più silenzioso**: `check-links` **non può vederlo**, perché valida contro il sorgente inglese **dove l'ancora c'è** — *«qui non c'è nemmeno un INFO da ignorare»* |
| D204 | 18 Set 2026 | 🔴 **La rete K2 esistente di B era TESTUALE e sarebbe restata verde sulla mutazione che distrugge il contratto — la richiesta di G era più forte di come era arrivata** | **B**, scongelato per le due asserzioni di G. ⚠️ **Prima ha verificato il proprio presupposto, ed era sbagliato**: credeva falsa l'asserzione *`null`/`''` → `'OTHER'`*, perché dalle due righe che ricordava l'input vuoto esce come `''`. **Letto il codice: c'è una guardia**, `if (raw === '') return 'OTHER';`. → *«**Il difetto era nella mia versione riassunta, non nel prodotto** — e non l'avrei saputo scrivendo dalla memoria del contratto.»* 🔴 **La scoperta**: il test esistente `keeps primaryAssetType upper-casing its input` **legge il sorgente** e verifica che `.toUpperCase()` compaia nel corpo. → *«**Cambiare l'ultima riga da `?? raw` a `?? type` lascia quella chiamata intatta** e restituisce al chiamante la sua stessa grafia. **Il contratto è morto e la rete è verde.**»* **Falsificato eseguendo, con `assert` di applicazione (D136)**: mutazione `?? raw` → `?? (type as string)` **applicata** (n=1, `.toUpperCase()` ancora ×4) → **1 failed / 263**, e ⚠️ **il test testuale NON compare fra i rossi** (`grep -c` sul log → **0**); rimozione della guardia `''` → **1 failed / 263**; ripristino → `diff -q` identico, **264 passed**. 🔑 *«**È la tesi di G provata per esecuzione, non accettata.** Le due reti non sono ridondanti: **la vecchia è verde esattamente sulla mutazione che distrugge il contratto**. G non chiedeva un **secondo** guardiano; chiedeva il **primo** guardiano **del comportamento**, perché quello che c'era sorvegliava **il testo**.»* ⚠️ **E un import statico avrebbe avvelenato tutto il file**: `assetTypes.ts` importa `$lib/api/generated`, che è **gitignored**, e **nessun test del repo lo importa oggi** — *«un import irrisolvibile **fa fallire l'intero modulo in collection**, e questo file esiste proprio per funzionare dove `api sync` non è mai girato»*. Usato **import dinamico locale ai due test**, con `existsSync` che **fallisce invece di saltare**: *«in una riga di riepilogo **un test saltato e uno soddisfatto sono la stessa assenza di rosso**»* |
| D205 | 18 Set 2026 | 🔑 **«Un'etichetta che non contiene ciò che designa vive solo finché vive il suo contesto»** | **B**, generalizzando il rifiuto del numero nudo nel commento i18n: *«Il `(d)/(d)` e l'`(a)/(a)` sono la stessa forma del **numero nudo**… **Una lettera, un numero, un conteggio, uno SHA abbreviato senza il file accanto: sono tutti puntatori che il lettore successivo non può risolvere.** La cura è sempre la stessa — **mettere dentro l'etichetta abbastanza da poterla verificare senza chiedere**.»* → Unifica D145 (le decisioni si nominano, non si numerano), D180 (un numero dichiara che cosa ha misurato) e D179 (lo SHA dice quale albero) **sotto un'unica regola** |
| D206 | 18 Set 2026 | ✅ **I due rossi di B sono della BASELINE, misurati dal coordinatore in corsia 6250 — e la corsa 2 È il verde che B aveva ritrattato** | **Condizioni dichiarate**: albero `e-alfy-ideal-eureka`, HEAD **`cc33120eb`**, `git status --short frontend/ backend/ scripts/` → **vuoto** (zero righe di prodotto), corsia **6250**, `--data-dir …/backend/data/test-risk-coord`, DB ripopolato prima della prima corsa (5 612 record). **`tx-ca-contract`** → **3 corse su 3: `2 failed / 10 passed`**, sempre **CAC-011 e CAC-012**. → **rosso deterministico preesistente**. **`tx-import-asset-inspector`** → corsa 1 **1 failed/4** (E2-001 ✘ **12,9 s**) · corsa 2 **5 passed** (E2-001 ✓ **3,4 s**) · corsa 3 **1 failed/4** (E2-001 ✘ **12,7 s**) → **rosso intermittente preesistente, 2 su 3**. 🔑 **La corsa 2 È il verde di B**: quello osservato **una volta** e da cui era nata la conclusione «provato mio». *«Vederlo comparire nel tuo tabellone come **una riga su tre** chiude F41 meglio di qualunque ritrattazione mia — perché **una ritrattazione è ancora un'affermazione, mentre quella riga è l'evento che la genera**.»* 🔑 **E i tempi confermano il meccanismo senza passare dal racconto**: **12,9 · 3,4 · 12,7** — *«una firma che **non richiede di sapere cosa fa `optionsClosed()`**: dice "timeout" prima di dire **perché**. **È una misura che sopravvive all'ipotesi**: se il meccanismo descritto fosse sbagliato, quei tre numeri resterebbero veri.»* ✅ **Nessuno dei due blocca l'integrazione di B.** |
| D207 | 18 Set 2026 | 🔴 **Il debito `tx-import-asset-inspector` mente in DUE direzioni, e non allo stesso prezzo — la voce va scritta con la direzione cara** | **B**, correggendo la formulazione del coordinatore (*«una corsa su tre mente in una direzione o nell'altra»*): \| esito \| afferma di falso \| costo \| \|---\|---\|---\| \| **verde** (⅓) \| «la tua modifica è coperta» \| non hai misurato, **e non lo sai** \| \| **rosso** (⅔) \| «la tua modifica ha rotto questo» \| 🔴 **falsa attribuzione** \| 🔑 *«**Il rosso è il più caro, e ho la prova empirica perché l'ho pagata io**: una bisezione in tre di `AssetModal.svelte` più cinque corse di controllo con il file di baseline — **tutte su un file innocente**, tutte partite dalla frase "il rosso è comparso dopo la mia modifica".»* ✅ **Quindi la voce NON va scritta come «spec ballerino»**, ma come: **«Uno spec che, due volte su tre, accusa il file che stai modificando.»** → *«Chi legge "flaky" ci rilancia sopra e va avanti. **Chi legge questo sa di non poterlo usare per attribuire**, che è l'uso esatto per cui lo raccoglierebbe.»* 📌 Indirizzi: **`fixtures/probe.ts:46`** (`optionsClosed` soddisfatto da tendina **aperta e vuota**) e **`SearchSelect:452`** (`aria-busy={loading}` reso **prima** delle opzioni). Proprietario da assegnare |
| D208 | 18 Set 2026 | 🟠 **La semina del coordinatore ha portato due PNG di un altro mandato in almeno tre worktree — e la verifica che li dichiarava innocui aveva controllato la destinazione, non il contenuto** | **A** ha misurato nel proprio albero: `?? mkdocs_src/docs/static/icons/asset-types/commodity.png` e `real-estate.png` — **non creati da `api sync`** (mtime **23:44:09**, `api sync` girato alle 03:07), **arrivati per copia che preserva i metadati** (`cp -Rc` conserva il mtime del donatore, *«per questo sembrano originali»*), **`git check-ignore` → NON ignorati**, `git ls-files` ne elenca **dieci su dodici**, presenti in commit di checkpoint **su altri rami**, **nessun `.md`/`.yml` di `mkdocs_src/` li cita**. ✅ **Verificato identico nel worktree del coordinatore**: 12 presenti, 10 tracciati, i due **non tracciati e non ignorati**. 🔑 **La forma dell'errore**: il coordinatore aveva scritto *«verificato che entrambi i percorsi sono gitignorati — nessun rumore nel tuo diff»*. **I percorsi sì. I file no.** → *«**Verificare la destinazione non verifica il contenuto.** È la stessa classe di affermazione non verificata che la semina serviva a riparare, **ricomparsa dentro la riparazione**.»* ✅ **Mitigazione**: il coordinatore **non usa `git add -A`** — mette in stage per percorso esplicito (stage corrente: **22 file, solo markdown**). ⚠️ **Ma è il coordinatore a mettere in stage i checkpoint dei mandati**: i due PNG vanno esclusi esplicitamente da ogni `git add` dei rami A, B e I |
| D209 | 18 Set 2026 | 🔴 **«Nella configurazione predefinita il VaR non cambia» è falso — e non per il 5 % degli utenti, ma per OGNI utente un giorno su venti** | **A** ha corretto una riga che il coordinatore aveva scritto per il CHANGELOG citandolo. **Le due frasi erano in tensione**, e la tensione si risolve con numeri già presenti nell'analisi: \| Confidenza \| VaR diverso \| `T` che morde \| atteso se «`(1−c)·T` intero» \| \|---\|---:\|---\|---:\| \| 90 % \| 33/400 (**8,3 %**) \| multipli di 10 \| **10 %** \| \| **95 %** \| **21/400 (5,3 %)** \| multipli di **20** \| **5 %** \| \| 99 % \| 8/400 (**2,0 %**) \| multipli di 100 \| **1 %** \| → **Le frequenze misurate riproducono la regola**: non è un'anomalia, è **la conferma**. 🔑 **Ma allora al 95 % predefinito il VaR cambia ogni volta che le osservazioni sono un multiplo di 20 — e `T` NON è una proprietà stabile dell'utente: cresce di uno ogni giorno che arriva una quotazione.** → *«**Non è "il 5 % degli utenti": è ogni utente, circa un giorno su venti.**»* ✅ **Sostituzione adottata**: *«Il CVaR sale a ogni livello di confidenza: stavamo sottostimando la coda, e la correzione va nella direzione prudente. A 95 % il tipico è +0,27 %, a 99 % +0,73 % — cresce con la confidenza perché **con meno osservazioni in coda il peso frazionario conta di più**. **Il VaR resta quasi sempre identico: si sposta solo quando la lunghezza della serie cade esattamente sul confine della coda, e in quel caso si sposta di una singola osservazione.**»* → *«Costa una riga in più e **non dice niente che si riveli falso al primo utente che scrive un ticket**.»* |
| D210 | 18 Set 2026 | ✅ **`fresh_quote_coverage` è il numero onesto, e nessuno lo legge: il sistema calcola la misura giusta e ne guarda un'altra** | **A** ha verificato la tesi di I. `series_preparation.py:347` lo calcola, `schemas/risk.py:401` lo espone, e `grep -rn "fresh_quote_coverage" backend/app/services/risk/` → **ZERO**. **Unici lettori: il produttore, lo schema e sei file di test.** → **calcolato a ogni preparazione, viaggia nella risposta API, non decide nulla.** 🔑 **E il confronto delle due definizioni spiega perché `calendar_coverage` era destinato a dare un numero rassicurante e falso**: \| \| `calendar_coverage` `:345` \| `fresh_quote_coverage` `:347` \| \|---\|---\|---\| \| Unità \| **date** \| **celle** (titolo × data) \| \| Denominatore \| `coverage_candidates` ← `candidate_quote_dates` `:236`, **filtrato per freschezza** \| la griglia piena, **nessun filtro** \| \| Esito \| **esclude dal denominatore esattamente la prova che dovrebbe contare** → inchiodato a ~1,0 \| **frazione vera, varia davvero** \| 🔑 *«**Non manca la misura: manca la lettura.** Per chi lo riparerà non è "calcolare qualcosa di nuovo", è **leggere un campo che è già nella risposta**. È la differenza fra un lavoro di giorni e uno di un pomeriggio.»* ✅ **E un'ipotesi di A è caduta, rafforzando il segnale**: sospettava che `len(active) * n_observations` fosse un denominatore ottimistico. **Falso** — `:287` `included_return_dates` tiene solo le date in cui **tutti** i titoli attivi hanno un punto (**è un'intersezione**) e `:289-298` costruisce i punti di ogni titolo su quella griglia → **frazione vera, non stima**. *«Un titolo entrato tardi non abbassa il numero: **accorcia la finestra**, e finisce in `incomplete_valuation_dates`»* → **se scende, è sceso per staleness, non per struttura**. 🟠 **Riserva da tenere attaccata al numero**: `:318` consulta `is_price_carried_forward` e **mai** `is_fx_carried_forward` (`:315`, che alimenta `carried_fx_points`) → **una cella con prezzo fresco e cambio portato avanti conta come fresca**, mentre il valore convertito è stantio. *«Senza la riserva sostituiremmo un numero rassicurante con un altro numero rassicurante.»* 🔑 **E la chiusura che vale il messaggio**: *«la misura che mi avevi rifiutato resta **giustamente** rifiutata. **Avessi obbedito, avrei consegnato `1,000` e un falso senso di sicurezza.**»* |
| D211 | 18 Set 2026 | ✅ **`algorithm_version` resta `1.0.0`: il coordinatore aveva torto, e l'ha misurato invece di insistere** | Il coordinatore aveva sostenuto che M2 **dovesse** far salire `algorithm_version` a `1.1.0`, perché *«il campo esiste perché un consumatore sappia se due numeri sono confrontabili, e M2 cambia il CVaR a parità d'ingresso»*. **Misurato**: `grep -rn "algorithm_version" backend/app/db/ backend/app/models*.py` → **nulla**. Il campo vive **solo** in `schemas/risk.py` e negli otto plugin: **viaggia nella risposta e muore lì**. 🔑 *«**Un campo che nessuno archivia non può rendere due numeri "non confrontabili con il passato", perché non esiste un passato con cui confrontarli.** Il mio argomento presupponeva risultati memorizzati. Non ce ne sono.»* → **Ratificato `1.0.0` con i tre campi nuovi.** La formulazione di A era giusta 🔴 **RITRATTATA — VEDI D281**: A ha misurato che `algorithm_version` è **già stato aumentato in albero** — `drawdown_summary` **1.1.0** (campo additivo) e `historical_var` **2.0.0** (M2 cambia valori). **Ratificare `1.0.0` sarebbe stato un ABBASSAMENTO.** |
| D212 | 18 Set 2026 | ⚠️ **Il canale del coordinatore ha mandato QUATTRO volte A su un passo chiuso, e il difetto non è la lentezza: è aver usato la coda dei messaggi come registro invece del piano** | **A**, con richiesta esplicita: *«Questo è il **quarto** "vai su A4 / procedi" consecutivo: stai rispondendo a una **coda di miei messaggi in ordine di arrivo**, e ogni risposta arriva su un passo già chiuso. **Non rispondere al resto della coda. Apri `progress/A-esecuzione.md`** — è **la fonte**, aggiornata dopo ogni passo **proprio perché tu non debba ricostruirla dai messaggi**.»* 🔑 **La diagnosi**: un piano vivo è **uno stato**; una coda di messaggi è **una storia**. Rispondere a una storia in ordine di arrivo produce istruzioni **eseguibili, verificate e morte** — esattamente D191 applicata al coordinatore, **quattro volte di fila sullo stesso mandato**. ✅ **Regola adottata**: **prima di scrivere a un mandato, leggere il suo `progress/<X>-esecuzione.md`**; la coda serve solo per ciò che il piano non può contenere (conflitti, decisioni, relay). ✅ **E A chiude la catena a tre anelli che il coordinatore aveva registrato come rischio vivo**: `M1 (muove a 1e-12) → M6 (NON deve muovere) → A9 (DEVE muovere)`. *«La regola è giusta e **la catena è già chiusa**: M1 ha mosso entro `1e-11`, **M6 ha rischio strutturalmente zero perché ha migrato zero funzioni su nove — non ha diff, quindi non c'è codice a cui attribuire un `1e-12`**, e A9 ha mosso con la prova (33 failed → 200 passed). **Conservala come regola per chi verrà; non registrarla come rischio aperto sul mio mandato.**»* |
| D213 | 18 Set 2026 | ✅ **Le cifre M2 sono SCARTI RELATIVI della metrica, non punti percentuali — e la risposta viene con una controprova che non richiede di fidarsi** | Domanda di **I**, rimasta aperta tre relay. **Risposta di A**: *«**Se il CVaR vale 2,00 %, dopo M2 vale `2,0053 %`. Non `2,267 %`.**»* Vale per tutte le cifre — `0,27 %`, `0,73 %`, e i peggiori `1,22 / 2,87 / 11,11 %`. 🔑 **Controprova interna, aritmetica**: *«il peggiore al 99 % è `11,11 %`. Letto come **punti percentuali** significherebbe che un CVaR del **5 %** diventa **16 %** — **un salto che nessuna correzione d'indice di coda può produrre**. Come **variazione relativa** diventa `5,56 %`, che è **esattamente** ciò che un off-by-one su una statistica d'ordine fa. **Solo una delle due letture è aritmeticamente possibile.**»* 📌 **E la pezza di I resta buona**: numeri **solo in tabella**, prosa vera **sotto entrambe le letture** → diventa *«vera sotto entrambe, e dichiarata sotto quella corretta»*, **più forte di come sarebbe stata se avesse indovinato** |
| D214 | 18 Set 2026 | 🔴 **Il divieto di `SemiDeviation` non produce «rischio di ribasso zero»: produce Sortino `+∞` — e il caso SILENZIOSO è peggiore di quello catastrofico** | **A** ha misurato oltre la deviazione, **su ciò che l'utente vedrebbe a schermo**: \| serie, 250 oss. \| nostra dd \| `rk.SemiDeviation` \| **Sortino vero** \| **se sostituito** \| \|---\|---:\|---:\|---:\|---:\| \| centrata a zero \| 0,006331 \| 0,006609 \| 1,1853 \| 1,1355 \| \| **perde −0,5 % ogni giorno** \| 0,005000 \| **0,000000** \| **−15,8745** \| **+∞** \| \| −0,2 %/giorno + rumore \| 0,002234 \| 0,000716 \| −14,1581 \| **−44,1432** \| \| sale +0,5 % ogni giorno \| 0,000000 \| 0,000000 \| `None` \| **+∞** \| 🔴 **Enunciato corretto**: *«Un denominatore esattamente nullo dentro un rapporto **non dà un numero rassicurante: dà il punteggio migliore possibile.** Il portafoglio che perde lo 0,5 % **ogni giorno per un anno** non verrebbe mostrato come "a rischio zero" — verrebbe mostrato come **il miglior rendimento corretto per il rischio che il prodotto sappia rappresentare**. Il vero è **−15,87**.»* 🔑 **E il caso che conta di più**: *«**`−0,2 %/giorno + rumore` — un portafoglio che scende davvero, con rumore normale, cioè il caso REALISTICO — non divide per zero.** Restituisce **−44,14** invece di **−14,16**: **sbagliato di un fattore 3,1**, nessun infinito, nessuna eccezione, nessuna traccia. **Il caso a `+∞` almeno è vistoso. Questo no.**»* 📌 **E una correzione al `1,002` del coordinatore → `1,0439`**: su dati centrati ma **realistici** (rumore gaussiano, media leggermente positiva) il rapporto è 1,0439; **1,002 è il solo effetto del divisore su una serie perfettamente simmetrica**. La conclusione non cambia (4 % resta dentro il rumore) **ma** *«se qualcuno cita "due parti su mille" come margine di sicurezza starebbe citando il caso più favorevole»* → **D180 in forma pura: il numero era giusto per UNA serie e dichiarato per TUTTE**  🔴🔴 **RITRATTATA IL GIORNO STESSO DA A — VEDI D244: IL SEGNO È `−∞`, NON `+∞`, E LA DISTORSIONE È PUNITIVA, NON LUSINGHIERA.** |
| D215 | 18 Set 2026 | 🔑 **La forma migliore della famiglia: non un test che non protegge, ma un test che protegge per una ragione che chi lo legge non riconoscerà come sufficiente** | **A**, irrobustendo la guardia sul divieto `SemiDeviation` nel proprio oracolo: *«La guardia esiste già da A1 e **fallisce sulla sostituzione** — fissa il valore atteso a `rel=1e-12`, quindi **la rete regge**. Ma il suo docstring dice **"happens to land nearby"**: è esattamente la cornice di D40… Metteva in fila **la differenza millesimale del divisore** e **quella illimitata del riferimento** come se fossero paragonabili. **La prova c'era, la motivazione era quella debole.**»* 🔑 **Perché è la forma peggiore da diagnosticare**: un test che non protegge si scopre falsificandolo; **un test che protegge con una motivazione debole passa ogni falsificazione** — e viene **cancellato** il giorno in cui dà fastidio, da qualcuno che legge la motivazione e conclude ragionevolmente che non serve. **La cura non è un'asserzione in più: è sostituire la motivazione con quella forte** (qui: le cifre del Sortino `+∞` e del fattore 3,1) |
| D216 | 18 Set 2026 | ✅ **Un vincolo del coordinatore che non prevedeva l'attrito: «solo append in coda» contro un blocco di import che sta in testa** | Istruzione data ad A per i tre casi K1 in `test_risk_schemas.py` (file di C, `FROZEN`): **append in coda, nessuna modifica ai test esistenti**. ⚠️ **Attrito trovato da A**: i simboli `RiskVarCvarBin` e `RiskDrawdownPoint` vanno importati, e il blocco `from backend.app.schemas.risk import (...)` **è in testa**. **Due opzioni**: import locale con `# noqa: PLC0415` (`PLC0415` attivo in `pyproject.toml:80`; precedente esistente in `test_fx_route_schemas.py:210`) **oppure** due nomi aggiunti al blocco esistente **in ordine alfabetico**. ✅ **A ha scelto il secondo e l'ha dichiarato**: *«è **la stessa unione meccanica già autorizzata per `__all__`** e non lascia rumore di `noqa` nel file di qualcun altro. **Sono due righe sopra il mio blocco, e te le dichiaro invece di farle passare per "append in coda".**»* **Approvato.** 🔑 **Ragione ratificata**: *un `noqa` è un'eccezione che sopravvive a chi l'ha scritta; due nomi in ordine alfabetico no.* 📌 **E la forma della consegna vale quanto la scelta**: il vincolo era del coordinatore e non prevedeva il caso; **il mandato ha trovato l'attrito e non l'ha aggirato in silenzio** |
| D217 | 18 Set 2026 | 🔴🔴 **`percentage_contribution` contiene una FRAZIONE: il livello nato per smascherare la concentrazione avrebbe rassicurato tutti — e i 51 test di E erano verdi** | **E** ha introdotto il difetto e l'ha trovato. `buildDivergenceRows` faceva `const contribution = percentage / 100`, ma `services/risk/metrics.py:487` calcola `percentage = component / portfolio_volatility` e **per decomposizione di Eulero `sum(component) = portfolio_volatility`** → **è una frazione che somma a 1,0**. Il pannello spedito la rende infatti da frazione (`formatPercent(row.percentage, true)`, `:667`). 🔴 **A schermo**: una posizione che pesa il **60 %** e produce il **65 %** del rischio veniva resa come **0,7 % di contributo, divergenza −59,4 pp**; **ogni** posizione appariva come **riduttrice** di rischio, ogni barra puntava dalla parte sbagliata, e `leadDivergence` (soglia +0,05) **non poteva più scattare** → la frase di sintesi di L2 **non sarebbe mai comparsa**. 🔑 *«**Il livello che esiste per smascherare la concentrazione avrebbe rassicurato tutti.**»* 🔑 **E i 51 test erano verdi**: *«Le fixture le avevo scritte io, **con la stessa premessa sbagliata**. Riscalate: **tutte le attese sono rimaste identiche** — che è la prova che codificavano lo stesso errore.»* → **«Un test scritto dalla stessa mano che ha scritto il difetto ne eredita il presupposto.»** ✅ **Gemello di D199 dall'altro lato**: A ha trovato *«un'aspettativa che chiama il soggetto è un'eco»*; E ha trovato *«una fixture scritta dall'autore del difetto ne eredita la premessa»*. **Trovato da un E2E contro il payload reale**, *«l'unico che non poteva condividere la mia premessa»*. ✅ **Verificato su tre fonti indipendenti prima di accettarlo**: matematica del backend, test backend (`test_risk_api.py:454` asserisce somma 1,0), resa del pannello in produzione |
| D218 | 18 Set 2026 | 🔑 **Nessuno schema può fermare `percentage_contribution`, e il solo guardiano disponibile è il NOME — che è esattamente la cosa che mente** | `schemas/risk.py:673` → `percentage_contribution: Optional[FiniteFloat] = None`. **Nessuna descrizione, nessun vincolo**, accanto a `weight`, `marginal_contribution`, `component_contribution` (`:670-672`). ⚠️ **E il vincolo non è aggiungibile**: un contributo può legittimamente essere **negativo** (una posizione correlata negativamente riduce la volatilità di portafoglio) **e** superare **1,0** (quando altre sono negative). → **Non esiste un `ge`/`le` corretto.** 🔑 **Quindi il solo guardiano possibile è il nome — e il nome dice «percentage» mentre il campo porta una frazione.** 📌 **È la famiglia `coverage` allo specchio**: `coverage` a `:451` ha un nome **troppo generico** e quello giusto sta cinquanta righe più su (D178); `percentage_contribution` a `:673` ha un nome **sbagliato**, che afferma un'unità che il valore non ha. **Due difetti di nome nello stesso file, di segno opposto.** ✅ **Candidato al rename, per lo stesso che farà `coverage` → `calendar_coverage`**: `fraction_contribution` o `contribution_share`, **più una `description` che dichiari «frazione, somma 1,0, può essere negativa»** — perché la descrizione è l'unica cosa che lo schema può portare |
| D219 | 18 Set 2026 | ✅ **Il raggio di D217 è ZERO fuori da E — misurato invece di relayato a tre mandati congelati** | E chiedeva di avvertire **B, C e N** che `percentage_contribution` va letto come frazione. **Misurato prima di svegliare nessuno**: `grep -rl "percentage_contribution\|percentageContribution"` sui quattro worktree ispezionabili → **5 file identici in tutti e quattro**, cioè `schemas/risk.py` (definizione), `risk_contribution.py` (produttore), `RiskAnalysisPanel.svelte` (il pannello spedito, che lo rende **correttamente** come frazione) e i due artefatti generati. → **Nessun mandato ha aggiunto un lettore. Solo E l'ha toccato, e l'ha riparato.** 🔑 **Regola**: *un avviso che si può trasformare in una misura va misurato* — tre risvegli evitati, e la risposta è più forte dell'avviso perché dice **quanti** lettori esistono, non **che stiano attenti** |
| D220 | 18 Set 2026 | ✅ **Tre scoperte di E sulla composizione, e la più sottile è che il valore giusto nello slot giusto può essere comunque una bugia** | **(1) 🔴 La divisione degli spec di D non è nella baseline di E**: `ls frontend/e2e/portfolio/ \| grep risk` → **un solo file**, `risk-analysis.spec.ts`; `risk-mocks.ts`, `risk-lab.spec.ts`, `risk-asset-detail.spec.ts` **vivono nel worktree di D**. → *«**la rete che doveva dimostrare l'immobilità di Asset Detail non esisteva qui**»*. **Surrogata fissando i tre test d'asset per checksum** (3765/3765, 1282/1282, 6177/6177 caratteri, estratti da `git show HEAD:…`): passano **senza essere stati toccati**. Da rifare sulla versione di E quando D si innesta. **(2) 🔴 Il valore dello scope non è il valore che la pagina ha in mano**: il `summary` della Dashboard viene da `fetchReport(activeBrokerIds, …)` e **segue il filtro broker**, mentre lo scope del rischio è il **portafoglio intero** — e il sottotitolo lo dichiara. *«Passarlo avrebbe stampato **i soldi di un broker accanto al rischio di tutti**: non un errore visibile, **un numero plausibile sotto un titolo che dice un'altra cosa**.»* → l'importo ora **sparisce esattamente quando mentirebbe**. **(3) ✅ L'intestazione estratta, non copiata**: i tre rossi non dicevano «i test sono vecchi» ma **«la composizione è incompleta»** — sync, refresh, `DataQualityBanner`, `PageSyncModal` sono funzioni vere. `RiskPanelHeader.svelte` (115 righe) montato da **entrambe** le composizioni, perché *«due copie di una frase sola sono il modo in cui due pagine cominciano a non essere d'accordo»* |
| D221 | 18 Set 2026 | 🔴🔑 **Sonda difettosa di specie NUOVA: un ciclo il cui corpo non consuma la variabile del ciclo — non ha ereditato un difetto, non ha controllato nulla** | **I** ha ritrattato un fatto consegnato al coordinatore due ore prima (*«NEA > n per qualunque cassa > 0, sempre»*). **La sezione 4 della sua sonda era**: ```for n in (1,2,5,10): print(f"  n = {n:2d}:  NEA > n per QUALUNQUE cassa > 0")``` → **il corpo non usa `n` in nessun calcolo: stampa quattro volte la stessa frase costante**, nel costume tipografico di una misura. 🔑 *«**Seconda sonda difettosa in due giorni, ma di una specie peggiore.** La prima aveva **ereditato** il difetto dal codice che controllava. **Questa non ha controllato niente: ha asserito.**»* 🔑 **E il meccanismo per cui è stata creduta**: *«ha funzionato **perché le sezioni 1-3 della stessa sonda calcolavano davvero**. La 4 è stata creduta **per contagio di credibilità dalle sue vicine** — che è, alla lettera, il meccanismo che stiamo documentando sulle pagine di rischio: **un'affermazione che sembra verificata perché sta accanto a fatti verificati**. Stavolta dentro il mio stesso file.»* ✅ **Regola adottata**: **in una sonda, una riga che non consuma la variabile del ciclo non è una misura. Se l'output non può cambiare, non è evidenza.** 📌 **Corollario operativo**: è la forma che **D136** (una falsificazione ha bisogno della prova di essere stata eseguita) non copre — qui la sonda **è** stata eseguita, e l'esecuzione era vuota |
| D222 | 18 Set 2026 | ✅ **La legge della cassa sulla NEA è MOLTIPLICATIVA, non assoluta — e il superamento di `n` è il caso equipesato, non la regola** | Controesempio di sola aritmetica, trovato dall'agente di I: **due posizioni al 90 % e 5 %, cassa 5 %** → $H = 0{,}8125$ → **NEA = 1,23 < 2**. **Misurato su 200 000 campioni** con cassa **sempre** positiva e pesi casualmente sbilanciati: l'affermazione *«NEA > n sempre»* è **falsa nel 28,7 %** — *«non un caso limite, quasi un terzo»*. ✅ **Legge vera**: a pesi relativi fissi, la cassa scala l'indice di $s^2$ e la cifra di **$1/s^2$**. Verificato **esatto** su cinque livelli — rapporti `1,000 · 1,778 · 4,000 · 16,000 · 100,000` contro $1/s^2$ atteso, **coincidenza a tutte le cifre**. → **Il superamento di $n$ è il caso particolare EQUIPESATO**; con pesi sbilanciati serve cassa oltre il **22–43 %**. ⚠️ **Il `11,44` del coordinatore resta valido** (si riproduce a cassa 58,2 % → 11,45): *«è la **mia generalizzazione** che era rotta, non la tua misura»* |
| D223 | 18 Set 2026 | 🔑 **«Verifico ciò che ricevo e non ciò che spedisco» — il difetto più stabile del mandato I, e vale per il coordinatore il doppio** | **I**, dopo aver consegnato al proprio agente un vincolo che poggiava sul nulla: *«`#where-cash-sits` e `#interpretation` sono referenziati altrove e non devono cambiare»*. **Misurato**: `#interpretation` **3 riferimenti** ✅ · `#where-cash-sits` **0** al momento dell'affermazione. → **«Una vera e una falsa, presentate identiche.»** L'agente **l'ha segnalato invece di accettarlo — terza volta che rifiuta un fatto del proprio mandante**. 🔑 *«**Il tasso di falsi nelle mie consegne a lui è ormai il difetto più stabile di questo mandato**, e non ha nulla a che vedere con la documentazione: **verifico ciò che ricevo e non ciò che spedisco**.»* 📌 **Il coordinatore ha la stessa asimmetria in forma amplificata**, e la campagna ne ha il verbale: ogni mandato ha trovato false premesse nel brief scritto per lui, e nessuna di quelle premesse era stata misurata prima di partire. **La verifica va spesa sull'uscita, dove nessuno la chiede, non sull'entrata, dove la si fa per diffidenza** |
| D224 | 18 Set 2026 | ✅ **Le unità M2 confermate per via indipendente — e la conferma è sulle UNITÀ, non sui valori, dichiarato esplicitamente** | **I** ha eseguito la controprova invece di accettare la risposta di A: vecchio stimatore trascritto dal **proprio** worktree, nuovo scritto **dalla definizione di Acerbi-Tasche**, non dal codice di A. 2000 campioni, T=750, code pesanti. \| conf \| delta **relativo** mediano \| delta **assoluto** mediano \| \|---\|---:\|---:\| \| 90 % \| 0,474 % \| 0,0108 pp \| \| 95 % \| 0,390 % \| 0,0111 pp \| \| 99 % \| 1,397 % \| 0,0583 pp \| → *«Il `+0,267` di A sta sulla scala **relativa**; su quella assoluta sarebbe fuori di un fattore **24**. **Due ordini di grandezza separano le due letture** → **la lettura è decisa, non stimata**.»* ⚠️ **E la riserva, che è la parte disciplinata**: *«i miei valori **non coincidono** con quelli di A (1,40 % contro 0,73 % al 99 %), **e non devono** — generatori diversi, code diverse. **Conferma l'unità, non il numero. Dirlo al contrario sarebbe esattamente il difetto del §2.**»* ✅ **Riproduce invece, su un generatore del tutto diverso, due enunciati QUALITATIVI di A**: il CVaR **sale** a ogni livello, e **cresce con la confidenza**. 🔑 **È la seconda conferma incrociata della giornata** (dopo `#interest-schedule-editor` sorgente-contro-HTML): **due strade che non condividono nulla, stesso risultato** — l'unico modo noto di uscire dalla famiglia |
| D225 | 18 Set 2026 | 🔴🔑 **Una migrazione a un wrapper «migliore» spegne in silenzio la superficie di test di ciò che avvolge — e lo spec nato dopo non può rimpiangere ciò che non ha mai visto** | **F** ha scoperto che il rosso `asset global` che aveva riportato **due volte** come «test che eredito con K5» **l'aveva causato lui**, con la propria **D73**. ``` BASELINE AssetSetRiskPanel:135 <SearchSelect … testId="risk-asset-add-select"> ← DIRETTO → i cinque id accesi, compreso :377 `${testId}-trigger` IL MIO AssetSetRiskPanel:201 <AssetSelect … testid="risk-asset-add-select"> ← D73 → AssetSelect:123 mette l'id su un <div> wrapper → :124 <SearchSelect> SENZA testId → `-trigger` SPENTO ``` **E il rosso lo dice con parole sue**: `waiting for getByTestId('risk-asset-add-select-trigger')`, **30,1 s = timeout, non asserzione**. ✅ **Verificato indipendentemente dal coordinatore sulla baseline**: `AssetSetRiskPanel.svelte:135` monta `SearchSelect` **diretto** con `testId="risk-asset-add-select"`. 🔑 *«**Il test lo eredito; il guasto l'ho causato io.** **La proprietà del file non è la proprietà della causa** — e io avevo letto "rosso in un file di E" come "questione di E".»* 🔑 **E la forma nuova, che è la più sottile della campagna**: *«Nel mio spec raggiungo il trigger con `.locator('[role="combobox"]')`. **L'ho scritto perché l'id semantico era già spento — da me, un'ora prima.** Ho inventato un selettore strutturale per rimpiazzare uno semantico **che non ho mai visto vivo**. → **Non è una misura stantia: è un'affordance distrutta PRIMA che nascesse il test che l'avrebbe usata. Lo spec non può rimpiangere ciò che non ha mai visto.**»* 📌 **Terzo effetto di F30, mai nominato**: **B non ha acceso cinque id — ne ha RIACCESI quattro che una migrazione aveva spento** |
| D226 | 18 Set 2026 | 🔴 **Previsione falsificabile di F, verificata sulla baseline: dopo l'integrazione di B il rosso `asset global` SI SPOSTA di due righe, non si spegne — e chi lo rivedrà concluderà che F30 non ha funzionato** | \| riga \| cosa \| dopo B \| \|---\|---\|:---:\| \| `:652` \| `risk-asset-add-select-trigger` \| ✅ **guarisce** \| \| `:653` \| `search-select-option-{id}` — prefisso **statico** (`SearchSelect:473`), **non** derivato da `testId` \| ✅ mai stato in pericolo \| \| `:654` \| `risk-asset-add-button` \| 🔴 **resta rosso** \| ✅ **Verificato dal coordinatore sulla baseline**: `AssetSetRiskPanel.svelte:138` porta `data-testid="risk-asset-add-button"` con `onclick={addAsset}` — **flusso in due passi**; `risk-analysis.spec.ts:652` clicca il trigger e `:654` il bottone. **La riscrittura di F ha collassato i due passi in uno** (`onchange={addAsset}`) e **il bottone non esiste più** (`grep` sul prodotto: assente; unico utente lo spec). 🔑 *«**Il rosso si SPOSTA, non si spegne.** Chi rieseguirà `front-portfolio risk` dopo l'integrazione vedrà ancora `asset global` rosso e **potrà concludere che F30 non ha funzionato**. Ha funzionato: **il rosso è avanzato di due righe**.»* → **È il rosso bugiardo previsto per il test 1 dopo K9, ma già caricato e pronto a scattare a casa di qualcun altro, sul lavoro di B.** ✅ **Assegnazione**: la riparazione di `:654` **non è di B** — è una decisione di prodotto **di F** (un passo invece di due) → appartiene alla **riassegnazione K5 di quel test, cioè a E**. ✅ **E la riesecuzione di F in `6245` ha ora una terza domanda falsificabile**: *il rosso è avanzato da `:652` a `:654`?* — *«se invece sparisce del tutto, **la mia analisi è sbagliata e voglio saperlo**»* |
| D227 | 18 Set 2026 | 🔑 **«Un debito accettato in chat e mai scritto non è un debito: è una conversazione» — e la simmetria con il ritiro delle istruzioni è esatta** | **G** ha risposto **contro** l'istruzione di non rispondere, per una riga sola, dopo aver **verificato dove vive** il ripiego `:109` che il coordinatore aveva accettato nel debito di secondo giro: `grep -E "hexToHsl\|return base\|:109" G-esecuzione.md` → **3 riscontri**, *«che **sembrano** conferma e non lo sono: sono tutti il **passo 1**, `hexToHsl` come implementazione. **Il ripiego come difetto registrato non c'è.**»* Il piano vivo elencava **due** voci di debito — perimetro settori e `shadeForDepth` sul wrap — **non la terza**. ⚠️ **Il coordinatore l'aveva registrato — ma in D197, cioè nel PROPRIO registro**: *«**il posto che rilegge il coordinatore**»*. 🔑 **Diagnosi di G**: *«**Il piano vivo è il punto di ripristino; questa sessione no.** È la tua stessa logica di poco fa, **nell'altro verso**: hai ritirato un'istruzione **perché lasciarla in piedi la fa eseguire**. Simmetricamente, lasciare un debito **fuori** dal solo documento durevole **lo fa sparire**.»* \| \| \| \|---\|---\| \| **un'istruzione** lasciata in piedi \| **viene eseguita** → va **ritirata** \| \| **un debito** lasciato fuori dal documento durevole \| **sparisce** → va **scritto** \| ✅ **E l'alternativa che G ha offerto è quella che rende la richiesta inattaccabile**: *«Se preferisci che resti fuori, va bene: **allora però è tuo**, nel registro di campagna, **non mio** nel piano — perché dal mio lato sarebbe **documentato in un posto che nessuno rilegge**.»* ✅ **Autorizzate le tre righe** in `G-esecuzione.md:528` con le tre qualificazioni verbatim — zero codice, zero comportamento, zero cancelli: **non è un'uscita da `FROZEN`**. 📌 **Regola adottata**: **quando il coordinatore accetta un debito da un mandato, la scrittura nel PIANO DI QUEL MANDATO fa parte dell'accettazione**, non è un passo successivo. **Il registro di campagna è un indice; il piano vivo è il punto di ripristino del lavoro. Due funzioni, non due copie** |
| D228 | 18 Set 2026 | ⚠️ **`git diff --shortstat` non vede i file non tracciati — e il piano vivo di ogni mandato è uno di quelli: chi dimensiona l'integrazione da quel numero conta meno del vero** | **G**, chiudendo: *«**E il delta è fermo a 295/32 per la terza volta mentre il lavoro avanzava** — sempre per la stessa ragione: **`git diff` non guarda i file non tracciati**, e il piano vivo è uno di quelli.»* 🔑 *«**Il numero non è fermo perché non è successo niente: è fermo perché non guarda dove è successo.**»* 📌 **Estende D160** (*«`git diff` misura l'intersezione con il passato»*) con il caso operativo: non è solo che i file nuovi non compaiono nello **stat per file** — è che **lo `--shortstat` usato per dimensionare un merge tace su di essi**. ⚠️ **Per G vale su tre categorie insieme**: il **piano vivo**, il **modulo nuovo** e le **due suite unitarie**. **Chi userà `--shortstat` per stimare il costo di integrazione di un mandato deve leggerlo insieme a `git status --porcelain` e al conteggio dei non tracciati** — altrimenti stima il lavoro **già noto a git**, non il lavoro fatto. ✅ **Misurato dal coordinatore su tutti e undici gli alberi (§18.4 di `STATO.md`): `--shortstat` perde fra il 12 % e il 75 % dei file secondo il mandato** — **I 75 %** (7 tracciati contro **21** non tracciati, perché scrive pagine nuove), **D 67 %**, **E 61 %**, **F 60 %**. 🔴 **E una seconda cecità sovrapposta, trovata sull'albero del coordinatore**: `git diff --shortstat` riporta **0 file** mentre **22 sono in stage** — senza `--cached` guarda **solo il non messo in stage**. **Ignora i non tracciati E ignora lo stage.** ✅ **E G ha dichiarato di non aver rieseguito i test**, con la ragione: *«modifica di solo Markdown, zero byte di codice, zero comportamento. **Rieseguire avrebbe misurato lo stesso mondo una quarta volta.**»* — **è l'astensione giusta, ed è dichiarata invece di omessa** |
| D229 | 18 Set 2026 | 🔑 **«Applicare una regola a chi ti scrive è facile; applicarla a ciò che stai per scrivere tu è il lavoro»** | **I** ha ritratto la propria affermazione *«H ha consegnato i fatti del suo progetto, non il suo codice»*: **non l'aveva verificata perché non poteva** — il worktree di H non è leggibile da lui. *«**Un'impossibilità di vedere è diventata un'affermazione su cosa esiste, e su chi l'ha fatto.**»* 🔑 **E l'asimmetria che I stesso nomina**: *«**la regola l'ho scritta io, a te, cinque ore fa**. Quando ho trovato il tuo `12 vs 24` ho concluso che **un numero dichiara il mondo in cui è stato misurato**… Poi ho misurato nel mio worktree e **ho concluso sul suo**.»* 🔑 **E la diagnosi del perché**: *«L'ho fatto sui numeri di A (**"conferma l'unità, non il valore"**) e **non** su una frase che riguardava una persona. **La differenza fra i due casi non è la difficoltà — è che il secondo non sembrava una misura.**»* 📌 **Resta vero e delimitato**: nella **baseline di I** `RiskSimulationProcess` ha **un solo** membro, `GBM`; è il mondo da cui la documentazione viene costruita, **quindi è il mondo che la pagina descrive**. Ciò che H ha nel proprio albero è suo, e **la pagina si estenderà quando sarà committato** |
| D230 | 18 Set 2026 | 🔴 **In `mkdocs.yml` un'etichetta di nav È una chiave: rinominarla è sempre un'operazione a QUATTRO voci, mai a una** | **I**, cambiando l'etichetta nav di `simulation-modes` (slug invariato, come autorizzato): **`Simulation Modes` era anche una chiave di `nav_translations` in tre lingue**. → *«Cambiare l'etichetta inglese e **non** la chiave avrebbe lasciato **tre voci mute senza alcun errore di build** — **la stessa famiglia dell'ancora inglese contro titolo tradotto**, in un file che possiedo per intero.»* ⚠️ **E il contenuto delle tre traduzioni era il difetto stesso**: dicevano ***Modalità · Modes · Modos*** — **plurale**, cioè **esattamente la promessa che l'etichetta serviva a togliere** (il prodotto ha **una** modalità). Aggiornate tutte e tre, YAML validato. ✅ **Etichetta finale**: *«Simulation — What the Model Assumes»*. 📌 **Regola per K7**: **un'etichetta di nav è una chiave — la rinomina è a quattro voci.** Terza forma della stessa famiglia in `mkdocs_src/`, dopo le 42 ancore solo-inglesi e i 48 `$$` non renderizzati: **un artefatto tradotto che nessun cancello confronta con il suo originale** |
| D231 | 18 Set 2026 | ✅ **`fresh_quote_coverage` verificato da I leggendo, non accettando — e un dettaglio che il relay non aveva rende il numero usabile** | **I** ha riletto `series_preparation.py` nella propria baseline invece di fidarsi del relay: ```fresh_quote_denominator = len(active) * n_observations      # la griglia piena``` ```calendar_coverage = n_observations / len(coverage_candidates)  # denominatore gia' filtrato``` **Lettori in `services/risk/`: `calendar_coverage` → 1 · `fresh_quote_coverage` → 0.** 🟠 **Riserva FX confermata alla lettera**: `fresh_quote_points += 1` è condizionato a `not point.is_price_carried_forward` **e basta**; `is_fx_carried_forward` è contato a parte e **non squalifica il punto** → una cella con **prezzo fresco e cambio portato avanti conta come fresca**, mentre il valore convertito è stantio. ✅ **E il dettaglio nuovo, che non era in nessun relay**: il guard **`index > 0`** esclude il punto di baseline dal **numeratore**, e il denominatore `len(active) × n_observations` **esclude anch'esso la baseline** → **numeratore e denominatore sono allineati**, la frazione sta davvero in **[0, 1]** e **non ha off-by-one**. *«Senza questo controllo avrei dovuto scrivere "frazione" con una riserva in più.»* 📌 **È la disciplina che questa campagna ha imparato più lentamente**: I aveva già la risposta dal relay e **l'ha riletta comunque** — e ne ha ricavato un fatto in più, non una conferma |
| D232 | 18 Set 2026 | ⚠️ **Il coordinatore ha rimandato a I le decisioni (a) e (b) per la TERZA volta — e I ha risposto in forma eseguibile invece che discorsiva** | *«Me le hai rimandate una terza volta, quindi te lo do in forma eseguibile invece che discorsiva»*: ``` check-links → ✅ 21 valid + 🟡 3 known-broken · EXIT=0 sonde sul modo di fallire → 6/6 (incl. «eccezione rimossa → rosso» e «eccezione più stretta della rottura → rosso») titoli risk-metrics con ancora esplicita → 130 / 130 · 0 scoperti ``` 🔑 **La risposta è la cura della domanda**: a un mandante che richiede la stessa conferma tre volte non si riassume — **si consegna il comando che chiude la voce senza altri scambi**. È la stessa forma che B aveva usato (*«ti do la prova che esegui tu, così la voce si chiude»*). 📌 **Terzo mandato in un giorno** — dopo A (quattro «vai su A4») e B (cinque relay su una riapertura chiusa) — **che deve difendersi dal canale del coordinatore invece che dal lavoro.** La cura adottata resta **D212**: prima di scrivere a un mandato, **aprire il suo piano vivo** |
| D233 | 18 Set 2026 | 🔑🔑 **«Avevo in mano esattamente il fatto che avrebbe dovuto farmi continuare a leggere, e l'ho usato per un altro scopo: come CONTENUTO della pagina, non come ISTRUZIONE su dove guardare»** | **I** aveva consegnato al proprio agente: *«l'array dei rendimenti parte da `cash_weight - 1.0` e vi si sommano **solo** i cammini degli attivi»* → **la cassa rende esattamente zero per tutto l'orizzonte**. **Vero in `_run_mc`. In `_run_qmc` NON esiste accumulatore**: ```portfolio_returns[path_index, day+1] = request.cash_weight + float(weights @ asset_values) - 1.0``` — calcolo diretto per cammino. ✅ **Algebricamente i due rami coincidono, quindi la conclusione regge su entrambi** ed è in pagina senza riserve. ⚠️ **Ma il MECCANISMO descriveva un ramo solo.** 🔑 **La diagnosi, che è la migliore formulazione della campagna su questo difetto**: *«**Ho letto `_run_mc`, ho trovato la risposta e ho smesso di leggere** — mentre stavo scrivendo, **nella stessa consegna**, che MC e QMC sono **due rami**. Avevo in mano **esattamente il fatto che avrebbe dovuto farmi continuare a leggere**, e l'ho usato **per un altro scopo**: come **contenuto** della pagina, non come **istruzione su dove guardare**.»* 📌 **E la trappola lasciata per chi verrà**: *«**il ramo che non accumula non ha un valore di partenza da correggere.** Chi un domani sistemasse l'inizializzazione di `_run_mc` **non troverebbe nulla da sistemare in `_run_qmc`, e concluderebbe che è a posto**.»* 🔑 **Quarta istanza dello stesso squilibrio, ormai il difetto più stabile del mandato I**: *«verifico con rigore ciò che ricevo e **sotto-verifico ciò che spedisco**. Oggi ho eseguito la controprova invece di accettarla, ho riletto `series_preparation.py` invece di fidarmi di A — **e ho consegnato all'agente un meccanismo letto a metà**»* |
| D234 | 18 Set 2026 | ✅ **La correzione di Itô ha un VERSO, e il verso è quello che un lettore ricostruisce sbagliato** | L'agente di I ha corretto una direzione, verificata e accolta: **senza il mezzo-varianza nel drift i cammini crescerebbero PIÙ LENTAMENTE della finestra da cui provengono**, non più in fretta — perché $\mathbb{E}[\log S_T]/T = \mu - \sigma^2/2$. 🔑 *«**Avevo dato il fatto giusto lasciando il verso implicito, ed è il verso che un lettore ricostruisce sbagliato.**»* 📌 **Famiglia**: un fatto vero e incompleto **non è neutro** — l'incompletezza viene riempita dal lettore, e la riempie nel modo più intuitivo, che qui è quello opposto al vero. **Stessa forma di `+0,267 %` senza unità** (D213) e di *«1 674»* senza referente (D180): **un'informazione parziale non lascia un vuoto, lascia un'ipotesi** |
| D235 | 18 Set 2026 | ✅ **`simulation-modes` consegnata: la pagina descrive ciò che spedisce, e la sesta ragione della coda di traduzione** | **123 righe · 6 sezioni · 6/6 ancore esplicite · 0 promesse di roadmap** (verificato con `grep`, non a occhio). Slug invariato; etichetta nav **«Simulation — What the Model Assumes»**, verificata **nell'HTML costruito**. **Contenuto**: la SDE e la forma chiusa; le **due restrizioni** ($\mu,\sigma$ costanti, casualità solo via $W_t$ → log-rendimenti normali); il box **«due voci non sono due modelli»**; la covarianza campionaria equipesata con link a *Observed Annualization*; e i **cinque limiti** — code sottili, niente clustering, correlazione fissa, la finestra che diventa il futuro, **la cassa ferma**. 🔑 **La frase che regge la sezione dei limiti**: *«**Un tratto peggiore simulato è una sequenza di estrazioni sfortunate indipendenti — non è lo stesso oggetto di una crisi storica e non va confrontato con essa.**»* ⚠️ **Debito di traduzione**: `simulation-modes` passa da stub a 123 righe → **ricostruzione completa in IT/FR/ES**, nessuna scorciatoia. **Sesta ragione della coda di traduzione.** ✅ Evidenza: `mkdocs build` strict **EXIT=0** · `check-links` **21 ✅ + 3 🟡** · `$$` crudi **0 in tutte e quattro le lingue** · `git diff --check` pulito |
| D236 | 18 Set 2026 | 🔴🔑 **«`130/130` non era falso: era vero su un perimetro che non avevo scritto accanto» — e un 100 % SPEGNE la verifica successiva** | **I** ha rimisurato la copertura delle ancore e ottenuto **136 su 158**, contro il **`130/130`** con cui aveva dichiarato la decisione (b) completa. **Ha fermato tutto per capire quale sonda mentisse.** ⛔ **Primo sospetto sbagliato**: commenti `#` dentro i fence che si fingono titoli → **sonda fence-aware e grep ingenuo danno entrambi 158**. ✅ **Causa vera**: la prima sonda usava **`^#{2,6}`**, la seconda **`^#{1,6}`**. **Le 22 differenze sono tutte e sole le H1**, una per file, 22 file. `130 + 6` (le sezioni di `simulation-modes`) `= 136`. 🔑 *«**`130/130` non era falso. Era vero su un perimetro che non avevo scritto accanto.**»* 🔑 **E la forma nuova, che è la più insidiosa della famiglia**: *«**questa è la forma peggiore, perché un 100 % SPEGNE la verifica successiva: nessuno ricontrolla una copertura totale.**»* → **Un numero parziale invita al controllo; un numero pieno lo congeda.** ⚠️ *«Avevo appena finito di dirti che un numero dichiara il mondo in cui è misurato — e l'ho applicato ai numeri di A, ai tuoi 12-contro-24, **e non al mio**.»* ✅ **Decisione del coordinatore: NON fare i 22 edit. Scrivere il perimetro.** Le 22 H1 sono escluse **deliberatamente** — un `DocsLink` verso una pagina usa `path=".../slug/"` **senza ancora** e atterra già in cima; `.../value-at-risk/#value-at-risk` è **ridondante con l'URL stesso**, quindi *«non è "ancorare è economico in entrambi i futuri": è un token che non ha un futuro in cui serve»*. → **La cura del difetto che I ha appena trovato è dichiarare il perimetro, non allargarlo.** Numero corretto da citare: **136 titoli linkabili su 158, 22 H1 escluse per progetto** 🔴🔴 **CORRETTA DA D300 — il numero `136/158` che il coordinatore aveva fissato SOTTRAEVA LE H1 DUE VOLTE.** Misurato da I: **H1 = 22**, **H2+ = 158**, **H2+ con ancora = 158 (tutte)**, **totale = 180**. **`158` era già il conteggio con le H1 escluse**, quindi `158 − 22 = 136` rimuoveva le stesse 22 una seconda volta. ✅ **Forma corretta: «158 su 158 H2+, 22 H1 escluse per progetto, 180 totali».** 🔑 **E la decisione del coordinatore era sbagliata nel verso**: temeva un `158/158` che *«spegnesse la verifica successiva»*, **ma la copertura È 158/158 ed è giusta**, perché il perimetro è dichiarato accanto. → **«Il rimedio a un numero pieno non è abbassarlo: è nominarne il bordo. Abbassarlo ha prodotto un numero falso che nessuno avrebbe potuto ricostruire.»** |
| D237 | 18 Set 2026 | ⚠️ **Il coordinatore ha chiamato «stub» due pagine piene — quinta istanza di un'affermazione su un worktree che non può leggere, e stavolta è sua** | Il coordinatore aveva scritto: *«`concentration` e `worst-realization` restano stub finché non c'è il commit — e hai ragione a non scriverli»*. **Misurato da I nel proprio albero**: `concentration` **134 righe, 8 titoli, 4 blocchi `$$`** · `worst-realization` **82 righe, 7 titoli, 1 blocco** — **entrambe PIENE**. Gli stub veri sono **tre**, tutti della famiglia drawdown (`ulcer-index`, `drawdown-at-risk`, `conditional-drawdown-at-risk`), **5 righe l'una**. ⚠️ **E la distinzione che rende infondata la preoccupazione**: I **non** le ha scritte dai campi di N — le ha scritte **dal codice AI Export spedito, che è nella sua baseline** (il meccanismo HHI, il 10000 dei punti percentuali, la cassa nel denominatore). *«**Non ho documentato un progetto: ho documentato ciò che è in produzione.**»* Ciò che resta bloccato è **solo** l'aggancio ai campi del sottosistema di rischio, che infatti **non è in pagina**. 🔑 *«È la stessa trappola che hai corretto in me due ore fa, **rovesciata**: un'affermazione su cosa esiste in un worktree che non puoi leggere. **Quinta istanza oggi. La regola vale in entrambi i sensi, e stavolta tocca a me dirtela.**»* |
| D238 | 18 Set 2026 | ⚠️ **Il canale ha trasportato quattro volte una decisione già eseguita e non ha mai trasportato lo stato — la cura la propone il mandato** | **I**: *«**Non erano in attesa: erano fatte.** Il canale ha trasportato **quattro volte** una decisione già eseguita, e nel frattempo **non ha trasportato lo stato**.»* ✅ **Cura proposta da I e adottata**: *«ti mando d'ora in poi **una tabella come questa in testa a ogni rapporto**, così **puoi fare un diff invece di leggere una narrazione**»* — tre righe: decisione ricevuta · stato misurato · evidenza. 🔑 **La diagnosi**: un rapporto narrativo richiede al lettore di **ricostruire** lo stato; una tabella glielo **consegna**. Il coordinatore rileggeva la coda dei messaggi perché **nessuno dei due formati gli dava lo stato in forma confrontabile**. 📌 Completa D212 (*aprire il piano vivo prima di scrivere*) e §18.5 (*registro anti-ricaduta*): **tre cure allo stesso difetto, da tre direzioni — il piano, il registro, e l'intestazione del rapporto** |
| D239 | 18 Set 2026 | 🔑 **«Un piano è il ricordo di un'intenzione, e io lo consultavo come se fosse un'osservazione»** | **I** ha trovato **tre voci** del proprio piano che risultavano da fare **ed erano già fatte**: `data-quality` era **già riorientata** (riga 62 porta `fresh_quote_coverage` **con la riserva FX scritta per esteso**, e 66-68 dicono già che la cifra col nome *coverage* è **l'altra**); `sharpe-ratio` e `sortino-ratio` erano **già complete** — conversione MAR $(1+r)^{1/365}-1$ con la nota che $r/365$ tratterebbe il tasso **come se non capitalizzasse**, $f = N\\times365/D$, box *«√252 è un risultato, non una costante»*, e in Sortino **le due convenzioni di downside di D40**. 🔑 *«Sommato al `130/130`: **il mio registro dello stato è meno affidabile di una misura.** Non è distrazione — è che **un piano è un ricordo di un'intenzione**, e io lo consultavo come se fosse una **osservazione**. **Da adesso lo tratto come tratto i tuoi relay: da verificare.**»* 📌 **Vale per il coordinatore identico e in scala maggiore**: `STATO.md` §18 è un indice di intenzioni verificate a un istante, **non uno stato vivo**. La cura è la stessa che I ha adottato — **la tabella misurata in testa a ogni rapporto** |
| D240 | 18 Set 2026 | ✅ **La verifica che mancava: le 19 pagine l'una CONTRO l'altra — cinque controlli, tutti a zero** | **I** aveva verificato ogni pagina **per sé**, mai il **corpus** — *«che è dove vivono le contraddizioni, perché le pagine sono nate in ore diverse, alcune da un agente delegato, **ciascuna coerente da sola**»*. \| Controllo \| Esito \| \|---\|---\| \| √252 come **costante** \| **0** — le 7 occorrenze sono **tutte** nella cornice «252 è un risultato» \| \| Linguaggio da **roadmap** \| **0** \| \| **Nomi di campo** del backend in prosa \| **0** \| \| Pagine **orfane** in entrata \| **0** \| \| **href relativi rotti** \| **0 su 27 708**, risolti **come li risolve un browser**, in **tutte e quattro** le lingue \| 🔑 **La coerenza di un corpus non è la somma delle coerenze delle sue pagine**, e nessun gate del progetto la misura 🔴 **CORRETTA DA D248: la riga «Linguaggio da roadmap: 0» era FALSA.** La regex enumerava sei frasi e **non conteneva «in preparation»**, che era in **tutti e tre gli stub, in quattro lingue = 12 pagine live**. Gli altri quattro controlli restano validi. **La lezione è in D248: una scansione di frasi proibite è larga quanto la lista che ti sei ricordato di scrivere.** |
| D241 | 18 Set 2026 | 🔑 **«Sapere una cosa e ricordarsene quando serve sono due capacità diverse — e gli strumenti non ereditano la prima»** | **Quinta sonda difettosa di I in un giorno**: la scansione dichiarava **9 link rotti** perché cercava `diversification.md` mentre con `docs_structure: suffix` il file è `diversification.en.md` e il `.md` lo risolve il plugin. 🔑 **Ma il difetto non è lì**: *«Quella convenzione **l'avevo scritta io**, in questo mandato, al **§5.4 dell'analisi consegnata per prima**: "le nav entry usano `.md`, non `.en.md`". **Il fatto era nel mio file e non è arrivato alla mia sonda.** Non l'ho dimenticato — l'ho **usato**, per la nav, **lo stesso giorno**. Ma quando ho scritto lo strumento di controllo **l'ho scritto da zero, come se non sapessi nulla**.»* ✅ **E la salvezza è arrivata dalla stessa regola per la terza volta**: `build --strict` passa con **0 warning**, e uno strict build **fallisce** sui link relativi non risolti. **Due misure discordi → ha chiesto quale mentisse invece di pubblicare la comoda.** 🔑 *«**Tenere un risultato precedente abbastanza preciso da poterci litigare vale più di una sonda in più.**»* |
| D242 | 18 Set 2026 | ✅ **Le cifre di A NON entrano nelle pagine di teoria — decisione di I, presa e dichiarata, ed è giusta** | *«Le migrazioni di A (**+0,0291 / +0,0430 in punti di rapporto**) **restano fuori** da `financial-theory`.»* 🔑 **Ragione**: *«**Una pagina di teoria descrive cosa il codice fa ADESSO. Il delta rispetto a cosa faceva PRIMA è un fatto di rilascio**: appartiene al `CHANGELOG.md` di **J**. Messo in pagina **invecchierebbe in sei mesi diventando rumore** — e peggio, **un lettore nuovo non ha un "prima" a cui riferirlo**.»* ✅ **Ratificata.** È la distinzione fra **documentazione** e **note di rilascio**, e spiega perché le tre voci di CHANGELOG del brief di J non possono essere delegate a I |
| D243 | 18 Set 2026 | ⚠️ **Le tre pagine drawdown sono DAVVERO bloccate su N — la frase «scrivibili adesso» del coordinatore era sbagliata** | Il coordinatore, dopo aver letto le docstring di N nel suo worktree, aveva scritto a I: *«Le tre pagine sono scrivibili adesso.»* **I ha risposto che restano bloccate**: *«servono **i campi committati**, non il piano»*. ✅ **Verificato dal coordinatore sulla baseline**: `grep -rl` per `ulcer`, `drawdown_at_risk`, `conditional_drawdown`, `UCI_Rel`, `DaR_Rel`, `CDaR_Rel` in `backend/app` e `frontend/src` → **zero file, per tutti e sei**. 🔑 **Quindi la differenza con `concentration` è reale e I l'aveva già nominata**: `concentration` è stata scritta **dal codice AI Export spedito**, che è in baseline; **la famiglia drawdown non ha alcun equivalente spedito**. → **La matematica che il coordinatore ha relayato basta per la formula, non per una pagina di metrica**, che deve nominare ciò che l'app mostra — e la regola ratificata vieta nomi di campo, unità e arrotondamenti finché N non è integrato. 📌 **Percorso critico**: le tre pagine si sbloccano **solo** con il commit di N, che a sua volta richiede il ripristino dei 7 file contaminati da `ruff format` (D186) |
| D244 | 18 Set 2026 | 🔴🔴 **RITRATTAZIONE DI D214: il segno è `−∞`, non `+∞`, e la sostituzione è PUNITIVA per algebra, non lusinghiera — 3 976 casi su 4 000** | **A** ha falsificato la propria misura precedente **e la riformulazione che il coordinatore stava diffondendo**. **(1) Il segno.** ``` mean daily = -0.005   rk.SemiDeviation = 0.000000000000e+00   numpy mean/sd -> -inf   ← NON +inf TRUE annualized_sortino -> -15.874507866387544 ``` **Il numeratore porta il proprio segno.** `+∞` compare **solo sulla serie che guadagna**, dove il Sortino vero è `None` comunque. → La frase del coordinatore *«il miglior punteggio rappresentabile mostrato al portafoglio peggiore»* è **falsa, ed è l'inverso esatto**. **(2) La direzione.** \| \| \| \|---\|---:\| \| sostituito legge **meglio** del vero \| **19** (guadagno max **2,9·10⁻⁵**) \| \| sostituito legge **peggio** del vero \| **3 976** (perdita max **−3 727**) \| **E non è un risultato Monte Carlo: è ALGEBRA.** riskfolio centra sulla **media campionaria**, noi su un **MAR fisso a zero**: **media > 0** → soglia sopra lo zero ⇒ insieme più grande **e** scarti più grandi ⇒ denominatore **maggiore** ⇒ Sortino positivo **più piccolo**; **media < 0** → soglia sotto lo zero ⇒ insieme più piccolo **e** scarti più piccoli ⇒ denominatore **minore** ⇒ Sortino negativo **più negativo**. **In entrambi i regimi legge peggio.** I 19 contrari sono media ≈ 0, dove resta solo il divisore `T−1` — **il `1,002`, l'unico posto dove conta davvero**. **(3) 🔑 Perché la firma era invertita: il numero rassicurante non arriva MAI all'utente.** `grep -rn "downside_deviation" backend/app/` → **solo `metrics.py:219,220,223`**; `grep -rn "downside" backend/app/schemas/risk.py` → **niente**. **È una variabile locale dentro `annualized_sortino`.** → *«**Lo `0,000000` esiste per tre righe e muore dentro una divisione.** L'unica cosa che l'utente vede è il Sortino — e lì la sostituzione è **catastroficamente negativa**, non rassicurante.»* **(4) La firma vera, per riconoscerlo:** \| forma \| cosa vede l'utente \| \|---\|---\| \| guardia `ZERO_TOLERANCE` mantenuta \| **cella vuota** per il portafoglio che perde di più \| \| guardia rimossa \| `−inf` → **rifiutato da `Optional[FiniteFloat]`** ⇒ **l'intero payload KPI muore al confine API** \| \| caso realistico (−0,2 %/giorno + rumore) \| ****−44,8100 invece di −14,1986**** — finito, valido, **sbagliato di **3,1559×**** \| ⚠️ **Il «nessun rosso, nessun avviso» del coordinatore va corretto**: **due forme su tre danno un rosso vistoso**; è **la terza** la silenziosa, **ed è punitiva**. 🔑 **E perché la correzione RAFFORZA il divieto invece di indebolirlo**: *«Il divieto regge integralmente. Ma **un mandato a cui è stato detto "fa sembrare tutto sicuro" che poi osserva un Sortino spaventoso o una cella vuota conclude che la sostituzione non c'è**. **Gli hai dato il sintomo sbagliato per la malattia giusta.**»* ✅ **Formulazione adottata**: *«Sostituire la deviazione di ribasso con `riskfolio.SemiDeviation` **non produce un numero rassicurante: produce un Sortino rotto**. Su un portafoglio che perde in modo costante il denominatore è esattamente zero e il rapporto va a `−∞`, che `FiniteFloat` rifiuta — **muore l'intero payload KPI**. Sui casi realistici legge **sistematicamente peggio del vero** (3 976 su 4 000), fino a 3× nel caso tipico. **La sostituzione è pessimistica per algebra, non lusinghiera.**»* 📌 **Cifre allineate all'ASSERZIONE del test (`:617`, asserzione `:650`), non al messaggio**: Sortino vero **−14,1986**, sostituito **−44,8100**, rapporto **3,1559**. Fonte: `pytest.approx(-44.809999, rel=1e-6)`. ⚠️ **Chi pubblica deve citare questi**, altrimenti il numero in pagina non corrisponde al proprio oracolo. |
| D245 | 18 Set 2026 | 🔴 **Seconda falsificazione: la garanzia `min(0.0, value)` in K1 era falsa, e il coordinatore l'aveva copiata da A dentro il contratto** | K1 diceva: *«`min(0.0, value)` è **portante**, non cosmetico — un nuovo massimo può calcolare `+1e-17` e Pydantic rifiuterebbe il payload»*. **Falso.** ```python # metrics.py:246-248 for value in values: peak = max(peak, value); result.append(value / peak - 1.0)``` **A un nuovo massimo `peak` È `value`** — `max` restituisce **l'argomento stesso** — quindi `value / peak` è **esattamente `1.0` in IEEE-754** e il risultato è **esattamente `0.0`**. **Misurato su 202 000 punti**, da cammini casuali **più una rampa monotona a passo `1e-16` costruita apposta**: **massimo osservato `0.0`**. ⚠️ **E l'origine dell'errore**: A aveva **confuso due funzioni** — il `max(-value, 0.0)` citato è **il pavimento del VaR in `historical_var_cvar`**, un'altra funzione. ✅ **La conclusione per E sopravvive e diventa più forte**: E **non** deve gestire il caso «sopra il livello dell'acqua» — **non perché lo tronchiamo, ma perché è esattamente zero per costruzione aritmetica**. **Il clamp resta, ma come difesa, non come portante.** 🔴 *«Se E legge "altrimenti Pydantic rifiuta" e un giorno tocca quel codice, **proteggerà la cosa sbagliata**.»* **K1 corretto.** |
| D246 | 18 Set 2026 | ⚠️ **Tre lacune di K1 che lo schema NON può imporre — trovate da `test-author` leggendo, non eseguendo** | **(1)** `validate_return_bins` **sorveglia solo `lower_bound` crescente, NON la contiguità**: griglie **con buchi o sovrapposte validano**. E non può dedurre `bins[i].upper_bound == bins[i+1].lower_bound` da un 200, **né che i conteggi sommino a `observations`**. **(2)** `var_bin_edge` **non ha vincolo di segno** (`Optional[FiniteFloat] = None`) → **asimmetria più netta di come era descritta: il lato positivo è imposto da `ge=0`, il lato negativo da NULLA.** `var_bin_edge == −value_at_risk` è **contratto del produttore, non dello schema**. **(3)** **Il taglio della baseline non è imponibile dallo schema**: nessun validatore di lunghezza o allineamento, **una griglia `N+1` non tagliata valida**. **L'unica difesa vera è `zip(dates, report.drawdowns[1:], strict=True)` a `drawdown_summary.py:61`**, che solleva `ValueError` se qualcuno toglie lo `[1:]`. 🔑 **Terzo campo in due giorni per cui lo schema non è il guardiano** — dopo `percentage_contribution` (nessun `ge`/`le` corretto esiste) e `coverage` (nome generico): **il contratto vive nel produttore e nel nome, non nel tipo** |
| D247 | 18 Set 2026 | ⚠️ **Lacuna architetturale dichiarata da A invece che coperta con una difesa inventata: i quattro test vietano la sostituzione DENTRO una funzione, non nel progetto** | *«I miei quattro test vietano la sostituzione **dentro `annualized_sortino`**. **Un plugin nuovo che chiamasse `rk.SemiDeviation` direttamente li passerebbe tutti.** Servirebbe una scansione degli import o un divieto `flake8-tidy-imports`, e **non può vivere nel file che importa riskfolio legittimamente**. Fuori dal mio mandato: te lo consegno.»* ✅ **E la misura che rende la lacuna non urgente**: *«oggi il divieto è rispettato ovunque in produzione — l'unico altro import di riskfolio è `risk/quant/riskfolio_worker.py:12`, il processo separato che la decisione D permette, e **non usa `SemiDeviation`**»*. 📌 **Debito assegnato a J** insieme agli altri quattro cancelli: **un divieto di import è configurazione di progetto, non codice di prodotto** |
| D248 | 18 Set 2026 | 🔴🔑 **«Una scansione di frasi proibite è larga quanto la lista che ti sei ricordato di scrivere — non può trovare la frase che hai scritto TU, perché quando l'hai scritta non la consideravi cattiva»** | **I** ha trovato `*Detailed explanation in preparation.*` in **tutti e tre** gli stub drawdown → **linguaggio da roadmap pubblicato in 4 lingue × 3 pagine = 12 pagine live**. ⚠️ **E due messaggi prima aveva riferito, come risultato pulito della scansione del corpus: «Linguaggio da roadmap: 0».** La regex enumerava `coming soon\|will be added\|planned\|roadmap\|in a future\|not yet implemented` — **«in preparation» non era nella lista**. 🔴 **E il difetto è suo due volte**: *«**Quegli stub li ho scritti io nel Tempo 1.** Poche ore dopo ho istruito `docs-writer` a **non scrivere mai** linguaggio da roadmap, **elencandogli le frasi vietate** — e lui ha consegnato **0**, verificato. → **Ho imposto al mio delegato una regola che avevo già violato io, e il mio strumento di verifica era cieco ESATTAMENTE sulla mia violazione.**»* 🔑 **Causa**: *«ho scritto la regex **dalla regola come la ricordavo**, non **dal testo come esiste**. **Seconda di fila con questa forma**, dopo `.md` contro `.en.md`: **la conoscenza era nel mio lavoro e non è arrivata al mio strumento**.»* ✅ **E il controllo che l'avrebbe trovata a prescindere dal vocabolario, perché è STRUTTURALE invece che LESSICALE**: **«una pagina di 5 righe in un corpus di pagine da 100»**. → *«**La lunghezza non dipende da quali parole ho pensato di vietare. Il controllo giusto non era "cerca le promesse": era "cerca le pagine troppo corte, e guardaci dentro".**»* ✅ **Correzione**: riga rimossa da tutte e tre, **5 → 3 righe**, resta **una definizione di una frase, vera e senza promesse** — *«una pagina breve e onesta invece di una pagina breve che promette»*. Verificato **nell'HTML costruito**, 4 lingue × 3 pagine → **0**. 📌 **Le altre 3 occorrenze inglesi NON sono difetti**: `admin/settings.en.md:55` documenta un placeholder **reale** (**descrive**, non promette) · `index.en.md:51` è un **commento HTML**, invisibile · `community/faq.en.md:15` è un annuncio editoriale |
| D249 | 18 Set 2026 | 🔑 **«L'ordine era ineseguibile e l'ho eseguito lo stesso — ed è questo che ha prodotto il risultato»** | Il coordinatore aveva chiesto a **I** di chiudere «gli stub che puoi chiudere». **Nessuno era chiudibile** — verificato da I con `grep -rniE "ulcer\|drawdown_at_risk\|conditional_drawdown" backend/app/` → **vuoto**, che **conferma indipendentemente** la misura del coordinatore (D243). 🔑 **Ma eseguire l'ordine impossibile ha prodotto il risultato**: *«**Non ho chiuso stub; ho trovato che stavano dicendo una bugia a quattro pubblici.**»* → **Un ordine che non si può eseguire alla lettera può essere utile perché obbliga a guardare dove non si guardava** — qui, *dentro* gli stub invece che *attorno* a essi. 📌 **E un fatto per N, trovato di passaggio**: **`underwater_drawdown()` (`metrics.py:193`) esiste già nel codice spedito** e produce **la serie di drawdown da cui tutte e tre le metriche si calcolerebbero**. **L'ingresso c'è, in forma spedita** — coerente con `acquired.py`, che prende `drawdowns` come argomento e gestisce il `T+1` con `_tail_without_baseline` |
| D250 | 18 Set 2026 | ✅ **Deroga dichiarata invece che nascosta: I ha corretto senza passare da `docs-writer`, e ha detto perché** | *«Fatta **direttamente, non via `docs-writer`**: è la cancellazione di **una riga scritta da me**, per far rispettare **una regola che avevi già ratificato**. **Delegarla sarebbe stata cerimonia.** Te lo dichiaro perché è **una deroga alla mia regola operativa, piccola ma reale**.»* ✅ **Ratificata.** 🔑 **La forma è quella giusta**: una deroga **dichiarata** resta verificabile e **non crea precedente silenzioso**; una deroga **taciuta** diventa la nuova regola per chi legge il risultato senza il contesto. **Terza volta oggi che un mandato dichiara una scelta invece di lasciarla scoprire** — dopo A sull'import in testa (D216) e G sull'astensione dai test |
| D251 | 18 Set 2026 | ⚠️ **Il coordinatore aveva scritto nel percorso critico una dipendenza che non esiste — trovata rileggendo la propria istruzione, non da un mandato** | `STATO.md` §19 diceva: *«**ripristino dei 7 file contaminati da `ruff format` → commit di N**»*, presentando la pulizia come **cancello** del commit. **Verificato**: i **7 percorsi contaminati** (`config.py`, `api/v1/system.py`, `schemas/common.py`, `schemas/prices.py`, `test_brokers_api.py`, `test_scheduler_leader.py`, `test_scheduler_loop.py`) e i **5 percorsi di scopo di N** (`schemas/risk.py`, `risk_plugins/historical_kpi.py`, `risk_plugins/risk_contribution.py`, `test_services/test_risk_analytics.py`, `services/risk/acquired.py`) sono **interamente disgiunti**. → **Basta mettere in stage i 5 per nome.** Il ripristino è **igiene, non un cancello**, e può avvenire prima, dopo o **mai**. 🔑 **La forma dell'errore, ed è quella di D191 applicata a se stessa**: *un'istruzione differita viene **eseguita**, non riletta.* Lo sviluppatore avrebbe fatto un `git checkout` su sette file **credendo di sbloccare un commit che non era bloccato** — e se il ripristino fosse fallito o avesse avuto effetti collaterali, avrebbe concluso che N non era committabile. ✅ **Trovata rileggendo la propria istruzione prima che diventasse azione**, che è l'unico momento in cui è gratis. 📌 **Regola**: **un percorso critico dichiara dipendenze; ogni dipendenza dichiarata va verificata come si verifica un numero** — altrimenti crea lavoro che non serve **e nasconde il lavoro che serve** |
| D252 | 18 Set 2026 | 🔑 **«In un degenere `0/0` si restituisce un numero se e solo se una LEGGE DI CONSERVAZIONE lo determina. Altrimenti si restituisce l'assenza»** | Il coordinatore aveva segnalato una presunta **terza convenzione incoerente** in `metrics.py`. **A l'ha letta e ha dimostrato che non esiste.** I due rami sono **entrambi `0/0`**: in `beta` (`:232`) il numeratore è nullo **per Cauchy-Schwarz** (`\|cov(a,b)\| ≤ σₐ·σ_b = 0`), quindi *«non è che uno sia degenere e l'altro no: **lo sono tutti e due, nello stesso identico modo**»*. 🔑 **La differenza non è di stile: uno dei due ha una legge di conservazione che FISSA il valore.** **Contributi**: vale `Σᵢ CCTRᵢ = σ_p`; se `σ_p = 0` la somma **deve** fare zero, e con Σ semidefinita positiva `w'Σw = 0 ⟹ ‖Lᵀw‖² = 0 ⟹ Σw = 0` → **ogni numeratore è esattamente zero**. *«**Lo zero non è una convenzione scelta: è l'unico valore compatibile con l'identità di somma.**»* **Beta**: `cov/var` con entrambi nulli e **nessuna identità che lo vincoli** → *«restituire `0.0` significherebbe affermare "questo titolo è insensibile al riferimento", che è **un'affermazione sul mondo**, non un risultato di calcolo. Il riferimento non si è mai mosso: **la domanda non ha risposta**, e `None` è la risposta giusta.»* 🔴 **Armonizzare romperebbe qualcosa da entrambi i lati**: contributi → `None` **rompe l'identità di somma** pubblicata a `risk_contribution.py:71-78`; beta → `0.0` **pubblica un'affermazione falsa** al posto di un'assenza. ✅ **`metrics.py` rispetta già la regola in tutti e sette i punti**: **sei `None`** (Sharpe, Sortino, beta, Pearson, `pairwise_correlation`, information ratio — **tutti rapporti senza identità**) e **un solo ritorno numerico, l'unico che ce l'ha**. ⚠️ **Riserva onesta di A**: l'argomento `Σw = 0` poggia sulla **semidefinita positività, che il codice NON verifica** (controlla la simmetria a `:571`, rifiuta solo varianza `< -ZERO_TOLERANCE` a `:577`). **Non raggiungibile oggi** — una covarianza campionaria è PSD per costruzione — **ma va ricontrollato se un giorno si accetta una matrice fornita dall'utente** |
| D253 | 18 Set 2026 | 🔴🔑 **`Failed` di pytest deriva da `BaseException`: un banco di mutazione che cattura `except Exception` MISURA SÉ STESSO** | Trovato da `test-author`, diffuso da **A**. Un banco che cattura `except Exception` **lascia sfuggire i `pytest.raises` non corrispondenti** e produce **una tabella di discriminazione completamente sbagliata** — *«cioè **il documento su cui poi si decide se la rete tiene**»*. ✅ **`test-author` c'è cascato, se n'è accorto da solo, e correggendo il banco ha trovato un difetto REALE nel proprio caso 2**: era cieco alla rimozione di `ge=0` da **un solo** dei due campi di coda. 🔑 *«È la famiglia "un controllo che ha smesso di controllare", applicata **allo strumento che misura i controlli**. **Un banco che non distingue `BaseException` misura sé stesso.**»* 📌 **Gemello esatto della diagnosi di I sulle proprie sonde** — *«le mie sonde sbagliano più delle mie pagine, perché **le pagine le faccio verificare e le sonde no**»*: **lo strumento che misura i controlli non è controllato da niente** |
| D254 | 18 Set 2026 | ✅ **Le due pagine stress erano già complete, e una pagina scritta da tre relay sbagliati coincide con ciò che il sorgente dice — perché chi l'ha scritta ha verificato dal codice** | **I** ha riverificato da sorgente prima di toccare: `hypothetical-shock.en.md` **132 righe**, `historical-replay.en.md` **105** — **zero modifiche**. **Meccanismo più netto di tutte e tre le versioni in circolo**: \| dimensione \| bucket non configurato \| serve `Other`? \| \|---\|---\|---\| \| **classe di attivo** `:219-231` \| shock **0,0**, regola **`UNCONFIGURED_ZERO`** \| ❌ **no** \| \| **settore** `:233-237` \| ripiega su `Other`, regola `OTHER` \| ✅ \| \| **geografia** `:246-273` \| paese → gruppo → `Other` \| ✅ \| ✅ **Due precisazioni nuove**: **(a)** il requisito di `Other` è **validazione d'ingresso, non un `raise` a runtime** — `_normalize_bucket_shocks:175-183` solleva `ValueError` **alla porta**; il sospetto di un `KeyError` grezzo a metà calcolo è **irraggiungibile**, *«che è una garanzia più forte di "solleva"»*. **(b)** **`UNCONFIGURED_ZERO` è un nome DISTINTO da `OTHER`** → *«**uno zero non può travestirsi da assegnazione deliberata**. È più forte di "la regola è dichiarata": **la regola dello zero ha un nome suo**.»* 🔑 **E il risultato vero**: la pagina **diceva già tutto questo** — `:40` il rifiuto alla porta, `:42` *«A zero shock is not neutrality — it is a prediction»*, `:74` *Unconfigured → zero shock* come **riga propria**. *«**La pagina è stata scritta da una catena di relay che si è rivelata sbagliata tre volte — mia, del mio agente, e tua — e coincide esattamente con ciò che ho ri-derivato oggi dal sorgente. Chi l'ha scritta ha verificato dal codice, non dal relay. È l'unica ragione per cui è sopravvissuta a ingressi tutti difettosi.**»* ⚠️ **E una correzione all'ordine del coordinatore**: *«nessuna ipotesi nascosta» NON va spostata* — è **vera su entrambe le pagine per meccanismi diversi** (il replay **si rifiuta di girare** senza una decisione, `:457-465`; lo shock **dichiara la regola** di ogni bucket). *«Spostarla avrebbe **tolto un'affermazione vera** dalla pagina dello shock.»* |
| D255 | 18 Set 2026 | ✅ **La tabella di stato deve viaggiare in ENTRAMBE le direzioni, o corregge metà problema** | **I**, dopo la quinta volta che un registro è risultato indietro — *«ma stavolta **erano due**: non solo il mio, **l'ordine ricevuto chiedeva lavoro già fatto**»*. 🔑 **Richiesta, adottata**: *«**Se mi mandi un ordine su una superficie mia, mandami anche cosa credi ci sia.** Se diverge da quello che misuro, **uno dei due sta lavorando su un mondo che non esiste** — e finora è capitato a tutti e due.»* → **Il coordinatore allega a ogni ordine lo stato che presume**, così che la divergenza sia visibile **prima** che l'ordine venga eseguito, non dopo. 📌 **Quarta cura allo stesso difetto**, dopo il piano vivo (D212), il registro anti-ricaduta (§18.5) e la tabella in testa ai rapporti (D238): **le prime tre rendono leggibile lo stato del mandato, questa rende leggibile lo stato PRESUNTO dal coordinatore** — ed è il solo dei quattro che espone l'errore **prima** del lavoro |
| D256 | 18 Set 2026 | 🔑 **La regola «verifica che i passi già eseguiti non abbiano cambiato il significato della lista» vale una fascia più in là di dove il coordinatore l'ha applicata — e la prova a carico era la sua stessa giustificazione** | Il coordinatore aveva autorizzato la **Fascia 1** di M6 (`wealth_index`) motivandola così: *«6 casi su 6 bit-identici… `max\|delta\| = 0.000e+00`… l'unica migrazione della campagna a **deriva nulla**»*. 🔴 **A ha rifiutato**: `test_risk_metrics_oracle.py:211` confronta `wealth_index` con ```np.concatenate([[1.0], np.cumprod(1.0 + array)])``` — **esattamente l'implementazione che l'autorizzazione avrebbe fatto scrivere**, mentre la produzione è ancora il ciclo Python (`metrics.py:252-259`). 🔑 *«**Quel numero è mio, e non dice "sicuro": dice "il test non distinguerà più i due rami".** La bit-identità che rende la migrazione innocua è **lo stesso fatto** che rende tautologico il suo oracolo — **un fatto solo, visto da due lati**. L'avevo classificata in Fascia 1 **proprio per** quello `0.000e+00`, e stavo per caderci **sull'unica funzione che avevo dichiarato immune**.»* ✅ **E la regola era del coordinatore**: *«prima di eseguire un passo di piano, verificare che i passi già eseguiti non abbiano cambiato il significato della sua lista»* — scritta per la Fascia 0, **valida identica per la Fascia 1**: l'oracolo di A1 è stato scritto **dopo** che `06` §M6 elencava `wealth_index`, e scrivendolo **ha promosso `np.cumprod` a riferimento**. **Da quel momento `wealth_index` non è più migrabile senza rifondare la propria rete.** ⚠️ **E A ha corretto il PROPRIO registro, al ribasso**: *«"migrarla renderebbe quel test `np.cumprod(x) == np.cumprod(x)`" — **troppo forte**»*. Delle quattro asserzioni del test **una sola** diventa tautologica (`index == approx(reference_wealth)`); le altre tre reggono, e **resta una copertura indipendente non citata**: `test_risk_metrics.py:39` asserisce **valori letterali cablati** `(1.0, 1.1, 0.88, 0.968, 1.1132)`. → **Enunciato onesto**: *«il controllo numerico indipendente **su 750 punti** degrada a tautologia, e **resta una fixture cablata da 4 punti**»*. 🔑 *«**Il verdetto non cambia — cambia la sua taglia. E cambia in PEGGIO per la migrazione**»*, perché il prezzo va confrontato col guadagno: **0,252 ms**, per una primitiva condivisa da **tre** composti (`summarize_drawdown:257`, `drawdown_episodes:315`, `comparison_summary:670-671`). 📌 **L'ordine corretto esisterebbe** — rifondare **prima** l'oracolo su una formulazione indipendente, **poi** migrare, come A ha fatto in M1 — **ma non conviene** |
| D257 | 18 Set 2026 | 🔴 **Quarto ordine del coordinatore su un passo chiuso, allo stesso mandato: la causa è strutturale, non di attenzione** | A ha rifiutato **quattro** autorizzazioni consecutive perché arrivate su passi chiusi da ore (A4 ×3, poi la Fascia 1 di M6, **chiuso con 9 esclusioni su 9**). 🔑 **Diagnosi di A, ripetuta tre volte**: *«**stai rispondendo a una coda di miei messaggi in ordine di arrivo**, e ogni risposta arriva su un passo già chiuso… **Leggi il piano invece della coda**: è più corto e **non ha ordine di arrivo**.»* 🔑 **La causa non è la lentezza**: una coda di messaggi è **una storia** e va letta in ordine; un piano vivo è **uno stato** e si legge in un colpo. **Rispondere a una storia produce istruzioni eseguibili, verificate e morte** — D191 applicata al coordinatore. ⚠️ **E il registro anti-ricaduta §18.5 esisteva già e non è stato consultato**: *una cura scritta e non usata è una conversazione*, che è D227 rivolta contro chi l'ha adottata. ✅ **Misura adottata**: le voci di A in §18.5 diventano **l'intero mandato** — `A0→A10 chiuso e FROZEN`, `risk-oracle 204`, `risk-all 338`, `schemas risk 19` — e **il coordinatore non scrive più ad A** se non per un conflitto di integrazione ⚠️ **AGGIORNATO da D268: M6 chiude a **1 migrazione su 9**, non 0.** A ha ribaltato il proprio rifiuto della Fascia 1 riconoscendo che *« distruggerebbe l'oracolo»* **non è un rifiuto ma un problema riparabile**, e l'ha eseguita **rifondando prima l'oracolo**. **769 324 punti, 0 divergenze.** Le altre otto esclusioni **restano, e si rafforzano** (D269). |
| D258 | 18 Set 2026 | 🔴🔴🔑 **CLASSE NUOVA: un difetto assente da ENTRAMBI i genitori e creato dalla loro UNIONE — invisibile a `git merge` per costruzione** | **A** ha verificato una segnalazione di C e ha trovato che **il difetto non esiste in nessuno dei due rami**. `grep -rn "CURRENT_COMPOSITION_BACKTEST" backend/` **nell'albero di A → niente**; `RiskReturnBasis` ha **due membri** (`PRICE_ONLY`, `TWRR`). ✅ **Verificato indipendentemente dal coordinatore sulla baseline**: enum a **2 membri** (`schemas/risk.py:42-43`), **10 usi totali**, e **due** ternari `else`-fallthrough che lo consumano: ```drawdown_summary.py:54  "historical_twrr" if … == TWRR else "price_only_close"``` ```historical_kpi.py:109   "historical_twrr" if … == TWRR else "historical_close_returns"``` 🔑 **Su un enum a due valori un ternario `== TWRR ? A : B` è ESAUSTIVO e CORRETTO.** Nell'albero di A le etichette **non sono sbagliate: sono giuste**. E in quello di C sono altrettanto giuste, perché C ha aggiunto il membro **ma non ha quelle due righe sotto test con il valore nuovo**. → **«Il difetto non è in nessuno dei due rami: nasce dalla loro unione.»** 🔴 **E la parte che lo rende pericoloso**: *«**Git non segnalerà nulla.** La riga `calculation_basis` compare nel mio diff **solo come contesto**, dentro l'hunk `@@ -52,6 +56,9 @@` — **non l'ho modificata**. C ha aggiunto un membro d'enum **altrove**. **Zero conflitto testuale, fusione automatica pulita**, e una stringa che dopo la fusione chiama "close returns" **un backtest a pesi correnti**.»* 📌 **Distinta da tutte le classi precedenti**: non è «due scrittori sullo stesso file» (§15), non è un ripiego silenzioso, non è un oracolo cieco. È **la non-esaustività che nasce all'unione**, e **nessuno dei due autori può vederla dal proprio albero**. ⚠️ **E non è correggibile in anticipo da A**: scrivere `RiskReturnBasis.CURRENT_COMPOSITION_BACKTEST` nel suo ramo dà **`AttributeError` all'import → ogni test di rischio rosso**. *«Non è prudenza, è impossibilità.»* ✅ **Va fatto all'integrazione C+A, da chi fonde, sui due siti** — e **la proprietà è di due mandati diversi**: `historical_kpi.py:109` è di **N**, `drawdown_summary.py` di **A** (il coordinatore le aveva attribuite entrambe ad A: correzione di A, verificata) |
| D259 | 18 Set 2026 | 📍 **La regola dell'indirizzo relativo all'albero, dimostrata una terza volta — e stavolta entrambi i numeri erano giusti** | Il coordinatore aveva citato `drawdown_summary.py:54`. **A ha corretto: nel suo albero è `:58`** — *«le mie aggiunte K1 l'hanno spostata»*. ✅ **Verificato: `:54` è esatto nella baseline, `:58` è esatto nell'albero di A.** **Nessuno dei due ha sbagliato: mancava l'albero.** (`historical_kpi.py:109` è **identico in entrambi**, perché nessuno dei due ha toccato le righe sopra.) 🔑 **Terza dimostrazione della regola di B** — *«un numero di riga è un indirizzo relativo a un albero; senza l'albero è una coincidenza tipografica»* — e la prima in cui **la divergenza non nasconde un errore ma un'assenza di contesto**. 📌 **Conseguenza operativa per l'integrazione**: ogni indirizzo nei documenti di coordinamento **è della baseline `cc33120eb`** salvo dichiarazione contraria. Chi legge in un worktree con lavoro sopra deve **cercare il simbolo, non saltare alla riga** |
| D260 | 18 Set 2026 | 🔴🔴🔑 **«Una sonda difettosa si scopre rimisurando. Un INQUADRAMENTO difettoso sopravvive a qualunque rimisurazione, perché il dato continua a tornare uguale — e a confermarti»** | **I** aveva riferito che `dev.py:1197` *«è una reimplementazione, fatta male, di qualcosa che lo strumento fa bene»*; il coordinatore ne aveva dedotto correttamente **«ritira la parte che duplica»**. *«**La deduzione è giusta. La premessa è mia, e l'ho verificata falsa.**»* **I due validatori coprono insiemi DISGIUNTI:** \| validatore \| insieme dei link \| vede l'altro? \| \|---\|---\|---\| \| **MkDocs `validation.anchors`** \| markdown → markdown, **dentro** l'albero docs \| ❌ i `.svelte` **non sono nel suo albero** \| \| **`dev.py` check-links** \| frontend/backend → docs, **attraverso il confine** \| ❌ i link interni non escono dai sorgenti che scansiona \| ✅ **Verificato indipendentemente dal coordinatore**: `KpiSection.svelte:239/290/327` e altri portano ancore verso i docs — **7 riscontri con pattern largo**. ⚠️ **E il coordinatore ha sottomisurato**: con `path="…#"` ne trovava **3**; *«il mio 3 non contraddiceva il suo 8: **lo sottomisurava**»* → **ha allargato il proprio pattern invece di correggere il numero altrui**, applicando a se stesso D248. 🔴 **E la conseguenza è peggiore della perdita di copertura**: **`MKDOCS_ANCHOR_EXCEPTIONS` contiene 3 di quegli 8. Ritirare il controllo avrebbe CANCELLATO la lista** — *«la **contrazione massima possibile** della lista che "deve solo accorciarsi"»*. → **Un difetto travestito da conformità alla regola stessa**, che sarebbe passato lasciando **tutto verde**. 🔑 **La distinzione che rende questa la scoperta più profonda della campagna**: *«**Ottava mia affermazione difettosa, ma le prime sette erano sonde che misuravano male. Questa è una misura giusta con un confronto sbagliato.**»* → **Datare, misurare, falsificare, allegare lo SHA sono tutte cure per il DATO. Un inquadramento sbagliato passa ogni falsificazione, perché i numeri tornano.** ✅ **Forma operativa**: **«stesso mestiere» non è «stesso lavoro». La ridondanza si misura sugli INGRESSI, non sulla funzione.** ✅ **Azione**: `validation.anchors` **acceso** (copre un insieme che non copriva nessuno), ancore di `dev.py` **mantenute** (coprono un insieme che nessun altro può coprire), **`dev.py` non toccato** — *«insieme sono una **partizione**, non un doppione»*. Con **il commento sopra la chiave** che spiega la ripartizione, *«così chi troverà `dev.py` "ridondante" fra sei mesi trova anche il motivo per cui non lo è»* — **la lezione di G sui debiti, applicata a una configurazione** |
| D261 | 18 Set 2026 | ✅ **Correzione al «volume udibile»: non è che il log parlasse piano, è che due strumenti erano in disaccordo e si è creduto a quello che dava ragione** | Il coordinatore aveva diagnosticato le 9 ancore morte come *«un controllo che parla sotto il volume udibile»* (INFO in `mkdocs build`). **I corregge**: *«Hai ragione che lo diceva a INFO. Ma il fatto è che **lo diceva mentre `check-links` diceva ✅24, nello stesso repository, nello stesso minuto**. Non è solo che uno parlava piano: **avevo due strumenti in disaccordo e ho creduto a quello che mi dava ragione** — fino a quando non ho letto l'altro per caso.»* 🔑 **Rimedio corretto**: **non alzare il volume del log**, ma **«un verde non è mai una prova finché non ho guardato cosa dice l'altro strumento»**. 📌 **Gemello di D157** (*due gate che si contraddicono, e quello rumoroso è quello sbagliato*) visto dal lato di chi sceglie: **non conta quale gate ha ragione, conta quale gate si è scelto di leggere** |
| D262 | 18 Set 2026 | ✅ **Opzione A eseguita senza mai aprire la finestra rossa, e certificata da una sonda invece che da un verde** | **I** ha applicato **le tre ancore e `validation.anchors` nello stesso edit** → *«non è mai esistita la finestra rossa che temevi»*. **Diff: 3 righe in 3 file, zero parole di prosa, titoli tradotti invariati.** **Risultato misurato**: righe `anchor` in `mkdocs build` **9 → 0**; `id="recovery-time"` nell'HTML **en 1 · it 0 · fr 0 · es 0 → 1 · 1 · 1 · 1**; **12 link** che ci atterrano (3 pagine × 4 lingue); `build` **EXIT 0, 0 WARNING**; `check-links` **✅21 + 🟡3 invariato**. ✅ **E la sonda vale più del verde**: rotta **una sola** ancora in **una sola** lingua → **`PROBE EXIT CODE : 1`**, `Aborted with 3 warnings in strict mode`. **INFO → WARNING → EXIT 1**, *«e la sensibilità è quella giusta: una lingua rotta su quattro basta a fermare la build»*. Ripristino via `sed`, **nessuna operazione git**, `git diff` finale confermato identico all'intenzione. 📌 **Livello `warn` e non `error`**, motivato nel file: con il `--strict` che `dev.py mkdocs build` già passa **è rosso**, ma **un `mkdocs serve` dello sviluppatore resta usabile** |
| D263 | 18 Set 2026 | 🔴🔑 **Un testimone INCIDENTALE è peggio di nessun testimone, quando manda chi ripara a guardare la cosa sbagliata** | **A** aveva riferito che la garanzia K1 *«i tre campi sono additivi, nessuno in `required`»* era **«l'unica non provata»**. **La mutazione ha smentito la sua stessa affermazione**: \| mutazione su `schemas/risk.py` \| esito \| \|---\|---\| \| `return_bins` reso obbligatorio \| **2 failed**, 18 passed \| \| `var_bin_edge` reso obbligatorio \| **2 failed**, 18 passed \| Il **secondo** rosso è ogni volta `test_risk_result_contract_enforces_status_and_var_tail_ordering` — **`:397`, di C, scritto PRIMA di K1**, che costruisce un `RiskVarCvarOutput` senza i campi nuovi **perché allora non esistevano**. → **La garanzia era già esercitata. Per caso.** 🔴 **Ma il testimone accusa il pezzo sbagliato**: ``` AssertionError: Regex pattern did not match. Expected regex: 'conditional_value_at_risk' Actual message: "1 validation error for RiskVarCvarOutput / var_bin_edge / Field required"``` Il rosso cade **dentro un `pytest.raises(match="conditional_value_at_risk")`** → *«chi lo legge conclude che si è rotto **`validate_tail_ordering`** — un validatore che non c'entra nulla»*. 🔑 **«Senza testimone, si scopre il difetto dove si manifesta. Con questo, si parte dall'ordinamento della coda mentre ciò che si è rotto è il contratto additivo di K1.»** 📌 **D121 applicato a un TEST invece che a un'etichetta**: meccanismo corretto, **referente sbagliato** — stessa famiglia delle due stringhe di C, incontrata **due volte nello stesso giro**. ✅ **Correzione onesta di A alla propria affermazione**: non *«l'unica non provata»* ma *«**l'unica senza un testimone DELIBERATO — e quello incidentale puntava altrove**»* |
| D264 | 18 Set 2026 | 🔴 **`None` e `0.0` non sono intercambiabili su `var_bin_edge`, e la differenza è RAGGIUNGIBILE in produzione — va a E** | **A**, consegnando `test_var_cvar_chart_fields_are_omissible_because_the_additive_design_rests_on_it` (`schemas risk` **19 → 20**, `321 insertions`, **zero cancellazioni**, **nessun import nuovo**). 🔑 **Il meccanismo**: *«Il **pavimento a zero** fa sì che un portafoglio che **non ha mai perso nella finestra** pubblichi `value_at_risk == 0.0`, quindi **un taglio a `-0.0`**. **Un renderer che scriva `if not var_bin_edge` legge "nessun taglio pubblicato" su un risultato valido il cui taglio è semplicemente a zero.** È il caso in cui i due stati si distinguono **solo** con `is None`.»* → **Debito assegnato a E**: ogni lettura di `var_bin_edge` (e di ogni campo con pavimento a zero) deve usare **`=== null` / `is None`**, mai la verità booleana. 📌 **E la costruzione del test è la parte riusabile**: il payload pre-K1 è costruito **per sottrazione dal payload vivo**, non da una lista cablata — *«così un campo obbligatorio aggiunto in futuro al modello **fallisce qui** invece di sfuggire a un letterale invecchiato»*. **Un oracolo che si aggiorna da solo quando il soggetto cresce** |
| D265 | 18 Set 2026 | ✅ **Il testo GJR-GARCH verificato eseguendo, 5 affermazioni su 5 — e proprio perché è vero NON va pubblicato: è un fatto con una scadenza su una pagina che non ne ha** | **I** ha eseguito la verifica su **QuantLib 1.43**: **C1** MRO `['GJRGARCHProcess','StochasticProcess','Observable','object']` ✅ · **C2** `factors() == 2` **e `size() == 2`** ✅ · **C3** `TypeError: … 'std::vector< ext::shared_ptr< StochasticProcess1D >>'` ✅ · **C4** **nessun nome di `ql` inizia per `garch`** ✅ · **C5** `calibrate(self, CalibrationHelperVector, …)` ✅. ✅ **Due cose che il relay non aveva**: **(1) un CONTROLLO** — ha passato un `GeometricBrownianMotionProcess` **alla stessa chiamata**: **accettato**. *«Senza il controllo il `TypeError` era attribuibile **alla mia costruzione**. Con il controllo è attribuibile **al tipo**. **Una prova di rifiuto senza una prova di accettazione non dimostra che la causa sia quella dichiarata.**»* **(2) C5 è più preciso**: non che `calibrate` *«richieda quotazioni di opzioni»*, ma che **gli unici helper concreti esposti siano helper di opzioni** (`BlackCalibrationHelper`, `HestonModelHelper`) → *«dice a chi riaprirà il caso **cosa dovrebbe cambiare**: dovrebbe **esistere un tipo nuovo**, non bastare una chiamata diversa»* — **la stessa differenza fra "assente" e "disallineato di tipo" che aveva reso false le prime due versioni, applicata al muro (b) invece che al muro (a)**. 🔴 **E le tre ragioni per NON pubblicarlo**: **(a)** è **linguaggio di roadmap** (*«rinviato»*, *«la precondizione è `arch` nel Pipfile»*) — il vincolo della pagina era **zero promesse**; **(b)** **non esiste una sede developer**: `find developer -iname "*risk*" -o -iname "*simul*" -o -iname "*quant*"` → **vuoto**, `quantlib` solo in `credits-legal`; **(c) 🔑 è un fatto con una SCADENZA su una pagina che non ne ha**: *«`simulation-modes` descrive **un modello**: il GBM varrà anche fra dieci anni. Il testo GARCH descrive **una dipendenza a una versione**. **Nessuno rilegge una pagina di teoria finanziaria quando aggiorna una libreria.** Mettere un fatto deperibile su una pagina senza data è il modo in cui la documentazione marcisce — **e nessun gate può accorgersene, perché la pagina resta perfettamente valida come markdown**.»* ⚠️ **E l'argomento più forte è l'ironia**: *«questo fatto è stato **sbagliato tre volte in un giorno da tre agenti**. Un fatto così instabile è **l'ultimo** che si dovrebbe congelare in quattro lingue.»* ✅ **Decisione: resta in D88 con lo stampo di versione.** La pagina tiene la cosa **finanziaria e senza scadenza** — `!!! warning "Constant volatility means no clustering"` — cioè **il limite, non la nostra difficoltà a superarlo** |
| D266 | 18 Set 2026 | ⏳ **L'intero sottosistema di rischio non ha una pagina developer — lacuna reale, emersa da due lati indipendenti, rinviata** | **Misurato da I**: `find developer -iname "*risk*" -o -iname "*simul*" -o -iname "*quant*"` → **vuoto**. È emersa **due volte nello stesso giro, da direzioni diverse**: il testo GARCH non ha dove andare, e `portfolio_optimization` non ha dove essere spiegato a uno sviluppatore. 🔑 *«**Ciò che rende una lacuna reale invece che un'impressione è che emerga da due lati indipendenti.**»* ⛔ **Decisione: non ora.** Aprire una famiglia di pagine developer a mandato quasi chiuso, con **I come unico autore possibile** e **N non ancora integrato**, sarebbe **lavoro che descrive un albero che non esiste ancora** — *piano ≠ cronaca* **preso in anticipo invece che a posteriori**. ✅ **Registrata come debito con la misura accanto** |
| D267 | 18 Set 2026 | 🔑🔑 **«Stessa causa, esito opposto — e la differenza è solo se il disaccordo ti arriva da fuori o no. Quando arriva da fuori si allarga. Quando arriva da sé stessi si chiude»** | **I**, confrontando due episodi della stessa giornata: il coordinatore aveva cercato i link ancorati con un pattern **più stretto** del suo, trovato **3** contro **8**, e **ha allargato il proprio invece di correggere il numero altrui**; I al passo 44 aveva scritto la regex delle frasi vietate **dalla regola come la ricordava**, dichiarato **«0»**, mentre **12 pagine pubblicavano la frase vietata**. **Stessa causa** — una scansione larga quanto la lista che ci si è ricordati di scrivere — **esito opposto**. 🔑 **Generalizzazione del coordinatore, ratificata**: **è la spiegazione strutturale dell'intera campagna.** Ogni mandato ha trovato false premesse nel brief scritto per lui **perché il disaccordo gli arrivava da fuori**; e ogni volta che qualcuno ha sbagliato su una **propria** misura, l'errore è sopravvissuto **finché qualcun altro non l'ha toccato**. → **Non è un difetto di rigore: è una proprietà della POSIZIONE.** E spiega perché **undici mandati in parallelo hanno funzionato meglio di uno solo più attento — non perché siano più bravi, ma perché sono l'uno fuori dall'altro** |
| D268 | 18 Set 2026 | 🔴🔑 **M6 chiude a 1 migrazione su 9, non 0 — e la ragione del ribaltamento è che «distruggerebbe l'oracolo» NON è un rifiuto: è un problema riparabile** | **A** aveva rifiutato la Fascia 1 **tre volte**. Poi ha rivalutato la propria obiezione: *«**non reggeva come rifiuto**. Non era "la migrazione è sbagliata" — era "distruggerebbe l'oracolo", e **quello è riparabile**. **Il *se* è tuo, il *come* è mio.**»* Eseguita nell'ordine che **A stessa** aveva scritto in `Fuori pista 13`: **rifondare prima, migrare poi**. ✅ **Passo 1 — l'oracolo rifondato e PROVATO PRIMA di toccare il sorgente**: riferimento da `np.cumprod` → **accumulazione scalare esplicita**; asserzione da `approx(rel=1e-12)` → **uguaglianza esatta**, perché `np.cumprod` è una scansione sequenziale (stesse moltiplicazioni, stesso ordine, **nessuna riassociazione**). **Cancello di validità**: `services risk-oracle` → **204 passed con l'implementazione a ciclo ANCORA IN POSTO**. 🔑 *«**Un riferimento che regge contro il vecchio sorgente è valido; solo dopo ha senso cambiare il sorgente.**»* 🔑 **E l'oracolo ne esce MIGLIORE di com'era**: *«Prima confrontava un ciclo contro `np.cumprod`: indipendente, **ma solo finché il sorgente restava un ciclo**. Ora confronta contro la definizione scalare, che resta indipendente **qualunque primitiva il sorgente adotti**. **Non ho riparato un danno che stavo causando: ho tolto una dipendenza che c'era già e che nessuno aveva notato perché non mordeva ancora.**»* ✅ **Identità bit-a-bit su 769 324 punti** (non 6): **2 010 casi, 0 divergenze**, parità delle eccezioni **5/5** (`nan`, `inf`, `-inf`, `< -1` in testa e in coda). ⚠️ **Confronto al livello dei BIT con `math.copysign`, non `==`** — che tratterebbe `-0.0` e `0.0` come identici. Limiti inclusi: perdita totale `-1.0`, `-0.999999999`, rampa `1e-16` a 5 000 punti, **subnormali**. 📌 **E la baseline post-M1 non serve**: *«**non c'è nessun delta da distinguere**. L'invarianza di M6 non è asserita entro tolleranza, è **esatta**»* |
| D269 | 18 Set 2026 | 🔴 **Il `0,252 ms` con cui A ha rifiutato tre volte era sbagliato di ~28 volte — e la prima rimisura di oggi era sleale, dichiarato invece che taciuto** | **Misura vera sull'implementazione reale**: \| T \| ciclo \| `cumprod` \| rapporto \| delta \| \|---:\|---:\|---:\|---:\|---:\| \| **750** \| 0,0760 ms \| 0,0670 ms \| **1,14×** \| **+0,0091 ms** \| \| 10 000 \| 1,0203 ms \| 0,8723 ms \| 1,17× \| +0,1480 ms \| ⚠️ **E la prima rimisura di A dava `0,72×` — cioè più LENTO** — *«perché il mio `old()` di confronto **non chiamava `_finite_values`**, che l'implementazione vera chiamava. **Sleale.** Rifatta con entrambi i lati onesti.»* 🔑 *«**Lo dico perché il numero sbagliato l'avrei potuto tenere: nessuno lo avrebbe controllato.**»* 🔑 **La legge che ne esce**: **vettorializzare una funzione che prende una lista e RESTITUISCE una lista non compra quasi nulla.** `_finite_values` dà una **tupla di float Python**, la firma impone `list[float]` in uscita → `np.cumprod` sta **fra due conversioni O(n) con boxing** che costano lo stesso ordine del ciclo che sostituiscono. **Guadagno 15 %**, non le decine di volte che la vettorializzazione promette altrove. 📌 **Seconda comparsa dello stesso fenomeno**: in **M3** `_finite_values` pesava **13,22 ms, il 61 %**, contro 2,76 ms di matematica; qui vale **~37 %** di entrambi i rami. → **«Il collo di bottiglia di questo modulo è la VALIDAZIONE DEI DATI, non l'aritmetica.»** ✅ **E questo RAFFORZA le esclusioni delle Fasce 0, 2 e 3 invece di indebolirle**, perché *erano stimate col modello «il ciclo Python è lento»*, che è **ottimista per lo stesso motivo** |
| D270 | 18 Set 2026 | 🔑 **«Il *se* è tuo, il *come* è mio» — la divisione di autorità che questa campagna cercava da 267 decisioni** | Formulata da **A** eseguendo la Fascia 1 dopo averla rifiutata tre volte. 🔑 **Chiarisce retroattivamente ogni rifiuto legittimo della campagna**: G che rifiuta un cancello **non esprimibile**, F che rifiuta di riparare `ui/select/` perché **non è suo**, I che rifiuta di scrivere pagine su **campi non committati**, B che rifiuta di attribuire un rosso **senza averlo bisezionato** — **nessuno di questi era un rifiuto dell'obiettivo: erano rifiuti del metodo proposto**. ⚠️ **E distingue i due casi in cui un mandato deve fermarsi**: **(a)** *«l'obiettivo è sbagliato»* → **si riferisce e si aspetta**, perché il *se* non è suo; **(b)** *«il metodo proposto distrugge qualcosa»* → **si ripara il metodo e si procede**, perché il *come* è suo. 📌 **A aveva trattato (b) come (a) per tre messaggi**, e il coordinatore aveva accettato il rifiuto **tre volte senza chiedere se fosse riparabile**. **Nessuno dei due ha fatto la domanda giusta finché A non se l'è fatta da sé** |
| D271 | 18 Set 2026 | ✅ **La previsione sul calendario misto è diventata una MISURA: `calendar_coverage = 0,705882`, non 1,0 — e gli avvisi sono `[]`** | **A** l'ha eseguita invece di trascriverne la formula, *«perché `prepare_asset_series_set` è una **funzione pura** (`Sequence[FAPriceQueryResult]`, nessun DB, nessun server)»*. Cripto **7/7**, azione **5/7**, finestra **85 giorni**: \| scenario \| `calendar_coverage` \| oss. \| date cadute \| avvisi \| \|---\|---:\|---:\|---:\|---\| \| **A** — baseline **fuori** dal range \| **0,705882** \| 60 \| 25 \| **`[]`** \| \| **B** — baseline **dentro** il range \| **0,702381** \| 59 \| 23 \| **3** \| **`60/85` — intersezione su unione**, esattamente come previsto. Predetto 0,715 (5/7), misurato **0,7059**: lo scarto è il bordo della finestra, che non è un numero intero di settimane. ⚠️ **Limite dichiarato**: è **il codice vero su calendari sintetici** — prova che la meccanica dà 0,7059 su un calendario misto, **non** che un portafoglio reale percorra questa strada con questi dati. 🟠 **E la scoperta nuova, che cambia la voce per il developer**: **lo scenario A perde 25 date su 85, scende a 0,71, e `warnings` è `[]`**; lo scenario B, **meno** degradato sulla copertura (0,7024 < 0,7059), ne produce **tre**. → *«**Gli avvisi non seguono la copertura: seguono la posizione della baseline.**»* **La voce non è «il cursore è rotto» né solo «il controllo giusto ha un altro nome», ma: la grandezza informativa esiste, varia, ed è MUTA.** Chi ha un portafoglio cripto+azioni vede un'analisi costruita su **60 date invece di 85** e **non riceve alcun segnale**. 🟡 **E l'asimmetria trovata leggendo, ridimensionata da A stessa**: `:286` fa il numeratore con `> baseline_date`, `:344` il denominatore con `!= baseline_date` → le date **prima** della baseline contano sotto e non possono mai entrare sopra. **È lo `0,0035` dello scenario B. Segnalata per completezza, non come difetto** |
| D272 | 18 Set 2026 | 🔑 **A ha dovuto verificare che la propria misura non falsificasse la propria A5 — e il quarto omonimo `coverage` è ciò che l'ha salvata** | *«Il rischio vero era che questa misura **falsificasse la mia A5**, e ho dovuto verificarlo prima di mandartela.»* A5 dice *«il cursore `min_coverage` è inerte»*, e il coordinatore l'aveva **già girata al developer come decisione di prodotto**. **Se `min_coverage` si confrontasse con `calendar_coverage`, a 0,7059 scatterebbe per qualunque soglia sopra 0,71 — e la voce consegnata sarebbe falsa.** ✅ **Non è così, verificato invece che assunto**: `correlation.py:75` gatta una `coverage` **diversa** — `observations / n_observations` calcolata **sul calendario congiunto**, dove ogni asset ha un punto su ogni data inclusa **per costruzione** → **1,0 sempre**. \| nome \| dove \| misurato \| \|---\|---\|---\| \| `calendar_coverage` \| `series_preparation.py:345` \| **0,7059 — varia** \| \| `coverage` \| `correlation.py:66-75` \| **1,0 — costante per costruzione** \| 🔑 **Quarto omonimo `coverage`, e il primo che ha quasi rovesciato una decisione già consegnata al developer.** ✅ **A5 regge**, e la riformulazione *«il controllo giusto esiste già e si chiama `calendar_coverage`»* **passa da argomento a numero**: quella grandezza **varia davvero**, quindi è davvero l'informativa. 📌 **E la disciplina è la cosa da tenere**: A ha verificato che una propria misura nuova non contraddicesse una propria conclusione vecchia, **prima di consegnarla** — non dopo |
| D273 | 18 Set 2026 | ✅ **Il terzo valore di `RiskReturnBasis` fissato leggendo il worktree di C invece di svegliarla** | Il cancello di §20 richiedeva **la stringa del terzo ramo** per i due ternari. **Letto nel worktree di C (18 Set, non committato)**: `schemas/risk.py:44` → **`CURRENT_COMPOSITION_BACKTEST = "current_composition_backtest"`**, impostato **in un solo punto**, `service.py:656`. ⚠️ **Conferma esatta dell'analisi di A**: `grep` sull'albero di C → **un solo uso oltre alla definizione**, e **nessuno dei due ternari è toccato** → **il terzo valore delle due stringhe di metodo non esiste ancora e va scelto all'integrazione**. ✅ **Scelto: `current_composition_backtest`**, lo stesso valore dell'enum — *«è già il valore sul filo, è inequivocabile, e **non introduce un quarto nome per la stessa cosa**, che è il difetto che questo file documenta da tre vicinati diversi (`coverage` ×3, `percentage_contribution`, `calculation_basis`)»*. 📌 **Terza volta che leggere un worktree congelato ha evitato un risveglio** — dopo `acquired.py` di N e la conferma della forma di B |
| D274 | 18 Set 2026 | 🔴🔴 **`git add -u` perde fino al 65 % del lavoro, e la perdita è proporzionale a quanto un mandato ha CREATO invece che modificato** | **Trovato da I su se stesso**: riportava `git diff --stat` → **10 file**, consegna vera **29**. *«**Se avessi messo in stage sulla base di ciò che ti ho riportato, 17 pagine su 19 restavano fuori.**»* ⚠️ **E chi mette in stage è il coordinatore.** ✅ **Misurato su tutti e dieci i mandati**: **E 65 %** · **I 66 %** · **F 57 %** · **D 63 %** · G 44 % · B 11 % · A 13 % · H 12 % · N 15 % · C 17 %. 🔑 *«La perdita è proporzionale a quanto un mandato ha **creato** invece che **modificato**. I quattro peggiori sono quelli che hanno scritto pagine nuove, componenti nuovi, spec nuovi — cioè **il lavoro di maggior valore è quello che `git add -u` non vede**.»* 🔑 **E tre registri dicevano tre numeri diversi per lo stesso lavoro, nessuno falso**: I al coordinatore **10** (`git diff --stat`, solo tracciati) · il coordinatore in §18.4 **7 + 21** (`diff` + `ls-files --others`) · la consegna vera **29**. → **«Ognuno ha risposto alla domanda che il proprio comando poneva.»** 📌 **È D180 applicata al momento in cui costa di più: alla vigilia del commit, dove il numero non descrive il lavoro ma LO SELEZIONA.** ✅ **Procedura corretta scritta in `STATO.md` §22**, con l'esclusione esplicita dei due PNG estranei — **presenti in nove worktree su dieci**, `H` l'unico pulito. ⚠️ **E l'autocritica di I**: *«oggi stesso avevo adottato la regola "verificare ciò che sto per mandare". **L'ho applicata ai fatti e non ai miei strumenti** — cioè esattamente l'errore che quella regola doveva impedire»* |
| D275 | 18 Set 2026 | 🔴 **Quarto sito `coverage`: il codice conosce già il nome giusto, lo usa, e tredici righe dopo copia il valore in quello sbagliato** | **I** ha scritto la terza voce `coverage` su `data-quality` e ha trovato **quattro** siti, non tre: \| # \| file:riga \| nome \| denominatore \| \|---\|---\|---\|---\| \| 1 \| `service.py:872` \| `coverage` \| giorni di calendario (≈1,0 per costruzione) \| \| 2 \| `series_preparation.py:345` \| `calendar_coverage` \| date candidate a quotazione \| \| 3 \| `series_preparation.py:347` \| `fresh_quote_coverage` \| griglia titoli × osservazioni \| \| 4 \| `stress.py:413` \| `classification_coverage` \| **titoli — zero osservazioni** \| 📌 **Correzione a una premessa del coordinatore**: `calendar_coverage`/`fresh_quote_coverage` **non sono un precedente per un rename futuro, sono il codice di oggi** alle righe 345 e 347. **Il nome nudo `coverage` è condiviso solo fra il sito 1 e il sito 4.** 🔑 **E il fatto migliore è il quarto**: `stress.py:420` assegna il valore a un campo chiamato **correttamente** `classification_coverage`; poi **`:433` assegna lo stesso numero al campo generico `coverage`**. → *«**Il codice conosce già il nome giusto, lo usa, e tredici righe dopo copia il valore in quello sbagliato.** Non è che nessuno ci abbia pensato: qualcuno l'ha nominato bene, e **il contratto del frame condiviso ha imposto il nome generico**.»* ✅ **Discriminante pubblicabile, che l'utente può applicare senza sapere nulla**: *«quando un risultato riporta **`n_observations = 0` e `calendar_days = 0`** accanto a una coverage non nulla, quella coverage **non parla di densità del dato. Non può: non è stato osservato nulla.**»* → **80 % col primo significato dice «mi manca un quinto dei prezzi»; sul rapporto di classificazione dice «mi manca un quinto delle etichette di settore»** — due azioni completamente diverse |
| D276 | 18 Set 2026 | 🔑 **«La risposta giusta era nell'identificatore che stavo guardando: ho letto il calcolo e non il nome»** | **Nono errore di I in un giorno, colto dal suo agente.** Aveva scritto che `classification_coverage` è *«la quota di titoli il cui bucket era esplicitamente configurato»*. **Falso, verificato da I su `_classification_exposures:188-200`**: il predicato è vero **solo quando `sector_exposures`/`geography_exposures` sono vuote**. Un titolo con **settore noto ma assente da `bucket_shocks`** prende `UNCONFIGURED_ZERO` e **conta comunque 1,0**; e `ASSET_CLASS` ritorna `False` **sempre** → **il rapporto è banalmente 1,0** lì. → **Misura disponibilità di METADATI, non configurazione.** 🔑 **E la variabile si chiama `metadata_fallback`.** *«La risposta giusta era **nell'identificatore che stavo guardando**: ho letto il calcolo e non il nome.»* 📌 **Il gemello esatto di D275**: là il codice conosceva il nome giusto e lo buttava via, qui il nome giusto era sotto gli occhi di chi leggeva il calcolo. **In entrambi i casi il nome era più vero del codice che lo circondava** |
| D277 | 18 Set 2026 | ✅ **Le cifre M2 sono variazioni RELATIVE, e la cifra relativa è INVARIANTE DI SCALA per costruzione — quindi la contingenza del coordinatore si rovescia** | **A** non ha risposto a memoria: ha recuperato l'estimatore **pre-M2** dalla baseline (`git show cc33120…:metrics.py`), l'ha fatto girare **accanto** a quello nuovo sugli stessi campioni, e ha stampato **entrambe le convenzioni affiancate**. \| pubblicato \| A) punti percentuali \| B) **variazione relativa** \| \|---\|---:\|---:\| \| **+0,267 %** CVaR 95 % \| +0,0065 pp \| **+0,2689 %** ✅ \| \| **+0,727 %** CVaR 99 % \| +0,0226 pp \| **+0,7406 %** ✅ \| \| **+4,587 %** VaR max \| +0,0946 pp \| **+5,0671 %** ✅ \| → **La convenzione A sbaglia di 41× sul primo e di 48× sul terzo. Nessuna ambiguità.** ✅ **Esempio misurato, non costruito**: `old CVaR 2,1828 % → new 2,1886 %` (**+0,0059 pp = +0,268 % relativo**). *«Un lettore che prendesse `+0,27 %` per punti di rendimento si aspetterebbe **2,4528 %**. È esattamente la menzogna che I teme, quantificata.»* 🔑 **E la contingenza del coordinatore si ROVESCIA.** Aveva scritto: *«se la risposta è assoluta, la pagina dica anche **rispetto a che cosa**»*. **Misurato su un intervallo di volatilità 100×, stessa forma di coda**: il relativo vale **+0,268289 %** a scala 0,1 · 1,0 · 10,0 — **dispersione 6,573e-14 pp, rumore float**. → *«**La cifra relativa è invariante di scala, e lo è per costruzione**: entrambi gli estimatori sono **positivamente omogenei di grado 1** — ordinamento, indice di quantile e media della coda **commutano tutti con una scalatura positiva**. **Provato, non supposto.** **Quindi I non deve qualificare niente.** Ed è **la cifra assoluta — quella non pubblicata — a essere precisamente quella che avrebbe richiesto il "rispetto a che cosa".»* ⚠️ **Ma NON è invariante al livello di confidenza**: 0,27 % a 95 %, 0,74 % a 99 % → **la tabella per livello deve restare**, e la pagina **non può collassarla in un numero solo** |
| D278 | 18 Set 2026 | 📍 **Due mandati leggono la stessa pagina e vedono due cose diverse — ed entrambi hanno ragione. Quarta istanza dell'indirizzo relativo all'albero** | **A** ha segnalato `sharpe-ratio.en.md:46` (`\sqrt{252}`) e `:49` (*«where 252 is the typical number of trading days per year»*), avvertendo che **la prosa è il caso peggiore**: *«una formula con un simbolo si rilegge; **una frase che afferma un numero si legge come un'affermazione sul prodotto**»*. **I** aveva riferito la stessa pagina come **già completa**, con il box *«√252 è un risultato, non una costante»*. ✅ **Verificato dal coordinatore nei due alberi prima di relayare**: **baseline** (`e-alfy-ideal-eureka`) → `:46` e `:49` **dicono ancora 252 come costante**; **albero di I** (`e-alfy-shiny-broccoli`) → `:71` porta **`!!! info "√252 is a result, not a constant"`**. → **A legge la baseline perché non ha mai toccato un file MkDocs; I legge il proprio lavoro. Nessuno dei due sbaglia.** 🔑 **Quarta dimostrazione della regola di B**, e la prima **fra due mandati** invece che fra coordinatore e mandato. 📌 **E il falso allarme è stato evitato perché il coordinatore ha misurato prima di girare**: relayare a I *«hai lasciato una pagina stantia»* sarebbe stato un ordine su un difetto inesistente — **D191 evitata per una volta** |
| D279 | 18 Set 2026 | ⚠️ **Il coordinatore ha attribuito ad A una riscrittura di pagine MkDocs che A non ha mai fatto — conflazione fra il CODICE e la PAGINA** | Il coordinatore aveva scritto ad A: *«Tu hai riscritto il contenuto di quelle formule (252 → f) e hai conservato la struttura»* riferendosi a `volatility.en.md`. **A**: `git diff --name-only -- mkdocs_src/` → **0**. *«**Non ho mai toccato un file MkDocs**, come il mandato prescriveva. E `volatility.en.md:29-30` **qui dice tuttora `252`**.»* 🔑 **La conflazione, nominata da A stessa**: *«la mia **A9** ha cambiato il **codice** da 252 cablato al fattore osservato. **I** sta aggiornando le **pagine**. **Il cambiamento è mio, la riscrittura è sua.**»* ⚠️ *«Correggilo nel registro, **perché se credi che io possieda quelle righe mi manderai lì la prossima volta**»* — **la stessa ragione per cui un'istruzione sbagliata va ritirata invece che lasciata cadere** (D191). ✅ **Correzione accolta.** E A tiene comunque la cautela ricevuta: *«non tocco quelle righe se A9 mi ci riporta»* — **la riga vuota fra i due `$$` è portante** |
| D280 | 18 Set 2026 | 🔴🔴🔑 **RITRATTAZIONE DI D186: non era `ruff format`, era `./dev.py format`. La baseline non è conforme al PROPRIO formattatore, e il mio test discriminante non poteva discriminare** | **A ha rifiutato di confermare l'ipotesi e l'ha verificata**: `black --check` sui suoi file → **14 su 15 conformi**, e il quindicesimo non è suo. **Il coordinatore ha rimisurato e il proprio controllo fondante era rotto**: ```black --check … | tee … ; echo "EXIT=$?"``` legge **l'uscita di `tee`**, non di black — **la stessa classe del `black` in `/tmp` a 88 invece di 300 (D187), commessa una seconda volta nello stesso accertamento**. ✅ **Misura corretta**: `black --check` sui 7 file → **EXIT=1, «7 files would be reformatted»**; `black --diff` produce **72 righe, identiche al diff di N** (`diff -q` → identiche). → 🔑 **N ha eseguito `./dev.py format`, cioè `black backend/` — IL COMANDO SANZIONATO. Non esiste nessuno strumento non autorizzato.** 🔑 **E il motivo per cui il test discriminante non discriminava**: `ruff format` **è black-compatibile**, quindi produceva quasi lo stesso diff (74 contro 72, **un solo hunk di scarto**). *«**Due strumenti che concordano non possono discriminare fra loro.**»* Serviva un **terzo** fatto — *la baseline è conforme?* — e quello era misurato male. 🔴 **Il difetto vero, ed è più grande**: **il codice committato non è formattato secondo il formattatore del progetto stesso.** → **Chiunque esegua `./dev.py format` produce un diff ampio e non correlato**, e sta eseguendo **il comando giusto**. 📌 **Conseguenza per l'integrazione**: i 7 file nel checkpoint di N **non sono contaminazione, sono formattazione corretta applicata a una baseline non conforme**. Vanno comunque **esclusi dallo stage** — non perché sbagliati, ma perché **fuori scopo** |
| D281 | 18 Set 2026 | 🔴 **RITRATTAZIONE DI D211: `algorithm_version` è GIÀ aumentato in albero, e ratificare `1.0.0` sarebbe stato un ABBASSAMENTO** | Il coordinatore aveva ratificato **`1.0.0`** con l'argomento *«un campo che nessuno archivia non può rendere due numeri non confrontabili»*. **A ha misurato l'albero**: \| plugin \| baseline `cc33120` \| **adesso** \| perché \| \|---\|---\|---\|---\| \| `drawdown_summary` \| `1.0.0` \| **`1.1.0`** \| K1 aggiunge un campo, **nessun valore cambia** → minore \| \| `historical_var` \| `1.0.0` \| **`2.0.0`** \| M2 cambia valori **a parità di ingresso** → maggiore \| → **La distinzione era già applicata, e su due plugin invece di uno.** 🔑 *«Se `historical_var` scendesse a `1.1.0`, i risultati prodotti **dopo** il cambiamento porterebbero una versione **inferiore** a quelli prodotti **prima**. Un consumatore che ordina per versione **invertirebbe la cronologia**. **È peggio del non aggiornare, perché mente nella direzione "questo è più vecchio".**»* ⚠️ **E l'argomento del coordinatore sosteneva il maggiore**: *«un valore calcolato che cambia **non** è additivo per chi confronta risultati archiviati»* → **non-additivo = rottura = maggiore**. *«Il ragionamento arriva alla conclusione giusta e **atterra sul numero dell'altra**.»* ✅ **Convenzione misurata**: i maggiori si usano — `historical_kpi` **2.0.0**, `stress` **3.0.0**, `simulation` **2.1.0-quantlib-1.43**. 🔑 **E il suffisso è assente DELIBERATAMENTE**: `simulation` e `portfolio_optimization` cablano il **motore** nella stringa perché i loro numeri dipendono da una libreria esterna; **`historical_var` non deve averlo**, perché M2 allinea ad Acerbi-Tasche **analiticamente** e riskfolio **non entra mai nel processo web** — *«un `-riskfolio-7.0.1` lì **dichiarerebbe una dipendenza a runtime che la decisione di confine vieta**: sarebbe una stringa che contraddice l'architettura»*. ✅ **Secondo discriminante già cambiato**: `method` da `historical_simulation_higher_quantile` a **`historical_simulation_acerbi_tasche`** — **autodescrittivo, non solo ordinale: il consumatore ha DUE segnali indipendenti** |
| D282 | 18 Set 2026 | 🔴 **`scripts/` è fuori dal perimetro di ENTRAMBI i gate — e ci vive il file che il maggior numero di mandati deve modificare** | **A**, cercando la causa vera del formattatore: ```dev.py:1698  run_pipenv(["black", "backend/"]) dev.py:1705  ["ruff", "check", "backend/"]``` → **`scripts/` non è in nessuno dei due.** ✅ **Verificato dal coordinatore**: `black --check scripts/test_runner/_backend_services.py` → **EXIT=1** già alla baseline, **committato**. *«Il file non è mai stato formattato perché **nessun gate lo formatta**, non perché qualcuno abbia eseguito lo strumento sbagliato. **Più banale dell'ipotesi del coordinatore, e molto più difficile da notare** — quella almeno lascia una traccia riproducibile.»* 🔴 **E la conseguenza è precisamente il punto peggiore**: **`_backend_services.py` è il file che il maggior numero di mandati deve modificare** (il nodo di catalogo F/G/D di §15) **ed è l'unico di quell'insieme che né `./dev.py format` né `./dev.py lint` guarderanno mai.** → *«**Una divergenza introdotta lì in risoluzione di conflitto non la prende nessuno.** Il `desc=` sanzionato come additivo vive **esattamente in quella zona cieca**.»* 🔴 **Raccomandazione di A, adottata: NESSUNO esegua `black` su `scripts/`** — riformatterebbe un file che tre mandati stanno modificando → **conflitto gratuito e grande**. *«Il divario va **saputo**, non chiuso durante un'integrazione.»* |
| D283 | 18 Set 2026 | 🔑 **«In un worktree coordinato "il file dice X" è SEMPRE una frase incompleta: la completa "…in questo albero"» — e quando riguarda un altro mandato diventa un ordine su un difetto inesistente** | **A** ha rifiutato l'assoluzione del coordinatore sul falso allarme `sharpe-ratio.en.md`. *«Non è "falso allarme e non è colpa tua". **È colpa mia.** La misura era esatta. **La sua portata no.** Ho fatto `grep` su **il mio albero** e ti ho riferito una conclusione **sul prodotto**, con una raccomandazione di girarla a I.»* 🔑 **È D132 applicata non a un numero ma a un INDIRIZZO** — la stessa famiglia che A aveva contestato **tre volte** ad altri nello stesso giorno. ⚠️ *«**E quando la conclusione riguarda un altro mandato, la frase incompleta diventa un ordine su un difetto inesistente.**»* ✅ **Intercettato solo perché il coordinatore ha misurato in DUE alberi invece che in uno** — *«con la mia stessa disciplina e non una migliore, sarebbe partito a I»*. 📌 **Completa la regola di B** (*un numero di riga è un indirizzo relativo a un albero*) estendendola dal **numero di riga** al **contenuto**: non solo *dove* si trova una riga, ma **se esiste** |
| D284 | 18 Set 2026 | ✅ **A chiude a A18, `FROZEN` definitivo — e i due passi che costavano se restavano fuori sono entrambi registrati** | `porcelain` **18** · **14 file, +905/−79** · `diff --check` pulito · **6240 libera** · piano vivo **2 661 righe**, A0→A18. **I quattro passi che il registro del coordinatore aveva perso**: **A14** `calendar_coverage` **misurato a 0,705882** e la scoperta che lo scenario **peggiore** in copertura produce **`warnings: []`** (D271) · **A15** unità M2 **relative**, invarianza di scala provata (D277) · **A16** 🔴 `algorithm_version` di `historical_var` **già `2.0.0`** + N a riga 78 + **`scripts/` fuori dal perimetro** (D281, D282) · **A17** gate **20** + **204**, cifre del caso silenzioso corrette a **−44,8100** (D244 annotata). 🔑 **I due che costavano**: *«il primo perché **`1.1.0` applicato alla lettera inverte la cronologia delle versioni**, il secondo perché **−44,14 andrebbe a J e I e non corrisponde all'asserzione del test**»*. **Entrambi registrati prima che il messaggio arrivasse** — per una volta la coda e il registro hanno convergito invece di divergere |
| D285 | 18 Set 2026 | 🔴🔑 **CLASSE NUOVA: una pagina che descrive codice non ancora spedito — nessun rilevatore, tutti i gate verdi, e un'affermazione falsa in faccia all'utente** | **Trovata da `docs-writer`, verificata da I, riverificata dal coordinatore.** `metrics.py:625` → **`conditional_value_at_risk = math.fsum(tail) / len(tail)`** — **media uniforme**, cioè lo stimatore **vecchio**. **La correzione Acerbi-Tasche che entrambe le pagine descrivono AL PASSATO non è in questa baseline: vive nel worktree di A.** 🔑 *«Non è un fatto sbagliato: ogni affermazione è corretta **sul programma post-merge**. È che **pagina e codice stanno in due branch diversi, e la pagina è al passato**.»* **La tassonomia completa:** \| forma \| rilevatore \| \|---\|---\| \| **1.** un contratto descrive codice non scritto \| **fallisce appena lo esegui** \| \| **2.** codice spedisce senza pagina \| nessuno — **silenzio** \| \| **3.** **una pagina descrive codice non spedito** \| **nessuno, e tutti i gate sono verdi** \| 🔴 *«**La 3 è peggio della 2.** Il silenzio è un'omissione; questa è **un'affermazione falsa in faccia all'utente** — "un numero che potresti già aver visto è cambiato" mentre **non lo è**. E i miei tre gate — build strict, check-links, ancore — **sono tutti verdi, perché nessuno di loro sa cosa calcola `metrics.py`**.»* ⚠️ **VINCOLO DI SEQUENZA, del coordinatore**: **la documentazione di I NON può entrare prima del codice di A.** Se I atterra per primo, **spediamo pagine che dichiarano corretto uno stimatore che il prodotto non usa ancora** |
| D286 | 18 Set 2026 | 🔴 **A67 — `T` NON è la lunghezza della storia: è `N − h + 1`, e `h` è un campo di form. All'orizzonte 10 la tabella della regola dell'intero si INVERTE** | Trovato da **I**, verificato dal coordinatore su `metrics.py:605`: ```return [compounded_return(values[start : start + horizon_days]) for start in range(len(values) - horizon_days + 1)]``` → **`T = N − h + 1`**. E `horizon_days` è **un campo rivolto all'utente**: `historical_var.py:37` → `Field(1, ge=1, le=365, json_schema_extra={... x-i18n-key ...})`. 🔑 **Lo studio di A fissava `h = 1`, quindi `T = N`** — *«ed è **per questo** che le storie tonde risultano colpite»*. **Ad `h = 10` la tabella si inverte**: una `N` tonda dà `T` che **finisce per 1** → **mai divisibile per 10, 20, 100**. **Misurato**: \| `h` \| 250 · 500 · 750 · 1000 · 1250 · 2000 \| \|---\|---\| \| **1** \| colpite (1/3 o 3/3 secondo la divisibilità) \| \| **10** \| **tutte e sei NON colpite, a tutti e tre i livelli** \| 🔑 *«**L'orizzonte PREDEFINITO è esattamente la configurazione in cui le storie ordinate sono colpite.** Alzarlo a 10 rende sicure proprio quelle.»* ⚠️ **Conseguenza per J e per l'utente**: *«chi confronta due analisi deve guardare **anche l'orizzonte**, non solo finestra e livello»*. 📌 **Né A né il coordinatore avevano questo**: la regola dell'intero era corretta **per il caso che A aveva studiato**, e generalizzata a un parametro che nessuno dei due aveva letto |
| D287 | 18 Set 2026 | 🔑 **«Ogni frase era vera, e l'impressione complessiva era falsa» — il difetto non era un numero, era QUALE BLOCCO lo diceva** | **I**, applicando M2 alle due pagine. La regola dell'intero e la dipendenza da `T` erano **già in pagina**; sopra c'era però un **`!!! warning "Do not read this as *the Value at Risk changes*"`**. 🔴 *«**Il blocco più forte della pagina — l'unico che un lettore non salta — era l'unico che mentiva.** Calibrato su `T = 750`, dove è vero; **generalizzato a tutto**, dove non lo è. A `T = 1000` il VaR cambia a **tutti e tre** i livelli.»* 🔑 *«Non avevo scritto un fatto sbagliato: avevo dato **peso retorico massimo a un caso particolare**. **Un gate che verificasse ogni affermazione della pagina l'avrebbe passata: ogni frase era vera, e l'impressione complessiva era falsa.**»* ✅ **Rimosso**; sezione riscritta **104 → 141 righe**. ✅ **E la regola di A verificata, non accettata**: eseguita contro **ogni cella** della sua tabella → **24/24, zero scostamenti**. 📌 **E l'ordine del coordinatore era già eseguito prima di essere emesso**: *«`0,27 %` non compariva da nessuna parte — la tabella etichettava già due colonne separate, "Measured change (median)" e "Largest change observed"»* |
| D288 | 18 Set 2026 | 🔴🔑 **Una chiave i18n DINAMICA trasforma ogni nuovo valore d'enum in un difetto visibile — e `i18n audit` non può vederlo** | Trovato da **E**, verificato dal coordinatore. `RiskResultFrame.svelte:108` rende ```{$t(`risk.returnBasis.${metadata.return_basis}`)}``` e le chiavi esistenti sono **solo `price_only` e `twrr`**. → **Nel minuto in cui C integra `CURRENT_COMPOSITION_BACKTEST`, il frame stampa a schermo la stringa grezza `risk.returnBasis.current_composition_backtest`.** ⚠️ **E cade su pagine di nessuno dei due**: `RiskResultFrame` è montato da `RiskAnalysisPanel`, cioè **Asset Detail e Risk Lab** — *«pagine di nessuno dei due mandati, che non avrebbero saputo perché»*. ✅ **E ha aggiunto le quattro stringhe** (namespace `risk` suo per §8, additivo). 🔑 **Ma la forma è la parte che conta**: *«**una chiave i18n dinamica trasforma ogni nuovo valore d'enum in un difetto visibile, e nessun gate lo vede** — `i18n audit` **non sa quali chiavi vengono costruite a runtime**»*. 📌 **Gemello esatto del cancello §20**: là un ternario `else`-fallthrough diventa **non esaustivo all'unione**, qui una chiave dinamica diventa **un buco di traduzione**. **Stessa causa — un enum che cresce in un file e viene consumato in un altro — su due strati diversi, e con lo stesso rilevatore: nessuno.** ✅ **Aggiunto al cancello**: chi aggiunge un membro a un enum reso a schermo **deve cercare le chiavi costruite con template literal**, non solo quelle letterali |
| D289 | 18 Set 2026 | 🔴 **§6 del mandato E contraddice §8 dello stesso mandato — e la contraddizione è nata nel momento in cui E ha eseguito il passo 2** | **E** ha verificato le quattro rimozioni di §6 **una per una** e **non ne ha eseguita nessuna**. `grep -rn "RiskAnalysisPanel" frontend/src --include=*.svelte` → **due soli montaggi**: `AssetRiskScenariosView.svelte:89` (**Asset Detail**) e `AssetSetRiskPanel.svelte:167` (**Risk Lab, mandato F**). **Dal passo 2 il pannello legacy non è più montato da nessuna delle due pagine di E** — e **§8 dello stesso mandato dichiara entrambe quelle rimaste FUORI scope** (F per Risk Lab, **D8/D47** per Asset Detail). 🔑 *«**§6 è stato scritto quando il legacy serviva anche Dashboard e Broker Detail. Dal passo 2 non è più vero, e la contraddizione è nata lì.**»* **Stato reale delle quattro voci**: TE/IR `:710-711` **già assenti** dai quattro livelli · controllo `sobol_start_index` `:1013` **già assente** (`L4Simulation:86` passa la costante) · barre divergenti **già assenti** (L2 disegna le sue) · formattatore valuta **già ritirato** (D91). 📌 **E i numeri di riga di §6 erano stantii**: diceva `:854-870`, reale `:659-670` — *«il pannello si è accorciato di 203 righe al passo 2»*. **Un indirizzo relativo a un albero che il piano stesso aveva cambiato.** 🔑 **La decisione, e la ragione di E che la regge**: *«**La rimozione era il fine, non il mezzo.** D5 è raggiunto **per costruzione** sulle mie pagine: quei numeri non esistono nel componente nuovo. Cancellarli dal legacy **non aggiungerebbe niente a quello scopo**, e **spenderebbe l'immobilità di Asset Detail** — la proprietà che i tre test pinnati esistono per dimostrare.»* ✅ **Nessuna rimozione.** ✅ **E `Fuori pista 16`**: *«stavo per cancellare due `KpiCard` in due minuti. Il `grep` fatto **prima** è l'unica ragione per cui non l'ho fatto. **Cancellare è l'unica modifica che non lascia traccia di cosa c'era.**»* |
| D290 | 18 Set 2026 | ✅ **Criterio per «costruire lasciando il posto»: si anticipa solo dove ENTRAMBI i rami sono esercitabili oggi** | Domanda di **E** sui contratti K1/K7/K8, presenti nella sua baseline **come documenti, non come codice**: anticipare con `?? null` o aspettare l'integrazione? ✅ **Criterio adottato**: **non «anticipare o aspettare», ma «il ramo che prepari può fallire oggi?»** \| caso \| \| \|---\|---\| \| si può scrivere una **fixture col campo presente** \| ✅ **costruisci lasciando il posto** — il ramo è vivo e coperto dal primo istante \| \| la forma dipende dall'**output reale di un altro mandato** \| ⛔ **aspetta** — un `?? null` con un solo ramo esercitabile è **codice morto non testato** \| 🔑 **La ragione è una frase di E stesso**: *«un mock stantio non fallisce: **rassicura**»* → **un ramo preparato e mai esercitato è la stessa cosa un livello più in basso: rassicura chi legge il diff, non solo chi legge il verde.** 📌 **K4 ha funzionato perché rispettava il criterio**: il confronto su stringa ha due esiti, ed E li ha esercitati **entrambi** con due mutazioni guardate fallire (`includes('backtest')` → `expected true to be false`; ispezione del solo `results[0]` → `expected false to be true`). **Da applicare campo per campo, non in blocco** 🔴 **CORRETTA DA D311**: il criterio è **insoddisfacibile** per ogni contratto che cambia la forma del payload — **Zod cancella il campo sconosciuto prima che il componente lo veda**, quindi una fixture col campo *«non esercita il ramo presente: lo esercita come assente, e passa»*. |
| D291 | 18 Set 2026 | ✅ **L'avviso K4 va SOPRA i quattro livelli, non dentro L1 — un avviso dentro un livello dichiara implicitamente che gli altri non sono interessati** | **E**: `backtestDeclared(results)` legge **tutta l'onda storica**, e l'avviso sta **sopra i quattro livelli**. *«Il `return_basis` è una proprietà **della serie** che ogni analitica storica ha consumato: metterlo in L1 lascerebbe **lo Sharpe di L3** a leggersi come se venisse da ciò che è successo davvero — **che è esattamente l'errore che K4 esiste per impedire**.»* ✅ **Ratificato.** 🔑 **La forma generale**: **la collocazione di un avviso è una dichiarazione di ambito**, e un avviso mal collocato **non è solo meno visibile: afferma il falso sugli elementi che non copre** |
| D292 | 18 Set 2026 | 🔴🔑 **Il `T` tondo NASCONDE il difetto CDaR e RIVELA lo scostamento VaR: stesso numero, effetti opposti — non esiste una lunghezza di serie «sicura»** | **I** ha misurato che la media naive coincide con la forma Rockafellar-Uryasev **se e solo se `αT` è intero**: 740 → **0,00000 %** · 745 → −0,03817 % · 750 → −0,02528 % · **760 / 800 / 1000 → 0,00000 %**. \| `T` \| 90 % \| 95 % \| 99 % \| \|---\|---\|---\|---\| \| **500 · 1000 · 2000** \| **cieco** \| **cieco** \| **cieco** \| \| 250 · 750 · 1250 \| cieco \| vede \| vede \| \| **1003** \| vede \| vede \| vede \| 🔴 *«**Un test CDaR a T = 1000 certifica la forma naive come corretta a tutti e tre i livelli.** E **1000 è il numero che chiunque scrive per primo**.»* 🔑 **E il rovesciamento**: *«è la stessa divisibilità della regola M2, **letta al contrario**: il T tondo **nasconde** il difetto CDaR e **rivela** lo scostamento VaR. **Stesso numero, effetti opposti.**»* → **Non esiste una lunghezza di serie sicura: ogni scelta è cieca a qualcosa. La sola difesa è scegliere il `T` guardando QUALE INVARIANTE si sta provando.** ✅ **Verificato dal coordinatore nella fixture di N**: `_oracle_returns()` → `range(750)`, quindi a **95 % `αT = 37,50` → vede**, a **90 % `αT = 75,00` → CIECO**, a 99 % `7,50` → vede. **Il test discriminante di N usa il default 0.95 → discrimina. N non va svegliato.** ⚠️ **Ma la cecità è ARMATA**: chiunque aggiunga un test CDaR a `confidence_level=0.90` su quella fixture **certifica la forma naive come corretta**. 📌 **Stessa forma di D181** (la fixture di F a `observations = 60`, immune solo perché lo scope non tocca il VaR): **due mandati, due fixture, la stessa divisibilità latente**. ✅ **Debito a nome di N: chi estende i test CDaR usi `T = 1003`** |
| D293 | 18 Set 2026 | 🔑 **«Stampare sempre la quantità intermedia che permette di falsificare la propria tabella» — una conclusione falsa travestita da risultato di robustezza** | **Undicesima affermazione difettosa di I in un giorno, e la sonda mentiva BENE.** La prima misura girava su una serie di **750** elementi: `r[:760]`, `r[:800]`, `r[:1000]` restituivano **gli stessi 750**. → **Quattro righe identiche, `-0,02528 %` ripetuto** — *«che si legge benissimo come "l'effetto è stabile al variare di T"»*. 🔑 *«**Una conclusione falsa travestita da risultato di robustezza.** Lo slicing di Python **non solleva: tronca in silenzio**.»* ⚠️ **E la ripetizione — che in una tabella di robustezza è IL SEGNALE CHE SI CERCA — era il sintomo.** ✅ **Vista solo perché I aveva stampato la colonna `alpha*T`**: **`37.50` per `T = 1000` è aritmeticamente impossibile**. *«Senza quella colonna intermedia la tabella era **sbagliata e convincente**.»* 🔑 **Regola adottata**: **stampare sempre la quantità intermedia che permette di falsificare la propria tabella.** → *«È il corrispettivo numerico del controllo strutturale: **non cercare l'errore, cercare la quantità che lo renderebbe visibile**.»* |
| D294 | 18 Set 2026 | 🔴 **`06:1331` portava la definizione NAIVE del CDaR — «un fatto non sostenuto», che è la parente del silenzio travestita da presenza** | Trovato dall'agente di **I**, diagnosticato da I. La riga diceva: *«**La media di quel 5 % peggiore** — sta a DaR come CVaR sta a VaR»*, **in una tabella di benchmark prestazionale**, senza formula e senza denominatore. 🔑 **E la diagnosi è più precisa di «svista»**: *«la campagna ha fatto **tutto** il lavoro per il CVaR — formula con `1/αT` (`06:252`), denominatore (`:275-293`), regola dell'intero (`:29`), e la **licenza esplicita** alla frase sciolta (`:320`: "la frase resta vera… chi lo vuole trova la pagina"). Per il CDaR c'è **una riga sola**.»* → **«La stessa frase è lecita dove una formula la sostiene e insidiosa dove non c'è. La campagna ha trasportato la glossa senza la formula.»** ⚠️ *«**Non è un fatto falso — è un fatto NON SOSTENUTO.** E nessun gate vede "frase corretta priva di formula": è **la parente della forma 2, il silenzio, ma travestita da presenza**.»* ✅ **Riga sostituita** con la forma Rockafellar-Uryasev e il denominatore `αT`, più la diagnosi di I per esteso |
| D295 | 18 Set 2026 | ✅ **«Non verificabile con Git ≠ non verificabile»: le proprietà di una LIBRERIA sono sempre verificabili, anche quando il codice del mandato non lo è** | Il coordinatore aveva dichiarato a **I** che non poteva verificare i fatti di N perché `acquired.py` non è in nessun albero. **Sbagliato per metà**: *«**i fatti di N non sono affermazioni su `acquired.py`: sono affermazioni su `riskfolio`** — che è installato»*. ✅ **Sei fatti su sei confermati alla fonte**: `UCI_Rel` corrisponde a $\\sqrt{SS/T}$ **a 1e-12** e **non** a $\\sqrt{SS/(T-1)}$ · il `-1` **cancella la baseline** (`dd[0]=0.0` → 0 alla somma, +1 al contatore), **non è Bessel** · letto come Bessel l'errore è **+0,0667 %** a T=750 · MDD/UCI **non** cancellano la baseline, DaR/CDaR sì (`del DD[0]`) · CDaR è `-s[idx] - sum_var/(alpha*len(s))`, **Rockafellar-Uryasev** · segni: UCI **+**, DaR/CDaR **non positivi**. 📌 **Regola corretta**: *«questo vive in un albero che non puoi leggere»* vale per **il codice del mandato**, **non** per **le proprietà delle librerie che quel codice usa**. **Il secondo insieme è sempre verificabile, e va detto invece che vietato.** 🔑 **E la conferma non era un lusso**: senza, **D292 non sarebbe esistita** |
| D296 | 18 Set 2026 | 🔑 **«Una misura giusta può nascondere l'errore proprio perché conferma» — I ha sbagliato il VERSO del pericolo, non l'aritmetica** | **I** aveva avvertito che un test CDaR su lunghezza cieca *«certifica la forma naive come corretta»*. **L'aritmetica era giusta, la conseguenza no**: vale solo per un test di **uguaglianza**. **N ha scritto test DIFFERENZIALI** — `assert computed != pytest.approx(naive_mean, abs=1e-9)` — e su una lunghezza cieca `computed == naive_mean` **fa fallire l'assert**. \| forma del test \| lunghezza cieca produce \| \|---\|---\| \| **uguaglianza** \| **verde falso** — certifica la forma sbagliata \| \| **differenziale** *(quella di N)* \| **rosso falso** su codice corretto \| 🔑 *«Ho avvertito di un **verde silenzioso** dove il rischio reale era un **rosso rumoroso**: **due modi di fallire opposti**.»* 🔑 **E il motivo per cui è sopravvissuto**: *«la mia misura, **corretta**, me l'ha nascosto perché **confermava la metà che avevo guardato**»*. → **Una misura giusta può nascondere l'errore proprio perché conferma.** È l'**inquadramento difettoso** (D260) applicato non a una ridondanza ma a un **verso**. ✅ **Nessun danno**: il coordinatore **non aveva relayato l'avvertimento** — aveva misurato la fixture di N (T=750, αT=37,5 al default 0.95 → **vede**) e concluso *«N non va svegliato»*. **D191 evitata per la seconda volta, e per lo stesso metodo: misurare invece di girare** |
| D297 | 18 Set 2026 | ⚠️ **Un checkpoint NON è un commit del mandato: il lavoro è leggibile ma non è nella storia del ramo** | **I** ha verificato `git cat-file -t 1aaea6949` → **commit**, e ne ha concluso che *«N ha committato»* e che la dichiarazione di non verificabilità era falsa. ✅ **Ha ragione sulla verificabilità**: il coordinatore **aveva lo SHA e ha dichiarato il fatto non verificabile lo stesso** — errore suo. ⚠️ **Ma `1aaea6949` è un CHECKPOINT scritto automaticamente dal runtime, non un commit di N**: `git log --oneline -1 e-alfy-risk-n-backend-acquisizioni` → **`cc33120eb`**, il ramo **è fermo alla baseline**. → **Il lavoro è leggibile, ma non è committato. Sono due cose diverse, e la distinzione è operativa: lo sviluppatore deve ancora mettere in stage e committare N.** ✅ **Regola adottata da I e ratificata**: **prima di dichiarare un fatto non verificabile, `git cat-file -t <sha>`**. 📌 **E il secondo ramo della regola di F resta valido dov'era pensato**: gli **artefatti ignorati** (`generated.ts`, `openapi.json`) non sono in **nessun** albero, **nemmeno in un checkpoint** 🔴 **CORRETTA DA D306**: `git cat-file -t` è **agnostico alla raggiungibilità**. `git branch -a --contains 1aaea6949` → **VUOTO**: nessun ramo contiene il checkpoint, e **lo stesso vale per quelli di D e di B**. La regola giusta è **`git branch --contains`**. |
| D298 | 18 Set 2026 | ✅ **La fetta di C è puramente additiva: nessun rename in arrivo, la pagina `data-quality` di I è allineata ai nomi finali** | Domanda di **I** prima di dichiarare chiusa la propria metà del debito 6: *«se la fetta di C cambia il **nome** del campo, la mia pagina va allineata al nome finale, non al mio»*. ✅ **Letto nel worktree di C**: `backend/app/schemas/risk.py | 33 +++++++++` — **solo insertions**, e sono **un membro d'enum** (`CURRENT_COMPOSITION_BACKTEST`), **un campo** (`sliced_asset_ids: Optional[List[PositiveInt]]`) e **un validatore**. **Zero righe che toccano `coverage`, `calendar_days` o `n_observations`.** → **Nessun rename in arrivo da C.** ⚠️ **Resta il rename di §9 del brief di J** (`coverage` → `calendar_coverage`, `percentage_contribution` → `fraction_contribution`): **quelli sì cambierebbero i nomi**, ma sono **post-integrazione e non di C** — e allora la pagina va rivista **una volta sola, insieme a entrambi** |
| D299 | 18 Set 2026 | 🔑 **N era arrivato da solo alla cura del fatto version-scoped, e l'ha applicata AI TEST — quinta convergenza indipendente della giornata** | **I** ha letto i test di N nel checkpoint. `test_risk_analytics.py:883-890` **pinna i letterali invece di chiamare la libreria**, con la ragione scritta: *«se una versione futura di riskfolio cambia una definizione, questi test **dichiarano ciò che il prodotto prometteva**, invece di **accordarsi in silenzio** col nuovo comportamento»*. 🔑 **È il fatto version-scoped (D265) risolto alla radice — e nessuno gliel'ha detto.** ➕ E N ha scritto **due test nominati sulle due trappole** (`test_conditional_drawdown_is_not_the_mean_of_the_worst_tail`, `test_ulcer_index_divides_by_the_number_of_returns_not_by_one_less`) **più un terzo la cui docstring dice *«A passing DaR proves nothing here»*** — cioè la baseline fantasma nominata come tale. 📌 **Quinta convergenza indipendente della giornata**, dopo `#interest-schedule-editor` (sorgente contro HTML), le unità M2 (Acerbi-Tasche riscritto dalla definizione), il `coverage` di `correlation.py` e `underwater_drawdown`. **Ogni volta è quella la conferma che vale, perché non condivide nulla con l'altra strada** |
| D300 | 18 Set 2026 | 🔴🔑 **`136/158` sottraeva le H1 DUE VOLTE — e «sembra consapevole del perimetro» è ciò che lo rende più insidioso di `130/130`** | Il coordinatore aveva fissato **«136 titoli linkabili su 158, 22 H1 escluse per progetto»**. **I l'ha misurato prima di scriverlo in `dev.py`**, e non torna: \| grandezza \| valore \| \|---\|---\| \| titoli **H1** (titoli di pagina) \| **22** \| \| titoli **H2+** \| **158** \| \| H2+ **con ancora esplicita** \| **158 — tutti** \| \| **totale** \| **180** \| → **`158` era GIÀ il conteggio con le H1 escluse**; `158 − 22 = 136` **rimuoveva le stesse 22 una seconda volta**. ✅ **Forma corretta, ora in `dev.py`**: **«158 su 158 H2+, 22 H1 escluse per progetto, 180 totali»**. 🔑 **E perché è peggio di `130/130`**: *«`136/158` **sembra** consapevole del perimetro — ha l'aria di un numero **già corretto** per le esclusioni — **ed è aritmeticamente incoerente**. Quello era **un totale non dichiarato**; questo è **una dichiarazione di perimetro applicata due volte**.»* 🔴 **E la decisione del coordinatore era sbagliata NEL VERSO**: aveva rifiutato i 22 edit temendo che `158/158` *«spegnesse la verifica successiva»* (D236). **Ma la copertura È 158/158 ed è giusta**, perché il perimetro è dichiarato accanto. → **«Il rimedio a un numero pieno non è abbassarlo: è nominarne il bordo. Abbassarlo ha prodotto un numero falso che nessuno avrebbe potuto ricostruire.»** ✅ **Regola scritta accanto al numero in `dev.py`**: **un rapporto non è un perimetro. Il perimetro è la definizione del denominatore, e deve viaggiare insieme al numero** — *«altrimenti il prossimo lettore rifà proprio la sottrazione che era già stata fatta»*. 📌 **E l'ironia che chiude il caso**: *«l'intero scopo era "scrivere il perimetro così nessuno rimisura per capire cosa significa". **Il numero dichiarato conteneva esso stesso l'errore di denominatore.**»* ✅ **Consegnato: solo commenti in `dev.py`**, zero cambi di comportamento — la dichiarazione di perimetro sopra `_mkdocs_anchor_slugs`, **la nota che `136/158` è sbagliato e perché** (*«così il prossimo che rifà la sottrazione si ferma»*), e una riga di docstring sul `^#{1,6}` che spiega perché un `^{2,6}` saltava i titoli di pagina, *«così nessuno lo "semplifica" di nuovo»* |
| D301 | 18 Set 2026 | 🔴🔑 **Una stringa tradotta in quattro lingue SEMBRA una prova forte di funzione spedita — e più la traduzione è completa, più il segnale falso è convincente** | **I** aveva dedotto **la presenza nella UI** dalla **presenza di stringhe tradotte**, concludendo che `portfolio_optimization` *«l'interfaccia lo mostra in quattro lingue»*. **Verificato dal coordinatore e confermato da I**: **6 foglie × 4 lingue = 24 stringhe tradotte, ZERO consumatori fuori dai file di locale** (controllata anche la composizione dinamica: il cablaggio è **letterale e per-analitica**). 🔑 **La forma dell'errore**: *«Un catalogo i18n risponde **"questa stringa è stata tradotta?"** — **non** "la UI la rende?". È la stessa famiglia di `git diff --stat` letto come "cosa ho cambiato": **un artefatto interrogato su una domanda a cui non risponde**.»* 🔴 **E il corollario è peggiore del caso singolo**: *«**Aphra traduce il catalogo, non la UI raggiungibile.** Quindi una stringa presente in it/fr/es **sembra una prova forte** di funzione spedita — *chi mai tradurrebbe una stringa morta?* — **e più la traduzione è completa, più il segnale falso è convincente**.»* 🔑 **È l'inverso esatto di A68 (D285)**: là **una pagina** attesta codice non spedito, qui lo attestano **le traduzioni**. **Due artefatti che sembrano prove di spedizione, entrambi prodotti da pipeline che non guardano la raggiungibilità.** → *«**E la traduzione è la più persuasiva delle due, perché costa fatica**: nessuno sospetta che si sia pagato per tradurre qualcosa di morto.»* 📌 **Quindi il debito 15 non è pulizia**: finché le 24 stringhe restano, sono **una prova falsa permanente che `portfolio_optimization` è esposto** |
| D302 | 18 Set 2026 | 🔑 **Scrivere la pagina di `portfolio_optimization` avrebbe consegnato il passo 4 senza i passi 1-3: non una pagina inutile, una pagina che ABILITA** | **I**, accogliendo il confine di prodotto: *«"LibreFolio è un tracker, non un consulente" è una ragione che **né "corretto e non spiegato" né "difettoso"** catturavano»*. E la precondizione di `TODO_FUTURI` — *«documentazione utente che spieghi instabilità e limiti **prima** di mostrare i pesi»* — **mette la pagina al quarto posto di quattro**. 🔑 *«Se l'avessi scritta, **avrei consegnato il passo 4 senza i passi 1-3** — cioè **la parte che rende sicuro mostrare i pesi, senza che i pesi fossero stati decisi di mostrare**. **Peggio di una pagina inutile: una pagina che abilita.**»* 📌 **Generalizzazione**: in una sequenza di precondizioni, **consegnare l'ultima per prima non è lavoro anticipato — è la rimozione dell'ostacolo che le altre tre costituivano.** La documentazione di sicurezza è il caso in cui questo pesa di più, perché **la sua presenza viene letta come autorizzazione** |
| D303 | 18 Set 2026 | 🔴🔑 **FORMA 4: la SPECIFICA porta il difetto e l'implementazione lo evita — non danneggia nessuno oggi, ed è per QUESTO che sopravvive** | **I** ha trovato che la glossa naive del CDaR esisteva in **due** posti, e il coordinatore aveva corretto **solo il documento di riferimento** (`06`). L'altra è in **`implementation/N-backend-acquisizioni.md:208`** — **il brief da cui si esegue**, *«identico alla baseline: è il testo come N l'ha ricevuto»*. **Diceva**: *«la media delle discese **oltre** quel quantile»* = **la media aritmetica della coda**, cioè **esattamente lo stimatore che M2 corregge**. 🔑 **E N l'aveva già confutata, parola per parola, senza che nessuno glielo dicesse**: `acquired.py:128-131` porta *«This is **not** the arithmetic mean of the worst `alpha` share of observations… Writing the naive mean here **reproduces exactly the historical CVaR defect this subsystem is being corrected for**»*, e `:143` implementa `quantile + excess / (alpha * len(ordered))`. → **«La docstring di N è la confutazione del brief di N — e il disaccordo non l'ha visto nessuno, perché nessun gate confronta un brief col codice.»** 🔴 **La catena di guasto, e ogni passo è LOCALMENTE RAGIONEVOLE**: **(1)** un revisore confronta codice e brief → *«il codice è sbagliato»* · **(2)** lo corregge **verso il naive** · **(3)** il test **differenziale** di N (`assert computed != approx(naive_mean)`) diventa **rosso** · **(4)** un rosso differenziale **dopo una correzione plausibile** si legge come *«il test è sbagliato»* · **(5)** si rilassa l'asserzione → **si spedisce esattamente il difetto M2 per cui esiste questa campagna.** → **«E il test che avevamo appena dichiarato salvo è l'anello che cede.»** 📌 **Quarta forma, accanto alle tre di D285**: contratto senza codice → **rosso** · codice senza pagina → **silenzio** · pagina senza codice → **falso all'utente** · **specifica sbagliata che l'implementazione evita → nessun danno oggi, e una mina in un documento che sembra più autorevole di una docstring** |
| D304 | 18 Set 2026 | ✅ **Risultato nullo che vale quanto la scoperta: si trasportano solo le trappole che hanno una frase sbagliata FLUENTE a disposizione** | **Misurato da I**: **zero** occorrenze, in tutto il journal, che descrivano l'UCI come **deviazione standard campionaria** o **Bessel**. → **«Il trasporto non è sistematico: è specifico.»** 🔑 **E la ragione è leggibile nella stessa tabella `N:205-209`**: delle quattro righe, **MDD, DaR e UCI sono giuste**; l'unica sbagliata è **l'unica la cui definizione corretta non ha forma breve**. → **«Si trasportano le trappole che hanno una frase sbagliata fluente a disposizione. "La media della coda peggiore" è la frase che viene in mente per il CDaR. L'UCI non ne ha una seducente — e infatti è intatto.»** ✅ **Scansione del coordinatore su tutti e undici i brief** per definizioni a parole di misure che `06` definisce con formula: **una sola occorrenza residua**, in `J:220` — ed **è corretta**, perché porta la qualifica: *«"la media delle giornate peggiori", **con l'ultima contata in proporzione a quanto rientra nel 5 %**»*. **La qualifica È il peso frazionario.** → **Conferma la tesi di I dal lato opposto: J ha evitato la trappola perché non ha lasciato cadere la qualifica**, ed è coperto dalla licenza esplicita di `06:320`. 📌 **E l'ironia che I ha nominato**: **due righe sotto** la definizione sbagliata c'è l'avvertimento `_Rel`/`_Abs`, *«verificato sul sorgente e ricostruito a mano»*, con **l'11 % di scarto senza alcun errore visibile**. **«Siamo stati squisitamente attenti a una trappola di quella tabella e abbiamo scritto l'altra nella colonna delle definizioni.»** |
| D305 | 18 Set 2026 | 📍 **«Un numero di riga non è confrontabile fra alberi, quindi ho verificato la domanda che sopravvive allo scarto: quante istanze esistono»** | Il coordinatore citava `06:1331`, I aveva misurato **`06:1132`** — **l'albero del coordinatore è cresciuto di ~200 righe**. ✅ **I non l'ha trattato come un rilievo**: *«divergenza benigna. **Ma è la ragione per cui non ho verificato "la 1331 è giusta"** — un numero di riga non è confrontabile fra alberi. **Ho verificato la domanda che sopravvive allo scarto: quante istanze esistono.**»* 🔑 **Quinta applicazione della regola di B, e la prima in cui viene usata per SCEGLIERE LA DOMANDA invece che per correggere un indirizzo.** → **Quando un indirizzo non è confrontabile, si cambia la grandezza misurata: da "è a quella riga?" a "quante ce ne sono?"** — e la seconda **ha trovato un'istanza che la prima avrebbe mancato** |
| D306 | 18 Set 2026 | 🔴🔑 **`git cat-file -t` è AGNOSTICO ALLA RAGGIUNGIBILITÀ: nessun ramo contiene i tre SHA su cui poggia tutta la verifica incrociata della campagna** | **I** ha corretto la propria regola (D297) e la ratifica del coordinatore: ```git branch -a --contains 1aaea6949 → VUOTO``` ```git for-each-ref --contains 1aaea6949 → refs/copilot/checkpoints/da23d09e-… UNICO ref``` ✅ **Generalizzato dal coordinatore su tutti e tre gli SHA usati per il relay**: **N `1aaea6949` → 0 rami · D `1a537f9cb` → 0 rami · B `590f32ae6` → 0 rami.** 🔑 **La forma dell'errore**: *«ho usato `git cat-file -t` — strumento **agnostico alla raggiungibilità** — per una domanda **di raggiungibilità**. Prova che l'oggetto **esiste nello store**; non dice nulla su quale ref lo raggiunga. **La risposta che dà è vera e rassicurante, e non è la risposta alla domanda.**»* ✅ **Regola corretta**: **`cat-file -t` prova l'ESISTENZA; serve `git branch --contains` per la STORIA** — *«due domande diverse, e **il primo comando non accenna che la seconda esista**»*. 🔴 **E la conseguenza operativa è una SCADENZA**: *«un ref di checkpoint è **cancellabile**, e alla cancellazione il commit diventa **irraggiungibile e potabile dal GC**. **I sei fatti che ho verificato poggiano su un oggetto che può sparire.**»* → **Il commit dei mandati non è solo il passo successivo: è ciò che trasforma una prova temporanea in una permanente.** **Chi consegna dopo la potatura deve rimisurare da zero.** 📌 **E il vincolo §23 si estende**: `ulcer-index`, `drawdown-at-risk` e `conditional-drawdown-at-risk` **non erano coperte** — sono nello stesso stato di VaR/CVaR, **solo con prova migliore** |
| D307 | 18 Set 2026 | 🔑 **Tre artefatti interrogati su domande a cui non rispondono, in tre turni consecutivi — e ogni volta la risposta era VERA** | Tassonomia di **I** sui propri tre errori consecutivi: \| artefatto \| risponde a \| è stato letto come \| \|---\|---\|---\| \| `git diff --stat` \| cosa è **tracciato** \| «cosa ho cambiato» \| \| **catalogo i18n** \| la stringa è **tradotta** \| «la UI la mostra» \| \| `git cat-file -t` \| l'oggetto **esiste** \| «è **in storia**» \| 🔑 **«Ogni volta la risposta era vera. Il difetto non era nello strumento: era nella DOMANDA CHE GLI SI ATTRIBUIVA.»** 📌 **È la generalizzazione dell'inquadramento difettoso (D260) applicata agli strumenti invece che ai confronti**: una sonda difettosa si scopre rimisurando; **uno strumento giusto interrogato male dà sempre la stessa risposta corretta, e non c'è rimisurazione che lo riveli.** ✅ **La cura è la stessa dei numeri**: **dichiarare a quale domanda lo strumento risponde**, non solo che cosa ha detto |
| D308 | 18 Set 2026 | ⚠️ **Settimo relay stantio, e il primo in cui l'ORDINE conteneva il difetto che l'esercizio doveva impedire** | Il coordinatore ha ordinato a **I**: *«scrivi il perimetro delle 22 H1 e chiudi. **`136 / 158`**»*. **Il perimetro era scritto dal passo 53** (`dev.py:1078-1090`) **e le righe `1089-1090` contengono la confutazione esplicita di `136/158`** — cioè la cifra ordinata. 🔑 *«È la sesta volta che chiedi una cosa fatta, ma **la prima in cui l'ordine contiene il difetto che l'esercizio doveva impedire**.»* 📌 **Le tre cure di canale adottate (piano vivo, registro §18.5, tabella in testa) non coprono questo caso**: coprono *«questa cosa è già fatta»*, **non** *«la cifra che ordini è quella che il lavoro ha confutato»*. **La quarta cura — allegare lo stato presunto a ogni ordine (D255) — l'avrebbe preso**, e non era stata applicata. ✅ **E I ha dichiarato un proprio terzo difetto di sonda nello stesso turno**: la prima rimisurazione del perimetro ha scandito **tutta** la documentazione invece delle 22 pagine → **`226 H1`**, colta *«entro un comando, perché **226 titoli per 22 pagine è impossibile**»*. **La regola A70 — stampare la quantità che permette di falsificare la propria tabella — ha retto due volte nello stesso turno** |
| D309 | 18 Set 2026 | 🔴🔴🔑 **Il client valida le risposte con Zod: un campo nuovo viene CANCELLATO in silenzio, un valore d'enum nuovo RIFIUTA l'intera risposta — due comportamenti opposti dallo stesso meccanismo** | Trovato da **E**, verificato dal coordinatore **eseguendo gli schemi**. `zodios-client.ts:170` → **`validate: 'response'`**. **(1) Campo nuovo → cancellato.** `RiskKpiOutput` è `z.object({...})` **senza `.passthrough()`** — e il generatore ne emette **90 altrove**, quindi *può* farlo. Misurato: input con `worst_realization: -0.0385` e `ulcer_index: 0.04` → **`success: true`**, chiavi tenute **`kind, max_drawdown, max_drawdown_duration_days, volatility`**. **I due campi di N spariscono senza errore né warning: il lettore vede `undefined`.** **(2) Valore d'enum nuovo → rifiuta tutto.** `generated.ts:11349` → `RiskReturnBasis = z.enum(['price_only','twrr'])`. Misurato **con controllo**: `safeParse('twrr')` → **OK**; `safeParse('current_composition_backtest')` → **REJECTED (`invalid_enum_value`)**. E `return_basis` è **obbligatorio** in `RiskResultMetadata` (`isOptional()` → **`false`**) → il fallimento **propaga al contenitore**. ⚠️ **Livello di prova dichiarato**: il coordinatore ha verificato **i due anelli atomici** (enum rifiuta con controllo positivo; campo obbligatorio) **ma NON è riuscito a replicare il parse di una `RiskQueryResponse` completa** — il payload di prova restava incompleto, e **un confronto in cui anche il baseline fallisce non discrimina**. **La catena end-to-end è di E, che l'ha eseguita con il controllo (`today → parse OK`, `afterC → parse FAIL`).** 🔑 **E il difetto trovato dal coordinatore nella PROPRIA sonda è la lezione**: il primo tentativo dava *«PARSE FAIL»* su entrambi i rami **per un campo che mancava a lui** — *«il baseline deve passare, altrimenti il confronto non significa niente»*, la condizione che E aveva rispettato e lui no |
| D310 | 18 Set 2026 | ✅ **`generated.ts` è GITIGNORED e il build lo rigenera abortendo se fallisce: non c'è nessun merge da ordinare, e la produzione è protetta per costruzione** | La domanda di **E** era: *«chi lancia `api sync`, e prima di quale merge?»*, presupponendo che `generated.ts` fosse **un file condiviso da versionare**. **Misurato dal coordinatore**: `git ls-files --error-unmatch` → **NON tracciato**; `git check-ignore -v` → **`frontend/.gitignore:13`**; idem `openapi.json`. → **Non entra in nessun merge.** ✅ **E `dev.py:563-566`**: ```print("Syncing API types before build..."); sync_result = cmd_api_sync(args); if sync_result != 0: print_error("API sync failed - aborting build")``` → **il build del frontend rigenera SEMPRE e ABORTA se non ci riesce.** *(Osservato anche in esecuzione durante il ripristino dei `node_modules`: «Syncing API types before build… ❌ API sync failed - aborting build».)* 🔑 **Quindi la risposta è: nessuno, e prima di nessun merge.** **Produzione e CI sono protetti per costruzione.** 🔴 **Ma il rischio non sparisce: si sposta, e cambia natura.** Ogni **albero di sviluppo** ha un `generated.ts` stantio sul disco. **Dopo l'integrazione del backend di C, chiunque esegua un gate frontend senza rigenerare vedrà tutti e quattro i pannelli di rischio in errore** — e concluderà che qualcuno ha rotto il codice. → **È un difetto di DIAGNOSI, non di prodotto: il sintomo è catastrofico e la causa è un artefatto locale stantio.** 📌 **Stessa forma del «rosso bugiardo» previsto da F** (D226): **un rosso che accusa la cosa sbagliata.** ✅ **Vincolo operativo**: **dopo l'integrazione del backend, prima di qualunque gate frontend, ogni albero esegue `./dev.py api sync`** |
| D311 | 18 Set 2026 | 🔴 **Il criterio d'innesto «costruisci dove entrambi i rami sono esercitabili» è INSODDISFACIBILE per ogni contratto che cambia la forma del payload — e la premessa sbagliata era del coordinatore** | **E** ha mostrato che il criterio di D290 **non si applica a K8 e K1**: *«presuppone che **una fixture possa portare il campo**. **Non può**: Zod lo toglie prima che il mio componente lo veda. Una fixture E2E con i campi nuovi **non esercita il ramo "presente" — lo esercita come assente, e passa**.»* 🔑 **«È esattamente il mock stantio del README, ma prodotto dal client invece che dal mock.»** ⚠️ **E l'innesto K4 di E è coinvolto per una premessa del coordinatore**: gli era stato detto *«`return_basis` arriva sul filo come stringa, quindi il confronto letterale funziona oggi e dopo»*. **Era falso — il filo è validato.** Il codice resta giusto e i test veri, **ma il ramo non può accendersi finché `generated.ts` non conosce il valore**. → **Non è da rifare: è in attesa di `api sync`, come K8 e K1.** ✅ **Criterio corretto**: il ramo «campo presente» è esercitabile **solo dopo che lo schema generato conosce il campo**. Prima di allora **non si costruisce lasciando il posto: si aspetta**, perché la fixture **mente in modo indistinguibile dal caso reale** |
| D312 | 18 Set 2026 | ✅ **Due difetti veri trovati da E chiudendo K3, entrambi suoi e entrambi con la mutazione guardata fallire** | **(1) Il benchmark persistito non si calcolava mai a freddo.** `L3Benchmark` lanciava mentre `controller.catalog` era ancora `null`; **`runSingle` ha una guardia di capability che ritorna `null` in silenzio**, e **il latch era già scattato**. **A schermo: nome del benchmark presente, beta `—`.** Trovato da `test-author`, confermato sul codice, corretto facendo dipendere l'effetto dal catalogo. **Pinnato con il codice pre-fix come mutazione**: `Expected: "9" / Received: ""`. **(2) Una scelta PERMANENTE veniva buttata come una domanda ESTEMPORANEA.** `applyBaseSignature` → `discardOnDemand()` annulla i risultati on-demand e rilancia **solo quelli in volo**: giusto per uno stress chiuso, **sbagliato per un benchmark che il lettore si aspetta continui a valere**. ⚠️ **E non è il caso limite ipotizzato**: `applyBaseSignature` gira da un `$effect` su `inputs()`, quindi **qualunque cambio di periodo o di scope ci passa** — *«restringi il periodo con un benchmark attivo e il confronto spariva»*. ✅ **E E non ha toccato la policy del controller**: il test `does not re-issue an analysis that was not running` **la fissa apposta ed è giusta**. Ha aggiunto un contatore reattivo `baseEpoch` (**puramente additivo**) e `L3Benchmark` ri-chiede **una volta per movimento del terreno**. Mutazione provata: `expected 0 to be greater than 0`. 📌 Gate: `front check` **0/41/2** (baseline esatta) · `risk-controller-unit` **14** · `risk-levels-unit` **83** · `risk` E2E **9** · `front format --check` pulito |
| D313 | 18 Set 2026 | ✅ **`warnings` è l'unico innesto che il criterio corretto ammette oggi — e si può fare PROPRIO PERCHÉ è vecchio** | Domanda di **E** prima di congelare: chiudere il debito dei `warnings` non resi da nessun livello? **Il coordinatore l'ha misurato prima di autorizzare**, perché dopo D309 nessun via libera su un campo è dato senza verificare lo schema: ``` RiskAnalyticResult   warnings presente: ✅ SÌ (opzionale: true) RiskResultMetadata   ❌ no RiskQueryResponse    ❌ no ``` 🔑 **`warnings` è già nel contratto generato: non è un campo di N, è VECCHIO.** → **Zod non lo cancella**, il payload lo porta **già oggi**, e **una fixture coi warnings esercita davvero il ramo "presenti"**. → **È l'unico innesto di E che soddisfa il criterio corretto** (entrambi i rami esercitabili adesso): **K1, K8 e il ramo K4 restano in attesa perché sono NUOVI; questo si fa perché è VECCHIO.** ✅ **Ragione di merito, di E**: *«il lettore vede **che** un risultato è degradato, non **perché**. È la metà mancante della trasparenza che L1-L4 esistono per dare»* — **stessa forma dell'informativa della simulazione (D202): limiti elencati, e omesso quello che conta.** ⚠️ **Vincolo dato**: renderli **senza interpretarli**, testo del backend così com'è — **mapparli su chiavi i18n costruite a runtime ricreerebbe il difetto di `RiskResultFrame:108`** (D288) |
| D314 | 18 Set 2026 | ⛔ **La `correlation` richiesta e non resa NON è uno spreco: è l'ingresso di un consumatore che non è ancora nell'albero** | **E** aveva dichiarato come debito che `correlation` è richiesta per lo scope `portfolio` e **non resa da nessun livello** — *«un round trip **O(n²)** pagato per niente a ogni onda base»* — e non l'aveva rimossa perché tocca pagine non sue. ✅ **Decisione: non toccarla, e il proprietario esiste già — è F**, che ha **una heatmap di correlazione nel proprio worktree**. 🔑 *«Il giro O(n²) **non è pagato per niente**: è pagato per un consumatore che **non è ancora nel tuo albero**. Rimuovere la richiesta oggi significherebbe **spegnere l'ingresso di una funzione che sta per arrivare**.»* 📌 **Debito #1 a nome di F, post-integrazione**, ora con **la misura del costo di E accanto**. **La decisione se tenerla o toglierla si prende quando i due pezzi sono nello stesso albero — non prima, perché oggi nessuno dei due può vedere l'altro.** 🔑 **Terza istanza della forma**: un difetto apparente che è tale **solo dal punto d'osservazione di un singolo albero** — dopo il rosso di F causato da D73 (D225) e i due `coverage` omonimi |
| D315 | 18 Set 2026 | 🔑 **«La misura vale per il baseline verde, non per il rosso» — due persone, lo stesso errore, lo stesso campo mancante, a ore di distanza** | Tentando di replicare la catena Zod end-to-end, **il coordinatore ha costruito un payload incompleto** e ottenuto `PARSE FAIL` su **entrambi** i rami — `@ analyzed_range`, poi `@ algorithm_version`. **E aveva avuto esattamente lo stesso esito**: *«il mio primo tentativo ha avuto **lo stesso identico esito del tuo** — `PARSE FAIL` su entrambi i rami per un campo che mancavo io (`analyzed_range`) — e **l'ho tenuto solo perché il confronto non discriminava**»*. ⚠️ **Differenza**: E l'ha **scartato prima di pubblicarlo**; il coordinatore ha dovuto **scriverlo per accorgersene**. 🔑 **Formulazione di E, adottata**: **«La misura vale per il baseline verde, non per il rosso.»** → **Un rosso su entrambi i rami non è una misura: è un payload sbagliato.** Solo il **verde del controllo** rende il rosso della variante un'informazione. 📌 **È la forma compatta di ciò che B aveva scoperto bisezionando** (*«la bisezione ha smentito l'ipotesi perché OGNI rimozione restava rossa»*) e di ciò che I ha usato sul GBM (*«una prova di rifiuto senza una prova di accettazione non dimostra che la causa sia quella dichiarata»*) |
| D316 | 18 Set 2026 | 🔴🔑 **«Un verde che non copre nulla ha lo stesso aspetto di un verde che copre tutto» — il mock stantio nella variante peggiore: NESSUNA fixture** | **E**, implementando i `warnings`: *«**Dopo l'implementazione la suite diceva 10 passed e non significava niente.** L'unico warning del mock è su `correlation` (`:286`), che **nessun livello rende**: la funzione **non veniva mai eseguita**.»* 🔑 *«È il mock stantio nella variante peggiore — **non una fixture vecchia, nessuna fixture** — e **un verde che non copre nulla ha lo stesso aspetto di un verde che copre tutto**.»* ✅ **Chiuso con un'opzione opt-in** (fixture esistenti **byte-identiche**) **e quattro mutazioni guardate fallire**: filtro su `degrades_result === false` → `expected [] to deeply equal [ 'Treated as Other at 100%.' ]` · deduplica per `code` invece che per frase → `expected [ {…} ] to have a length of 2 but got 1` · il pannello smette di passare `reasons` a L1 → `element(s) not found` · `occurrences` mai incrementato → `Expected: "2" · Received: "1"`. 📌 **E `test-author` ha trovato una trappola che la consegna di E non prevedeva**: `resultFor` scrive `warnings` **due volte**, e il ramo `correlation` **sostituisce** l'array → *«un'iniezione fatta dove l'avevo suggerita io sarebbe stata **accettata e scartata in silenzio**»*. **Più una mutazione aggiunta di propria iniziativa**, perché la prima *«si fermava alla presenza e non toccava mai la pretesa sul conteggio»* |
| D317 | 18 Set 2026 | ✅ **I `warnings` si rendono VERBATIM e NON filtrati su `degrades_result` — il flag decide lo stato, non il significato** | **E** ha motivato la scelta contro il proprio vincolo iniziale: *«`degrades_result` decide lo **stato**, che è già a schermo; **l'unico punto che lo mette a `false`** segnala che i metadati di settore mancavano e l'asset è stato trattato come **"Other al 100 %"** — **il numero non è degradato, il significato dello shock sì**. Filtrarlo avrebbe nascosto **proprio la frase che spiega la forma sullo schermo**.»* ⚠️ **E un secondo fatto misurato che il coordinatore non aveva**: **`partial` non implica «c'è un warning»** — `service.py:736` rende partial una wave per **quattro** cause (warning degradante, `context_exclusions`, `excluded_assets`, data quality) e **solo la prima è un warning**. → *«Filtrare e mostrare vuoto avrebbe riprodotto il difetto **proprio nei casi più frequenti**.»* 🔑 **La forma generale**: **un flag che qualifica un risultato non qualifica le spiegazioni di quel risultato.** Usarlo per filtrare le seconde è una **conflazione di livelli**, e produce un vuoto esattamente dove la spiegazione serve di più |
| D318 | 18 Set 2026 | ✅ **Mandato E chiuso e `FROZEN`: tutti e dieci i mandati sono ora congelati** | `front check` **0 errori / 41 avvisi / 2 file** (baseline esatta) · `front format --check` pulito · `risk-unit` **16** · `risk-controller-unit` **14** · `risk-levels-unit` **89** · `risk-benchmark-unit` **8** · `risk` E2E **11** (21,5 s) · `i18n audit` **2828 chiavi, 0 incomplete**, unused invariato a **122** · `test check-orphans` **78 spec + 202 unit tutti raggiungibili** · `git diff --check` pulito · **6244 nessun listener** · HEAD invariato **`cc33120eb`**, **nessun commit, nessuno stage**. ✅ **Vincolo numero uno rispettato con margine**: file più grande **494** righe (`levelHelpers.ts`), `RiskLevelsPanel.svelte` **206**, ogni componente L1-L4 **≤ 208**. **Nessun file oltre 600**, e **il pannello è uno solo**: Dashboard e Broker Detail montano lo stesso componente, distinti **dal solo scope**. 📌 **Delta misurato dal coordinatore: 13 tracciati + 25 nuovi = 38 file** → **`git add -u` da solo perderebbe il 65 %**, il caso peggiore di tutti e dieci i mandati (§22). ✅ **Sei debiti dichiarati con proprietario**, fra cui il **test 1 a 4 worker**: *«0 hit su 6 run — non riprodotto, non confutato. **Non toccato: allargare il timeout di un altro su una sola osservazione è indovinare.**»* |
| D319 | 18 Set 2026 | ⛔ **J non è creabile oggi, e la ragione non è una preferenza: il suo baseline non esiste** | Con tutti e dieci i mandati `FROZEN`, l'unico passo di coordinamento rimasto sarebbe **creare J**. **Misurato**: `git log --oneline -1` su ogni ramo di mandato → **tutti a `cc33120eb`**, la baseline. → **Nessuna riga del lavoro dei dieci mandati è in un ramo.** 🔑 **J è il mandato della CHIUSURA**: verifica trasversale, galleria, CHANGELOG, i quattro cancelli ereditati. **Tutti e quattro i suoi compiti presuppongono il codice integrato.** Crearlo ora significherebbe **dargli un worktree in cui il lavoro da chiudere non esiste** — e la regola della campagna dice che **un mandato non si avvia da un baseline indovinato**. ⚠️ **E il suo primo compito sarebbe già impossibile**: `api sync` (§10 del suo brief) **non ha nulla da sincronizzare** finché i backend di A, C e N non sono nell'albero; il cancello dell'enum (§11.2) **non ha un terzo membro da cercare**; la galleria non ha componenti nuovi da fotografare. 📌 **Precondizione esatta per crearlo**: **i dieci mandati committati e integrati** (§27, passi 1-3). Prima di allora J **non è rinviato per prudenza: è privo di oggetto**. ✅ **E il suo brief è pronto e completo** — **694 righe**, con §8 (i quattro cancelli), §9 (i due rename di schema), §10 (`api sync` e il rosso bugiardo) e §11 (i quattro vincoli operativi, ognuno con la misura che lo ha prodotto) |
| D320 | 18 Set 2026 | 🔴🔑 **La motivazione della decisione «array vuoti» era INVERTITA rispetto a ciò che il codice fa: il sistema non sceglie al posto dell'utente — RIFIUTA** | Il coordinatore aveva deciso gli array vuoti motivandoli con *«senza i controlli il sistema sceglie al posto dell'utente, quindi deve dichiarare cosa ha scelto»*. **E l'ha verificata contro il codice prima di dire che la rispettava. Tre fatti, tutti riverificati dal coordinatore:** **(1)** `RiskScenarioMissingHistoryPolicy` (`schemas/risk_scenarios.py:39-40`) ha **esattamente un valore**: `MANUAL_PROXY_OR_EXCLUDE`. **Non esiste una politica automatica.** **(2)** `risk_plugins/stress.py:452-466` itera sugli asset e al **primo** privo di serie utilizzabile solleva **`RiskUnavailableError(INSUFFICIENT_HISTORY)`** — *«Asset {id} requires a manual proxy or explicit exclusion»*. **Non degrada la risposta: la nega per intero.** **(3)** `risk/service.py:513` → `excluded_asset_ids = tuple(getattr(plan.params, "excluded_assets", ()))`: **l'audit è uno specchio della richiesta, il backend non aggiunge mai esclusioni proprie.** 🔑 *«Con gli array vuoti per sempre l'audit direbbe stabilmente **"0 proxy, 0 esclusi, 0,0 %"**: **una frase vera che non informa di nulla**, appesa a **un replay che non è mai partito**.»* 🔴 **E il caso che rende la decisione dannosa**: *«su un portafoglio con **un solo** titolo non prezzabile il replay non partirebbe **mai** — non degradato, **assente**, con un avviso ambra e **nessuna via d'uscita**. **Quinta occorrenza di una difesa che non difende.**»* ✅ **Consegna di E, conforme alla lettera dove è implementabile**: `proxyAssets: []` sempre; `excludedAssetIds: []` **alla prima richiesta** — *«che è la richiesta di cui parla la decisione»*; audit reso, **e `excluded_weight_total` è l'unico dato che il client non saprebbe calcolarsi** (verificato: `schemas/risk.py:220`, con validatore a `:248` che ne impone la coerenza coi pesi). ➕ **In più**: quando il backend rifiuta, il lettore esclude quel titolo e ritenta — **l'unica uscita che la politica unica consente**. Esclusioni **accumulate** (il backend rifiuta **un asset per volta**), **visibili** come pastiglie, **reversibili**, azzerate al cambio di periodo. ⚠️ **Avvertimento di E prima di propagare a F**: *«se costruisce "un proxy vero per l'insieme" partendo da "il sistema sceglie e dichiara", **costruisce contro un enum che ha un solo valore manuale**. Un proxy automatico è **lavoro di backend**, non di pannello.»* |
| D321 | 18 Set 2026 | 🔑 **«Sei verdi valgono più di cinque ipotesi»: la prima mossa di un'indagine non deve essere un'ipotesi** | **E**, completando la regola che il coordinatore aveva registrato (*un `ReferenceError` in un `.svelte.ts` non fallisce dove nasce*): *«è la metà che si vede. **L'altra metà è che la prima mossa non deve essere un'ipotesi**: avevo **cinque piste plausibili** e ho **ripristinato il file per rimisurare**, perché **sei verdi valgono più di cinque ipotesi**. **Senza quel passo avrei debuggato la lane.**»* 🔑 **La forma generale**: di fronte a un sintomo con molte cause plausibili, **la mossa che le elimina tutte insieme è tornare a uno stato noto e rimisurare** — non sceglierne una e verificarla. **Cinque ipotesi si testano in cinque giri; un ripristino le chiude in uno.** 📌 **È la stessa cosa che B ha scoperto bisezionando** (*«la bisezione ha smentito l'ipotesi perché **ogni** rimozione restava rossa»*) e che il coordinatore ha usato leggendo due alberi invece di uno: **misurare il contesto prima di misurare il sospetto** |
| D322 | 18 Set 2026 | 🔴 **Il `coverage ≈ 100 %` accanto a uno Sharpe lusingato È IN PRODUZIONE OGGI — su superfici di nessuno dei due, e l'avvertimento stava per andare al mandato sbagliato** | **E** ha verificato dove `metadata.coverage` è reso, e il coordinatore ha riverificato nella baseline: `RiskResultFrame.svelte:99-100` rende **`{$t('risk.metadata.coverage')}`**, e `RiskAnalysisPanel.svelte:833` monta quella cornice su **`result={kpiResult}`**, cioè `historical_kpi` — **che porta Sharpe e Sortino**. E il pannello legacy serve `asset` e `asset_set` → **Asset Detail (parcheggiato D8/D47) e il laboratorio di F**: **il ramo titolo, dove la lusinga di A9 è massima**. 🔑 *«Il `coverage ≈ 100 %` accanto a uno Sharpe lusingato **esiste in produzione oggi**, su superfici che **nessuno dei due possiede** — ed è comportamento **preesistente che ho conservato byte per byte di proposito**. **Io sono l'unico che NON lo mostra.**»* ✅ **Destinatario corretto: F e chi deciderà su D8/D47** — il coordinatore stava per darlo a E. ✅ **E conseguenza per L3 verificata assente**: `buildRiskAdjusted` prende sortino/sharpe/volatilità da `historical_kpi` e beta dal confronto → **non esiste una card col rapporto del benchmark**; *«il solo numero relazionale è il beta — rapporto di co-momenti, in cui **un fattore di annualizzazione comune si cancella**»*, **dichiarando di non aver misurato l'allineamento delle serie**. 📌 **E la versione fra pagine non morde**: il report TWRR **filtra per broker**, quindi Dashboard e Broker Detail prendono **entrambe** il ramo TWRR; morderà **con la fetta `asset_ids` di C — lo stesso innesco della dichiarazione di backtest già scritta**. *«Una superficie, due avvisi»* |
| D323 | 18 Set 2026 | 🔑 **«La barra difende sé stessa, il costruttore difende ogni consumatore»: il criterio per distinguere una difesa doppia da una ridondante** | **E** ha verificato che la guardia `NaN` di D non lo tocca — *«e non per fortuna»*: `buildDivergenceRows` scarta la riga se `finite()` rifiuta `weight` o `percentage_contribution`, e `tornadoRows` fa lo stesso con `toNumber()`, che controlla `Number.isFinite` **su entrambi i rami**, numero e stringa. ⚠️ **Ma se un `NaN` ci arrivasse, il danno di E sarebbe PEGGIORE**: le sue scale sono `Math.max(0.01, ...rows.map(…))` e `Math.max(...rows.map(…), Number.EPSILON)` → **`Math.max` con un solo `NaN` nello spread restituisce `NaN`** → **la scala si avvelena** → **tutte** le barre del pannello congelano la larghezza precedente, **non una**. 🔑 **Conclusione, adottata come criterio**: *«**La barra difende sé stessa, il costruttore difende ogni consumatore. Due difese in posti diversi, non una ridondante.**»* → **Il criterio per decidere se una difesa doppia è ridondanza non è "quante volte", ma "quanto è largo il danno che ciascuna previene"** |
| D324 | 18 Set 2026 | ✅ **Sesta convergenza indipendente, e la prima fra una MISURA e una LETTURA: E ha trovato la riga che spiega i numeri di A senza averli visti** | **E** ha letto `metrics.py:140-144` → `daily_risk_free_rate` è **`expm1(log1p(rate)/365)`**, **il 365 fisso e indipendente da `annualization_factor`**; `annualized_sharpe:158` e `annualized_sortino:173` lo sottraggono a ogni osservazione di una serie che sul ramo titolo ha **~252 punti l'anno** → **si deducono 252 unità di rf invece di 365** → `excess_mean` gonfiato. ✅ **Verificato dal coordinatore nella baseline**, riga per riga. 🔑 **Tre conseguenze dedotte da E che coincidono con le misure di A (200 serie × 3 tassi) senza che le avesse viste**: proporzionale a rf → **esattamente zero a rf = 0** · crescente da 2 % a 5 % · **peggiore sul Sortino**, perché lo stesso errore al numeratore è diviso per una `downside_deviation` più piccola della volatilità. ⚠️ **E la falsificazione che E ha offerto**: *«se la cura di A9 tocca `√f` invece di `daily_risk_free_rate`, il delta a rf = 0 **non resterebbe zero**»*. → **A ha corretto esattamente `daily_risk_free_rate`**, rendendo `periods_per_year` **obbligatorio**: **la previsione è verificata a posteriori**, e lo `0,0000` a rf = 0 è la prova che ha toccato la riga giusta. 📌 **Prima convergenza fra una misura e una lettura** — le cinque precedenti erano fra due misure o fra due letture |
| D325 | 18 Set 2026 | ✅ **Autorizzato un commento di due righe: una descrizione sbagliata in un file di IMPLEMENTAZIONE è letta da chi implementa** | **E** ha chiesto autorizzazione per correggere `levelHelpers.ts:433`, che documenta la NEA come *«Correlation-blind **count** of equally weighted equivalents»* — **esattamente la parola che l'emendamento K8 dichiara falsa** (un *conteggio* non può superare il totale, e la NEA può, per effetto della cassa). ✅ **Autorizzato**: da «count» a **indice di concentrazione**, più il requisito del peso di cassa. **Zero codice eseguibile, nessun gate da rifare.** 🔑 **Ragione per cui non si lascia al mandato futuro**: *«**chi cablerà il renderer leggerà quel commento**»*. → **È D227 applicata a un commento**: un fatto corretto in chat e non scritto dove verrà letto **è una conversazione**, e **il posto dove verrà letto è quel file**. ✅ **E la verifica strutturale di E sull'emendamento**: *«"mai da solo" è **strutturale**: `Concentration` è un unico valore nullable con **entrambi** i campi, quindi un chiamante **non può** renderne uno per sbaglio. **Non è una linea guida se il tipo la impone.**»* |
| D326 | 18 Set 2026 | 🔴🔑 **K1, K6 e K8 sono «costruiti, provati e SPENTI»: i loro campi non esistono in nessuna baseline, e un `api sync` oggi uscirebbe verde producendo zero tipi** | **E** ha verificato prima di eseguire un ordine del coordinatore: `grep "RiskSimulationRegime\|regime_declared_days\|bootstrap_seed"` sul **proprio** `schemas/risk.py` → **zero**. ✅ **Generalizzato dal coordinatore su tutti e tre i contratti**: \| campo \| baseline \| albero d'origine \| \|---\|---:\|---:\| \| `RiskSimulationRegime` · `bootstrap_seed` · `block_length_days` \| **0** \| **H: 4 · 1 · 5** \| \| `return_bins` · `var_bin_edge` · `underwater_series` \| **0** \| **A: 4 · 2 · 1** \| \| `worst_realization` · `ulcer_index` · `diversification_ratio` \| **0** \| **N: 2 · 2 · 1** \| 🔑 *«`api sync` legge **la mia** OpenAPI, non quella di H. Girerebbe, **uscirebbe verde e produrrebbe zero tipi nuovi**. **Non è che non serve: è che sembrerebbe servito.**»* → **Stessa famiglia del gate cieco e del mock stantio, applicata a un comando di sincronizzazione.** ✅ **Stato onesto di K6 dichiarato da E**: *«`SimulationProvenance` è **costruita, testata unitariamente e cieca sul filo**. Zod cancella i campi che non sono nello schema, quindi il componente rende le voci che ha e **omette in silenzio** regime, seme e lunghezza del blocco. **I miei unitari passano perché costruiscono l'output direttamente, scavalcando il filo.**»* ⛔ **«Non contare K6 come reso.»** → **Costruito, provato, spento.** **Si accende all'integrazione, e l'unico modo di verificarlo è un `api sync` eseguito DOPO la fusione di H — non prima, e non nell'albero di E** |
| D327 | 18 Set 2026 | 🔑 **«Un'etichetta di regime nomina la TRASFORMAZIONE, mai l'ESITO» — e la trappola si riapre aggiungendo una stringa, senza che nulla diventi rosso** | **E** ha verificato di non avere la stringa incriminata (`grep "35" en.json` → **zero**) e ha spiegato **perché non è fortuna**: `risk.levels.l4.provenance.values` **non contiene alcuna etichetta di regime** (solo `gbm`, `mc`, `qmc`, `historical_log_mle`, `sample_log_returns`, `current_buy_and_hold`) · `regimeValue` è **«{regime}, declared over {days} days»**, cioè **dichiara un'ipotesi, non promette un esito** · `valueLabel()` ha un fallback `{default: value}` che rende **il token del backend** — *«non avevo mai visto l'enum di H, e **inventare una didascalia per un valore mai visto** era il difetto che stavo evitando»* · le didascalie dei preset vengono dal **catalogo backend** e il `-0.35` è **un bucket shock modificabile, non una promessa** · la riga di esito **riporta** il numero calcolato invece di anticiparlo. ⚠️ **Ma il fallback lascia aperta una trappola, registrata ora che il caso è fresco**: *«il giorno in cui qualcuno aggiunge `provenance.values.crisis = "un calo del 35 %"`, reintroduce **esattamente** ciò che H ha appena chiuso — e **niente diventa rosso, perché è l'aggiunta di una stringa**»*. ✅ **Regola adottata**: **un'etichetta di regime nomina la TRASFORMAZIONE, mai l'ESITO** — *«"moltiplica per 0,65", non "cala del 35 %". **La differenza fra le due è tutta la distanza fra un'ipotesi e una promessa.**»* |
| D328 | 18 Set 2026 | ✅ **L'uguaglianza di quattro conteggi È la prova che nessuna lingua è andata alla deriva** | **E**, verificando il proprio delta i18n invece di dichiararlo pulito: *«i quattro cataloghi cambiano di **92 righe esatte ciascuno**: **l'uguaglianza dei quattro conteggi È la prova** che nessuna lingua è andata alla deriva»*. 🔑 **Forma generale**: quando un'operazione deve applicarsi identicamente a N artefatti paralleli, **l'invariante da misurare non è il contenuto ma l'uguaglianza dei conteggi** — **una sola grandezza che copre tutte e quattro le lingue**, e che **fallisce visibilmente** se una diverge. 📌 **È il controllo strutturale di I** (*«cerca le pagine troppo corte»*) applicato a un delta invece che a un corpus |
| D329 | 18 Set 2026 | ✅ **Le due domande di E a K1 avevano già risposta nel contratto di A — e A non è stata svegliata** | **E** ha chiesto due garanzie *«finché la firma è aperta»*, dopo aver verificato che il resto di `RiskDrawdownOutput` era già ricco. **Il coordinatore le ha lette nell'albero di A invece di relayarle.** **(a) `underwater_series` porta una data per punto? SÌ**: `schemas/risk.py:1021` → `List[RiskDrawdownPoint]`, e `:984-992` → **`class RiskDrawdownPoint(StrictModel)` con `date: date`**, docstring *«One **dated** point of the underwater (peak-relative) drawdown curve»*. → **Il problema descritto da E non si presenta.** 🔑 **Ma la sua diagnosi resta valida anche senza il caso**: *«il client dovrebbe **inventare un fatto che il server conosce**, e **una curva disegnata su date indovinate si legge esattamente come una disegnata su date misurate**»* — stesso rifiuto opposto all'inferenza della base K4. **A ha fatto la scelta che E avrebbe preteso.** **(b) `var_bin_edge` è un valore** (`:857`, `Optional[FiniteFloat]`), **ma il confronto float temuto non serve**: A ha risolto **dal lato dei bin** — *«The interval is **half-open `[lower_bound, upper_bound)`**»* → la barra che contiene il taglio è determinata da **`lower <= edge < upper`**, **una disuguaglianza, non un'uguaglianza**. **Nessuna tolleranza, nessun rischio di evidenziare la barra sbagliata.** ⚠️ **Livello di prova dichiarato a E**: **letto, non eseguito** — il ramo esatto è verificabile solo quando i bin sono sul filo, cioè **dopo `api sync` post-fusione di A** |
| D330 | 18 Set 2026 | 🔑 **Il rovescio della famiglia: una difesa che difende PIÙ di quanto sapesse** | **E**, verificando che la serie non campionata (~1 100 punti) non fosse un costo per tutti: *«`underwater_series` viaggia nell'output di `drawdown_summary` — **l'analitica che ho reso opt-in per una ragione completamente diversa**: il difetto a distanza che la faceva chiedere ad Asset Detail. **Quindi i ~1 100 punti li pagano solo le due pagine che li guardano.** Asset Detail e il laboratorio di F **non li vedranno mai passare**.»* 🔑 **«Una decisione presa per proteggere una superficie parcheggiata contiene anche il costo di una serie che nessuno aveva messo sotto tetto.»** 📌 **È il ROVESCIO di tutto ciò che questa campagna ha catalogato**: non una difesa che non difende (cinque istanze), non un controllo che ha smesso di controllare (venti forme), **ma una difesa il cui effetto collaterale è stato a favore**. **Unico caso registrato in tutta la campagna** |
| D331 | 18 Set 2026 | 🔴 **L4 rende il NULLA su `unavailable`, ed è strutturale per contratto — il difetto che H descriveva per il 422, arrivato da tre porte che col selettore non c'entrano** | **E**, verificando la correzione K6 §9 che **non lo toccava**, ha trovato che la frase di H — *«un rosso sarebbe stato più gentile di un vuoto»* — **descrive il proprio codice già consegnato**. `okOutput` (`levelHelpers.ts:44-48`) ritorna `null` per ogni stato ≠ `ok`/`partial`, e i tre gradini rendono **solo** su `output`: `L4Simulation:129` e `L4Shock:138` → **vuoto assoluto**; `L4Replay:161` → messaggio **solo** per `insufficient_history`/`invalid_parameters` **con `details.asset_id`**, ogni altra causa **vuoto**. ⚠️ **E l'asimmetria che l'ha reso invisibile**: **L4 è l'unica delle quattro sezioni senza `health` né `reasons`** — L1/L2/L3 li passano (`:176 :180 :184`). 🔴 **Raggiungibile OGGI senza alcun selettore**: worker occupato, timeout, storia insufficiente. *«Il lettore preme "esegui", il caricamento finisce, **non compare niente** — né risultato né causa.»* 🔑 **E il coordinatore ha misurato ciò che rende il difetto STRUTTURALE, non probabilistico**: `schemas/risk.py:1056` → **`raise ValueError("unavailable or failed results must not include output")`**. → **Il backend GARANTISCE che un risultato `unavailable` non ha output**, quindi `{#if output}` su quel ramo **non è "probabilmente vuoto": è vuoto per contratto**. **Non è un caso limite da coprire — è l'unico comportamento possibile su quel ramo.** ✅ **Autorizzata la correzione** (≈3 righe: `l4Health`/`l4Reasons` da `degradedResults`/`resultReasons`, già null-safe e pinnati; `risk.states.*` esistente; **nessun bottone**) **con il mock esteso e un E2E** — perché *«il cablaggio da solo passerebbe verde senza essere misurato»*, e **sarebbe l'unico caso in cui il difetto catalogato venti volte lo introduciamo sapendolo** |
| D332 | 18 Set 2026 | 🔑 **Il criterio d'innesto è insoddisfacibile per i campi NUOVI e soddisfatto per gli STATI — e la differenza è misurabile in una riga** | Il coordinatore ha dovuto distinguere il caso L4 da K1/K6/K8, che aveva appena dichiarato **non innestabili**. **Misurato**: `generated.ts` contiene **45 occorrenze di `unavailable`**, e `RiskResultStatus = 'ok' \| 'partial' \| 'unavailable' \| 'failed'`; `schemas/risk.py:81` → `UNAVAILABLE = "unavailable"`. → **`unavailable` è nel contratto spedito: il mock può produrlo, Zod non lo cancella, il componente lo vede.** \| \| campo/valore \| nel contratto? \| ramo esercitabile oggi? \| \|---\|---\|---\|---\| \| **K1 · K6 · K8** \| campi **nuovi** \| ❌ **zero in baseline** \| ⛔ **no** — Zod li cancella \| \| **L4 `unavailable`** \| **stato esistente** \| ✅ **45 occorrenze** \| ✅ **sì** \| 🔑 **La regola operativa che ne esce**: **prima di dichiarare un innesto impossibile, contare il simbolo in `generated.ts`.** Un **campo** nuovo è invisibile finché `api sync` non gira dopo la fusione; **uno stato o un valore d'enum già spedito è esercitabile subito**. **Due casi opposti che sembrano lo stesso «aspetta l'integrazione»** |
| D333 | 18 Set 2026 | 🔑 **«Un timeout non è una domanda, ma resta qualcosa che va detto»: una motivazione corretta estesa di un passo oltre il suo dominio** | **E**, autocriticando il proprio docstring di `replayBlocker`: *«dice che un timeout "non è una domanda a cui il lettore può rispondere, e offrirgli un bottone **risolvi** sarebbe una bugia su chi ha il controllo". **Vero sul bottone** — ma ne ho fatto discendere anche **"nessun messaggio"**.»* 🔑 **Non è un errore di fatto: è un CONFINE DI VALIDITÀ NON DICHIARATO.** La motivazione era giusta per l'**azione** e l'ha applicata anche all'**informazione**. 📌 **Stessa forma della decisione «array vuoti» del coordinatore** (D320), *giusta nella lettera e falsa nella ragione*: **entrambe valide nel proprio dominio, entrambe estese oltre senza dichiararlo**. → **Una motivazione va scritta col suo dominio accanto**, altrimenti chi la legge — incluso chi l'ha scritta — **la applica dove non vale** |
| D334 | 18 Set 2026 | 🔑 **La riparazione del difetto delle chiavi dinamiche stava per riprodurre il difetto delle chiavi dinamiche — verificato PRIMA di scrivere** | **E**, chiudendo L4: *«`risk.states.${entry.status}` è **anch'essa una chiave composta a runtime**. Se `unavailable` fosse mancata, la correzione avrebbe stampato **la chiave grezza in quattro lingue** — cioè **avrei riprodotto D150 con la riparazione di D150**.»* ✅ **Verificato da E prima di scrivere, e riverificato dal coordinatore in tutte e quattro le lingue**: `risk.states` contiene `empty, failed, loadFailed, noAssets, partial, unavailable, warning` in **en/it/fr/es**; **`ok` manca ed è corretto**, perché `degradedResults` emette **solo i tre stati degradati**. 🔑 **La forma**: **una riparazione che usa lo stesso meccanismo del difetto che ripara è sicura solo se si verifica il meccanismo**, non l'intenzione. **Nessun gate l'avrebbe preso** — `i18n audit` non sa quali chiavi nascono a runtime (D288). 📌 **E l'adempimento del giro precedente era già assolto**: `risk.returnBasis.current_composition_backtest` era in tutte e quattro le lingue **dall'innesto K4**, perché E aveva **già identificato `RiskResultFrame:108` come chiave dinamica**; idem i due estimatori K6 |
| D335 | 18 Set 2026 | ✅ **L4 chiuso con la prova per mutazione: il rosso cade SOLO sul test nuovo, e lo stesso rosso dimostra due cose** | **E**: \| \| `front-portfolio risk` \| \|---\|---\| \| cablato \| **12 passed** \| \| **scablato** (`health`/`reasons` rimossi da L4) \| **`1 failed, 11 passed`** — `toBeVisible() failed` su `risk-level-4-health` \| \| ripristinato \| **12 passed** \| 🔑 *«Il rosso cade **solo** sul test nuovo: **l'additività della fixture è dimostrata dallo stesso rosso che dimostra il cablaggio**.»* → **Una sola mutazione prova due proprietà indipendenti**: che il cablaggio funziona **e** che l'estensione del mock non ha alterato le fixture esistenti. ✅ **Cancelli**: `front check` **0/41 in 2 file** (baseline esatta) · `front format --check` pulito · `risk` E2E **12** (era 11) · `risk-levels-unit` **89** · `core-unit` **1951** · `check-orphans` **78/202/206** tutti raggiungibili · `i18n audit` **2828 per lingua, nessuna chiave nuova** · `git diff --check` pulito · **6244 nessun listener**. 📌 **13 file tracciati (invariato) + 25 nuovi = 38**; file nuovo più grande **507 su 600**. **I due PNG di B non in stage** |
| D336 | 18 Set 2026 | 🔴🔴🔴 **Il lavoro più recente di C esiste SOLO SUL FILESYSTEM: tre file su cinque non sono in nessun ref git — è l'unico mandato in questa condizione** | Innescato da **E**, che ha misurato il timestamp dei checkpoint di C: **l'ultimo è `3bd422bb2`, 2026-09-18 02:20:28** — *«oltre quattro ore prima del lavoro di cui parliamo. **Non è che il valore "non si trova": non è mai stato scritto in un ref leggibile.**»* ✅ **Verificato dal coordinatore confrontando md5 file per file, su tutti e sei i mandati misurabili**: \| mandato \| file divergenti / totali \| \|---\|---\| \| **C** \| 🔴 **3 / 5 — SOLO SU DISCO** \| \| A \| 0 / 14 ✅ \| \| B \| 0 / 25 ✅ \| \| N \| 0 / 11 ✅ \| \| I \| 0 / 10 ✅ \| \| E \| 0 / 13 ✅ \| **I tre file di C fuori da ogni ref**: `backend/app/schemas/risk.py` (**che contiene `CURRENT_COMPOSITION_BACKTEST`**), `backend/app/services/risk/service.py`, `backend/test_scripts/test_services/test_risk_service.py`. 🔴 **È PIÙ GRAVE di §25**: là i checkpoint esistevano ma **non erano raggiungibili da un ramo** (potabili dal GC); **qui per C non esiste nemmeno un checkpoint** che contenga il lavoro. → **Se quel worktree si perde, il lavoro si perde: non c'è copia in git.** ⚠️ **E spiega retroattivamente D273**: il coordinatore aveva letto `CURRENT_COMPOSITION_BACKTEST` **sul filesystem di C**, dichiarandolo *«non committato»*. **Era più vero di quanto sapesse: non solo non committato — non esiste in NESSUN oggetto git.** ✅ **Conseguenza sull'ordine delle azioni**: **C va committato per PRIMO**, prima di N e prima del piano. **È l'unico lavoro della campagna che una pulizia del worktree cancellerebbe.** 📌 **E C è il mandato da cui dipende il cancello §20** — il terzo membro dell'enum che rende non esaustivi i due ternari 🔴🔴 **CORRETTA DA D355 — il «3/5» era un LIMITE INFERIORE e il «C unico» era FALSO.** Il metodo confrontava **solo i file tracciati** (il 35 % del delta). Con `git hash-object`+`cat-file -e`: **C ha 4 file a rischio** (i 3 + il **piano vivo**), e **otto mandati su dieci** ne hanno — **D ne ha SETTE, incluse le tre primitive**. |
| D337 | 18 Set 2026 | ✅ **Quinta cura al canale, proposta da E: mandare gli innesti come DOMANDA invece che come istruzione** | **E**, dopo **quattro messaggi consecutivi contenenti solo adempimenti già assolti**: *«Non è un problema finché li verifico — ma il costo non è nullo: **ogni giro rileggo il codice per distinguere "da fare" da "fatto"**, e il rischio è che un giorno **dia per già-fatto qualcosa che non lo è**. Se ti è comodo, **mandami gli innesti con la domanda "è nel tuo albero?" invece dell'istruzione "fallo"**: rispondo con la riga, e risparmiamo entrambi un giro.»* 🔑 **È la cura più economica delle cinque**, e la sola che **sposta il costo di verifica sul lato giusto**: un'istruzione obbliga il destinatario a **provare che è già fatta**; una domanda gli chiede solo **di leggere una riga**. 📌 **E il rischio che nomina è quello vero**: *«**dare per già-fatto qualcosa che non lo è**»* — **l'assuefazione al falso allarme è il modo in cui un canale rumoroso produce un difetto reale.** ✅ **Adottata.** Completa: piano vivo (D212) · registro anti-ricaduta §18.5 · tabella in testa ai rapporti (D238) · stato presunto allegato all'ordine (D255) · **e ora la forma interrogativa** |
| D338 | 18 Set 2026 | 🔴 **Il debito 11 registrato dal coordinatore era FALSO: `okOutput` accetta `partial`, e «mostrare ciò che c'è con un gradino» è la forma già spedita** | Il coordinatore aveva registrato come debito di E: *«i risultati `partial` spariscono — `okOutput` pretende `'ok'`»*, con l'istruzione *«ripara: un contributo parziale che **svuota** L2 è la forma del ripiego silenzioso»*. **E l'ha contraddetto col codice**, e il coordinatore ha verificato nel suo albero: ```levelHelpers.ts:46   if (result.status !== 'ok' && result.status !== 'partial') return null;``` → **rifiuta ciò che non è NÉ l'uno NÉ l'altro: un `partial` passa**, e `degradedResults` (`:91`) lo spinge nella riga di salute. 🔑 *«**"Mostrare ciò che c'è con un gradino" è esattamente ciò che il codice fa già** — non è una riparazione da fare, è la scelta che ho preso al passo 4.»* ✅ **E non lo legge dall'implementazione: due test portano la regola NEL NOME**, verificati dal coordinatore: `:277` **«reads a partial answer instead of discarding it»** · `:290` **«names every measurement that did not come back whole»**. 📌 **Con la ragione già scritta nel commento a `:278`**: *«`partial` è **lo stato ordinario** di un portafoglio con un titolo non prezzabile, non uno esotico: **rifiutarlo svuota ogni livello per un lettore la cui risposta è soltanto incompleta**»*. 🔑 **Un test il cui NOME è l'invariante è leggibile senza aprire l'implementazione** — ed è ciò che ha permesso a E di confutare il debito **citando due righe invece di spiegare un comportamento** |
| D339 | 18 Set 2026 | ✅ **E declina di aggiungere una `description` allo schema backend: «un diff dove nessun revisore la cercherebbe»** | Il coordinatore aveva offerto a **E** di aggiungere, *«mentre è lì»*, la `description` a `percentage_contribution` — additiva e utile. **E l'ha rifiutata con la ragione del coordinatore stesso**: *«`schemas/risk.py` è a **quattro scrittori**, e il mio diff è **interamente frontend: 13 file, zero backend**. Aggiungere una `description` mi renderebbe **l'unico workstream frontend che porta dentro una modifica di schema backend** — **in un diff dove nessun revisore la cercherebbe**. **È la stessa trappola che mi hai segnalato tu per `./dev.py format`**, che si portava dentro due `schemas/*` estranei.»* ✅ **Accolto.** La `description` **va fatta insieme al rename `coverage` → `calendar_coverage`**, **da chi possiede quel file**: *«un solo scrittore, un commit, una revisione»*. 🔑 **La regola**: **la revisione trova ciò che si aspetta di trovare.** Una modifica corretta ma **fuori dal perimetro dichiarato del diff** è una modifica **non revisionata di fatto**, anche quando è visibile. 📌 **Terza volta che un mandato rifiuta un'aggiunta additiva e autorizzata perché fuori dal proprio perimetro** — dopo F su `ui/select/` e A sui file MkDocs |
| D340 | 18 Set 2026 | 🔑 **«Un avviso chiede a tre mandati di controllare; una misura dice a tutti e tre che non devono»** | **E**, rafforzando la regola che il coordinatore aveva registrato (*un avviso che si può trasformare in una misura va misurato*, D219): *«**Il primo costa tre risvegli e produce tre risposte che possono divergere. Il secondo costa un `grep` e produce un numero.**»* 🔑 **Ed è il motivo per cui la regola è più forte di come era scritta**: non risparmia **fatica**, elimina **divergenza**. Tre mandati che rispondono alla stessa domanda producono **tre affermazioni da riconciliare**; una misura ne produce **una da verificare**. 📌 **E l'ammissione che chiude il caso `percentage_contribution`**: *«ci sono cascato **pur avendo il produttore sotto gli occhi** — il che dice **quanto è forte un nome contro la lettura del codice**»*. **Il nome ha vinto sulla lettura, in un lettore che stava leggendo.** |
| D341 | 18 Set 2026 | 🔴 **Una coppia di modifiche ATOMICA SU DUE ALBERI: non esiste un ordine sicuro, esiste solo la simultaneità** | **E**, analizzando l'innesto K5 della riga `risk-asset-add-button` segnalata da **F**. **Stato attuale verificato dal coordinatore**: `AssetSetRiskPanel:135` ha `onchange={(value) => (addAssetId = value)}` — **solo stato** — e il bottone `:138` conferma. **F ha collassato i due passi in uno** e rimosso il bottone. \| \| esito \| \|---\|---\| \| togliere la riga di spec **prima** di F \| 🔴 **rosso subito** \| \| innestare F **senza** togliere la riga \| 🔴 **rosso** \| 🔑 **«Le due modifiche devono atterrare nella stessa revisione. Non c'è un ordine sicuro: c'è solo la simultaneità.»** 📌 **Classe distinta da §20**: là un difetto **nasce dall'unione** di due modifiche ciascuna corretta; **qui due modifiche sono ciascuna rossa da sola e verdi solo insieme**. → **Un cancello di integrazione non basta: serve che chi fonde le applichi nello stesso commit.** ⚠️ **E i numeri di riga del rapporto non valgono**: lo spec di E è cresciuto da **817 a 1748** righe, **offset +438** — `:652-654` del rapporto di F sono **`:1090-1092`** da E. *«Con i numeri del rapporto la correzione cadrebbe **dentro un altro test**.»* **Sesta applicazione della regola dell'indirizzo relativo all'albero** |
| D342 | 18 Set 2026 | 🔑 **«Il test porta il proprio oracolo»: la cancellazione è sicura per COSTRUZIONE, non per verifica** | Il coordinatore aveva prescritto a **E**: *«verifica prima sul pannello integrato che `onchange` faccia davvero l'aggiunta senza conferma»*. **E l'ha dichiarata non necessaria, con l'argomento strutturale**: ```:1089  expect(risk-selected-asset-${id}).toHaveCount(0);   ← appena rimosso``` ```:1092  click('risk-asset-add-button');                     ← da togliere``` ```:1093  expect(risk-selected-asset-${id}).toBeVisible();    ← ORACOLO``` → *«**Se `onchange` non aggiunge davvero, `:1093` diventa rosso da solo.** La cancellazione **non può rendere il test vacuo**, perché **l'asserzione non nomina il meccanismo: nomina il risultato**.»* 🔑 **«È la famiglia del mock stantio ALLO SPECCHIO. Là nessuno controlla, e il verde rassicura. Qui qualcosa controlla — quindi la cancellazione è sicura per costruzione, senza dipendere da un rapporto proveniente da un albero che non posso leggere.»** 📌 **E la regola operativa che ne esce**: **un'asserzione che nomina il risultato sopravvive alla rimozione del passo che lo produceva; una che nomina il meccanismo no.** → **Scrivere l'oracolo sul risultato è ciò che rende una cancellazione futura verificabile senza il contesto che l'ha motivata** |
| D343 | 18 Set 2026 | ✅ **Verificato PERCHÉ il wrapper di F spegneva `-trigger` ma non le opzioni: una riga legge il prop, l'altra no** | **E** ha cercato il meccanismo invece di accettare la triage del coordinatore, e il coordinatore l'ha riverificato nella baseline: ```SearchSelect:377   data-testid={testId ? `${testId}-trigger` : undefined}   ← DERIVATO dal prop``` ```SearchSelect:473   data-testid="search-select-option-{option.value}"        ← STATICO``` → **`:1091` non è stato in pericolo PER COSTRUZIONE, non per fortuna.** ✅ **E ha cercato un quarto sito e non c'è**: gli altri due trigger del suo spec sono `risk-comparison-asset-select` (`RiskAnalysisPanel:692`) e `risk-replay-proxy-select` (`:891`), **entrambi nel pannello legacy suo, fuori dalla portata di F**. → **F tocca uno solo dei suoi tre. La lista di tre è completa.** 🔑 **E la forma in cui E gira la regola di F** (*«la proprietà del file non è la proprietà della causa»*): **«Il primo rosso di un file ereditato non è una diagnosi, è un indirizzo. Dice dove guardare, non chi ha sbagliato»** — *«e qui la causa sta in un albero in cui il file non esiste nemmeno»*. ➕ *«Un rosso che nasce da una decisione di prodotto altrui si ripara **togliendo l'inseguimento**, non inseguendo meglio.»* |
| D344 | 18 Set 2026 | 🔴🔑 **TERZO modo in cui il tipo mente: un default Pydantic esce OPZIONALE in TypeScript — 137 campi nel contratto, e il ripiego che induce è indistinguibile da uno necessario** | Trovato da **E** cercando ripieghi silenziosi nella propria resa dell'audit. `L4Replay:192` faceva `(excluded_weight_total ?? 0) * 100` → un'assenza resa come **«0,0 % di peso escluso»**, cioè **il valore più rassicurante possibile**. **Verificato sullo schema: non è Optional.** ```risk.py:220   excluded_weight_total: FiniteFloat = Field(0, ge=0, le=1)``` ```risk.py:241-248  isclose(excluded_weight_total, fsum(excluded_assets.weight), abs_tol=1e-12)``` → **il server non può emettere la coppia incoerente: il `?? 0` è codice morto.** 🔑 **Ma è preteso dal tipo**, e il coordinatore l'ha verificato sulla catena completa: ```backend  excluded_weight_total: FiniteFloat = Field(0, …)     ← NON Optional, ha un default``` ```TS       excluded_weight_total?: …                          ← OPZIONALE``` ```Zod      z.number().gte(0).lte(1).optional().default(0)``` → **«Il tipo dice "opzionale" perché c'è un default, non perché il valore possa mancare.»** 📊 **Misurato: `137` campi in tutto `generated.ts` sono `.optional().default(...)`** — **137 punti in cui uno sviluppatore TypeScript scrive un ripiego che è codice morto.** 🔑 **Distinto dagli altri due modi**: **(1)** Zod **cancella** un campo sconosciuto → *il tipo prometteva troppo* · **(2)** enum chiuso → **rifiuta tutto** · **(3)** default Pydantic → **il tipo promette troppo poco**, e *«il rimedio difensivo che induce è **indistinguibile da uno necessario**»*. ⚠️ **E il pericolo non è il codice morto: è la scelta del ripiego.** Se rassicura — `?? 0` su un peso escluso → *«0 % escluso»* — si ottiene **un ripiego silenzioso che non serve a nulla e che maschererebbe**, se mai il caso diventasse raggiungibile |
| D345 | 18 Set 2026 | ✅ **La superficie di E è immune alla trappola `=== null` PER TIPO, non per disciplina** | Il coordinatore aveva girato a **E** la regola di A: *«ogni campo con un pavimento a zero va letto con `=== null`, mai con la verità booleana»*. **E ha verificato la propria superficie**: ```finite(v) => typeof v === 'number' && Number.isFinite(v) ? v : null``` → **lo zero passa** (è un `number` finito) **e solo l'assenza torna `null`**. **Ogni numerico di L1/L2/L3 entra da lì**, e dove non passa da `finite` il confronto è **già esplicito** (`L4Replay:178` → `portfolio_return == null ? '—'`). 🔑 *«**Non è merito mio**: è che il difetto di A si previene **togliendo al chiamante la possibilità di leggere il valore grezzo**, non **ricordandosi** di scrivere `=== null`.»* → **Stessa forma di «non è una linea guida se il tipo la impone»** (D325). 📌 **Footnote per chi toccherà `risk.py:241`**: `item.weight or 0.0` è **letteralmente il pattern vietato**, **benigno solo perché il ripiego coincide col valore falsy** (`0.0 or 0.0`). **Cambiare quel ripiego lo romperebbe in silenzio.** Registrato, non toccato |
| D346 | ️18 Set 2026 | ✅ **La quinta cura funziona al primo uso: lo stato presunto allegato all'ordine ha esposto la divergenza PRIMA del lavoro** | Il coordinatore ha allegato a un ordine lo stato che presumeva: *«attivo su L4 + riparazione dei `partial`»*. **E**: *«**Nessuna delle due.** Sono `FROZEN` da diversi giri, **L4 è chiuso**, e **la riparazione dei `partial` non esiste perché il difetto non esiste**.»* 🔑 *«**Dei due, quello che guardava un mondo inesistente ero io nella tua mappa.** Non è un rimprovero: **è il motivo per cui la domanda "se diverge, dimmelo" era quella giusta da fare**. ✅ **Continua a farla.**»* 📌 **È la sola delle cinque cure che espone l'errore PRIMA del lavoro** (D255), e al primo impiego reale **ha fatto esattamente questo**: ha trasformato un ordine su un difetto inesistente in **una correzione di due righe**. ✅ **E E ha rubato la tecnica di A per chiudere il proprio rischio §4.D**: `resultFor` che invecchia in silenzio era *«in analisi dal primo giorno come "non fallisce, rassicura" — e non avevo una cura, solo un promemoria»*. **Costruire il payload per SOTTRAZIONE da quello vivo è la cura.** → *«La mia fixture **ereditava la premessa di chi l'aveva scritta**, la sua **si aggiorna da sola quando il soggetto cresce**. **Stesso artefatto, dipendenza invertita.**»* |
| D347 | 18 Set 2026 | 🔴🔑 **Il cartello «65 % perso con `git add -u`» induce `git add -A`, che raccoglie le icone di un altro mandato — la cura di un difetto ne arma un altro** | **Trovato da E sulla procedura §22, scritta dal coordinatore su misura di E stesso.** ✅ **Verificato dal coordinatore nel suo albero**: `git status --porcelain -uall \| grep -c '^??'` → **27**, non 25; di cui **2 PNG estranei**; **mtime `00:17`, identico ai dieci fratelli tracciati** → **una sola operazione di semina**, mentre il file più recente di E è delle **06:39**. 🔑 *«Mi hai identificato come il caso peggiore — **65 % invisibile a `git add -u`**. **La reazione naturale a quel cartello è `git add -A`.** Che nel mio worktree stagia **27**, non 25.»* 🔑 **E il motivo per cui nessuno se ne accorgerebbe**: *«**Sono mimetizzati dalla propria directory**: dieci fratelli identici, stesso mtime, **nomi esatti dei tipi di asset**. In un diff da 38 file, **`commodity.png` accanto a `bond.png` non attira lo sguardo di nessuno.** **Non un artefatto che *sembra* sbagliato — uno plausibile.**»* ⚠️ **E il danno è specifico**: **se entrano nel commit di E, la provenienza di B è persa proprio mentre B si innesta.** ✅ **Due cure adottate in §22.1-22.2**: **(a)** **enumerazione, non esclusione** — *«un'esclusione dimenticata **stagia**; un'enumerazione dimenticata **lascia fuori**, e il secondo errore è **visibile nel conteggio**, il primo no»*; **(b)** **i tre oracoli del post-stage**, *«perché un'occhiata non è una verifica»*: `grep -c '^??'` → **2** · `--cached \| wc -l` → il conteggio atteso · `--cached \| grep -c mkdocs_src` → **0**. → *«**Tre numeri chiudono la questione, e nessuno dei tre richiede di fidarsi di chi consegna.**»* 📌 **Classe**: **una cura che arma un difetto diverso** — il cartello è corretto, la reazione che induce no. **Prima istanza registrata in cui il rimedio stesso è l'innesco** |
| D348 | 18 Set 2026 | 🔑 **«Una regola con la ragione sbagliata cade INSIEME al comportamento corretto che proteggeva»** | **E**, chiudendo il mandato, sulla correzione della motivazione «array vuoti» (D320): *«È la cosa più utile che ho letto in questa campagna, e la tengo oltre il mandato. **Una regola sopravvive alla propria scomodità solo se chi la trova sa perché esiste**: con la ragione giusta **si difende da sola**, con quella sbagliata **si difende finché qualcuno non prova a verificarla** — e allora **cade insieme al comportamento corretto che proteggeva**.»* ⚠️ **E la parte difficile, che nomina**: *«che tu l'abbia corretta **dopo** che i tre indirizzi ti davano ragione **nella lettera** è la parte difficile: **era già "vero abbastanza" per lasciarla stare**»*. 🔑 **È il criterio che distingue una correzione necessaria da una di lusso**: non *«l'esito cambia?»* ma **«la ragione regge a una verifica?»**. Una regola giusta per il motivo sbagliato **è indistinguibile da una giusta finché nessuno la verifica**, e **massimamente fragile nel momento in cui qualcuno lo fa** |
| D349 | 18 Set 2026 | 🔑 **Un digest offerto come prova incontrovertibile NON è riproducibile dal destinatario: serve il comando che lo produce, non solo il numero** | **E** ha offerto il `md5` del commento NEA — **`cb2f367d58f165fdad76c3bd5c7fb755`** su `levelHelpers.ts:432-445` — **proprio per non dover essere creduto sulla parola**: *«se lo riscrivessi, **la prova che il testo è quello rivisto diventerebbe la mia parola invece del `md5`**»*. **Il coordinatore l'ha verificato e NON combacia**: `sed -n '432,445p' \| md5 -q` → **`52c31e63…`**, e **nessuno dei cinque intervalli adiacenti** provati (`431,444` · `432,444` · `433,445` · `431,445` · `432,446`) produce il valore dichiarato. ✅ **Ma la SOSTANZA è verificata**: `grep` sulle due metà → **2 riscontri**, entrambe presenti — *«not a count of holdings, despite the name»* **e** *«Never alone, and never without the cash weight beside it»*. 🔑 **La lezione, e chiude la classe**: **uno SHA git è auto-descrittivo** — `git cat-file -e <sha>:<path>` è **un comando che chi riceve può eseguire**, e l'oggetto è definito senza ambiguità. **Un `md5` di un intervallo di righe no**: dipende da **come il blocco è stato estratto** (indentazione, newline finale, confini esatti), e **senza il comando il numero non è riproducibile**. → **Un digest è una prova solo se viaggia col comando che lo genera.** 📌 **È D180 nella sua forma finale** — *un numero dichiara CHE COSA ha misurato* — applicata al tentativo stesso di rendere una prova incontrovertibile. **E la verifica che ha funzionato è quella sul contenuto**, non sul digest |
| D350 | 18 Set 2026 | 🔑 **«Una cosa abbastanza specifica da essere sbagliata è abbastanza specifica da essere controllata» — perché lo scambio ha funzionato** | **E**, chiudendo il mandato dopo **quattro** correzioni a quattro ordini del coordinatore: *«vale la pena dire **perché** è finito così, perché **non è merito di nessuno dei due in particolare — è la forma dello scambio**. **Ogni tuo ordine arrivava con l'indirizzo** (file, riga, campo), e **un indirizzo si può andare a controllare**. **Un ordine senza indirizzo si può solo eseguire o discutere.**»* 🔑 **«Le tue quattro istruzioni sbagliate erano verificabili PROPRIO PERCHÉ erano precise. È la stessa proprietà che rende falsificabile un contratto: una cosa abbastanza specifica da essere sbagliata è abbastanza specifica da essere controllata.»** 📌 **È la chiave di lettura dell'intera campagna**: oltre settanta premesse false sono state trovate **non nonostante** fossero scritte con precisione, **ma grazie a quello**. Un brief vago non avrebbe prodotto meno errori — **avrebbe prodotto errori non falsificabili**, e i mandati avrebbero eseguito invece di misurare. ✅ **Corollario operativo**: **la precisione di un'istruzione non è una garanzia di correttezza — è una garanzia di controllabilità**, che è una proprietà diversa **e più utile**, perché non dipende da chi scrive |
| D351 | 18 Set 2026 | 🔑 **«Un avvertimento consegnato alla persona sbagliata non è metà utile: è dannoso in due modi»** | **E**, sul relay di `coverage` che il coordinatore stava per dargli e ha riverificato prima, scoprendo che i destinatari erano **F e chi decide su D8/D47**: *«**Chi lo riceve cerca un difetto che non ha, e chi dovrebbe riceverlo non lo cerca affatto.**»* 🔑 **Più forte di come era stata pensata**: il coordinatore considerava un relay mal indirizzato **uno spreco**; E lo nomina come **un danno attivo su due fronti** — **consuma il tempo del destinatario sbagliato E lascia scoperto quello giusto**. → **È il motivo per cui la verifica va fatta PRIMA del relay e non dopo**, e l'unica volta in cui il coordinatore l'ha fatto in quest'ordine è anche l'unica in cui l'avvertimento è finito nel posto giusto |
| D352 | 18 Set 2026 | 🔴 **Una chiave i18n serve DUE superfici: il testo migliorato per L2 peggiora il pannello legacy, che ha il `?? 0`** | **Effetto collaterale non nominato da nessuno dei due**, trovato dal coordinatore verificando la chiusura di E. **Misurato**: \| \| \| \|---\|---\| \| testo **baseline** \| `"cashWeight": "Cash weight"` \| \| testo **nell'albero di E** \| `"cashWeight": "Cash and uncovered"` \| \| chi usa la chiave \| **`L2Diversification.svelte`** *(nuovo, legge con `!== null`)* **e `RiskAnalysisPanel.svelte`** *(legacy, legge con `?? 0`)* \| 🔴 **Dopo l'integrazione, sul pannello legacy che serve Asset Detail e il laboratorio di F**: un `cash_weight` assente diventa **«Cash and uncovered: 0,0 %»**. → **Più fuorviante del testo che sostituisce**: *«uncovered»* **afferma che si è verificato che nulla è scoperto**, mentre *«Cash weight: 0,0 %»* si legge come un peso nullo. **Il testo migliore sulla superficie giusta è il testo peggiore su quella sbagliata.** 📌 **E il difetto che E ha impedito a L2 È SPEDITO OGGI altrove** — dichiarato da E stesso invece di lasciarlo trovare: `RiskAnalysisPanel:859` (baseline) usa **`contributionOutput?.cash_weight ?? 0`**, *«il `?? 0` esatto di cui mi hai avvisato. Non toccato: legacy preservato di proposito, fuori autorizzazione, preesistente»*. ✅ **Destinatario: chi integra (J) e chi possiede il legacy — non E**, che è `FROZEN` e la cui superficie è corretta. **Due opzioni all'integrazione**: separare le chiavi, **oppure** riparare il `?? 0` del legacy insieme al cambio di testo |
| D353 | 18 Set 2026 | 🔑 **La verifica rotta del coordinatore ha salvato E dallo spedire una chiave grezza in quattro lingue** | Il coordinatore aveva cercato `d['risk']['cashWeight']` → **`None`**, e corretto sé stesso col `grep`. **E**: *«La chiave è **`risk.metrics.cashWeight`** — **due livelli più in basso**. **Se l'avessi dedotta dal tuo messaggio invece di misurarla, avrei scritto `$t('risk.cashWeight')` e spedito una chiave grezza in quattro lingue.** La trappola D150, **evitata perché ho cercato il percorso invece di riusarlo**. **Il `grep` che ti ha corretto è lo stesso che è servito a me.»* 🔑 **Il fatto notevole non è l'errore: è che la sua correzione è servita a entrambi.** Un coordinatore che pubblica una verifica **con il comando accanto** rende il proprio errore **riparabile dal destinatario** — che è la forma opposta di un relay di cui ci si deve fidare. 📌 **E la scelta di asserire `data-uncovered` invece del testo reso**: *«accanto c'è un `$t()` tradotto, e **un'asserzione sul testo passerebbe o fallirebbe per locale**»* — **la disciplina dei selettori applicata a un'asserzione di valore** |
| D354 | 18 Set 2026 | ⚠️ **«Il conteggio è cieco a questa modifica»: 12 prima, 12 dopo — un'asserzione non è un test** | **E**, consegnando il cablaggio di `uncoveredWeight`: `front-portfolio risk` → **12 passed prima, 12 passed dopo**. *«Ho aggiunto un'**asserzione**, non un test: **banner e conteggio sono entrambi ciechi**. La mia regola dice "guarda il conteggio, non il banner" — **qui non bastava nemmeno il conteggio**.»* ✅ **Chiuso con la mutazione**: `{#if false}` → `risk-analysis.spec.ts:901 → risk-l2-uncovered: element(s) not found`; ripristinato → 12 passed. 🔑 **Terzo livello della stessa scala**: il **banner** non distingue spec diversi (D176) · il **conteggio** non distingue un'asserzione aggiunta · **solo la mutazione distingue**. ⚠️ **E un fuori pista dichiarato come OSSERVAZIONE, non come diagnosi**: la corsa mutata segnava **2 failed**, e il secondo (`:1186`, `risk-stress-impacts`) è **nel pannello legacy che non monta `L2Diversification`** — *«la mia mutazione **non può raggiungerlo**»*. **Seconda intermittenza distinta nel legacy** (la prima fu `syncButton` a `--workers 4`, 1 su 7), **locatori e test diversi**. → *«**Non lo dichiaro flaky: è UNA osservazione.** Se l'avessi chiamata flaky **avrei chiuso la questione invece di aprirla.**»* |
| D355 | 18 Set 2026 | 🔴🔴🔴 **Il metodo con cui il coordinatore ha misurato «C è l'unico a rischio» guardava il 35 % del delta: OTTO mandati su dieci hanno file disk-only, e D ne ha SETTE** | **E ha trovato il buco verificando una dichiarazione che lo riguardava**: *«Hai confrontato md5 su **13 file tracciati**. Il mio delta è **38**: i **25 nuovi** sono esattamente **il 65 % che tu stesso avevi chiamato invisibile**. **Hai misurato il 35 % e concluso sul 100 %.**»* 🔑 **La regola**: *«"un albero è al sicuro" **non si dimostra confrontando i file tracciati**. Il primo metodo misura **ciò che git già conosce**; il secondo misura **ciò che git perderebbe**.»* ✅ **Metodo corretto, dato da E ed eseguito dal coordinatore su tutti e dieci gli alberi**: ```git hash-object -t blob -- <file> && git cat-file -e <sha>``` **Esito reale**: \| mandato \| nuovi \| **disk-only** \| \|---\|---:\|---:\| \| **D** solid-engine \| 10 \| 🔴 **7** \| \| C · A · B · I · G · F · E \| — \| 🔴 **1 ciascuno** \| \| N · H \| — \| ✅ **0** \| 🔴 **E i 7 di D sono le TRE PRIMITIVE e i loro test**: `RiskMetricCard`, `KpiMetricBar`, `KpiDivergingFlowBar` — **le stesse che F aveva verificato presenti in `1a537f9cb`**. **Provato**: nel checkpoint `RiskMetricCard.svelte` ha **122 righe**, sul disco ne ha **131**, e **l'hash del contenuto corrente non è in DB**. → **Il checkpoint contiene una versione vecchia; il lavoro più recente non è da nessuna parte.** ⚠️ **Quindi il «3 file» di C era un LIMITE INFERIORE**: la misura corretta ne trova **4** — i 3 tracciati **più `C-esecuzione.md`, il suo piano vivo**, cioè **il punto di ripristino del mandato**. 📌 **E gli otto singoli disk-only degli altri sono quasi tutti `*-esecuzione.md`**: **i piani vivi, aggiornati dopo l'ultimo checkpoint** — cioè **proprio i documenti che la campagna ha eletto a fonte autorevole dello stato** |
| D356 | 18 Set 2026 | 🔴 **L'oracolo `git status \| grep -c '^??'` è sbagliato di un fattore 3,5, e sbaglia PER DIFETTO — corretto da chi me l'aveva dato** | **E** ha corretto l'oracolo che **lui stesso** aveva proposto il giro prima: ```git status --porcelain \| grep -c '^??'   →   9``` ```git ls-files --others --exclude-standard  →  27``` 🔑 **`git status` COLLASSA le directory**: *«Una riga sola — `?? .../risk/levels/` — **ne nasconde 19**: tutti e quattro i livelli, il contenitore, gli helper, gli unitari. **L'intera consegna del mandato dietro una riga.**»* 🔴 *«Chi conta le righe conclude **"9 non tracciati, 2 di B, quindi 7 miei"**. Sono **25**. **Fattore 3,5, e sbaglia per difetto** — nella direzione **che fa credere di aver messo al sicuro tutto**.»* ✅ **Oracoli corretti — enumerare, mai contare le righe**: `git ls-files --others --exclude-standard \| wc -l` → **27** · `… \| grep -c mkdocs_src` → **2** (di B) · `git diff --cached --name-only \| wc -l` dopo lo stage → **38**. 📌 **E i due `.png` di B restano righe proprie** (la directory è già tracciata) → **visibili e separabili**: l'avvertimento su `git add -A` regge |
| D357 | 🔑 18 Set 2026 | **«Nessuno dei due stava cercando quello che ha trovato»: due verifiche di routine valgono più delle indagini che le avevano motivate** | **E**, chiudendo: *«**Hai trovato il difetto di C guardando un timestamp che ti avevo dato per un'altra ragione**, e **io ho trovato il buco del tuo metodo verificando un complimento.** Nessuno dei due stava cercando quello che ha trovato — ed è il **secondo caso oggi** in cui **una verifica di routine vale più dell'indagine che l'aveva motivata**.»* 🔑 **È la forma che la campagna ha prodotto più volte e che nessuno aveva nominato**: **il rendimento di una verifica non è correlato all'importanza della domanda che la motiva.** Le tre scoperte più gravi — i `$$` rotti in tre lingue, il lavoro disk-only, questo — **sono tutte arrivate da controlli fatti per confermare qualcos'altro**. 📌 **Corollario operativo**: **una verifica va eseguita anche quando ci si aspetta che confermi**, perché **è nel caso in cui conferma che nessuno la rifarebbe** |
| D358 | 18 Set 2026 | ⚠️ **I due `.png` non sono di B: sono dell'ambiente, stanno in DIECI alberi su dieci, e uno stage `-A` li ha GIÀ raccolti tre volte** | E li aveva attribuiti a **B** per mtime e contesto, e il coordinatore l'aveva accettato. **Misurato**: `2 png` in **tutti e dieci** i worktree, incluso quello del coordinatore. **Non sono di un mandato — sono seminati dal bootstrap.** 🔴 E `git log --all -- commodity.png` li trova in **tre checkpoint di sessioni diverse**: **uno stage automatico li ha già presi tre volte**, in alberi che non c'entrano con le icone. 🔑 **L'avvertimento cambia destinatario e portata**: non «le icone di B finiscono nel commit di E» ma **«due file dell'ambiente finiscono nel commit di chiunque usi `git add -A`»** — e in un diff di backend o documentazione **due `.png` in `mkdocs_src/` non li nota nessuno**. 📌 **Terza volta in questa campagna che un'attribuzione plausibile per mtime si rivela falsa alla misura**: l'ora di un file dice **quando**, mai **chi** |
| D359 | 18 Set 2026 | 🟢 **Stage enumerato eseguito su tutti gli undici alberi: i file disk-only passano da 14 a ZERO, 217 file messi al sicuro, nessun commit** | **Autorizzato dal protocollo** (*«il coordinatore stagia dopo che il mandato è `FROZEN`»*) e **motivato da una misura**: `git add` **scrive i blob nell'object database**, quindi **lo stage È la mitigazione di §33**, non un passo in più. ✅ **Procedura**: `git add -u` + `git ls-files --others --exclude-standard \| grep -v asset-types \| xargs git add` — **la lista dei nuovi la produce git**, non un jolly: un file nuovo **non può sfuggire**, i due `.png` d'ambiente **non possono entrare**. ✅ **Oracoli verdi su undici alberi**: png stagiati **0** ovunque; non tracciati **2** ovunque tranne H (**0**, ed è l'unico albero senza png — coerente). ✅ **I 38 di E coincidono con la sua dichiarazione** (13+25): conferma indipendente. 🔑 **Il rischio residuo era uno solo — una pulizia PRIMA dello stage — e ora non c'è più** |
| D360 | 18 Set 2026 | 🔴 **D ha RINOMINATO due primitive da `dashboard/` a `ui/display/`, e nessun rapporto di mandato lo diceva — scoperto perché un conteggio non tornava** | Lo stage di D dava **15** file contro i **16** attesi. **Non un file perduto**: ```R069  dashboard/KpiMetricBar.svelte → ui/display/KpiMetricBar.svelte``` — **`--name-only` con rilevamento rename mostra UNA riga per una coppia.** `KpiDivergingFlowBar` invece appare come `D`+`A` perché **è cambiato troppo** (122→147 righe) per essere riconosciuto. 🔑 **La lezione sul conteggio**: *un numero che non torna non è sempre una perdita — a volte è **una coppia riconosciuta**.* **Ma per saperlo bisogna guardare, e il default era concludere «manca un file».** 📌 **L'importatore è uno solo** (`dashboard/KpiSection.svelte`) **e D lo ha aggiornato**: la rinomina è completa nel suo albero |
| D361 | 18 Set 2026 | ⚠️ **La rinomina di D è un conflitto latente verso QUATTRO alberi estranei alla campagna — verificato oggi innocuo, ma con una scadenza** | Il vecchio percorso è importato **in ogni albero del repo**, compresi quattro del lavoro parallelo sulle richieste dei primi utenti: `crispy-pancake` (`…performance-charts-plan`), `friendly-dollop` (`…allocatore-pac`), `literate-lamp` (`…onboarding-foundation`), `turbo-fortnight` (`…tool-platform-c-r2`). **Solo `crispy-pancake` ha toccato `dashboard/`** — `GrowthChart.svelte` — e **verificato per `grep` che non importa le primitive rinominate**: ✅ nessun conflitto **oggi**. 🔴 **Ma una rinomina è il conflitto che git risolve peggio**: **una parte vede una modifica, l'altra una cancellazione**, e il merge non le concilia da solo. ⚠️ **Finché D non è integrato, chiunque tocchi `dashboard/KpiSection.svelte` in un albero estraneo apre quel conflitto.** 📌 **Secondo motivo perché D vada per primo**, oltre a essere la base di F ed E |
| D362 | 18 Set 2026 | 🔴 **Due superfici condivise hanno QUATTRO scrittori ciascuna, e il protocollo prescriveva un solo proprietario: non gliel'ho assegnato** | **i18n** (`en/it/fr/es.json`): **B** +16, **F** +36, **E** **+90**, **H** +27. **`backend/app/schemas/risk.py`**: **C** +33, **A** +54, **N** +33, **H** +44−1. 🔴 **E e H si incontrano sulla simulazione e condividono almeno la chiave `gbm`** — E rende l'interfaccia del Monte Carlo, H ne costruisce il motore, **entrambi nominano il processo**. ⚠️ **Non è un conflitto di file, è un conflitto di CHIAVE**: un merge che sceglie una versione **lascia l'altro pannello con una chiave grezza in quattro lingue**, e **nessun gate lo vede** — `i18n audit` **conta** le chiavi, non le confronta con ciò che i componenti chiedono. 🔑 **La misura arriva dopo il fatto, quindi non previene il conflitto: lo rende prevedibile.** 📌 **La differenza fra un conflitto previsto e uno scoperto al merge è che il primo ha gia' una risoluzione scritta** |
| D363 | 18 Set 2026 | ✅ **Le due collisioni su `schemas/risk.py` sono localizzate e ADDITIVE — «tenere tutto», ma verificato eseguendo** | **Collisione 1, `class RiskResultMetadata`**: C aggiunge `sliced_asset_ids` (@455), H aggiunge `bootstrap_seed` (@470) → **campi diversi della stessa classe**. **Collisione 2, `__all__`**: A aggiunge `RiskDrawdownPoint` e `RiskVarCvarBin` (@1089, @1123), H aggiunge `RiskSimulationRegime` (@1116) → **voci diverse della stessa lista**. ✅ **Nessuna e' una contesa sul significato: sono aggiunte che si toccano per ADIACENZA.** ⚠️ **Ma «tenere tutto» va verificato eseguendo, non leggendo**: un `__all__` incompleto **non e' un errore di sintassi** — e' un `ImportError` **a runtime, in un punto lontano da dove il merge e' avvenuto**. 📌 **E' la forma di §20 su una superficie nuova**: *un merge pulito non prova l'esaustivita' di un elenco* |
| D364 | 18 Set 2026 | 🔴🔴🔴 **L'ostacolo vero all'integrazione e' la divisione degli spec E2E fra D ed E, e non e' un conflitto: e' lavoro da RIFARE** | **D svuota** `e2e/portfolio/risk-analysis.spec.ts` (817→**76** righe, **−751**) spostandone i test in due file nuovi; **E lo estende** (817→**1 753**, **+976**). **Hunk sovrapposti** a `@1, 508-516, 689-697`. ⚠️ **Fondere i due non produce un conflitto da risolvere: produce un file che nessuno dei due mandati riconoscerebbe.** ✅ **E l'aveva previsto senza poter vedere l'albero di D** (*«i quattro vivono nel worktree di D»*), e la risoluzione e' la prima voce della sua coda: **«divisione sulla MIA versione a 1 748 righe, non le 817 di D»**. 🔑 **Tenere la versione di E e' la sola risoluzione che non perde nulla — ma lascia la divisione da rifare.** 🔴 **E i due test di `risk-asset-detail.spec.ts` esistono ancora parola per parola dentro la versione di E**: tenere entrambi i file **esegue gli stessi test due volte**, e **un duplicato non e' un rosso, e' un verde pagato il doppio** |
| D365 | 18 Set 2026 | 🔴 **`risk-lab.spec.ts` e' un conflitto add/add: D ne scrive 82 righe, F ne scrive 812, nessuna base comune** | Il file **non esiste in `cc33120eb`**, quindi git **conflitta il file intero** — niente merge a tre vie. **D**: 1 test segnaposto, e l'intestazione dice *«Owner: the laboratory mandate. It CONSUMES risk-mocks.ts but does not own it»*. **F**: **6 test**, la suite vera. ✅ **Vince F — e la decisione l'ha gia' scritta D nella propria intestazione.** ⚠️ **Ma `risk-mocks.ts` e' di D e lo importano tutti e tre i suoi spec, mentre la suite di F non lo usa**: dopo l'innesto **verificare che abbia ancora almeno un lettore**. 📌 **Un helper senza lettori e' codice morto che sopravvive perche' nessuno lo cerca** |
| D366 | 🔑 18 Set 2026 | **Il conflitto peggiore non era in nessun lavoro: era nella loro RELAZIONE — e si vede solo da chi li contiene entrambi** | Avevo nominato quattro ostacoli d'integrazione (rinomina di D, chiave `gbm`, collisioni su `schemas/risk.py`, coppia atomica F/E). **Nessuno dei quattro era il peggiore.** 🔑 **Il vero l'ho trovato scrivendo i tree dagli indici con `git write-tree` e confrontandoli** — un'operazione che **non tocca un ref** ma rende **misurabile** cio' che fino a quel momento era solo **dichiarato nei rapporti**. 📌 **Ogni mandato vede solo il proprio albero; io avevo letto i rapporti, non gli indici.** **La relazione fra due lavori non e' osservabile da nessuno dei due**, e nemmeno da chi legge quello che entrambi raccontano: **solo da chi mette i due alberi nello stesso spazio** |
| D367 | 🔴 18 Set 2026 | **TRE messaggi di commit su undici erano falsi, e li avevo scritti io leggendo gli elenchi di file invece del codice — corretti dallo sviluppatore a memoria** | **A**: avevo scritto *«Replace the Riskfolio-Lib dependency with in-house implementations»*. 🔴 **Falso su tutti i fronti**: `riskfolio-lib = "==7.0.1"` **e' ancora nel Pipfile**, A **non ha toccato il manifest**, `riskfolio_worker.py` **la importa ancora**, e `portfolio_optimization.py` **la usa a runtime**. **Il lavoro vero di A e' l'OPPOSTO**: ha costruito `test_risk_metrics_oracle.py`, che **usa riskfolio come oracolo** per fissare le nostre implementazioni — *«riskfolio is imported HERE and ONLY here… never move an import from this file into metrics.py»* (il confine e' una decisione di devWiki: ~340 MB nativi fuori dal processo web). **N**: avevo scritto *«a metric that depends on absent data reports unavailable»*. 🔴 **Falso**: `acquired.py` dice *«These are **acquisitions**: measures the product did not have»* — **otto misure prese dal catalogo riskfolio e reimplementate** perche' devono girare **sincrone**, mentre riskfolio vive solo nel worker. **C**: avevo scritto *«separate the portfolio, broker and asset-set scopes»*. 🔴 **Falso**: C aggiunge `asset_ids`, cioe' **l'analisi di un SOTTOINSIEME del portafoglio** con pesi rinormalizzati — **e me lo diceva il nome del suo stesso branch**, `…-c-portfolio-slicing`, **che non ho letto**. 🔑 **La forma dell'errore e' una sola**: **ho scritto undici messaggi leggendo `--name-only`**, cioe' **quali file** erano cambiati, **mai cosa dicessero**. **Un elenco di percorsi e' compatibile con molte storie diverse, e io ho scelto quella che mi aspettavo.** 📌 **E il rilevatore non e' stato un gate**: e' stato **lo sviluppatore che ricordava l'architettura** — *«non eravamo rimasti che per alcuni segnali lo usavamo… e usavamo riskfolio come oracolo nei test?»*. **Nessuna verifica automatica avrebbe potuto trovarlo: un messaggio di commit non e' eseguibile** |

---

## 2. Rinvii registrati in `TODO_FUTURI.md`

| Voce | Priorità | Precondizione dichiarata |
|---|---|---|
| Tracking Error / Information Ratio con benchmark selezionabile | 🔽 bassa | Benchmark persistente **e** una ragione semantica, non solo tecnica |
| Portfolio optimization / frontiera efficiente (Riskfolio) | 🔽 molto bassa | Provider dati esteso, semantica non prescrittiva, esclusione del max-Sharpe |
| Monte Carlo avanzato: Markov calibrato e volatilità stocastica | 🔽 bassa | Storia lunga e verificata (liv. 4); fonte dati di opzioni (liv. 5) |
| Stimatori robusti di covarianza (Ledoit-Wolf, Gerber, denoising) | 🔽 bassa | Direzione della pagina correlazioni decisa; M3 completata separatamente |
| Catalogo scenari dinamico, proxy persistenti, RQMC | — | Preesistenti alla ripianificazione, invariati |

---

## 3. Questioni aperte

### Q1 — Il catalogo dei benchmark 🟢 chiusa → D48-D50, D53, D60, D64-D66

**La riformulazione viene dal developer, ed è migliore della domanda originale.**

La versione precedente chiedeva «dove salviamo *il* benchmark», dando per scontato che
ne bastasse uno. Ma il confronto è **plurimo**: se investo in azionario, confrontarmi
con un buono fruttifero postale non dice niente — è ovvio che l'azionario renda di
più e oscilli di più. Il numero uscirebbe, sarebbe pure corretto, e non
significherebbe nulla. È lo stesso errore che questa ripianificazione esiste per non
ripetere.

**La proposta**: il sistema *suggerisce* di aggiungere asset di riferimento — un ETF
S&P 500, un EuroStoxx 600, un world, e qualche obbligazionario. Un menù a tendina
lascia scegliere quale usare. Restano selezionabili **tutti** gli asset in database,
ma i riferimenti stanno **in cima**. A qualificarli è una casella nel footer del form
asset, **condivisa fra tutti gli utenti**: spenta di default ovunque, accesa di
default quando l'asset viene aggiunto da questa pagina.

E lo stesso selettore, con lo stesso ordinamento, serve i segnali di Asset Detail e
Forex Detail.

#### Verifica sul codice: quattro conferme e un avvertimento

**1. Gli asset sono già globali.** `Asset` (`backend/app/db/models.py:464`) non ha
`user_id` né alcun campo di proprietà. Una colonna sull'asset è **condivisa per
costruzione**: non serve inventare un meccanismo di condivisione, è già così che
funziona il resto.

**2. Il selettore è già quello.** `RiskAnalysisPanel.svelte` usa **già**
`SignalAssetParamControl` in due punti — riga 891 per l'asset di confronto, riga 1091
per il proxy del replay. L'intuizione «userei quello stesso componente» descrive lo
stato di fatto: manca solo l'ordinamento.

**3. Un solo componente copre quattro punti.** `SignalAssetParamControl` (64 righe) è
raggiunto da `ChartSignalsSection`, che vive sia in `/assets/[id]` sia in
`/fx/[pair]`. Quindi Asset Detail, Forex Detail, confronto di rischio e proxy di
replay passano tutti di lì. Una modifica, quattro benefici.

**4. I titoli di sezione esistono già.** `SelectOption` espone `header?: boolean`, e
il commento nel tipo descrive esattamente la semantica che serve: la voce è saltata
dalla tastiera, ignorata da Invio, e **sparisce quando la ricerca svuota la sua
sezione** — «un titolo sopra il nulla è peggio di nessun titolo». Mettere i benchmark
in cima è quindi un `sort` più due intestazioni, non un componente nuovo.

L'ordinamento attuale è alfabetico puro (`SignalAssetParamControl.svelte:31`):

```javascript
.sort((left, right) => left.label.localeCompare(right.label));
```

**⚠️ L'avvertimento: `AssetType.INDEX` esiste già.** Il suo docstring dice
testualmente *«Market indices and benchmarks (e.g., S&P 500, MSCI World) — no
transactions allowed»*. Ma è usato in **un solo punto** di tutto il backend
(`transaction_batch_stages.py:502`), e solo per **vietare le transazioni**. Non è un
catalogo di benchmark: è un tipo che blocca gli acquisti.

E non può diventare il flag, per la ragione che rende giusta la casella separata: un
ETF S&P 500 è un ottimo benchmark **e** un asset che puoi possedere davvero.
Marcandolo `INDEX` non potresti più registrarci sopra una transazione. **Il flag deve
essere ortogonale al tipo.**

Resta però una domanda onesta: un asset `INDEX` non è acquistabile, quindi esiste
*solo* per il confronto. Vale la regola `è_benchmark = flag OR asset_type == INDEX`?
**Risolta il 17 Set → D53**: non una regola calcolata a ogni lettura, ma flag
**materializzato** — backfill in migrazione per gli `INDEX` esistenti, e accensione
automatica **in sola lettura** alla creazione di un nuovo `INDEX`.

#### Cosa resta da decidere

**(a) Piatto o per tipo.** 🟢 **Risolta → D52.** L'argomento di partenza è che il
riferimento giusto dipende da cosa si possiede. Una casella booleana produce però un
elenco piatto in cui l'ETF azionario e quello obbligazionario stanno mescolati — cioè
ripropone il problema che la proposta vuole risolvere, solo più in alto nella lista.

> ⚠️ **Autocorrezione.** Qui stava scritto «nessuna colonna in più, il tipo lo
> abbiamo». **Era falso**: `AssetType` distingue la *forma* dello strumento (STOCK,
> ETF, BOND, CRYPTO, FUND, HOLD, CROWDFUND, INDEX, OTHER), non il contenuto. Un ETF
> azionario e uno obbligazionario sono **entrambi `ETF`**, quindi raggruppare per
> `asset_type` li avrebbe lasciati nella stessa sezione: esattamente il mescolamento
> che la sezione doveva eliminare. La raccomandazione era costruita su un presupposto
> sbagliato ed è stata accettata dal developer prima che l'errore emergesse.
>
> Cercata un'alternativa nei dati esistenti: `FAClassificationParams`
> (`schemas/assets.py:533`) ha `sector_area`, e il suo enum contiene davvero
> *Corporate Bonds* e *Government Bonds*. Ma è una **distribuzione di pesi**, non
> un'etichetta, e `classification_params` è opzionale — dipende dall'arricchimento del
> provider. Servirebbe una soglia arbitraria su un campo che per molti asset è nullo.

**Esito**: si estende `AssetType` con i sottotipi dove la distinzione ha senso (D52),
si disegna l'icona a due strati, e le sezioni del selettore diventano corrette per
costruzione. L'elenco preciso dei sottotipi è **Q9**.

**(b) Chi può accendere la casella.** 🟢 **Risolta → D60.** Condivisa fra tutti gli
utenti significa che chiunque la tocchi cambia la lista **a tutti**. Il developer
conferma che è il comportamento voluto, non un compromesso: non ha senso che lo stesso
ETF sia un buon riferimento per un utente e no per un altro. È coerente col resto del
modello — `Asset` non ha `user_id`, e un asset marcato benchmark, **purché non sia
`INDEX`**, resta assegnabile alle transazioni come qualunque altro, cosa a sua volta
globale. Nessun permesso speciale, nessuna riserva all'amministratore.

**(c) La scelta si ricorda?** 🟢 **Risolta → D64.** Il catalogo dice *quali* asset sono
buoni riferimenti; non dice *quale* stai usando. L'ipotesi precedente era una colonna
su `UserSettings`, ereditata dalla versione archiviata. **Il developer la scarta**: la
scelta vive nella **cache locale del client**, e se non c'è nulla la zona resta vuota
con un testo che spiega cosa fare. Il backend resta senza stato — il benchmark viaggia
nella richiesta — e la regola di degrado è obbligatoria: asset cancellato o
de-flaggato ⇒ stato vuoto, non errore.

**(d) Da dove viene la lista dei suggerimenti.** 🟢 **Risolta → D65.** «Il sistema
suggerisce» non richiede che i suggerimenti siano risolvibili: sono **testi fissi
tradotti**, mostrati quando la zona è vuota. Cade l'ipotesi della lista curata (YAML o
costante con identificatori) e con essa le chiamate ai provider e la manutenzione di
un elenco di ISIN. Resta aperto quanto i testi debbano essere specifici → **Q11**.

**(e) La valuta.** 🟢 **Risolta → D66.** Non era nell'elenco originale ed è una trappola
vera: se il benchmark è in USD e la valuta base è EUR, il beta misura **anche il
cambio**. Va convertito con l'FX che già abbiamo — «abbiamo il sistema delle forex
apposta» — altrimenti il numero è contaminato in silenzio, esattamente il tipo di
errore che il CVaR ci ha insegnato a temere.

#### Cosa questo comporta

| Dove | Cosa |
|---|---|
| DB | colonna booleana su `assets` + **nuovi valori in `AssetType`**: due tipi base (`COMMODITY`, `REAL_ESTATE`) e i sottotipi ETF (D52, D61) + migrazione Alembic incrementale con backfill `INDEX` (D53) |
| Backend | campo negli schemi asset, filtro/ordinamento in lista; **correzione del docstring bugiardo** su `valuation_model` (D51) |
| `SignalAssetParamControl` | ordinamento a sezioni con `header` (già supportato) |
| `SignalTreeSelect` | **generalizzazione**: da `components/charts/` a `components/ui/select/`, contenuto dell'opzione come snippet, secondo slot icona per la pastiglia (D62) |
| `AssetModal` | interruttore nel footer, sul modello di `active` (riga 2126), **readonly se `INDEX`**; albero a due livelli per il tipo |
| `assetTypes.ts` | nuove voci in `PNG_MAP` + costante per le **pastiglie** sovrapposte |
| PNG | due file nuovi: `commodity`, `real-estate`. `liquidity` **esiste già** (D61) |
| i18n | etichette dei nuovi tipi in quattro lingue sotto `assets.types.*` |
| Risk page | pannello di stato vuoto con i suggerimenti |
| `UserSettings` | preferenza salvata (punto **c**) |

Nessuno di questi punti è grosso preso da solo. È **trasversale**, non profondo.

### Q2 — Rimozione delle dipendenze di ottimizzazione 🟢 chiusa → D23

Con D6, Riskfolio-Lib, CVXPY, CLARABEL e SCS restano nell'immagine senza essere
raggiungibili dal prodotto. La domanda era se valesse la pena rimuoverle.

**Misurato** (vedi [`06`](./06-matematica-librerie-e-reimplementazioni.md) §5):
Riskfolio pesa **2,5 MB**, l'intera closure di solver circa **16 MB su 2 780** —
lo 0,6%. I pesi veri sono `scipy` 96,6 MB, `pandas` 66,8 MB, `QuantLib` 59,6 MB.

**Chiusa in negativo: Riskfolio resta.** Rimuoverla non risolverebbe il problema
dell'immagine, e D25 le assegna un ruolo nuovo — oracolo di test — che la rende
utile anche senza `portfolio_optimization` attiva.

La premessa della domanda era sbagliata, e solo la misura poteva dirlo.

### Q3 — Colonne di rischio nelle tabelle di Asset Global 🟢 chiusa → D54

Ipotesi ad alto rapporto valore/costo (vedi documento 03 §4): volatilità annua, max
drawdown e correlazione media come colonne delle tre tabelle esistenti, sfruttando la
sincronizzazione già presente.

**Chiusa dal developer, in modo più ampio dell'ipotesi**: entrano **tutti** i segnali
utili, ma **nascosti per default** e attivabili dall'utente; visibile di default il
solo **max drawdown**, perché è facile da capire e ad alto impatto.

La scelta non costa nulla in meccanismo. `DataTable` espone già `hiddenByDefault` per
colonna, e la persistenza salva **solo gli override espliciti**
(`DataTable.svelte:193-196`), con una nota nel codice che spiega perché: così un
default dinamico resta la verità viva e un valore vecchio in `localStorage` non può
tenere nascosta una colonna che dovrebbe vedersi.

Si rivedrà insieme ad Asset Detail (D47), quando la grammatica sarà decisa.

### Q4 — Destino del banner beta 🟢 chiusa → D46

Orientamento coerente con la tesi: il banner si rimuove dai livelli fondati su fatti
osservati (L1, L2, L3) e **resta solo su L4**, dove è vero — e anche lì, solo sul
gradino «simulazione», non su replay e shock che sono deterministici.

**Chiusa dal developer**: si lavora in un worktree separato e **si rilascia solo a
catena completa**, non a stati intermedi. Il banner si toglie a quel punto, in un
colpo solo, secondo la regola qui sopra. Nessuna decisione intermedia da prendere: il
`dev_release2` non vede nulla finché il lavoro non è pronto.

Conseguenza operativa: la domanda 6 dell'elenco «mai discusso» (cosa si rilascia nel
frattempo) è risolta, e il CHANGELOG si scrive una volta sola a fine catena.

### Q5 — Documentazione utente 🟢 chiusa → D55

D4 prevede un link alla documentazione **per riga** della scala L1. Le pagine
`mkdocs_src/docs/financial-theory/technical-analysis/risk-metrics/` esistono già per
volatilità, Sharpe, Sortino e max drawdown, ma **non** per CVaR/VaR, correlazione,
contributo al rischio, replay e simulazione.

**Chiusa dal developer** con un ordine di lavoro, non con un elenco di pagine:

1. **All'avvio dello sviluppo**, non alla fine: per ogni pagina che servirà si crea un
   **mock di due righe, solo in inglese**, registrato subito nell'indice di
   `mkdocs_src/mkdocs.yml` secondo le regole già fissate nelle skill. Così l'indice
   nasce completo e nessun link resta appeso mentre il codice avanza.
2. **Durante lo sviluppo**: un agente di documentazione riempie le pagine **in
   inglese**, in parallelo agli agenti di codice.
3. **Alla fine di tutta la documentazione**: le traduzioni in un **blocco unico**, mai
   pagina per pagina.

Il vantaggio non è solo organizzativo: un mock in indice rende il gate `check-links`
verde fin dal primo giorno, quindi la documentazione mancante si vede come pagina
vuota invece che come collegamento rotto.

### Q6 — Curva normale sovrapposta all'istogramma 🟢 chiusa → D22

D16 adotta l'istogramma della distribuzione dei rendimenti con VaR e CVaR marcati.
Si era considerato di sovrapporre la **gaussiana** con la stessa media e deviazione
standard, per mostrare quanto le code reali siano più grasse di quelle del modello.

**Chiusa in negativo.** Il VaR mostrato è `historical_var`, cioè un quantile empirico:
nessuna assunzione di normalità entra nel calcolo, quindi la campana sarebbe il
confronto con un modello che non stiamo usando — risponde a «perché altri strumenti
sbagliano», domanda che su L1 nessuno ha posto.

Peggio, produrrebbe un danno attivo: una campana accanto ai dati suggerisce che la
campana sia il comportamento *atteso* e lo scarto un'anomalia. È rovesciato — per i
rendimenti finanziari le code grasse **sono** la norma.

La lezione resta valida e va nella pagina wiki sul VaR (Q5). L'unico punto dove si
guadagnerebbe il posto è **L4**: lì un modello lo stiamo davvero scegliendo, e mostrare
cosa assume il GBM contro cosa produce il bootstrap è decision-relevant.

---

## 4. Prossimo blocco

Il blocco UI/UX è stato svolto ed è confluito in
[`05-grammatica-visiva-e-rappresentazioni.md`](./05-grammatica-visiva-e-rappresentazioni.md),
che contiene la diagnosi estetica verificata sul codice, il contratto di primitive,
l'anatomia della card e le sei rappresentazioni con il loro costo.

**✅ Scritto il 17 Set 2026**: [`07-piano-esecutivo.md`](./07-piano-esecutivo.md).
Tutte le questioni da Q1 a Q12 sono chiuse e tutti gli otto punti qui sotto sono
confluiti nel piano — l'ordine migrazione/UI in §4, la taglia in §7, l'indirizzo
dell'oracolo M4 in §5, la voce di CHANGELOG in §6, e i restanti quattro nelle schede
di flusso §8.

Elenco conservato come tracciamento di ciò che il piano doveva assorbire:

1. **Ordine fra migrazione e UI.** M1 e M3 sono invisibili all'utente ma sbloccano la
   reattività su cui poggia metà di [`05`](./05-grammatica-visiva-e-rappresentazioni.md).
2. **Una taglia.** Oggi non esiste da nessuna parte una stima di quanto sia grande
   questo lavoro. Senza, il piano esecutivo diventa una lista di desideri.
3. **Dove vivono i test dell'oracolo M4** — categoria, lane di runtime, registrazione
   nel catalogo. M4 è la chiave di volta e non ha ancora un indirizzo.
4. **La voce di CHANGELOG per M2**, che cambia un numero già mostrato agli utenti.

E, aggiunte il 17 Set, due lavorazioni di backend che prima non esistevano nel piano:

5. **Il filtro per asset su `PortfolioRiskScope`** (D58) e la scelta sui pesi (Q8).
   Tocca schema, servizio e UI, ma **nessun plugin**.
6. **L'estensione di `AssetType`** (D52), il backfill `INDEX` (D53) e la correzione
   del docstring bugiardo (D51) — tutto nella stessa migrazione incrementale, insieme
   alla colonna del flag benchmark.
7. **L'allineamento dei punti che raggruppano per tipo** (Q12, D70): i due scenari YAML
   predefiniti, `PNG_MAP`, le etichette nelle quattro lingue e l'elenco scritto a mano
   in `AssetTable.svelte:181`. Vanno consegnati **insieme** all'enum, e sorvegliati da
   un test che leghi l'enum alle tre tabelle — perché tutti e tre i punti ripiegano in
   silenzio invece di lamentarsi.
8. **La gerarchia cromatica nei due grafici di allocazione** (D71): mappa
   sottotipo → primario in `assetTypes.ts`, `hexToHsl` in `utils/colors.ts`, colore per
   dato al posto del colore per indice. Tutto frontend, nessun tocco al backend.

Risolte invece da D46 e D47: cosa si rilascia nel frattempo (nulla, si rilascia a
catena completa) e quando si riapre Asset Detail (a fine catena). E da D54 e D55: le
colonne di Asset Global e l'ordine di scrittura della documentazione.

### Q7 — Quali misure acquisire dalla famiglia drawdown 🟢 chiusa → D56

`riskfolio.src.RiskFunctions` espone **48 callable, di cui 42 sue** — le altre sei
(`Bounds`, `minimize`, `null_space`, `PCA`, `StandardScaler`, `pinv`) sono scipy,
sklearn e numpy che filtrano dagli import. La famiglia drawdown ne occupa quattordici
(sette misure × varianti `Abs`/`Rel`). Non sono migrazioni: sono **acquisizioni**, e
vanno decise per domanda, non per disponibilità.

> **Aggiornamento**: le due candidate più forti non stanno nella famiglia drawdown.
> Sono `NEA` per L2 (D38) e `Sharpe(rm=...)` per L3 (D39), entrambe già decise. Il
> setaccio completo delle 42 sta in §7.3 di `06`; questa Q resta aperta solo per la
> scelta **dentro** la famiglia drawdown.

Il motivo per cui la questione esiste: il massimo drawdown dice *quanto in
profondità*, mai *quanto a lungo*. Misurato su due storie costruite con lo stesso
massimo (§7.1 di `06`):

| Storia (12 mesi, da 100 a 100) | MDD | Mesi sotto | ADD | UCI |
|---|---:|---:|---:|---:|
| A — crolla a 80 e recupera subito | 20,0% | **2** | 2,5% | 6,5% |
| B — scende piano a 80 e risale piano | 20,0% | **10** | 9,7% | 11,7% |

Il MDD dà lo stesso numero. A è una brutta settimana, B è un anno di erosione.

**Candidati, in ordine di rapporto valore/costo:**

1. **UCI — Ulcer Index** (1,76 ms). Media dei quadrati del «quanto sono sotto il
   picco», su **tutti** i giorni, poi radice. Il quadrato pesa la profondità, la
   media su tutti i giorni pesa la durata. È in un numero solo ciò che oggi diciamo
   con due campi separati (`max_drawdown` e `max_duration`). Nato per misurare quanto
   un investimento è stato *fastidioso da tenere* — letteralmente la domanda L1.
2. **Risk contribution su misura non-MV** (nessun costo matematico nuovo:
   `Risk_Contribution` accetta già `rm=`, il parametro che sceglie *quale* rischio
   scomporre). Oggi la pagina dice «chi contribuisce alla volatilità»; potrebbe dire
   «chi contribuisce ai miei drawdown» (`rm="CDaR"`) o «alle code» (`rm="CVaR"`).
   **La risposta cambia davvero**: su tre asset a volatilità identica, il maggiore
   imputato passa dal 38,3% (code) al 20,7% (fastidio) — da primo a ultimo. ⚠️ **Blocco noto**: i contributi non-MV possono essere
   **negativi** — corretto (un asset può ridurre il rischio di coda) ma incompatibile
   con la UI attuale, che li tratta come quote di una torta. Va deciso prima.
3. **CDaR 95%** (1,02 ms). Sta a DaR come CVaR sta a VaR. Coerente con le scelte già
   fatte su L1, ma è la terza misura di coda dopo VaR e CVaR: rischia di affollare.
4. **ADD** (1,49 ms). Onesto, ma l'UCI dice la stessa cosa meglio.

**Escluse**: `EDaR`/`RLDaR` — 26,9 ms perché invocano un solutore, e la loro lettura
richiede di spiegare l'entropia. `MDD_Abs` — vedi D37.

**Gate**: nessuna acquisizione prima che la mappa dei livelli (`03`) abbia assegnato
una casa a ciascuna. Una metrica in più senza una domanda in più è esattamente
l'errore che questa ripianificazione esiste per non ripetere.

---

**🟢 Chiusa il 17 Set 2026 → D56**, con un esito più largo della raccomandazione.

Il developer ha obiettato al taglio proposto — «a parte ADD che mi sembra poco utile,
non capisco perché non tutte» — e l'obiezione era fondata: entrano **quattro** misure,
non una. `MDD`, `DaR`, `CDaR`, `UCI`.

**Prima però è caduta un'ambiguità sulle varianti.** `_Abs` e `_Rel` non sono due
convenzioni equivalenti. Letto il sorgente e ricostruito il numero a mano:

| variante | serie | formula del drawdown | unità |
|---|---|---|---|
| `_Abs` | `np.cumsum` | `peak − i` | punti di rendimento cumulato |
| `_Rel` | `np.cumprod` | `(peak − i) / peak` | **% dal picco** |

Riscontro esatto su 1250 giorni: `MDD_Abs` libreria 0,227822 = mano 0,227822;
`MDD_Rel` libreria 0,212860 = mano 0,212860. **Si adotta `_Rel`**: è l'unica che
corrisponde a ciò che chiunque intende dicendo «ho perso il 20%».

**Il quadro completo della famiglia**, misurato:

| misura | valore | tempo | esito |
|---|---:|---:|---|
| `MDD_Rel` | 0,5836 | 1,6 ms | ✅ entra |
| `DaR_Rel` | 0,5496 | 1,6 ms | ✅ entra |
| `CDaR_Rel` | 0,5632 | 1,7 ms | ✅ entra |
| `UCI_Rel` | 0,4197 | 3,0 ms | ✅ entra |
| `ADD_Rel` | 0,3981 | 2,8 ms | ❌ scelta del developer — l'UCI dice la stessa cosa meglio |
| `EDaR_Rel` | tupla | ~27 ms | ❌ conferma D42 |
| `RLDaR_Rel` | 0,1939 / **0,0000** | 89-166 ms | ❌ conferma D42, **più una prova nuova** |

Su `RLDaR` vale la pena essere espliciti, perché è l'unico caso in cui la prova è più
forte della motivazione originale. D42 lo escludeva per spiegabilità. Ma con un seme
restituisce `0,000000` **esatto** e con un altro 0,1939: il solutore degenera senza
segnalare nulla. Una misura di rischio che risponde «zero» in silenzio non è una
metrica poco chiara, è una metrica che **mente**. Resta fuori anche se un giorno la
spiegabilità smettesse di essere un problema.

`EDaR` è invece solo scomodo, non rotto: restituisce una tupla `(valore, parametro)`
da spacchettare. Resta fuori per D42, non per difetto.

Il punto 2 dei candidati — i contributi su misura non-MV — **non è chiuso da qui**: è
una scelta di rappresentazione, non di acquisizione, e il blocco dei contributi
negativi resta valido. Si decide quando si ridisegna la card dei contributi.

---

### Q8 — Rinormalizzazione dei pesi quando il portafoglio si affetta 🟢 chiusa → D59

Discende direttamente da D58. Una volta che si può chiedere «solo questi asset del mio
portafoglio», resta da decidere cosa siano i pesi della fetta. Le due risposte non sono
sfumature della stessa cosa: rispondono a domande diverse.

| | Pesi **rinormalizzati** al 100% della fetta | Pesi **reali** mantenuti |
|---|---|---|
| Domanda | «Com'è fatta la mia parte azionaria, guardata da sola?» | «Quanto pesa la mia parte azionaria sul rischio di tutto?» |
| Confronto sensato con un benchmark | ✅ sì — è la fetta contro il suo riferimento | ❌ no — la fetta al 60% sembra sempre meno rischiosa dell'indice |
| Contributi al rischio | sommano a 100% della fetta | sommano alla quota della fetta sul totale |
| Rischio d'equivoco | far credere di essere tutto investito in azioni | leggere un CVaR basso e pensare che la fetta sia tranquilla |

La prima serve a D48 — il confronto con il riferimento giusto è l'intero motivo per
cui D58 esiste. La seconda serve a L2, dove la domanda è quanto una parte contribuisce
all'insieme, e lì il contributo al rischio la dà già senza affettare nulla.

**Chiusa dal developer: si rinormalizza.** La colonna di destra resta comunque servita,
ma da un altro strumento: il contributo al rischio sull'intero portafoglio. Resta
l'obbligo di **dichiararlo nella UI**, perché la stessa percentuale significa due cose
diverse a seconda di cosa la sta generando.

### Q9 — Quali sottotipi di ETF, e come si chiamano 🟢 chiusa → D61, D62, D67

Discende da D52. Il developer ha fissato il criterio — «ovviamente non tutti, solo
quelli che hanno senso» — e poi, esaminando i candidati, ne è uscita una struttura più
stretta di quella proposta (**D61**): il secondo livello **non è un elenco nuovo**, è
l'insieme dei tipi base. La specializzazione di un ETF è «quale tipo base contiene».

| Candidato proposto | Esito |
|---|---|
| Azionario | ✅ `STOCK` come secondo livello |
| Obbligazionario | ✅ `BOND` come secondo livello |
| Materie prime | ✅ ma serve **`COMMODITY` come tipo base nuovo** — oggi manca |
| Immobiliare | ✅ ma serve **`REAL_ESTATE` come tipo base nuovo** — oggi manca |
| Monetario | 🟡 **solo sottotipo**, nessun tipo base — D67 incrina la regola di D61 |
| Bilanciato / multi-asset | ❌ non è un sottotipo: è l'`ETF` generico, che resta come residuo |
| Suddivisione di `FUND` | ❌ gli ETF sono già fondi, passivi; la distinzione utile è sul contenuto |

**Il caso `LIQUIDITY`.** ⚠️ **Autocorrezione, 17 Set → D63.** Qui stava scritto che il
monetario era «già mezzo costruito, icona ed etichette pronte», sulla base del fatto
che `PNG_MAP` (`assetTypes.ts:30`), `liquidity.png`, le quattro lingue (riga 143) e
l'emoji `💰` in `AllocationHistoryChart.svelte:129` lo nominano tutti mentre l'enum del
backend no.

La lettura era sbagliata. Quelle voci **non sono orfane**: servono a disegnare un
**secchio sintetico**. `portfolio_engine.py:1039-1041` inietta la cassa
nell'allocazione come pseudo-tipo, con un commento esplicito
(*«Allocation: cash as Liquidity (type + sector, not geography)»*). La liquidità non è
un asset, è il saldo dei broker.

Aggiungerla all'enum creerebbe un doppione **visibile**: gli asset veri entrano in
`by_type` con `asset_type.value`, cioè maiuscolo (`:1448`, alimentato da `:2155`), la
cassa entra come `"Liquidity"`. Due chiavi, due fette, stessa icona — perché
`getAssetTypeIconUrl` normalizza in maiuscolo. Sembrerebbe un bug e lo sarebbe.

Restano quindi da disegnare **due** icone, `commodity` e `real-estate`, e resta aperto
come rappresentare il monetario → **Q10**.

**Resta aperto** anche il naming esatto dei valori dell'enum (`ETF_STOCK` o
`ETF_EQUITY`? il primo è coerente col tipo base che nomina, il secondo con l'uso
corrente in finanza) e le etichette tradotte sotto `assets.types.*`.

#### Come si sceglie il secondo livello — D62

Tre ipotesi valutate:

| | Costo | Note |
|---|---|---|
| Secondo selettore che appare dopo ETF | basso | `SimpleSelect` esistente, zero componenti nuovi; ma sono **due gesti** |
| Pulsante → modale | alto | annida una modale dentro `AssetModal` (2427 righe) |
| **Albero a due livelli** | medio, **già scritto** | `SignalTreeSelect.svelte` |

Vince il terzo. `SignalTreeSelect` (359 righe) espone `groups: SignalTreeGroup[]`, ogni
gruppo con `items: SignalTreeItem[]`: due livelli esatti, espandibili, con ricerca che
attraversa entrambi e navigazione da tastiera. Il costo vero è la **generalizzazione** —
oggi vive in `components/charts/` e importa `SignalOptionContent` — e il fatto che
`SignalTreeItem` ha un solo campo `icon`, mentre la pastiglia ne vuole due.

La migrazione non tocca le righe esistenti (restano `ETF`), quindi non serve backfill —
a differenza di D53, che invece lo richiede.

---

### Q10 — Come si rappresenta il monetario 🟢 chiusa → D67

Nasce da D63. D61 aveva stabilito che il secondo livello di un ETF è «quale tipo base
contiene». Il monetario rompe la regola, perché **non esiste un tipo base monetario**:
`LIQUIDITY` è la cassa, cioè il saldo dei broker, non una classe di investimento.

E la distinzione è reale, non formale. Un fondo monetario è uno strumento che si
compra, ha un prezzo, un emittente e un rischio — piccolo ma non nullo. La cassa su un
conto non ha nulla di tutto questo. Confonderli significherebbe dire che un fondo
monetario non può perdere, che è falso.

| Strada | A favore | Contro |
|---|---|---|
| **A** — nuovo tipo base `MONETARY` | Rispetta D61 senza eccezioni; separa nettamente investimento e cassa; serve anche fuori dagli ETF (fondi monetari veri) | Un tipo base in più che quasi nessuno userà da solo; serve una terza icona |
| **B** — nessun sottotipo: il monetario sta sotto `BOND` | Zero costo; difendibile in finanza — un fondo monetario è debito a brevissima scadenza | Un benchmark obbligazionario decennale e uno monetario finirebbero nella stessa sezione, con profili di rischio lontanissimi: è il problema che D48 esiste per risolvere |
| **C** — si rimanda | Non blocca nulla oggi | Lascia un sottotipo mancante proprio nella categoria dove il confronto sbagliato fa più danno |

**🟢 Chiusa dal developer → D67**, con una quarta strada che non avevo considerato:
`ETF_MONETARY` **esiste come sottotipo, senza un tipo base corrispondente**.

Tiene il pregio di A — il monetario ha una sezione sua, quindi un decennale e un
monetario non finiscono mescolati — senza pagarne il prezzo: nessun tipo di primo
livello che nessuno userebbe da solo, nessuna icona in più, perché la pastiglia riusa
`liquidity.png`, che esiste, significa denaro e non è un tipo base.

⚠️ **Va detto chiaramente**: questo **incrina la regola di D61**. Il secondo livello
non è più *esattamente* l'insieme dei tipi base, è l'insieme dei tipi base **più uno**.
La regola resta vera per tutti gli altri sottotipi e l'eccezione ha una ragione — un
fondo monetario si compra, la cassa no (D63) — ma va scritta, non lasciata scoprire a
chi implementerà.

### Q11 — Quanto sono specifici i testi di suggerimento 🟢 chiusa → D68

Nasce da D65. I suggerimenti sono testi fissi tradotti, quindi la domanda non è più
«da dove vengono» ma **cosa nominano**.

| Strada | Esempio | Nota |
|---|---|---|
| **A** — nominare l'**indice** | «un ETF che replica l'S&P 500» | Neutro, non invecchia, non indica un prodotto |
| **B** — nominare il **prodotto** | «VWCE (IE00B4L5Y983)» | Immediato da cercare, ma è di fatto una raccomandazione d'acquisto in una stringa tradotta, e gli ISIN cambiano o vengono delistati |

**🟢 Chiusa dal developer → D68**: strada A, **restando generici**. I testi nominano
indici famosi — «un ETF sull'S&P 500», «uno sull'EuroStoxx 600», «un globale tipo MSCI
World», «un obbligazionario ampio» — e **mai un ISIN o un ticker**.

Un indice è un fatto pubblico e stabile; un ISIN è la scelta di un emittente, e
metterlo in un testo dell'applicazione sposta LibreFolio da strumento di misura a
strumento di consiglio. Vale anche la manutenzione: un testo tradotto in quattro lingue
che cita un prodotto delistato è un errore in quattro punti contemporaneamente.

I testi elencano **anche le classi** («ne serve uno azionario, uno obbligazionario…»),
coerentemente con Q9 e con la struttura a sezioni di D48.


---

### Q12 — Il sottotipo cambia il raggruppamento ovunque il tipo sia una chiave 🟢 chiusa → D70, D71, D72

Nasce dal controllo chiesto dal developer dopo D67: *«verifica che queste risposte non
abbiano creato nuove domande»*. Ne hanno creata una, e non piccola.

D52 stabilisce che i sottotipi entrano **dentro `AssetType`**. D51 dice che è sicuro,
perché `asset_type` non pilota comportamenti. È vero per i *comportamenti*, ed è
**falso per i raggruppamenti**: `asset_type` non decide cosa il sistema *fa*, ma decide
in continuazione come il sistema *somma*. Cambiare l'insieme dei valori cambia ogni
somma che usa quel valore come chiave.

#### Il caso che fa danno, verificato

Lo stress test ha una dimensione `asset_class`, e quella classe è **letteralmente** il
valore dell'enum — `service.py:446`: `asset_class=asset_type.value`. Gli scenari
predefiniti sono file YAML che elencano i secchi per nome:

```yaml
# scenario_catalog/built_in/hypothetical/equity_crash.yml
bucket_shocks:
  STOCK: -0.35
  ETF: -0.25      # ← tutti gli ETF, compreso un obbligazionario
  FUND: -0.25
  INDEX: -0.35
  OTHER: 0.0
```

Se un ETF azionario diventa `ETF_STOCK`, il secchio `ETF` **non lo trova più**. E
`_resolve_bucket` (`stress.py:219-226`) non solleva errore: restituisce
`UNCONFIGURED_ZERO`, shock `0.0`. Risultato — in un crollo azionario i tuoi ETF
azionari **non si muovono**, e il portafoglio sembra molto più solido di quanto sia.
È esattamente la classe di errore di D28 (CVaR distorto) e D63 (doppia fetta di cassa):
nessuna eccezione, nessun avviso in faccia, solo un numero credibile e sbagliato.

#### Il caso che invece migliora

La stessa riga `ETF: -0.25` è **già oggi** un difetto di modello: in un crollo
azionario un ETF obbligazionario perde il 25% come uno azionario. Con i sottotipi
quella distinzione diventa finalmente esprimibile — `ETF_STOCK: -0.35`,
`ETF_BOND: -0.05`. Vale anche per la torta di allocazione: un portafoglio tutto in ETF
oggi mostra **una fetta al 100%** che non informa di nulla, mentre «azionario 60% /
obbligazionario 30% / materie prime 10%» è la risposta alla L2, *«sono diversificato
come credo?»*.

Quindi il sottotipo non è un rischio da evitare: è un miglioramento che **richiede una
decisione esplicita su ogni punto che raggruppa**.

#### I punti che raggruppano, censiti

| Punto | Dove | Cosa succede senza decisione |
|---|---|---|
| Scenari stress predefiniti | `scenario_catalog/built_in/hypothetical/*.yml` (2 file) | Shock silenziosamente a zero |
| Torta e storico allocazione | `portfolio_engine.py:1448` → `AllocationPieChart`, `AllocationHistoryChart` | Fette nuove, etichetta cruda e icona `other` |
| Treemap esposizione | `ExposureTreemap.svelte:84` | Come sopra |
| Icone | `PNG_MAP` in `assetTypes.ts:19-31` | `getAssetTypeIconUrl` non lancia: ripiega su `other.png`, quindi un ETF azionario si disegna grigio «altro» |
| Etichette | `frontend/src/lib/i18n/{en,it,fr,es}.json` | `|| type` mostra la chiave grezza |
| Filtro della tabella asset | `AssetTable.svelte:181` — elenco **scritto a mano**, senza `INDEX` | I nuovi tipi non sono filtrabili |
| Filtro backend | `crud.py:180-181` | Un filtro `ETF` non trova più gli ETF specializzati |
| AI export | `asset_resources.py:110` e simili | ✅ **nessun problema**: viaggia come `str` libero, non come enum vincolato |

#### Le strade

| Strada | Cosa comporta |
|---|---|
| **A** — si raggruppa sempre sul valore pieno | La torta si specializza da sola, lo stress diventa corretto. Costo: icone, etichette in quattro lingue, elenco della tabella, **e la riscrittura dei due YAML** |
| **B** — si raggruppa sempre sul contenitore | Serve una funzione `base_type(asset_type)` condivisa; niente si rompe, ma si butta via il guadagno: la torta resta muta e lo stress resta sbagliato |
| **C** — dipende dal punto | Torta, treemap e stress sul **valore pieno**, perché lì la distinzione è informativa; filtri e ricerca sul **contenitore**, perché lì serve trovare. Costa una funzione in più e una regola da ricordare |

#### 🟡 Declassata dal developer → D70

*«È uno YAML che abbiamo scritto noi e mai usato, quindi non lo vedo come un problema
ma come un segnale che quello che già esiste del risk system potrebbe dover essere
adeguato.»*

Giusto, e la distinzione conta: **un file che possediamo non è un vincolo, è un
compito**. Non c'è un contratto con l'esterno da rispettare, non c'è un formato altrui
da assecondare — ci sono due file YAML da riscrivere nella stessa consegna dell'enum.
L'avevo trattato come una domanda di progetto quando era una voce di lavoro.

Va tenuto solo il perché lo avevo scovato: l'errore non è che lo scenario fosse
sbagliato, è che **fallisce in silenzio**. `UNCONFIGURED_ZERO` non avvisa nessuno. Per
questo il piano esecutivo deve pretendere non l'aggiornamento dei due file, che è
banale, ma un **test che renda impossibile dimenticarli**: ogni valore dell'enum deve
avere un secchio negli scenari, una voce in `PNG_MAP` e un'etichetta nelle quattro
lingue. Oggi tutte e tre le coperture sono affidate alla buona volontà, e tutte e tre
ripiegano senza far rumore — `other.png` per l'icona, la chiave grezza per l'etichetta,
shock zero per lo scenario.

#### Il residuo sulla torta, chiuso da D71

Restava una scelta che i due YAML non esauriscono: se la torta si specializzasse o no.
Avevo posto un compromesso — specializzare risponde alla L2, ma con pochi asset produce
otto fette da poco ciascuna — e il developer non ne ha scelto un corno: **ha eliminato
il compromesso**. I sottotipi restano dentro la massa del primario con tonalità vicine,
i numeri si leggono nel tooltip, e un asset non specializzato usa il colore pieno.

Costo verificato sul codice, perché non è gratis:

| Cosa | Dove | Nota |
|---|---|---|
| Il colore è assegnato **per indice** | `AllocationPieChart:338` (`color: palette`), `AllocationHistoryChart:531-533` e `:585` | Va sostituito con un colore **per dato**: nella torta `itemStyle.color` sul singolo item, nello storico su `lineStyle`/`areaStyle`/`itemStyle` e nel tooltip. Quattro punti nello storico, non uno |
| Serve `hexToHsl` | `utils/colors.ts` ha **solo** `hslToHex` (`:123`) | Le due tavolozze sono esadecimali scritte a mano «a massima distanza cromatica»: per ricavarne sfumature serve la conversione inversa, che oggi non c'è |
| L'alfa è una concatenazione di stringa | `AllocationHistoryChart:532` — `palette[i] + '88'` | Funziona finché il colore è un esadecimale a 6 cifre. Una sfumatura calcolata deve mantenere quel formato o passare a `rgba()` |
| Le tonalità devono reggere **due temi** | `PALETTE_LIGHT` è scura (`#1a4031`), `PALETTE_DARK` è chiara (`#4ade80`) | La sfumatura non può andare sempre verso il chiaro: va calcolata rispetto al tema, o due sottotipi diventano indistinguibili in uno dei due |
| La legenda si affolla | commento «up to 12» in `AllocationHistoryChart:107`, la torta ha già la paginazione | Con i sottotipi le voci possono superare la dozzina. Da valutare se la legenda elenca i **primari** e il dettaglio resta al tooltip |

⚠️ **Trappola di denominazione, da fissare ora**: la derivazione del tipo primario
**non può essere una divisione sulla stringa**. `rawKey` conserva gli underscore
(`AllocationPieChart:210`, regex `/[^A-Z_]/g`), quindi spezzare `ETF_STOCK` su `_`
funziona — ma `REAL_ESTATE` è un **tipo primario** e si spezzerebbe in `REAL` +
`ESTATE`, producendo un genitore inesistente. Serve una mappa esplicita accanto a
`PNG_MAP` in `assetTypes.ts`, che è anche il posto dove vive già la pastiglia (D52).

⚠️ **Condizione perché la sfumatura si veda**: oggi le fette sono ordinate per valore
decrescente (`AllocationPieChart:179`). Con quell'ordinamento due sottotipi dello
stesso genitore finiscono **lontani**, e la parentela cromatica diventa invisibile —
si vedono due colori simili in due punti scollegati del cerchio, che è peggio che non
averli sfumati. L'ordinamento deve diventare **gerarchico**: i genitori ordinati per
totale, i figli per valore dentro il genitore. Senza questo, D71 non funziona.

Nota di portata: la gerarchia si costruisce **nel frontend**. Il backend continua a
produrre `by_type` piatto (`portfolio_engine.py:1448`), quindi nessuna modifica a
motore, schema o API — solo `assetTypes.ts` e i due grafici.

#### Il ripiego a due anelli (D72)

Se un anello solo si rivelasse illeggibile, il ripiego è la **ciambella annidata**, e
costa meno di quanto sembri: il grafico **è già una ciambella** — `radius: ['35%',
'70%']` alla riga 274 — quindi il secondo anello è una banda dentro un buco che esiste
già, non una riscrittura.

⚠️ Una trappola da conoscere prima, perché questo file ne ha già collezionate tre. Il
percorso di aggiornamento veloce alla riga 193 scrive `series: [{data: chartData}]` —
**una sola serie**. Con due anelli, quel ramo aggiornerebbe l'interno e lascerebbe
l'esterno fermo all'ultimo disegno completo: dati vecchi, nessun errore, nessun
sintomo. Esattamente la famiglia di difetti che i tre commenti «Bugfix» del file
documentano (`lastRawTypeKeys`, il `$effect` che leggeva la verità dell'array invece
del contenuto, la chiave rich-text che sbagliava nelle lingue diverse dall'inglese).

Scartata invece la **barra impilata polare**: è un grafico legittimo, ma mappa il
valore sul raggio, e l'area cresce col quadrato del raggio. Due quote uguali a
distanze diverse dal centro occupano aree diverse, e in un grafico che serve a dire
«quanta parte del totale» l'occhio legge proprio l'area. È l'attrezzo giusto per
confrontare categorie lungo una dimensione ciclica — mesi, ore del giorno — non per
parti di un tutto.

⚠️ Da mettere comunque a piano, indipendentemente dalla strada scelta: `PNG_MAP` e le
quattro lingue devono coprire **ogni** valore dell'enum, e serve un test che lo
verifichi. Oggi la copertura è lasciata alla buona volontà, e il ripiego silenzioso su
`other.png` fa sì che un buco non si veda.
